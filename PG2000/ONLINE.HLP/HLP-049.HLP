$Header hlp-049.hlp 2.31 97/07/08 00:02:00 Goebl A. Exp $
%%
1,0:1    verfgbare Programme und Funktionen
1,1:50   <HM><Datei>
1,2:51   <HM><Bearbeiten>
1,3:52   <HM><Objekt>
1,4:53   <HM><Projekt>
1,5:54   <HM><System>
1,6:55   <HM><Einstellungen>
1,7:56   <HM><Tools>
1,8:57   <HM><Hilfe>
2,0:602   KOP
2,1:602      Dat
2,2:602      Su
2,3:602      Bl
2,4:602      Set
2,8:620      Popup
2,9:640      Popup Kontakt-Auswahltabelle
3,0:402   PL2000
3,1:402      Dat
3,2:402      Su
3,3:402      Zeile
3,4:402      Modus
3,8:402      Popup
4,0:326   GDM
4,1:326      GDM
4,2:378      Sym
4,3:379      Datb
4,4:374      Task
4,8:334      Popup
4,9:330      Popup Taskebene
4,10:332     Popup Editor
5,0:502     AWL
5,1:502      Dat
5,2:502      Su
5,3:502      Bl
5,4:502      Mod
5,8:502      Popup
6,0:903   Meldungsfenster
7,0:752   Texteditor
7,1:752      Dat
7,2:752      Su
7,3:752      Bl
7,4:752      Mod
7,8:752      Popup
9,0:565   Source Level Debugger
9,1:565      Task
9,2:565      Daten
9,3:565      Break
9,4:565      Text
10,0:850  PV - Monitor
10,1:850    Variable
10,2:850    PV-Liste
10,3:850    Task
10,4:850    Debug
12,0:660  KOP-Statustest
12,1:660    PV-Liste
12,2:660    Task
12,3:660    Debug
12,4:660
12,8:660    Popup
15,0:702  FUB-Editor
17,0:843  Datentyp-Editor
18,0:350  Bereichseditor
40,0:4    Projektverwaltung
50,0:898  Dialogboxen
52,0:800  VD
52,1:801    NAME
52,2:802    Kennung
52,3:803    Datentyp
52,4:804    L„nge
52,5:805    I/O Typ
52,6:808    Init-Wert
52,7:807    Langname
53,0:800  VD
53,1:801    N A M E
53,2:802    Kennung
53,3:803    Datentyp
53,4:804    L„nge
53,5:805    I/O Typ
53,6:808    Init-Wert
53,7:807    Langname
54,0:812  Strukt-VD
54,1:813    KOMPONENTE
54,2:814    Datentyp
54,3:815    L„nge
55,0:805  Moduldefinition
55,1:822  2010-I/O
55,2:822
55,3:828    Popup Modultyp
55,4:822
56,0:805  Moduldefinition
56,1:823  Remote-I/O
56,2:823
56,3:823
56,4:823
56,5:823
56,6:828    Popup Modultyp
56,7:823
56,8:823
57,0:805  Moduldefinition
57,1:821  2005-I/O
57,2:821
57,3:821
57,4:828    Popup Modultyp
57,5:821
58,0:805  IP-IO
59,0:805  Moduldefinition
59,1:820  2003-I/O
59,2:820
59,3:828    Popup Modultyp
59,4:820
59,5:820
60,0:894  Fileselektor - Box
65,0:254  <SYSTEM><Info>
69,0:304  <EINSTELLUNGEN><Dokumentation>
70,0:251  <SYSTEM><Analyse>
71,0:314  <TOOLS><Wiederherstellen>
72,0:316  <TOOLS><Logfile>
73,0:319  <HILFE><Librarys>
74,0:278  <EINSTELLUNGEN><Editor><KOP>
75,0:270  <BS laden>
78,0:805  Moduldefinition
78,1:825  CAN-I/O
78,2:825
78,3:825
78,4:825
78,5:828    Popup Modultyp
78,6:825
78,7:825
81,0:236   <PROJEKT><Datenbank><RIO-Konfiguration>
83,0:10   <PROJEKT><Erzeugen> bzw. <Kopieren>
84,0:274  <EINSTELLUNGEN><Editor><PL2000>
85,0:274  <EINSTELLUNGEN><Editor><Text>
86,0:274  <EINSTELLUNGEN><Editor><Anweisungsliste>
87,0:110   <OBJEKT><Parameter>
88,0:103   Suchfenster GDM
89,0:103   Suchfenster KOP
90,0:104   Suchen/Ersetzen im KOP
91,0:104   Suchen/Ersetzen im Texteditor
93,0:283  <EINSTELLUNGEN><Compiler>
94,0:272  <SYSTEM><Modem>
95,0:282  <EINSTELLUNGEN><Editor><Bereichseditor>
96,0:279  <EINSTELLUNGEN><Editor><Variable>
97,0:842  Variablendeklaration: Dialogbox Filter
98,0:910  <Objekt><Parameter><CFG>
101,0:66   Projekt Export
102,0:75   Projekt Import
105,0:221   <PROJEKT><Abgleichen>
106,0:220   <PROJEKT><Vergleich>
107,0:295  <EINSTELLUNGEN><Editor><PV-Monitor>
108,0:911  Konfiguration
109,0:914  Speicher
110,0:918  Module
111,0:922  Taskklassen
112,0:928  HS Taskklassen
113,0:934  Exceptions
114,0:937  Interrupts
115,0:940  TimeOut
116,0:944  Betriebssystem
117,0:951  Remote I/O
118,0:973  PB Indizes
119,0:969  2003 I/O
120,0:961  CAN I/O
121,0:974  Kommunikation
999,0:750  typedef-Dialogbox (z.Zt. nicht realisiert)
999,0:672  Datenmoduleditor  (z.Zt. Einstieg in Texteditor)
999,0:170  <EINSTELLUNGEN><Editor><SPS Abtastrate>
999,0:945  <EINSTELLUNGEN><Dokumentation><Objekt>
999,0:178  <EINSTELLUNGEN><Dokumentation><Projekt>
999,0:31   <DATEI><™ffnen>
999,0:33   <DATEI><L”schen>
999,0:35   <DATEI><Export><Editor->XPE>
999,0:38   <DATEI><Export><Task->XPE>
999,0:39   <DATEI><Export><FUB->XPE>
999,0:41   <DATEI><Export><Datentyp->XPE>
999,0:42   <DATEI><Export><Editor->ASCII>
999,0:46   <DATEI><Import><XPE->Editor>
999,0:47   <DATEI><Import><XPE->Objekt>
999,0:48   <DATEI><Import><ASCII->Editor>
999,0:49   <DATEI><Import><Library>
999,0:50   <DATEI><Import><Task Rebuild>
%%
.1
    PG2000  PROGRAMMIERSOFTWARE
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ[@050[  H A U P T M E N š  ]]ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°°°°°°°°ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°°°°º
  º°°°°³@325[ G D M - EDITOR         ]³°°°°°°°°°°°³@800[ VARIABLENDEKLARATION   ]³°°°°°°°º
  º°°°°³  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°°°°³  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°º
  º°°°°³  ³@400[ P L 2 0 0 0 - EDITOR   ]³°°°°°°°°³  ³@002[ D E B U G G I N G      ]³°°°°º
  º°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°º
  º°°°°°°°³  ³@500[ A W L - EDITOR         ]³°°°°°°°°³  ³@880[ P G - BEDIENUNG        ]³°º
  º°°°°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°ÀÄÄ´  Mens, Fenster, ...   ³°º
  º°°°°°°°°°°³  ³@600[ K O P - EDITOR         ]³°°°°°°°°³   ... [SHIFT]-Taste   ³°º
  º°°°°°°°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°³  ^ ... [CTRL]-Taste    ³°º
  º°°°°°°°°°°°°°³  ³@700[ F U B - EDITOR         ]³°°°°°³   ... [ALT]-Taste     ³°º
  º°°°°°°°°°°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ°º
  º°°°°°°°°°°°°°°°°³  ³@750[  T E X T - EDITOR      ]³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°ÉÍÍÍÍÍÍÍÍÍÍÍÍ»°°°°³  ³@770[ DATENMODUL - EDITOR    ]³°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°º@004[  PROJEKT   ]º°°°°ÀÄÄ´                        ³°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°º VERWALTUNG º°°°°°°°³                        ³°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°ÈÍÍÍÍÍÍÍÍÍÍÍÍ¼°°°°°°°ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ°°°°°°ÕÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°³@007[  INFO ber HILFE  ]º
  ÈÍ[@000[ HILFE beenden ]]ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
.2
    PG2000  PV-MONITOR und SOURCE-LEVEL-DEBUGGER
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°³@850[ P V - MONITOR          ]³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°³  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°°°°°°°°ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°º
  º°°°°³  ³@460[ P L 2 0 0 0 - DEBUGGER ]³°°°°°°°°°°°³@001[ š B E R S I C H T      ]³°°°°º
  º°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°°°°³ - HAUPTMENš            ³°°°°º
  º°°°°°°°³  ³@560[ A W L - DEBUGGER       ]³°°°°°°°°³ - PG-BEDIENUNG         ³°°°°º
  º°°°°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ°°°°º
  º°°°°°°°°°°³  ³@660[ K O P - MONITOR        ]³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°ÀÄÄ´                        ³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°³                        ³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°ÕÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°³@007[  INFO ber HILFE  ]º
  ÈÍ[@000[ HILFE beenden ]]ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
.4
    PG2000  PROJEKTVERWALTUNG
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ[@010[  H A U P T M E N š  ]]ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°³@026[ A L L G E M E I N E S  ]³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°³  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°³  ³@012[ PROJEKT - ERZEUGEN     ]³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°ÀÄÄ´  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄ¿°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°³  ³@018[ PROJEKT - ™FFNEN       ]³°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°ÀÄÄ´                        ³°°°°°°°ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿°°°º
  º°°°°°°°°°°³                        ³°°°°°°°³ Alle weiteren Funktionen ³°°°º
  º°°°°°°°°°°ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ°°°°°°°³ sind im HAUPTMENš        ³°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°³ beschrieben              ³°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°º@001[   P G 2 0 0 0   ]º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°º
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°ÕÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°³@007[  INFO ber HILFE  ]º
  ÈÍ[@000[ HILFE beenden ]]ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
.7
    INFO šBER HILFE
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º <F10>       ³ Kontextsensitive ONLINE-HILFE des PG2000 aufrufen          º
  º <BACKSPACE> ³ Zurck zur VORHERGEHENDEN Hilfeseite                       º
  º <ESC>       ³ VERLASSEN der ONLINE-HILFE                                 º
  º <BUCHSTABE> ³ Auswahl eines Verweises mit Hilfe des ANFANGSBUCHSTABENS   º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    <VERWEISE auf weitere Themen/Hilfeseiten>:   @007[<A>] / @007[ B1 ] / @007[<C>] / @007[ B2 ] / @007[<E>]

    * AUSWAHL eines Verweises (werden invers dargestellt):
      - Der gewnschte Verweis wird entweder mit Hilfe der CURSOR-Tasten oder
        mit dem ANFANGSBUCHSTABEN ausgew„hlt.
    * ANZEIGEN von HILFE zum gew„hlten Thema (Wechsel der Hilfeseite):
      - Bet„tigen der <ENTER>-Taste
      - Ein einfacher MAUSKLICK

    <KOPF-ZEILE> ... AKTUELLE KAPITELšBERSCHRIFT des angezeigten Hilfethemas
    <FUSS-ZEILE> ... Tasten fr die Navigation in der ONLINE-Hilfe


    @008[ Fortsetzung ]                          @004[ PROJEKT ] / @002[ DEBUGGING ] / @001[ šbersicht ]
.8
    SCHEMATISCHE DARSTELLUNG der HILFE-šBERSICHTSSEITEN

 PROJEKTVERWALTUNG:        PROGRAMMIERSOFTWARE:      DEBUGGING:
 ÚÄÄÄÄÄÄÄÄÄÄ HM ÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄÄÄÄ HM ÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
 ³ ÚÄÄÄÄÄÄÄ¿             ³ ³ ÚÄÄÄÄÄÄÄ¿             ³ ³ ÚÄÄÄÄÄÄÄ¿             ³
 ³ ³ÚÄÄÄÄÄÄÁ¿            ³ ³ ³ÚÄÄÄÄÄÄÁ¿  ÚÄÄÄÄÄÄÄ¿ ³ ³ ³ÚÄÄÄÄÄÄÁ¿  ÚÄÄÄÄÄÄÄ¿ ³
 ³ À´       ³            ³ ³ À´       ³  ³ DEB.. ³ ³ ³ À´       ³  ³ šBE.. ³ ³
 ³  ÀÄÄÄÄÄÄÄÙ            ³ ³  ÀÄÄÄÄÄÄÄÙ  ÀÄÄÄÄÄÄÄÙ ³ ³  ÀÄÄÄÄÄÄÄÙ  ÀÄÄÄÄÄÄÄÙ ³
 ³ ÉÍÍÍÍÍÍÍ»             ³ ³ ÉÍÍÍÍÍÍÍ»             ³ ³                       ³
 ³ º PG..  º             ³ ³ º PROJ..º             ³ ³                       ³
 ³ ÈÍÍÍÍÍÍÍ¼         INFO³ ³ ÈÍÍÍÍÍÍÍ¼         INFO³ ³                   INFO³
 À[HILFE beenden]ÄÄÄÄÄÄÄÄÙ À[HILFE beenden]ÄÄÄÄÄÄÄÄÙ À[HILFE beenden]ÄÄÄÄÄÄÄÄÙ
       ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ          ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
 WECHSELN zwischen den šbersichtsseiten  WECHSELN zwischen den šbersichtsseiten
 mittels:  <PG2000> und <PROJEKT>        mittels:  <DEBUGGING> und <šBERSICHT>

 * Auf den šbersichtsseiten wird die Hilfe mittels [HILFE beenden] verlassen.

 * Mit den Verweisen [PROJEKT]/[DEBUGGING]/[šbersicht] kann (von auáerhalb der
   šbersichtsseiten) zur entsprechenden šbersicht gewechselt werden.

                                           @004[ PROJEKT ] / @002[ DEBUGGING ] / @001[ šbersicht ]
.10
    PROJEKTVERWALTUNG  HAUPTMENš
  Aktivierung:  P                                                    @011[< WEITER >]
ÉÍÍÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
ºPROJEKTº @011[Hilfe]                                                                ³
ÈÍÍÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@012[ Erzeugen       ]³ ... ANLEGEN eines neuen Projektes
³@018[ ™ffnen         ]³ ... Projekt in der Programmierumgebung BEARBEITEN
³@019[ Kopieren       ]³ ... KOPIEREN eines Projektes
³@021[ S„ubern        ]³ ... SUBERN eines Projektes (Backup-Dateien werden gel”scht)
³@022[ L”schen        ]³ ... L™SCHEN eines Projektes (Projektdaten werden gel”scht)
³@023[ Durchsuchen    ]³ ... Kopierte Projekte in die Projektverwaltung AUFNEHMEN
³ Beenden    F4 ³ ... AUSSTIEG aus der PG2000 Programmiersoftware
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

  Dieses Men umfaát alle Funktionen fr die PROJEKTVERWALTUNG (Erzeugen,
  ™ffnen, Kopieren, usw.).
  Die Hilfetexte fr die Men-Funktionen <Projekt><ERZEUGEN> und <™FFNEN>
  sind auch ber die šbersichtsseite erreichbar, da diese die am h„ufigsten
  verwendeten Men-Funktionen der Projektverwaltung sind.

                                                                       @004[ PROJEKT ]
.11
    PROJEKTVERWALTUNG  HAUPTMENš
  Aktivierung:  H                                                    @010[< WEITER >]
ÚÄÄÄÄÄÄÄÄÉÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@010[Projekt] ºHILFEº                                                               ³
ÀÄÄÄÄÄÄÄÄÈÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@007[ Hilfe  F10 ]³ ... Online-Hilfe fr die PG2000 PROGRAMMIERSOFTWARE
³ Index     ³ ... Fehlernummern und vom Anwender selbst erstellte Hilfedateien
³ PG-Info    ³ ... Informationen zur PG2000 PROGRAMMIERSOFTWARE
ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     (Version, Projektname, verfgbarer Arbeitsspeicher, usw.)

  šber dieses Men k”nnen sowohl die ONLINE-HILFE fr die PG-Bedienung (Men-
  Funktion <Hilfe>), als auch globale INFORMATIONEN ber das Programmiersystem
  PG2000 (Men-Funktion <PG-Info>) angezeigt werden.
  šber die Men-Funktion <Index> kann ein Popup-Men mit einer Auswahl zus„tz-
  licher HILFETHEMEN eingeblendet werden. Auch der Anwender hat die M”glich-
  keit, Hilfedateien zu erstellen und in diese Liste einzubinden (siehe dazu
  Hilfethema "Wie erstellt man eine Helpdatei").



                                                                       @004[ PROJEKT ]
.12
    PROJEKTVERWALTUNG

    <PROJEKT><Erzeugen>:

    Zum Anlegen eines neuen Projektes w„hlen Sie die Men-Funktion <Projekt>
    <Erzeugen>. In der dadurch ge”ffneten Dialogbox k”nnen Sie alle fr die
    Projekterzeugung erforderlichen Daten eingeben:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄErzeugenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@013[Pfad         ]: C:\PROJEKTE                  ³ ... Projektverzeichnis
    ³@013[Projektname  ]: HALLE_1A                      ³ ... Projektname
    ³@013[Beschreibung ]: Geb„ude-Automatisierung       ³ ... Kurzbeschreibung
    ³@013[RPS_Name     ]: TEST_RPS                      ³ ... RPS-Name
    ³                                             ³
    ³          [@014[    OK    ]]  [  Cancel  ]         ³ ... <OK>:
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     Projekt erzeugen
                                                        <Cancel>:
                                                        Abbrechen ohne Aktion



    @013[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.13
    PROJEKTVERWALTUNG

    <PROJEKT><Erzeugen>:    Projektdaten

    Pfad         : Fr jedes neue Projekt wird auf Ihrer Festplatte eine eigene
                   Verzeichnisstruktur erzeugt.
                   In diesem Eingabefeld k”nnen Sie den Pfad eingeben/anw„hlen,
                   unter dem das neue Projekt angelegt wird. Um eine klare
                   Trennung zwischen Projektdaten und Entwicklungswerkzeug
                   (Programmiersystem PG2000) zu erhalten, empfiehlt es sich,
                   die Verzeichnisstruktur des Projektes nicht unter dem
                   PG2000-Verzeichnis anzulegen.
    Projektname  : Name des neuen Projektes (max. 8 Zeichen).
                   Die Verzeichnisstruktur des Projektes wird unter dem Ver-
                   zeichnis "Pfad\Projektname.PGP" angelegt.
    Beschreibung : In diesem Eingabefeld k”nnen Sie eine Kurzbeschreibung
                   (max. 30 Zeichen) fr das neue Projekt eingeben.
    RPS_Name     : Name der Standard-RPS des neuen Projektes (max. 8 Zeichen).



    @014[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.14
    PROJEKTVERWALTUNG

    <PROJEKT><Erzeugen>:    Best„tigung der Projektdaten mit <OK>

    Durch Anw„hlen des Best„tigungsfeldes <OK> werden diese Projektdaten an
    das Programmierger„t bergeben.
    Bevor das neue Projekt angelegt wird, erscheint folgende Sicherheits-
    abfrage:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄErzeugenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Projekt C:\PROJEKTE\HALLE_1A.pgp mit RPS TEST_RPS anlegen ?³
    ³                                                           ³
    ³                   [  Ja   ]  [ Nein  ]                    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ







    @015[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.15
    PROJEKTVERWALTUNG

    <PROJEKT><Erzeugen>:    Best„tigung der Sicherheitsabfrage mit <Ja>

    Nach der Best„tigung dieser Nachfrage durch das Anw„hlen des Best„tigungs-
    feldes <Ja> wird das Projekt "HALLE_1A" erzeugt.
    Dabei legt das Programmiersystem unter dem angegebenen Pfad (C:\PROJEKTE)
    folgende Verzeichnisstruktur an:
    C:\
    ÀÄÄPROJEKTE              ... eingegebener Pfadname
       ÀÄÄHALLE_1A.PGP       ... Projektverzeichnis "<Projektname>.PGP"
          ÃÄÄDBK             ... Datenbank fr PG-interne Daten
          ³  ÃÄÄLIBFILES     ... Verzeichnis fr Standard-Libraries
          ³  ÀÄÄTEST_RPS.SPS ... Datenbankpfad fr interne RPS-Daten
          ÀÄÄPGM             ... Projekt-Arbeitsverzeichnis
             ³                   Alle vom Anwender erstellten Programme und
             ³                   Dateien werden in diesem Verzeichnis abgelegt.
             ÀÄÄTEST_RPS     ... Unter diesem Verzeichnis werden alle lauf-
                                 f„higen RPS-MODULE (B&R-Module) der RPS
                                 "TEST_RPS" gespeichert.

    @016[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.16
    PROJEKTVERWALTUNG

    <PROJEKT><Erzeugen>:    Verzeichnisstruktur des Projektes wird erstellt

    Nach dem Erstellen der Verzeichnisstruktur des Projektes wird die Datenbank
    initialisiert (alle ben”tigten internen Daten der Standard-RPS werden
    vorinitialisiert und die System-Library SYSTEM.SLB wird in die Datenbank
    importiert). Die Datenbankinitialisierung erm”glicht das ™ffnen des neuen
    Projektes und das Verwenden von Funktionen der System-Library im ge”ffneten
    Projekt.
    Nach der Datenbankinitialisierung wird in einer Dialogbox abgefragt, ob das
    erzeugte Projekt ge”ffnet werden soll:

    ÚÄÄÄÄÄÄÄÄErzeugenÄÄÄÄÄÄÄÄÄ¿
    ³                         ³
    ³Projekt HALLE_1A ”ffnen ?³
    ³                         ³
    ³ [   Ja   ]  [  Nein  ]  ³ ==> <Ja> .... ™ffnen des PROJEKTES
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     <Nein> .. Rckkehr in die PROJEKTVERWALTUNG


    @017[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.17
    PROJEKTVERWALTUNG

    <PROJEKT><Erzeugen>:    Best„tigung der Frage mit <Ja>

    Durch das Anw„hlen des Best„tigungsfeldes <Ja> wird das Projekt "HALLE_1A"
    ge”ffnet und von der Projektverwaltung in die Programmierumgebung des PG2000
    gewechselt.
    In der Titelzeile (erste Bildschirmzeile) der Programmierumgebung wird der
    Projektname und der RPS-Name des GE™FFNETEN Projektes angezeigt. Beispiel:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³B&R Programmierger„t Vx.x         HALLE_1A.TEST_RPS                  13:25³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ









    @012[ Projekt erzeugen ]                @010[ Hauptmen ]                      @004[ PROJEKT ]
.18
    PROJEKTVERWALTUNG

    <PROJEKT><™ffnen>:

    Durch diese Funktion wird eine Liste aller in der Projektverwaltung ver-
    walteten Projekte eingeblendet (Projekt-Auswahlliste):

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³  TEST_PRO : Testprojekt              ³
    ³  HALLE_1A : Geb„ude-Automatisierung  ³
    ³  HALLE_1  : Maschinensteuerung       ³
    ³  HALLE_2  : Maschinensteuerung       ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
        ³          ³
        ³          ÀÄÄÄ Kurzbeschreibung des Projektes
        ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Projektname

    Nach der Selektion eines Projektes wird das Projektmen (PROJEKTVERWALTUNG)
    durch das vollst„ndige Hauptmen (PROGRAMMIERUMGEBUNG) ersetzt.
    In der Programmierumgebung stehen integrierte Programmier-, Compilier- und
    Debug-Werkzeuge zur Verfgung.
                                      @010[ Hauptmen ]                      @004[ PROJEKT ]
.19
    PROJEKTVERWALTUNG

    <PROJEKT><Kopieren>:

    Mit dieser Funktion k”nnen Sie ein Projekt (inklusive aller Projektdaten)
    in ein anderes Projekt kopieren. Diese Funktion wird vor allem zum Anlegen
    von Projekt-Sicherheitskopien bzw. zum Duplizieren von Projekten verwendet.
    Durch Aufruf der Funktion <Projekt><Kopieren> wird eine Auswahlliste aller
    in der Projektverwaltung verwalteten Projekte eingeblendet. In dieser Liste
    k”nnen Sie das gewnschte Quellprojekt anw„hlen. Nach der Auswahl eines
    Quellprojektes wird eine Dialogbox ge”ffnet, in der Sie alle erforderlichen
    Daten fr das Zielprojekt eingeben/anw„hlen k”nnen:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄKopierenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Pfad         : C:\PROJEKTE                  ³ ... Projektverzeichnis
    ³Projektname  : HALLE_2A                      ³ ... Projektname
    ³Beschreibung : Geb„ude-Automatisierung       ³ ... Kurzbeschreibung
    ³                                             ³
    ³          [@020[    OK    ]]  [  Cancel  ]         ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @020[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.20
    PROJEKTVERWALTUNG

    <PROJEKT><Kopieren>:    Best„tigung der Projektdaten mit <OK>

    Durch Anw„hlen des Best„tigungsfeldes <OK> werden diese Projektdaten an
    das Programmierger„t bergeben und das Quellprojekt auf das Zielprojekt
    kopiert. Anschlieáend kann das kopierte Projekt mit <Projekt><Durchsuchen>
    in die Projektverwaltung aufgenommen werden.













    @019[ Projekt kopieren ]                @010[ Hauptmen ]                      @004[ PROJEKT ]
.21
    PROJEKTVERWALTUNG

    <PROJEKT><S„ubern>:

    Zum L”schen aller Backup-Dateien (*.BAK und *.?_?) eines Projektes w„hlen
    Sie die Funktion <Projekt><S„ubern>. In der dadurch eingeblendeten Projekt-
    Auswahlliste k”nnen Sie das gewnschte Projekt anw„hlen. Nach der Selektion
    eines Projektes erscheint folgende Sicherheitsabfrage am Bildschirm:

    ÚÄÄÄÄÄÄÄÄÄÄS„ubernÄÄÄÄÄÄÄÄÄÄÄ¿
    ³L”schen aller Backup-Files ?³
    ³                            ³
    ³    [  Ja   ]  [ Nein  ]    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Nach der Best„tigung dieser Nachfrage durch das Anw„hlen des Best„tigungs-
    feldes <Ja> wird das selektierte Projekt ges„ubert.
    Das Programmiersystem legt beim Sichern von Source-Dateien automatisch
    Backup-Dateien an (maximal werden drei Backup-Dateien angelegt, Beispiel:
    TASK.SRC => TASK.S_0, TASK.S_1 und TASK.S_2).
    
                                      @010[ Hauptmen ]                      @004[ PROJEKT ]
.22
    PROJEKTVERWALTUNG

    <PROJEKT><L”schen>:

    Mit dieser Funktion k”nnen Sie ein Projekt (inklusive aller Projektdaten,
    Datenbank-Informationen und der Verzeichnisstruktur) vom Programmierger„t
    l”schen. Durch den Aufruf dieser Funktion wird die Projekt-Auswahlliste
    eingeblendet. Nach der Selektion eines Projektes erscheint folgende Sicher-
    heitsabfrage am Bildschirm:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄL”schenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³                                                   ³
    ³Projekt C:\PROJEKTE\HALLE_1A.pgp wirklich l”schen ?³
    ³                                                   ³
    ³              [   Ja   ]  [  Nein  ]               ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    
    Nach der Best„tigung dieser Nachfrage durch <Ja> wird das selektierte
    Projekt gel”scht.
    Vorsicht: Beim L”schen eines Projektes gehen ALLE Projektdaten verloren!

                                      @010[ Hauptmen ]                      @004[ PROJEKT ]
.23
    PROJEKTVERWALTUNG

    <PROJEKT><Durchsuchen>:

    Mit dieser Funktion k”nnen Sie nichtverwaltete Projekte in die Projekt-
    verwaltung aufnehmen. Diese Funktion wird zum Beispiel verwendet, um
    kopierte Projekte in die Projektverwaltung aufzunehmen. Nichtverwaltete
    Projekte werden nicht in der Projekt-Auswahlliste angezeigt und k”nnen
    deshalb auch nicht ge”ffnet oder bearbeitet werden.
    In der durch Aufruf der Funktion <Projekt><Durchsuchen> eingeblendeten
    Dialogbox k”nnen Sie das Verzeichnis (oder Laufwerk) angeben/anw„hlen,
    welches nach nichtverwalteten Projekten durchsucht werden soll:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄDurchsuchenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Pfad         : c:\                           ³
    ³                                              ³
    ³          [@024[    OK    ]]  [  Cancel  ]          ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ



    @024[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.24
    PROJEKTVERWALTUNG

    <PROJEKT><Durchsuchen>:    Best„tigung der Suchpfades mit <OK>

    Nach Anwahl des Best„tigungsfeldes <OK> wird das angegebene/ausgew„hlte
    Verzeichnis (oder Laufwerk) nach Projekten durchsucht, die nicht in der
    Projektverwaltung erfaát sind.
    Wird im angegebenen Verzeichnis (oder Laufwerk) ein nichtverwaltetes
    Projekt gefunden, wird eine Dialogbox mit den Daten dieses Projektes ein-
    geblendet:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄAufnehmenÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@025[Projektname  ]: HALLE_2A             ³ ... Name des gefundenen Projektes
    ³@025[Beschreibung ]: Geb„ude-Automatisier ³ ... Kurzbeschreibung eingeben
    ³@025[RPS_Name     ]: TEST_RPS             ³ ... RPS-Name des gefundenen Projektes
    ³                                    ³
    ³     [@025[    OK    ]]  [  Cancel  ]     ³ ... <OK>:
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     Projekt aufnehmen
                                               <Cancel>:
                                               Abbrechen ohne Aktion

    @025[ Fortsetzung ]                     @010[ Hauptmen ]                      @004[ PROJEKT ]
.25
    PROJEKTVERWALTUNG

    <PROJEKT><Durchsuchen>:    Projektdaten

    Projektname  : Name des nichtverwalteten Projektes.
    Beschreibung : In diesem Eingabefeld k”nnen Sie eine Kurzbeschreibung
                   (max. 30 Zeichen) fr das Projekt eingeben.
    RPS_Name     : Name der Standard-RPS des nichtverwalteten Projektes.

    Durch Anw„hlen des Best„tigungsfeldes <OK> werden diese Daten an das
    Programmierger„t bergeben und das gefundene Projekt in die Projekt-
    verwaltung aufgenommen. Anschlieáend wird der angegebene Suchpfad nach
    weiteren Projekten, die nicht in der Projektverwaltung erfaát sind,
    durchsucht.







    @023[ Projekt aufnehmen ]               @010[ Hauptmen ]                      @004[ PROJEKT ]
.26
    PROJEKTVERWALTUNG

    <ALLGEMEINES>:

    Die PROJEKTVERWALTUNG untersttzt das ARBEITEN MIT MEHREREN PROJEKTEN
    mittels Funktionen, wie dem WECHSELN zwischen Projekten, dem ANLEGEN von
    neuen Projekten, dem ERSTELLEN von Projekt-Kopien zur Datensicherung oder
    dem DUPLIZIEREN von Projekten.
    WECHSEL zwischen PROJEKTVERWALTUNG und PROGRAMMIERSOFTWARE:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»  in die  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    º PROJEKTVERWALTUNG:       º=========>³ PG2000 PROGRAMMIERSOFTWARE  ³
    º mit <Projekt><™ffnen>    º          ³ (PROGRAMMIERUMGEBUNG)       ³
    º                          º          ³ + projektspezifische DATEN  ³
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼          ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿  zurck  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    ³ PROJEKTVERWALTUNG        ³<=========º PG2000 PROGRAMMIERSOFTWARE  º
    ³                          ³   zur    º (PROGRAMMIERUMGEBUNG):      º
    ³                          ³          º mit <Projekt><Schlieáen>    º
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ          ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

                                                                       @004[ PROJEKT ]
.50
    PG2000  HAUPTMENš
  Aktivierung:  D                                                    @051[< WEITER >]
ÉÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
ºDATEIº @051[Bearbeiten]  @052[Objekt]  @053[Projekt]  @054[System]  @055[Einstellungen]  @056[Tools]  @057[Hilfe]       ³
ÈÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@060[ Neu            ]³ ... NEUE Datei erstellen (PL2000, AWL, KOP, usw.)
³@061[ ™ffnen          ]³ ... BESTEHENDE Datei laden
³@062[ Schlieáen  ^F4  ]³ ... OBJEKT (aktives Fenster) schlieáen
³@062[ Sichern         ]³ ... OBJEKT (aktives Fenster) als ASCII-Datei sichern
³@062[ Sichern als     ]³ ... OBJEKT umbenennen und als ASCII-Datei sichern
³@063[ L”schen         ]³ ... PG-OBJEKT ausw„hlen und l”schen
³@064[ Export         ]³ ... PG-OBJEKT ausw„hlen und exportieren
³@075[ Import          ]³ ... EXPORTDATEI ausw„hlen und importieren
³@079[ Import Library  ]³ ... LIBRARY ausw„hlen und importieren
³@085[ Drucken        ]³ ... PG-OBJEKT ausw„hlen und ausdrucken
³ Beenden    F4  ³ ... BEENDEN der PG2000-Programmiersoftware und
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     šBERPRšFEN auf ungespeicherte nderungen

  Dieses Men enth„lt alle DATEI-Funktionen (™ffnen, L”schen, usw.) und Dienste
  fr den EX- und IMPORT von Daten.
                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.51
    PG2000  HAUPTMENš
  Aktivierung:  B                                                    @052[< WEITER >]
ÚÄÄÄÄÄÄÉÍÍÍÍÍÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@050[Datei] ºBEARBEITENº @052[Objekt]  @053[Projekt]  @054[System]  @055[Einstellungen]  @056[Tools]  @057[Hilfe]       ³
ÀÄÄÄÄÄÄÈÍÍÍÍÍÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@100[ Markieren          ]³ ... Markiermodus EIN-/AUSSCHALTEN
³@100[ Ausschneiden  Del ]³ ... Markierten Bereich L™SCHEN und in Ablage KOPIEREN
³@101[ Kopieren      ^Ins ]³ ... Markierten Bereich in Zwischenablage KOPIEREN
³@101[ Einfgen      Ins ]³ ... Zwischenablage in aktives Fenster EINFšGEN
³@102[ L”schen            ]³ ... Markierten Bereich L™SCHEN
³@103[ Suchen             ]³ ... SUCHKRITERIEN definieren und SUCHE starten
³@103[ Weiter        F5  ]³ ... WEITERSUCHEN des Suchmusters im aktiven Fenster
³@104[ Ersetzen           ]³ ... ERSETZEN eines Suchmusters im aktiven Fenster
³@105[ Gehe zu            ]³ ... CURSOR auf eine bestimmte Zeile stellen
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

  Dieses Men umfaát alle globalen EDITOR-Funktionen (Markieren, Kopieren,
  Einfgen, usw.) sowie SUCH-Funktionen und eine Funktion fr die direkte
  SEITENANWAHL.

                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.52
    PG2000  HAUPTMENš
  Aktivierung:  O                                                    @053[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÉÍÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@050[Datei]  @051[Bearbeiten] ºOBJEKTº @053[Projekt]  @054[System]  @055[Einstellungen]  @056[Tools]  @057[Hilfe]       ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÈÍÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@110[ Parameter         ]³ ... OBJEKT-PARAMETER (z.B. Taskparameter) einstellen
³@200[ InitUP            ]³ ... INIT-UP des Objektes erstellen, aufrufen oder l”schen
³@203[ Variable          ]³ ... VARIABLENDEKLARATION des Objektes aufrufen
³@205[ Compile       F9  ]³ ... COMPILIEREN des angew„hlten Objektes
³@208[ Optimieren        ]³ ... OPTIMIEREN des angew„hlten Objektes
³@209[ R U N        F9  ]³ ... COMPILIEREN des Objektes und šBERTRAGEN in RPS
³@211[ Speichern         ]³ ... Objekt auf RPS (Anwender-ROM oder FIX-RAM) SPEICHERN
³@215[ Entfernen         ]³ ... Objekt L™SCHEN (auf RPS / in GDM / Source-Module)
³@217[ Monitor       F11 ]³ ... PV- bzw. KOP-MONITOR fr angew„hltes Objekt aufrufen
³@217[ Debugger     F11 ]³ ... Entsprechenden DEBUGGER fr Objekt aufrufen
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
  Dieses Men umfaát alle ANWENDUNGSSPEZIFISCHEN FUNKTIONEN fr TASKS (INIT-UP,
  Taskparameter, PVs, Compilieren, Download, L”schen, Fehlersuche, usw.).
  DIESE FUNKTIONEN BEZIEHEN SICH AUF DAS AKTIVE FENSTER ODER GDM-SYMBOL !

                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.53
    PG2000  HAUPTMENš
  Aktivierung:  P                                                    @054[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÉÍÍÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@050[Datei]  @051[Bearbeiten]  @052[Objekt] ºPROJEKTº @054[System]  @055[Einstellungen]  @056[Tools]  @057[Hilfe]       ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÈÍÍÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@219[ G D M      F12 ]³ ... GDM-EDITOR aufrufen
³@220[ Vergleich      ]³ ... PROJEKT-VERGLEICH (PG2000-Projekt und RPS-Projekt)
³@221[ Abgleich       ]³ ... UPLOAD aller ben”tigeten Module der RPS
³@225[ Datentyp       ]³ ... PROJEKT-SPEZIFISCHE Datentypen DEFINIEREN/EDITIEREN
³@226[ Konstanten     ]³ ... PROJEKT-SPEZIFISCHE Konstanten DEFINIEREN/EDITIEREN
³@227[ Netzwerk       ]³ ... Konfiguration der NETZWERKANKOPPLUNG
³@229[ Datenbank     ]³ ... DATENBANK-Funktionen (Listen, Libraries l”schen, usw.)
³@246[ S„ubern        ]³ ... SUBERN eines Projektes (Backup-Dateien werden gel”scht)
³@247[ Schlieáen      ]³ ... SCHLIESSEN des aktuellen Projektes / WECHSEL in die
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     PROJEKTVERWALTUNG

  Dieses Men umfaát alle PROJEKTSPEZIFISCHEN Funktionen (GDM, DBK, Datentypen,
  Konstanten, usw. eines Projektes) und den WECHSEL zur PROJEKTVERWALTUNG.


                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.54
    PG2000  HAUPTMENš
  Aktivierung:  S                                                    @055[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÉÍÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@050[Datei]  @051[Bearbeiten]  @052[Objekt]  @053[Projekt] ºSYSTEMº @055[Einstellungen]  @056[Tools]  @057[Hilfe]       ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÈÍÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@250[ Monitor     ]³ ... System-MONITOR (Darstellung/Manipulation der RPS-Objekte)
³@251[ Analyse     ]³ ... System-ANALYSE (RPS-FEHLERLISTE laden und anzeigen)
³@254[ Info        ]³ ... System-INFO (Speicherbedarf, Hochlaufgrund, usw.)
³@259[ RPS Halt    ]³ ... System ANHALTEN (alte Bezeichnung: "RESET")
³@260[ Warmstart   ]³ ... System INITIALISIEREN (alte Bezeichnung: "INIT")
³@261[ Service    ]³ ... SERVICE-Dienste (Kaltstart, Diagnose, usw.)
³@272[ Modem      ]³ ... MODEM-Kommunikation zur RPS
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

  Dieses Men umfaát alle systemspezifischen Funktionen fr:
  - Objektverwaltung (Download, Brennen, usw.)
  - Taskklassen-Manipulation (Start, Stop)
  - Fehleranalyse der letzten RPS-Fehler
  - Service-Dienste (Kaltstart, Diagnose, Speicher l”schen, usw.)

                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.55
    PG2000  HAUPTMENš
  Aktivierung:  E                                                    @056[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÉÍÍÍÍÍÍÍÍÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@050[Datei]  @051[Bearbeiten]  @052[Objekt]  @053[Projekt]  @054[System] ºEINSTELLUNGENº @056[Tools]  @057[Hilfe]       ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÈÍÍÍÍÍÍÍÍÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@273[ Editor         ]³ ... Konfiguration der EDITOREN (PL2000-, AWL-, TEXT-, usw.)
³@283[ Compiler        ]³ ... COMPILER-OPTIONEN einstellen (Rckbersetzung, usw.)
³@297[ On-Line         ]³ ... ONLINE-OPTIONEN (Abtastrate, PV-Anzeige, usw.)
³@301[ Verbindung      ]³ ... VERBINDUNGSDEFINITION und VERBINDUNGSAUF- bzw. -ABBAU
³@304[ Dokumentation   ]³ ... DRUCK-HEADER und DRUCK-OPTIONEN einstellen
³@310[ Sichern         ]³ ... PG-Einstellungen DAUERHAFT sichern
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
  ALLE PG-EINSTELLUNGEN dieses Mens werden erst nach Aufruf der Funktion
  <Einstellungen><Sichern> DAUERHAFT fr das aktuelle Projekt bernommen!
  Mit Hilfe dieses Mens k”nnen alle globalen EDITOR-, COMPILER-, MONITOR-
  und DRUCKEROPTIONEN eingestellt und gespeichert werden.




                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.56
    PG2000  HAUPTMENš
  Aktivierung:  T                                                    @057[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÉÍÍÍÍÍ»ÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@050[Datei]  @051[Bearbeiten]  @052[Objekt]  @053[Projekt]  @054[System]  @055[Einstellungen] ºTOOLSº @057[Hilfe]       ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÈÍÍÍÍÍ¼ÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@311[ Fensterliste     ]³ ... Anzeige der Fensterliste / Fenster wechseln
³@312[ Datei kopieren    ]³ ... PC-Dateien kopieren
³@313[ Datei umbenennen  ]³ ... PC-Dateien umbenennen
³@314[ Wiederherstellen  ]³ ... Wiederherstellen von Tasks aus Backup-Datei
³@316[ Logfile           ]³ ... Informationen zur LOG-Datei / L”schen der LOG-Datei
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

  Dieses Men umfaát Funktionen fr FENSTER- und DATEIVERWALTUNG.







                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.57
    PG2000  HAUPTMENš
  Aktivierung:  H                                                    @050[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÉÍÍÍÍÍ»ÄÄÄÄÄÄ¿
³@050[Datei]  @051[Bearbeiten]  @052[Objekt]  @053[Projekt]  @054[System]  @055[Einstellungen]  @056[Tools] ºHILFEº      ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÈÍÍÍÍÍ¼ÄÄÄÄÄÄÙ
ÚÄÄÄÄÄÄÄÄÄÄÄÄ¿
³@007[ Hilfe  F10 ]³ ... Online-Hilfe fr die PG2000 PROGRAMMIERSOFTWARE
³@318[ Index     ]³ ... Fehlernummern und vom Anwender selbst erstellte Hilfedateien
³@319[ Libraries  ]³ ... Library-Hilfe (Online-Hilfe, Hilfe zu den Funktionen, usw.)
³ PG-Info    ³ ... Informationen zur PG2000 PROGRAMMIERSOFTWARE
ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     (Version, Projektname, verfgbarer Arbeitsspeicher, usw.)

  Dieses Men umfaát INFORMATIONSDIENSTE und HILFETEXTE fr die PG-Bedienung
  und den Funktionsumfang des PGs. Zus„tzlich ist ein HILFE-SYSTEM fr alle
  Library-Funktionen (Standard-Funktionen und vom Anwender erstellte Funktions-
  bl”cke) verfgbar. Diese Library-Hilfe umfaát alle importierten Libraries,
  alle Standard-Funktionen (Funktionsbl”cke und Inline-Funktionen) und die vom
  Anwender selbst erstellten Funktionsbl”cke. Die integrierten Info-, Hilfe-
  und Such-Funktionen steigern die šbersichtlichkeit und untersttzen den
  Anwender bei seiner Suche nach den gewnschten Informationen.

                                    @882[ HM-Bedienung ]                   @001[ šbersicht ]
.60
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Neu>:
    Mit der Men-Funktion <Datei><Neu> k”nnen Sie die Art der zu erstellenden
    (neuen) Datei w„hlen. Entsprechend Ihrer Auswahl wird AUTOMATISCH ein
    Standard-Dateiname vergeben. Auch die im Dateinamen enthaltene Numerierung
    (z.B. "source01.src") wird beim ™ffnen des Editors automatisch vom PG ver-
    geben.
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Kontaktplan        ³    KOP-Editor:        NEUE Datei "source??.src"
    ³ PL2000             ³    PL2000-Editor:     NEUE Datei "source??.src"
    ³ Anweisungsliste    ³    AWL-Editor:        NEUE Datei "source??.src"
    ³ Datenmodul         ³    Datenmodul-Editor: NEUE Datei "data__??.dat"
    ³ Funktionsbaustein ÄÅÄ¿  FUB erstellen:     NEUE Datei "funct_??.fub"
    ³ Text               ³ ³  Text-Editor:       NEUE Datei "noname??.txt"
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                           À´ Kontaktplan      ³  FUB in KOP-Editor erstellen
                            ³ PL2000           ³  FUB in PL2000-Editor erstellen
                            ³ Anweisungsliste  ³  FUB in AWL-Editor erstellen
                            ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.61
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><™ffnen>:
    Mit dieser Funktion k”nnen Sie eine bestehende Datei in den entsprechenden
    Editor laden. Die Auswahl der Datei erfolgt ber eine Dateiauswahlbox.
    Nach der Best„tigung der AUSGEWHLTEN DATEI mit <Enter>, <OK>, oder durch
    einen Mausklick wird diese in den entsprechenden EDITOR geladen.

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄDateienÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Default : c:\projekt.pgp\pgm    ³ <- aktuelles PROJEKTVERZEICHNIS
    ³ Name    : *.*                  ³ <- Hier kann der Dateiname direkt mit
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ³    der Tastatur eingegeben werden.
    ³³ ..\         ³ ³ file1.ext    ³ ³ <- Zwei Auswahllisten:
    ³³ RPS1\       ° ³ file2.ext    ² ³    1. Auswahl von Laufwerk/Verzeichnis
    ³³ A:           ³ file3.ext    ° ³    2. Dateiliste
    ³ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³ file4.ext    ° ³
    ³  [   OK   ]    ³ file5.ext     ³ <- BESTTIGUNGSFELDER:
    ³  [ Cancel ]    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³    <OK>     .. Angew„hlte Datei laden
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <Cancel> .. Abbrechen der Funktion


                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.62
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Schlieáen>:             ^<F4>
    Das aktive Fenster wird geschlossen. Wurden im aktiven Fenster nderungen
    vorgenommen, wird in einer Dialogbox abgefragt, ob diese nderungen ge-
    speichert werden sollen.


    <DATEI><Sichern>:
    Die Daten im aktiven Fenster werden gespeichert (entsprechend dem Editor).
    Vor dem Speichern werden (abh„ngig vom aktuellen Editor) verschiedene 
    šberprfungen durchgefhrt (alle PVs deklariert, Syntax korrekt, usw.).


    <DATEI><Sichern als>:
    Die Datei im aktiven Fenster wird unter dem eingegebenen Namen gespeichert.
    Vor dem Speichern werden (abh„ngig vom aktuellen Editor) verschiedene 
    šberprfungen durchgefhrt (alle PVs deklariert, Syntax korrekt, usw.).



                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.63
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><L”schen>:
    Mit dieser Funktion k”nnen Sie eine Datei l”schen. Die Auswahl der Datei
    erfolgt ber eine Dateiauswahlbox. Vor dem L”schen wird in einer Dialog-
    box abgefragt, ob die Datei tats„chlich gel”scht werden soll.
    Beim L”schen von Source-Dateien (z.B.: "task.src") werden auáerdem ALLE
    Dateien gleichen Namens ("task.*") gel”scht!
    
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄDateienÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Default : c:\projekt.pgp\pgm    ³ <- aktuelles PROJEKTVERZEICHNIS
    ³ Name    : *.*                  ³ <- Hier kann der Dateiname direkt mit
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ³    der Tastatur eingegeben werden.
    ³³ ..\         ³ ³ file1.ext    ³ ³ <- Zwei Auswahllisten:
    ³³ RPS1\       ° ³ file2.ext    ² ³    1. Auswahl von Laufwerk/Verzeichnis
    ³³ A:           ³ file3.ext    ° ³    2. Dateiliste
    ³ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³ file4.ext    ° ³
    ³  [   OK   ]    ³ file5.ext     ³ <- BESTTIGUNGSFELDER:
    ³  [ Cancel ]    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³    <OK>     .. Angew„hlte Datei l”schen
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <Cancel> .. Abbrechen der Funktion

                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.64
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Export>:
    Mit Hilfe der Funktion <Datei><Export> k”nnen einzelne Objekte oder kom-
    plette Projekte in Form von Dateien exportiert werden (zur Datensicherung,
    zum Transferieren auf einen anderen PC oder in ein anderes Projekt).
    ÚÄÄÄÄÄÄÄÄÄÄ¿
    ³ @065[Objekt  ] ³ ... Exportieren eines Objektes
    ³ @066[Projekt ] ³ ... Exportieren eines Projektes
    ÀÄÄÄÄÄÄÄÄÄÄÙ











                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.65
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Export><Objekt>:
    ÚÄÄÄÄÄÄÄÄÄExport ObjektÄÄÄÄÄÄÄÄÄ¿
    ³Quelle:                        ³ ... QUELLOBJEKT:
    ³   RPS/CPU      : TEST_RPS:CP ³ ... RPS- und Prozessor-Name
    ³   Typ          : Task        ³ ... Objekt-Typ
    ³   Objekt       : visual_1    ³ ... Auswahl des Objektes/der Objekte
    ³Ziel:                          ³ ... ZIELPROJEKT:
    ³   Format       : XPE         ³ ... Export-Format
    ³   Verzeichnis  :              ³
    ³   C:\PROJEKTE\HALLE_2A.PGP\P ³ ... Zielpfad fr die Exportdatei
    ³   Name         : visual_1     ³ ... Name der Exportdatei
    ³                               ³
    ³    [   OK   ]  [ Cancel ]     ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ





    @064[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.66
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Export><Projekt>:
    ÚÄÄÄÄÄÄÄÄÄExport ProjektÄÄÄÄÄÄÄÄ¿
    ³Quelle:                        ³ ... QUELLOBJEKT:
    ³   RPS/CPU      : TEST_RPS:CP ³ ... RPS- und Prozessor-Name
    ³                               ³
    ³   Libraries    : -alle-      ³ ... Libraries, die exportiert werden
    ³   Datenmodul   : -alle-      ³ ... Datenmodule, die exportiert werden
    ³   Erweiterungen: -alle-      ³ ... Systemmodule, die exportiert werden
    ³   Tasks        : -alle-      ³ ... Tasks, die exportiert werden
    ³                               ³
    ³Ziel:                          ³ ... ZIELPROJEKT:
    ³   Format       : XPE         ³ ... Export-Format
    ³   Verzeichnis  :              ³
    ³   C:\PROJEKTE\HALLE_2A.PGP\P ³ ... Zielpfad fr die Exportdatei
    ³   Name         : halle_1a     ³ ... Name der Exportdatei
    ³                               ³
    ³    [   OK   ]  [ Cancel ]     ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @064[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.75
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Import>:
    Dateien, die mit der PG-Funktion <Datei><Export> exportiert wurden, k”nnen
    mit <Datei><Import> in das aktuelle PG-Projekt importiert werden.
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄImportÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Quelle:                        ³ ... QUELLOBJEKT:
    ³   Verzeichnis  :              ³
    ³   C:\PROJEKTE\HALLE_1A.PGP\   ³ ... Quellpfad der Exportdatei
    ³   Name         : visual_1.xpe ³ ... Name der Exportdatei
    ³   Inhalt       : Task         ³ ... Inhalt der Exportdatei
    ³                               ³
    ³Ziel:                          ³ ... ZIELPROJEKT:
    ³   RPS/CPU      : TEST_RPS:CP ³ ... RPS- und Prozessor-Name
    ³                               ³
    ³Optionen:                      ³
    ³   Existierende : Nachfragen  ³
    ³                               ³
    ³    [   OK   ]  [ Cancel ]     ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.79
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Import Library>:
    Bevor Sie die Funktionsbl”cke und/oder Inline-Funktionen einer Library in
    Ihrem Projekt verwenden k”nnen, mssen Sie die Library in die entsprechende
    Projekt-Datenbank importieren (einbinden). Dazu steht Ihnen die Funktion
    <Datei><Import Library> zur Verfgung. Nach Aufruf dieser Funktion werden
    in einer Dateiauswahlbox alle unter dem Pfad C:\PG2000\LIBRARY\ abgelegten
    *.SLB-Dateien (Libraries) angezeigt. Nach Auswahl der gewnschten Library
    wird diese in die aktuelle Projekt-Datenbank importiert. Die entsprechende
    *.SLB-Datei wird im Verzeichnis fr Standard-Libraries des aktuellen
    Projektes (<Projektname>.PGP\DBK\LIBFILES) abgelegt. In der Meldungszeile
    wird der schrittweise Import der einzelnen Funktionen, die die Library
    enth„lt, angezeigt.

    Die System-Library wird bereits beim Erzeugen eines Projektes automatisch
    in die Datenbank importiert (eingebunden). D.h. die Funktionen der System-
    Library sind sofort nach der Projekt-Erzeugung verfgbar.



                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.85
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Drucken>:
    Ausdrucken von projektspezifischen Dateien:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Editor             ³ ... Ausdrucken des Editor-Inhalts
    ³@086[ Datei              ]³ ... Ausdrucken einer ASCII-Datei
    ³@087[ Projekt            ]³ ... Ausdrucken des gesamten Projektes
    ³@090[ Modulbeschriftung  ]³ ... Ausdrucken von Modul-Beschriftungsstreifen
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    * Auswahl des Druckers und die Einstellung der Drucker-Optionen werden
      mittels der Men-Funktion <Einstellungen><Dokumentation> vorgenommen.
    * Das Format des Ausdrucks wird ebenfalls der Men-Funktion <Einstellungen>
      <Dokumentation> festgelegt.





                                      @050[ HM-Datei ]                     @001[ šbersicht ]
.86
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Drucken><Datei>:

    Beim Ausdrucken von ASCII-Dateien k”nnen zus„tzlich zu den (mit Hilfe der
    Funktion <Einstellungen><Dokumentation>) definierten Druckereinstellungen
    objektspezifische Einstellungen festgelegt werden:

    ÚÄÄÄÄÄÄÄÄÄÄObjektÄÄÄÄÄÄÄÄÄÄ¿
    ³Beschreibung :     Ja    ³ ... Zugeordnete Textdateien ausdrucken Ja/Nein
    ³Parameter    :     Ja    ³ ... Taskparameter ausdrucken Ja/Nein
    ³Variablen    :     Ja    ³ ... Variabendeklaration ausdrucken Ja/Nein
    ³Datentypen   :     Nein  ³ ... Datentyp-Liste ausdrucken Ja/Nein
    ³                          ³
    ³  [   OK   ]  [ Cancel ]  ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ





    @085[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.87
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Drucken><Projekt>:
    Beim Ausdrucken eines Projektes k”nnen zus„tzlich zu den (mit Hilfe der
    Funktion <Einstellungen><Dokumentation>) definierten Druckereinstellungen
    projektspezifische Einstellungen festgelegt werden:

    ÚÄÄÄÄÄÄÄÄÄÄÄProjektÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@088[Inhaltsverzeichnis ]:     Ja    ³
    ³@088[GDM                ]:     Ja    ³
    ³@088[Konfiguration      ]:     Ja    ³
    ³@088[Globale Variable   ]:     Ja    ³
    ³@088[Datentypen         ]:     Ja    ³
    ³@089[Cross-Referenz     ]:     Ja    ³
    ³Objekte                         ³
    ³      @089[Beschreibung ]:     Nein  ³
    ³      @089[Parameter    ]:     Nein  ³
    ³      @089[Variablen    ]:     Nein  ³
    ³      @089[Datentypen   ]:     Nein  ³
    ³     [   OK   ]  [ Cancel ]     ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    @085[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.88
    PG2000  HAUPTMENš FUNKTIONEN

    <Datei><Drucken><Projekt><Inhaltsverzeichnis>:
    Drucken eines gegliederten Projekt-Inhaltsverzeichnisses (Ja/Nein).

    <Datei><Drucken><Projekt><GDM>:
    Drucken der Projekt-GDM (Ja/Nein).

    <Datei><Drucken><Projekt><Konfiguration>:
    Drucken der zum Projekt geh”renden Systemkonfiguration (Ja/Nein). D.h. wenn
    Sie bei diesem Eintrag "Ja" w„hlen, werden auch alle Einstellungen des RPS-
    Konfigurationsmoduls (auch "$$sysconf"-Modul genannt) ausgedruckt.

    <Datei><Drucken><Projekt><Globale Variable>:
    Drucken der Variabendeklaration fr das ganze Projekt (Ja/Nein).
    
    <Datei><Drucken><Projekt><Datentypen>:
    Drucken der Datentyp-Liste fr das ganze Projekt (Ja/Nein).



    @087[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.89
    PG2000  HAUPTMENš FUNKTIONEN

    <Datei><Drucken><Projekt><Cross-Referenz>:
    Drucken einer Querverweis-Liste (Ja/Nein).

    <Datei><Drucken><Projekt><Objekt><Beschreibung>:
    Textdateien fr jede Task einzelnen ausdrucken (Ja/Nein).

    <Datei><Drucken><Projekt><Objekt><Parameter>:
    Taskparameter fr jede Task einzelnen ausdrucken (Ja/Nein).

    <Datei><Drucken><Projekt><Objekt><Variablen>:
    Variabendeklaration fr jede Task einzelnen ausdrucken (Ja/Nein).

    <Datei><Drucken><Projekt><Objekt><Datentypen>:
    Datentyp-Liste fr jede Task einzelnen ausdrucken (Ja/Nein).





    @087[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.90
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Drucken><Modulbeschriftung>:
    Mit dieser Funktion k”nnen Modul-Beschriftungsstreifen fr E/A-Module (mit
    namentlichen E/A-Anschlssen) erstellt werden. Der Ausdruck kann als MODUL-
    EINSCHUBSTREIFEN (KLEMMENBELEGUNG) eingesetzt werden.
    Nach Aufruf der Funktion <Datei><Drucken><Modulbeschriftung> wird folgendes
    Pop-Up-Men eingeblendet:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ 2010-I/O    ³  ... 2010-Ein-/Ausgangsmodul
    ³ REMOTE-I/O ³  ... REMOTE-Ein-/Ausgangsmodul
    ³ 2005-I/O    ³  ... 2005-Ein-/Ausgangsmodul
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Nach Auswahl des Hardware-Typs k”nnen Sie in einer Dialogbox die Modul-
    nummer (Eintrag "Modulnr."), den Modultyp (Eintrag "Modulmode") sowie
    alle weiteren notwendigen Daten (Eintrag "Rckwand" bei 2005-I/O, Ein-
    tr„ge "Station" und "Masternr." bei REMOTE-I/O) eingeben.


    @091[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.91
    PG2000  HAUPTMENš FUNKTIONEN

    <DATEI><Drucken><Modulbeschriftung>:
    Anhand der Modulnummer werden alle im Projekt definierten E/A-Anschlsse
    des Moduls ermittelt. Anschlieáend werden die gefundenen PV-NAMEN an der
    entsprechenden Position am BESCHRIFTUNGSSTREIFEN eingetragen.
    Das Format der Modulbeschriftung (Maske) ist exakt an den ausgew„hlten
    Modultyp angepaát. (Bei der PG-Installation werden alle vorhandenen Modul-
    Masken "*.msk" im Verzeichnis "PG2000.EXE\MASKEN" abgelegt und stehen
    somit zur Verfgung.)
    Der Ausdruck erfolgt in die Datei "mxxx.hbs" (wobei "xxx" der Modulnummer
    entspricht) und wird im Verzeichnis "<Projektname>.PGP\PGM\LIST.TXT" ge-
    speichert.








    @085[ Fortsetzung ]                     @050[ HM-Datei ]                     @001[ šbersicht ]
.100
    PG2000  HAUPTMENš FUNKTIONEN

    <BEARBEITEN><Markieren>:
    Mit dieser Funktion wird im aktiven Fenster der Markiermodus EIN- bzw.
    AUSGESCHALTET.
    Ist der Markiermodus eingeschaltet, kann mit den Cursor-Tasten der ge-
    wnschte Bereich markiert werden.


    <BEARBEITEN><Ausschneiden>:     <DEL>
    Der MARKIERTE BEREICH im aktiven Fenster wird GEL™SCHT und in die ZWISCHEN-
    ABLAGE kopiert. Der Markiermodus wird dabei automatisch ausgeschaltet.
    Bei graphischen Editoren (GDM, KOP, usw.) wird diese Funktion haupts„chlich
    zum VERSCHIEBEN von Editor-Bereichen verwendet.







                                    @051[ HM-Bearbeiten ]                  @001[ šbersicht ]
.101
    PG2000  HAUPTMENš FUNKTIONEN

    <BEARBEITEN><Kopieren>:         ^<INS>
    Der MARKIERTE BEREICH im aktiven Fenster wird in die ZWISCHENABLAGE
    KOPIERT. Der Markiermodus wird dabei automatisch ausgeschaltet.
    Der in die Zwischenablage kopierte Bereich kann auch in ein anderes
    Fenster eingefgt werden (nur bei gleichen Fenstertypen sinnvoll).


    <BEARBEITEN><Einfgen>:         <INS>
    Der in der Zwischenablage gespeicherte Bereich kann mit dieser Funktion
    an der AKTUELLEN Cursor-Position EINGEFšGT werden. Der Inhalt der Zwischen-
    ablage bleibt dabei erhalten, d.h. der Vorgang kann beliebig oft wiederholt
    werden.
    Vor dem Einfgen wird eine interne šberprfung der Datenkompatibilit„t
    durchgefhrt, d.h. graphische Bereiche aus KOP-Editoren k”nnen nur in
    KOP-Editoren eingefgt werden. Textuelle Bereiche k”nnen in fast allen
    Text-Editoren (PL2000-, AWL-, Text-, Daten-Editor) eingefgt werden.



                                    @051[ HM-Bearbeiten ]                  @001[ šbersicht ]
.102
    PG2000  HAUPTMENš FUNKTIONEN

    <BEARBEITEN><L”schen>:
    Der MARKIERTE BEREICH im aktiven Fenster wird GEL™SCHT.
    Der gel”schte Bereich wird NICHT in die Zwischenablage kopiert.
















                                    @051[ HM-Bearbeiten ]                  @001[ šbersicht ]
.103
    PG2000  HAUPTMENš FUNKTIONEN

    <BEARBEITEN><Suchen>:
    In einer Dialogbox k”nnen Sie das SUCHMUSTER (Text, Symbol, bestimmte
    Angaben der Variablendeklaration, usw.), die SUCHRICHTUNG (Vor, Zurck)
    und die STARTPOSITION des Suchvorgangs (Cursor, Anfang, Ende) angegeben.
    Beim Suchmuster wird zwischen Groá- und Kleinschreibung unterschieden.
    Falls das Suchmuster nicht gefunden wird, erscheint eine entsprechende
    Meldung.


    <BEARBEITEN><Weiter>:           <F5>
    Die Suche nach dem Suchmuster, welches beim letzten Such-Befehl (Funktion
    <Bearbeiten><Suchen>, siehe oben) definiert wurde, wird ab der AKTUELLEN
    Cursor-Position fortgesetzt.
    Wird das Suchmuster nicht gefunden, erscheint eine entsprechende Meldung.





                                    @051[ HM-Bearbeiten ]                  @001[ šbersicht ]
.104
    PG2000  HAUPTMENš FUNKTIONEN

    <BEARBEITEN><Ersetzen>:
    Mit dieser Funktion wird im aktiven Editor ein bestimmtes SUCHMUSTER
    (Text, Symbol, bestimmte Angaben der Variablendeklaration, usw.) gesucht
    und durch ein ERSATZMUSTER ersetzt. In der Dialogbox, die durch diese
    Funktion aufgerufen wird, k”nnen Sie sowohl Such- als auch Ersatzmuster
    eingegeben. Die SUCHRICHTUNG (Vor, Zurck) und STARTPOSITION des Such-
    vorgangs (Cursor, Anfang, Ende) wird ebenfalls in dieser Dialogbox ein-
    gegeben.
    Wurde das Suchmusters im aktiven Fenster gefunden, bietet das PG in der
    Meldungszeile folgende M”glichkeiten an:
    (J)a   ... Gefundenes Suchmuster wird durch das Ersatzmuster ersetzt.
               Die Suche wird in der gew„hlten Richtung fortgesetzt.
    (N)ein ... Gefundenes Suchmuster wird NICHT ersetzt. Die Suche wird in
               der gew„hlten Richtung fortgesetzt.
    (A)lle ... Alle in der gew„hlten Richtung vorhandenen Suchmuster werden
               ohne Rckfrage ersetzt.
    (E)nde ... Die Funktion <Bearbeiten><Ersetzen> wird abgebrochen.


                                    @051[ HM-Bearbeiten ]                  @001[ šbersicht ]
.105
    PG2000  HAUPTMENš FUNKTIONEN

    <BEARBEITEN><Gehe zu>:
    Der Cursor kann durch Eingabe einer ZEILENNUMMER zu einer bestimmten Zeile
    bewegt werden.
    Treten beim Compilieren Fehler auf, werden diese im Meldungsfenster proto-
    kolliert. Zu jeder Fehlermeldung wird die Zeilennummer, in der der Fehler
    aufgetreten ist, angegeben. Mit der Funktion <Bearbeiten><Gehe zu> k”nnen
    Sie direkt zur fehlerhaften Zeile springen und den Fehler korrigieren.












                                    @051[ HM-Bearbeiten ]                  @001[ šbersicht ]
.110
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter>:
    Diese Funktion bezieht sich auf das markierte GDM-SYMBOL im AKTIVEN FENSTER
    bzw. auf den ENTSPRECHENDEN EDITOR-TYP !
    Abh„ngig vom Objekt-Typ wird eine entsprechende Dialogbox ge”ffnet, in der
    die Objekt-Parameter eingestellt oder editiert werden k”nnen:

    * @111[Zyklische Task         ] : TC#x- oder HS#x-Symbol (PL2000/AWL/KOP-Editor)
    * @130[Interrupttask          ] : IRQ-Symbol (PL2000/AWL/KOP-Editor)
    * @133[Exceptiontask          ] : EXC-Symbol (PL2000/AWL/KOP-Editor)
    * @138[Datenmodul             ] : DAT-Symbol
    * @139[B&R Modul / Systemmodul] : B&R-Symbol
    * @910[Konfiguration          ] : "sysconf"-Symbol

    Enth„lt das aktive Fenster den Source-Code eines FUBs, wird der FUB-EDITOR
    ge”ffnet. Im FUB-Editor kann die FUB-MASKE erstellt bzw. editiert werden. 

    * @708[FUB-Editor             ] : Aktivierung aus FUB-Source


                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.111
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr zyklische Tasks:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ<Taskname>ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ <- Im Rahmen der Dialogbox
    ³@113[Objekt Art         ]: PL2000         Task ³    wird der Name der Task
    ³@113[Init Routine       ]:                      ³    anzeigt
    ³@114[Resource           ]: TC4                 ³
    ³@115[Start (Irq/Exc) Nr.]: 0                    ³
    ³@116[Transfer Mode      ]: Default             ³
    ³@120[Installation       ]: Default             ³
    ³@121[Start              ]: Default             ³
    ³@122[Spoolen            ]: Default             ³
    ³@123[Rebuild Info       ]: Default             ³
    ³@124[Zielspeicher       ]: USR-ROM             ³
    ³@125[Range Check        ]: Default             ³
    ³@126[Check adr(0)       ]: Default             ³
    ³@127[Opt. I/O Emulation ]: Default             ³
    ³                                          ³
    ³                   ...                    ³

    @112[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.112
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr zyklische Tasks (Fortsetzung):

    ³                   ...                    ³
    ³                                          ³
    ³@128[Modul Info    ]: Version: **.**.*          ³
    ³                Datum  : *****            ³
    ³                L„nge  : ***** Byte       ³
    ³@128[Speicher lokal]: Analog : 0     Byte       ³
    ³                Digital: 0     Bit        ³
    ³    [@128[   OK   ]]  [@128[  Text  ]]  [@128[ Cancel ]]    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ








    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.113
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Objekt Art>:
    ANZEIGE der Programmiersprache (Kontaktplan, PL2000 oder Anweisungsliste)
    des Source-Programms. Die Programmiersprache wird beim Setzen eines neuen
    GDM-Symbols definiert.
    Die Programmiersprache des Source-Programms ("Objekt Art") kann nachtr„g-
    lich nicht mehr ge„ndert werden!


    <OBJEKT><Parameter><Init Routine>:
    ANZEIGE der Programmiersprache (Kontaktplan, PL2000 oder Anweisungsliste)
    des Initialisierungs-Unterprogramms. Das INIT-UP k”nnte theoretisch in
    einer anderen Programmiersprache als das Source-Programm erstellt werden.
    Die Programmiersprache des INIT-UPs ("Init Routine") kann nachtr„glich
    nicht mehr ge„ndert werden!
    Falls KEIN INIT-UP vorhanden ist, wird ein LEEREINTRAG ANGEZEIGT.




    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.114
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Resource>:
    Dieses Feld zeigt die Taskklasse der Task an. Standardm„áig wird die TC4
    (normale Taskklasse 4) zugeordnet. Die gewnschte Taskklasse kann ber
    ein Popup-Fenster ausgew„hlt werden. Taskklassen-Standardkonfiguration:

    ³ Taskklasse ³ Zykluszeit ³ erlaubte Zykluszeitverletzung ³
    ÃÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
    ³     TC1    ³     10ms   ³              20ms             ³
    ³     TC2    ³     50ms   ³              50ms             ³
    ³     TC3    ³    100ms   ³             100ms             ³
    ³     TC4    ³     10ms   ³           30000ms             ³
    ³     HS1    ³   3000æs   ³             KEINE             ³
    ³     HS2    ³   5000æs   ³             KEINE             ³
    ³     HS3    ³   7000æs   ³             KEINE             ³
    ³     HS4    ³   9000æs   ³             KEINE             ³

    Anstatt der hier angegebenen Taskklassen k”nnen Sie auch die Kennnug fr
    @130[INTERRUPTTASKS] ("IRQ") oder @133[EXCEPTIONTASKS] ("EXC") eingetragen.

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.115
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Start (Irq/Exc) Nr.>:
    Die Start-Nummer bestimmt, in welcher Reihenfolge die Tasks auf der RPS
    gestartet werden (Hochlaufverhalten der RPS). D.h. mit der Start-Nummer
    k”nnen Sie die Ablaufreihenfolge der Tasks innerhalb einer Taskklasse
    festlegen. Der Zahlenwert kann zwischen 0 und 128 liegen und wird wie
    folgt interpretiert:

    - Tasks mit den niedrigeren Nummern werden zuerst abgearbeitet.
    - Tasks mit gleicher Nummer werden in die Reihenfolge der anderen Tasks
      eingeordnet. Die Reihenfolge untereinander ist jedoch nicht festgelegt.
    - Tasks mit der Nummer 0 werden irgendwann innerhalb eines Zyklus abge-
      arbeitet.

    Standardm„áig ist der Wert 0 eingetragen.
    Fr EXCEPTIONTASKS (EXC-Symbol) wird an dieser Stelle die entsprechende
    Exception-Nummer eingetragen.
    Fr INTERRUPTTASKS (IRQ-Symbol) wird die Modulnummer (Moduladresse des
    Moduls, das den Interrupt generiert) eingetragen.

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.116
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Transfer Mode>:
    Default:  Die Einstellung der Compiler-Option "Transfer Mode" wird
              bernommen. Die Compiler-Optionen werden mit der Funktion
              <Einstellungen><Compiler> definiert.
    @118[Exchange]: Tasks, die bereits auf der RPS vorhanden sind, werden zun„chst
              auf der RPS gel”scht und erst dann bertragen. 
    @119[Update  ]: Tasks, die bereits auf der RPS vorhanden sind, werden beim šber-
              tragen stoáfrei ausgetauscht. D.h. die Task wird nicht gel”scht,
              sondern einfach berschrieben. Der šbertragungsmodus Update ist
              jedoch nicht immer m”glich. Wurden zum Beispiel nderungen in
              der Speicherbelegung der Task vorgenommen, kann die šbertragung
              nicht im Modus Update durchgefhrt werden. In diesem Fall wird
              der šbertragungsvorgang abgebrochen und es erscheint eine ent-
              sprechende Meldung im Meldungsfenster. Die šbertragung kann dann
              nur im Modus Exchange durchgefhrt werden.

    Vor dem Einstellen des šbertragungsmodus k”nnen Sie mit der Funktion
    <Projekt><Vergleich> das aktuelle PG2000-Projekt und das RPS-Projekt

    @117[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.117
    PG2000  HAUPTMENš FUNKTIONEN

    (Projekt, das auf der RPS l„uft) miteinander vergleichen. Dabei werden
    alle Unterschiede bei den B&R-Modulen und in der Variablendeklaration
    gemeldet. Das Ergebnis dieses Vergleichs kann Ihnen bei der Auswahl des
    korrekten šbertragungsmodus helfen.















    @110[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.118
    PG2000  HAUPTMENš FUNKTIONEN

    Exchange:
               ³
    System-    ³     10 ms
    zyklus     ³<------------>³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
               ³   (3)        ³               ³               ³           Zeit
               ³<------->³    ³               ³               ³
    Zyklisches ÃÄÄÄÄÄÂÄÄÄ´    ÃÄÄÄÄÄ¿         ÃÄÄÄÄÄÂÄÄÄ¿     ÃÄÄÄÄÄÂÄÄÄ¿
    System     ³     ³(1)³    ³     ³         ³     ³(2)³     ³     ³(2)³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÅÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÁÄÄÁÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÁÄÄÄÄ>
               ³         ³    ³     ³                 ³
               ³         ÃÄÂÄÄ´     ÃÄÄÄÄÄ¿           ³
    Restzeit   ³         ³ ³°°³     ³°°°°°ÃÄÄÄÄÄÄÄÄÄÄÄÙ
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÁÅÁÅÄÅÄÄÄÄÄÁÅÄÄÄÄÁÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
                          ³ ³        ³
    (1) ... alte Task     ³ ÀÄÄÄÄÄÄÄÄÁÄ šbertragung der neuen Task auf die RPS
    (2) ... neue Task     ÀÄÄÄÄÄÄÄÄÄÄÄÄ L”schen der alten Task       
    (3) ... zyklische Tasks

    @116[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.119
    PG2000  HAUPTMENš FUNKTIONEN

    Update: (stoáfreier Austausch einer Task)
               ³
    System-    ³     10 ms
    zyklus     ³<------------>³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
               ³   (3)        ³               ³               ³           Zeit
               ³<------->³    ³               ³               ³
    Zyklisches ÃÄÄÄÄÄÂÄÄÄ´    ÃÄÄÄÄÄÂÄÄÄ¿     ÃÄÄÄÄÄÂÄÄÄ¿     ÃÄÄÄÄÄÂÄÄÄ¿
    System     ³     ³(1)³    ³     ³(1)³     ³     ³(2)³     ³     ³(2)³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÅÄÄÄÄÅÄÄÄÄÄÁÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÁÄÄÁÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÁÄÄÄÄ>
               ³         ³    ³         ³             ³
               ³         ÃÄÄÄÄ´         ÃÄÄÄ¿         ³
    Restzeit   ³         ³°°°°³         ³°°°ÃÄÄÄÄÄÄÄÄÄÙ
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÁÄÅÄÄÅÄÄÄÄÄÄÄÄÄÁÄÅÄÁÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
                           ³              ³
    (1) ... alte Task      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄ šbertragung der neuen
    (2) ... neue Task                          Task auf die RPS
    (3) ... zyklische Tasks

    @116[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.120
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Installation>:
    Default:            Bei dieser Einstellung wird das lauff„hige Modul der
                        Task nach jedem Download oder Boot-Vorgang auto-
                        matisch auf der RPS installiert. D.h., daá die Task
                        abh„ngig vom Eintrag im Eingabefeld <Start> entweder
                        automatisch oder manuell (vom Anwender) gestartet
                        wird.
    nicht Installieren: Bei dieser Einstellung befindet sich die Task nach
                        dem Download auf der RPS, ist aber nicht lauff„hig.
                        D.h. die Task muá vom Anwender nach jedem Download
                        oder Boot-Vorgang mit der Funktion <Install> des RPS-
                        Monitors manuell installiert werden. Vorgangsweise:
                           1. Funktion <System><Monitor> aufrufen
                           2. Taskklasse mit der <ENTER>-Taste anw„hlen
                           3. Task anw„hlen und <SPACE>-Taste bet„tigen
                           4. Funktion <Install> aufrufen
                        Erst dann ist es m”glich, die Task mit der Funktion
                        <Neustart> des RPS-Monitors manuell zu starten.

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.121
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Start>:
    Default:       Bei dieser Einstellung wird die Task nach jedem Download
                   oder Boot-Vorgang automatisch gestartet. Dies gilt jedoch
                   nur, wenn im Eingabefeld <Installation> der Eintrag
                   "Default" definiert ist.
    nicht Starten: Bei dieser Einstellung muá der Anwender die Task nach
                   jedem Download oder Boot-Vorgang mit der Funktion
                   <Neustart> des RPS-Monitors manuell starten.
                   Vorgangsweise:
                      1. Funktion <System><Monitor> aufrufen
                      2. Taskklasse mit der <ENTER>-Taste anw„hlen
                      3. Task anw„hlen und <SPACE>-Taste bet„tigen
                      4. Funktion <Neustart> aufrufen
                   Die Einstellung "nicht Starten" wird meist fr Testzwecke
                   verwendet.




    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.122
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Spoolen>:
    Default:       Die Einstellung der Compiler-Option "Spoolen" wird ber-
                   nommen. Die Compiler-Optionen werden mit der Funktion
                   <Einstellungen><Compiler> definiert.
    bei Download:  Die Task wird SOFORT nach dem šbertragen (Download) zum
                   spezifizierten IO-Prozessor (Steckplatz bekannt) gespoolt.
    bei Warmstart: Die Task wird zun„chst in die CPU bertragen. Der Spool-
                   vorgang von der CPU an den IO-Prozessor erfolgt erst nach
                   einem Warmstart (siehe dazu Funktion <System><Warmstart>).
    nie Spoolen:   Bei dieser Einstellung wird die Task zwar als nicht zu
                   installieren gekennzeichnet, allerdings wird die Task weder
                   nach dem šbertragen (Download) noch bei einem Warmstart zum
                   IO-Prozessor gespoolt. Damit ist es m”glich, ausschlieálich
                   mit den Funktionen der I/O-Prozessor-Library (Spool-Library)
                   zu arbeiten.




    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.123
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Rebuild Info>:
    Default: Die Einstellung der Compiler-Option "Rebuild Info" wird
             bernommen. Die Compiler-Optionen werden mit der Funktion
             <Einstellungen><Compiler> definiert.
    Ein:     Aus einem lauff„higen Modul (*.BR-Module), das mit dieser
             Einstellung erzeugt wurde, kann durch Rckbersetzen mit der
             Funktion <Rebuild> (siehe Popup-Men der Task-Ebene) oder durch
             einen <Projekt><Abgleich> die komplette Quellinformation
             (Source-Code, INIT-UP, Taskparameter, VD, usw.) wiedergewonnen
             werden.
    Aus:     Beim Compilieren wird im lauff„higen Modul (*.BR-Module) keine
             Rckbersetzungsinformation abgelegt, d.h. die mit dieser Ein-
             stellung erzeugten Module k”nnen nicht rckbersetzt werden.






    @110[ Objekt-Paramter ]                 @052[ HM-Objekt ]                    @001[ šbersicht ]
.124
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Zielspeicher>:
    USR-ROM: Ist als Zielspeicher "USR-ROM" eingestellt, wird das Objekt
             beim Ausfhren der Funktion <Objekt><Speichern> ins Anwender-
             ROM programmiert.
    FIX-RAM: Ist als Zielspeicher "FIX-RAM" eingestellt, wird das Objekt
             beim Ausfhren der Funktion <Objekt><Speichern> ins FIX-RAM
             programmiert.
    MEM-CRD: Ist als Zielspeicher "MEM-CRD" eingestellt, wird das Objekt
             beim Ausfhren der Funktion <Objekt><Speichern> auf die PCMCIA-
             Speicherkarte (auch "MEM-Card" genannt) bertragen.









    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.125
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Range Check>:
    Default: Die Einstellung der Compiler-Option "Range Check" wird bernommen.
             Die Compiler-Optionen werden mit der Funktion <Einstellungen>
             <Compiler> definiert.
    Ein:     Bei Tasks, die mit der Option <Range Check>=Ein compiliert wurden,
             wird w„hrend der Laufzeit jeder Feld-Zugriff berwacht. Erkennt
             die CPU einen Zugriff auf ein nichtexistierendes Element einer
             Feldvariable, wird die Exception "Range Overflow" ausgel”st.
    Aus:     Feld-Zugriffe werden NICHT berwacht.










    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.126
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Check adr(0)>:
    Default: Die Einstellung der Compiler-Option "Check adr(0)" wird
             bernommen. Die Compiler-Optionen werden mit der Funktion
             <Einstellungen><Compiler> definiert.
    Ein:     Bei Tasks, die mit der Option <Check adr(0)>=ein compiliert
             wurden, wird w„hrend der Laufzeit jeder Zeiger-Zugriff ber-
             wacht. Erkennt die Zentraleinheit einen Zugriff auf einen
             nichtinitialisierten Zeiger (Adresse=0), wird die Exception
             "Null Pointer" ausgel”st.
    Aus:     Zeiger-Zugriffe werden NICHT berwacht.









    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.127
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Opt. I/O Emulation>:
    Beim Steuerungssystem B&R 2005 sind System- und I/O-Bus der Basisrckwand
    nicht getrennt. Das bedeutet, daá auf der Basisrckwand kein I/O-Prozessor
    arbeitet. Die Funktion des I/O-Prozessors wird bei diesem System durch die
    Software emuliert (I/O-Emulation).
    Ab Version 1.92 des Betriebssystems RPSSW ist eine zweite (optimierte) Form
    der I/O-Emulation m”glich. Bei der optimierten I/O-Emulation wird fr die
    Basisrckwand des Systems B&R 2005 anstelle der I/O-Schaufeltabelle ein
    Code zum Kopieren der I/O-Punkte erzeugt. Dieser Code wird nicht vom PG,
    sondern vom Betriebssystem generiert (w„hrend der Installation der Task).
    Die optimierte Form der I/O-Emulation verbraucht weniger CPU-Zeit als die
    herk”mmliche I/O-Emulation (I/O-Emulation mit I/O-Schaufeltabelle).

    Default: Die Einstellung der Compiler-Option "I/O-Emulation" wird
             bernommen. Die Compiler-Optionen werden mit der Funktion
             <Einstellungen><Compiler> definiert.
    Ein:     Die optimierte I/O-Emulation wird verwendet.
    Aus:     Die herk”mmliche I/O-Emulation wird verwendet.

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.128
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Modul Info>:
    Version: Anzeige der Modul-Version.
    Datum:   Anzeige des Erzeugungsdatums und der -uhrzeit.
    L„nge:   Anzeige der Modul-L„nge.

    <OBJEKT><Parameter><Speicher lokal>:
    Analog:  Gr”áe des lokalen Analogspeichers, den diese Task belegt
             (in Byte).
    Digital: Gr”áe des lokalen Digitalspeichers, den diese Task belegt
             (in Bit).

    <OK> ....... Best„tigen der angezeigten/ver„nderten Taskparameter.
    <Text> ..... Texteingabefeld fr Task-Beschreibung ”ffnen.
    <Cancel> ... Abbrechen ohne šbernahme der Taskparameter.





    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.130
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr Interrupttasks:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ<Taskname>ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ <- Im Rahmen der Dialogbox
    ³@113[Objekt Art         ]: PL2000         Task ³    wird der Name der IRQ-
    ³@113[Init Routine       ]:                      ³    Task anzeigt
    ³@132[Resource           ]: IRQ                 ³
    ³@132[Start (Irq/Exc) Nr.]: 3                    ³
    ³@116[Transfer Mode      ]: Default             ³
    ³@120[Installation       ]: Default             ³
    ³@121[Start              ]: Default             ³
    ³@122[Spoolen            ]: Default             ³
    ³@123[Rebuild Info       ]: Default             ³
    ³@124[Zielspeicher       ]: USR-ROM             ³
    ³@125[Range Check        ]: Default             ³
    ³@126[Check adr(0)       ]: Default             ³
    ³@127[Opt. I/O Emulation ]: Default             ³
    ³                                          ³
    ³                   ...                    ³

    @131[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.131
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr Interrupttasks (Fortsetzung):

    ³                   ...                    ³
    ³                                          ³
    ³@128[Modul Info    ]: Version: **.**.*          ³
    ³                Datum  : *****            ³
    ³                L„nge  : ***** Byte       ³
    ³@128[Speicher lokal]: Analog : 0     Byte       ³
    ³                Digital: 0     Bit        ³
    ³    [@128[   OK   ]]  [@128[  Text  ]]  [@128[ Cancel ]]    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ








    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.132
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Resource>:
    Dieses Feld zeigt die Taskklasse der Task an.
    Durch die Auswahl der Taskklasse IRQ wird eine Task fr die Interrupt-
    Behandlung eines interrupt-f„higen Moduls parametriert.

    <OBJEKT><Parameter><Start (Irq/Exc) Nr.>:
    Bei Interrupttasks muá in diesem Feld die Modulnummer (Moduladresse des
    Moduls, das den Interrupt generiert) eingetragen werden.











    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.133
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr Exceptiontasks:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ<Taskname>ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ <- Im Rahmen der Dialogbox
    ³@113[Objekt Art         ]: PL2000         Task ³    wird der Name der EXC-
    ³@113[Init Routine       ]:                      ³    Task anzeigt
    ³@135[Resource           ]: EXC                 ³
    ³@136[Start (Irq/Exc) Nr.]: 128                  ³
    ³@116[Transfer Mode      ]: Default             ³
    ³@120[Installation       ]: Default             ³
    ³@121[Start              ]: Default             ³
    ³@122[Spoolen            ]: Default             ³
    ³@123[Rebuild Info       ]: Default             ³
    ³@124[Zielspeicher       ]: USR-ROM             ³
    ³@125[Range Check        ]: Default             ³
    ³@126[Check adr(0)       ]: Default             ³
    ³@127[Opt. I/O Emulation ]: Default             ³
    ³                                          ³
    ³                   ...                    ³

    @134[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.134
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr Exceptiontasks (Fortsetzung):

    ³                   ...                    ³
    ³                                          ³
    ³@128[Modul Info    ]: Version: **.**.*          ³
    ³                Datum  : *****            ³
    ³                L„nge  : ***** Byte       ³
    ³@128[Speicher lokal]: Analog : 0     Byte       ³
    ³                Digital: 0     Bit        ³
    ³    [@128[   OK   ]]  [@128[  Text  ]]  [@128[ Cancel ]]    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ








    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.135
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Resource>:
    Dieses Feld zeigt die Taskklasse der Task an.
    Durch die Auswahl der Taskklasse EXC wird eine Task fr die Behandlung
    einer bestimmten Exception parametriert.















    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.136
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Start (Irq/Exc) Nr.>:
    Bei Exceptiontasks muá in diesem Feld die entsprechende Exception-Nummer
    eingetragen werden.
    Folgende Exceptions werden vom Exceptionhandler EXERMO.BR Version 1.20
    untersttzt:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Exception-Nummer ³ Bezeichnung der Exception          ³
    ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
    ³          2       ³ Busfehler                          ³
    ³          3       ³ Adreáfehler                        ³
    ³          4       ³ Illegaler Befehl                   ³
    ³          5       ³ Division durch Null                ³
    ³          6       ³ Range Overflow                     ³
    ³          7       ³ Null Pointer                       ³
    ³          8       ³ Privilegverletzung                 ³
    ³         10       ³ Nicht implementierter Befehl       ³
    ³         24       ³ Falscher Interrupt                 ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @137[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.137
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Start (Irq/Exc) Nr.>:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Exception-Nummer ³ Bezeichnung der Exception          ³
    ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
    ³        128       ³ I/O-Exception                      ³
    ³        144       ³ TK-Zykluszeitverletzung            ³
    ³        145       ³ TK-Max. Zykluszeitverletzung       ³
    ³        146       ³ TK-Input-Zykluszeitverletzung      ³
    ³        147       ³ TK-Output-Zykluszeitverletzung     ³
    ³        160       ³ HSTC-Max. Zykluszeitverletzung     ³
    ³        161       ³ HSTC-I/O-Zykluszeitverletzung      ³
    ³        162       ³ Systemzykluszeitverletzung         ³
    ³        176       ³ RIO-HSTC-Zykluszeitverletzung      ³
    ³        177       ³ RIO-Exception bei Ready bzw. Error ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ




    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.138
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr Datenmodule:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ<Datenmodul-Name>ÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ <- Im Rahmen der Dialogbox
    ³Objekt Art         : Datenmodul             ³    wird der Name des Daten-
    ³                                            ³    moduls angezeigt.
    ³@122[Spoolen            ]: Default               ³
    ³@123[Rebuild Info       ]: Default               ³
    ³@124[Zielspeicher       ]: USR-ROM               ³
    ³                                            ³
    ³@128[Modul Info    ]: Version: 2.00               ³
    ³                Datum  : 10.02.1997 14:16:05³
    ³                L„nge  :  4368 Byte         ³
    ³                                            ³
    ³         [    OK    ]  [  Cancel  ]         ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ




    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.139
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr B&R-Module und Systemmodule:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ<Objekt-Name>ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ <- Im Rahmen der Dialogbox
    ³Objekt Art         : nicht bekannt          ³    wird der Objekt-Name an-
    ³                                            ³    gezeigt.
    ³@120[Installation       ]: Default                ³
    ³@121[Start              ]: nicht Starten          ³
    ³@122[Spoolen            ]: Default               ³
    ³@123[Rebuild Info       ]: nicht vorhanden        ³
    ³@140[Zielspeicher       ]: USR-ROM               ³
    ³                                            ³
    ³@128[Modul Info    ]: Version: 1.16               ³
    ³                Datum  : 29.05.1996 01:16:00³
    ³                L„nge  : 13368 Byte         ³
    ³                                            ³
    ³         [    OK    ]  [  Cancel  ]         ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.140
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Zielspeicher>:
    USR-ROM: Ist als Zielspeicher "USR-ROM" eingestellt, wird das Modul
             beim Ausfhren der Funktion <Objekt><Speichern> ins Anwender-
             ROM programmiert.
    FIX-RAM: Ist als Zielspeicher "FIX-RAM" eingestellt, wird das Modul
             beim Ausfhren der Funktion <Objekt><Speichern> ins FIX-RAM
             programmiert.
    MEM-CRD: Ist als Zielspeicher "MEM-CRD" eingestellt, wird das Modul
             beim Ausfhren der Funktion <Objekt><Speichern> auf die PCMCIA-
             Speicherkarte (auch "MEM-Card" genannt) bertragen.
    SYS-ROM: Ist als Zielspeicher "SYS-ROM" eingestellt, wird das Modul beim
             Ausfhren der Funktion <Objekt><Speichern> ins System-ROM ber-
             tragen.
             Diese Einstellung ist erst ab der Version 1.92 des Betriebssystems
             RPSSW m”glich. Ab dieser RPSSW-Version k”nnen Systemmodule auch im
             System-ROM installiert werden. Dieser Speicher kann jedoch nicht
             fr Applikationsdaten benutzt werden.


    @141[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.141
    PG2000  HAUPTMENš FUNKTIONEN

             Beim L”schen eines Moduls wird der (vom Modul belegte) Speicher
             nicht freigegeben. Um den Speicher wieder verwenden zu k”nnen,
             muá das gesamte System-ROM (inklusive Betriebssystem RPSSW) ge-
             l”scht werden. Dies wird nur durch Aufruf der Funktion <System>
             <Service><RPSSW Update> (Betriebssystem-Update) erreicht.














    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.200
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><InitUP>:
    Zu jeder Task kann ein sogenanntes Initialisierungs-Unterprogramm (INIT-UP)
    angelegt werden, das Bestandteil des lauff„higen Moduls (*.BR-Moduls) ist.
    INIT-UPs werden nur in der Hochlaufphase der RPS (nach einem Kalt- oder
    Warmstart) bzw. nach dem šbertragen der Task in die RPS ausgefhrt und
    eignen sich daher fr Wert-Initialisierungen.
    Auch Programmfunktionen, die nur einmalig ausgefhrt werden mssen, k”nnen
    in das INIT-UP der entsprechenden Task eingebunden werden.
    Ein INIT-UP ist keine eigene Task, sondern Bestandteil einer Task. D.h.
    beim L”schen, Compilieren, šbertragen, usw. einer Task wird automatisch
    auch das zugeh”rige INIT-UP gel”scht, compiliert oder bertragen.

    Mit Hilfe der Funktion <Objekt><InitUP> k”nnen Sie ein Initialisierungs-
    Unterprogramm (INIT-UP) @201[ERSTELLEN], @202[EDITIEREN oder L™SCHEN]. Die Funktion
    <Objekt><InitUP> kann nur aus dem KOP-, AWL- oder PL2000-Editor aufgerufen
    werden!



                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.201
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><InitUP>:

    ERSTELLEN eines INIT-UPs:
    Zum Erstellen eines INIT-UPs rufen Sie die Funktion <Objekt><InitUP> aus
    dem Editor der entsprechenden Task auf. Dadurch wird ein Popup-Men zur
    Auswahl der gewnschten Programmiersprache fr das INIT-UP eingeblendet:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Kontaktplan       ³
    ³ PL2000            ³
    ³ Anweisungsliste   ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Die Erstellung von INIT-UPs in der Programmiersprache Kontaktplan ist
    derzeit noch nicht m”glich. Nach Anwahl der Programmiersprache wird der
    entsprechende Editor (AWL- oder PL2000-Editor) zum Erstellen des INIT-UPs
    ge”ffnet.


    @200[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.202
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><InitUP>:

    ™FFNEN oder L™SCHEN eines INIT-UPs:
    Ist fr das aktuelle Anwenderprogramm bereits ein INIT-UP vorhanden, wird
    beim Aufruf der Funktion <Objekt><InitUP> folgendes Popup-Men einge-
    blendet:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ InitUP ™ffnen   ³
    ³ InitUP L”schen  ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Nach Anwahl der Funktion <InitUP ™ffnen> wird das INIT-UP des aktuellen
    Anwenderprogramms im entsprechenden Editor angezeigt. Durch die Funktion
    <InitUP L”schen> wird das INIT-UP des aktuellen Anwenderprogramms aus der
    Datenbank gel”scht.



    @200[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.203
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Variable>:
    Durch diese Funktion wird ein Tabelleneditor fr die VARIABLENDEKLARATION
    (kurz "VD" genannt) aufrufen.
    Im Source-Code wird fr eine Prozeávariable (PV) immer ein SYMBOLISCHER
    NAME verwendet. Mit der Variablendeklaration k”nnen Sie die Beziehung
    zwischen den symbolischen Namen und der Hardware herstellen:
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Name     º gltig    ³ Datentyp ³ L„nge ³ I/O-Typ  ³ Langname         º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    ºpv_1      ºtk_global  ³BYTE      ³1      ³MERKER    ³                  º
    ºpv_arr1   ºtk_global  ³WORD      ³5      ³MERKER    ³                  º
    ºpv_str1   ºtk_global  ³typedef   ³1      ³MERKER    ³                  º
    ºpv_dyn    ºdynamisch  ³LONG      ³1      ³Referenz  ³                  º
    :          :           :          :       :          :                  :
    ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    Die Darstellung des Tabelleneditors h„ngt von den mit Hilfe der Funktion
    <Einstellungen><Editor><Variable> festgelegten Einstellungen ab.

    @204[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.204
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Variable>:
    Die Funktion <Objekt><Variable> kann sowohl direkt aus dem entsprechenden
    Editor als auch aus allen Ebenen der GDM aufgerufen werden:

    * Task-Ebene oder Editor (PL2000/AWL/KOP/usw.):
      Aufruf der Variablendeklaration fr die angew„hlte Task.
    * Prozessor-Ebene:
      Aufruf der VD fr alle Tasks des angew„hlten Prozessors.
    * RPS-Ebene:
      Aufruf der VD fr alle Tasks der angew„hlten RPS.

    Eine detaillierte Beschreibung der Variablendeklaration finden Sie im Ab-
    schnitt @800[< VARIABLENDEKLARATION >].






                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.205
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Compile>:              <F9>
    Mit dieser Funktion wird ein Compiler aufgerufen, der das Anwenderprogramm
    (Source, INIT-UP) in die Maschinensprache des Prozessors (RPS) bersetzt.
    Dabei entsteht ein "LAUFFHIGES MODUL" mit dem Dateinamen "*.BR", das in
    die RPS bertragen werden kann.
    Der Compilier-Vorgang wird von den eingestellten Compiler-Optionen (siehe
    Funktion <Einstellungen><Compiler>) beeinfluát und kann in einzelne Teil-
    abschnitte unterteilt werden:
    - ZEILENPARSER:  šberprft die syntaktische RICHTIGKEIT jeder QUELLTEXT-
                     ZEILE und tr„gt unbekannte PVs in die Datenbank ein.
    - BLOCKPARSER:   šberprft die syntaktische RICHTIGKEIT eines BLOCK
                     (Kontrollstrukturen, Schleifen, usw.) des Quelltextes.
    - CODEGENERATOR: Erzeugt eine OBJEKTDATEI, die den Maschinen-Code und die
                     symbolischen PV-Namen enth„lt. Zudem wird berprft, ob
                     alle verwendeten Variablen in der aktuellen Datenbank
                     deklariert wurden.
    - TASKBUILDER:   Erzeugt aus der Objektdatei und den Informationen in der
                     Datenbank ein LAUFFHIGES MODUL (*.BR-Module).

    @206[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.206
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Compile>:              <F9>
    Wurden also nur in der Variablendeklaration (I/O-Typ, Gltigkeitsbereich,
    L„nge) oder in den Taskparametern nderungen durchgefhrt, ist es nicht
    notwendig, einen vollst„ndigen Compiler-Durchlauf durchzufhren.















    @207[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.207
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Compile>:              <F9>
    Die Funktion <Objekt><Compile> kann sowohl direkt aus dem entsprechenden
    Editor als auch aus allen Ebenen der GDM aufgerufen werden:

    * Task-Ebene oder Editor (PL2000/AWL/KOP/DAT/usw.):
      Das MARKIERTE OBJEKT (die markierte Task) wird entsprechend den einge-
      stellten Compiler-Optionen (siehe Funktion <Einstellungen><Compiler>)
      in ein lauff„higes Modul bersetzt.
    * Prozessor-Ebene:
      ALLE OBJEKTE (alle Tasks) des MARKIERTEN PROZESSOR-SYMBOLS werden ent-
      sprechend den eingestellten Compiler-Optionen (siehe dazu Funktion
      <Einstellungen><Compiler>) in lauff„hige Module bersetzt.
    * RPS-Ebene:
      ALLE OBJEKTE (alle Tasks) des MARKIERTEN RPS-SYMBOLS werden entsprechend
      der eingestellten Compiler-Optionen mit den erforderlichen Compiler-
      Schritten (siehe dazu Funktion <Einstellungen><Compiler>) in lauff„hige
      Module bersetzt. Dieser Vorgang entspricht einer kompletten PROJEKT-
      GENERIERUNG !

                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.208
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Optimieren>:
    Mit dieser Funktion werden alle nicht mehr ben”tigten Prozeávariablen (PVs)
    des markierten RPS- bzw. PROZESSOR-SYMBOLS aus der Datenbank (DBK) gel”scht
    (Optimierung der DBK). Vor dem Optimierungsvorgang erfolgt noch eine
    Sicherheitsabfrage.

    Bei der Datenbankoptimierung wird folgende Reihenfolge eingehalten:
    - Alle in der DBK vorhandenen PVs des angew„hlten RPS- bzw. Prozessor-
      Symbols werden als "l”schbar" markiert.
    - Compilieren der einzelnen Tasks des angew„hlten RPS- bzw. Prozessor-
      Symbols. Beim Compilieren der Tasks werden alle ben”tigten PVs in der
      DBK als "reserviert" gekennzeichnet.
    - Alle PVs des angew„hlten RPS- bzw. Prozessor-Symbols, die als "l”schbar"
      markiert sind, werden gel”scht. D.h. es bleiben nur mehr die tats„chlich
      ben”tigten PVs (als "reserviert" gekennzeichnet) in der DBK gespeichert.




                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.209
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><R U N>:                <F9>
    Das Anwenderprogramm (Source, INIT-UP) wird gegebenenfalls compiliert und
    anschlieáend in die RPS bertragen.

    - VORBEREITUNGSSCHRITT -> "VERSIONS"-KONTROLLE:
      Ist die Task unver„ndert, entf„llt die Compilierung. Wenn die lauff„higen
      Module (*.BR-Module) in PG und RPS gleich sind, wird auch der Download-
      Vorgang nicht ausgefhrt.
    - VORBEREITUNGSSCHRITT -> COMPILIEREN DES OBJEKTES:
      Wurden Source-Code oder Taskparameter ge„ndert, wird das ausgew„hlte
      Objekt vor dem šbertragen in die RPS COMPILIERT.
    - šBERTRAGEN DES LAUFFHIGEN MODULS:
      Das lauff„hige *.BR-Modul wird in die RPS šBERTRAGEN. Beim šbertragen
      einer Task ins Anwender-RAM wird zwischen den Funktionen "Download" und
      "Overload" unterschieden. Befindet sich bereits eine „ltere Version der
      Task auf der RPS, erfolgt die šbertragung mit der Funktion Overload.
      Existiert die zu bertragende Task hingegen noch nicht auf der RPS, wird
      zum šbertragen automatisch die Funktion Download verwendet.

    @210[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.210
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><R U N>:                <F9>
    Die Funktion <Objekt><R U N> kann sowohl direkt aus dem entsprechenden
    Editor als auch aus allen Ebenen der GDM aufgerufen werden:

    * Task-Ebene oder Editor (PL2000/AWL/KOP/DAT/usw.):
      Das MARKIERTE OBJEKT wird nach Abschluá der @209[Vorbereitungsschritte] in die
      RPS bertragen.
    * Prozessor-Ebene:
      JEDES eingetragene OBJEKT des MARKIERTEN PROZESSOR-SYMBOLS durchl„uft
      als erstes die @209[Vorbereitungsschritte]. Erst nach dem FEHLERFREIEN Ab-
      schluá dieser Vorbereitungen werden die lauff„higen Module in die RPS
      bertragen.
    * RPS-Ebene:
      JEDES eingetragene OBJEKT des MARKIERTEN RPS-SYMBOLS durchl„uft als
      erstes die @209[Vorbereitungsschritte]. Erst nach dem FEHLERFREIEN Abschluá
      dieser Vorbereitungen werden die lauff„higen Module in die RPS ber-
      tragen. Dieser Vorgang entspricht einem PROJEKT-DOWNLOAD !


                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.211
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Speichern>:
    Das Anwenderprogramm (Source, INIT-UP) wird gegebenenfalls compiliert
    und direkt in den nichtflchtigen Speicher bertragen. In den Objekt-
    Parametern wird definiert, in welchen nichtflchtigen Speicher das
    Objekt beim Speichern bertragen werden soll:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ<Objekt-Name>ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    :                                            :   ÚÄÄÄÄÄÄÄÄÄ¿
    ºZielspeicher       : USERROM           ÄÄÄÄ×ÄÄÄ³@212[ USR-ROM ]³
    :                                            :   ³@212[ FIX-RAM ]³
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼   ³@212[ MEM-CRD ]³
                                                     ³@212[ SYS-ROM ]³
                                                     ÀÄÄÄÄÄÄÄÄÄÙ






    @213[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.212
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Speichern>:
    USR-ROM: Anwender-ROM. Im Anwender-ROM (EPROM oder FlashPROM) k”nnen
             Anwenderprogramme nullspannungssicher gespeichert werden.
    FIX-RAM: Das FIX-RAM ist jener Teil des Anwender-RAMs, der sich beim
             Kaltstart wie das Anwender-ROM verh„lt.
    MEM-CRD: Auf der PCMCIA-Speicherkarte (auch "MEM-Card" genannt) k”nnen
             Anwenderprogramme nullspannungssicher gespeichert werden.
             Die PCMCIA-Speicherkarte kann wie der FlashPROM-Anwenderspeicher
             (Anwender-ROM) verwendet werden.
    SYS-ROM: Diese Einstellung ist erst ab der Version 1.92 des Betriebssystems
             RPSSW m”glich. Ab dieser RPSSW-Version k”nnen Systemmodule auch im
             System-ROM installiert werden. Dieser Speicher kann jedoch nicht
             fr Applikationsdaten benutzt werden.
             Beim L”schen eines Moduls wird der (vom Modul belegte) Speicher
             nicht freigegeben. Um den Speicher wieder verwenden zu k”nnen,
             muá das gesamte System-ROM (inklusive Betriebssystem RPSSW) ge-
             l”scht werden. Dies wird nur durch Aufruf der Funktion <System>
             <Service><RPSSW Update> (Betriebssystem-Update) erreicht.

    @211[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.213
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Speichern>:

    - VORBEREITUNGSSCHRITT -> "VERSIONS"-KONTROLLE:
      Ist die Task unver„ndert, entf„llt die Compilierung.
    - VORBEREITUNGSSCHRITT -> COMPILIEREN DES OBJEKTES:
      Wurden Source-Code oder Taskparameter ge„ndert, wird das ausgew„hlte
      Objekt vor dem Speichern COMPILIERT.
    - SPEICHERN DES LAUFFHIGEN MODULS:
      Das lauff„hige *.BR-Modul wird ins Anwender-ROM oder FIX-RAM der RPS
      šBERTRAGEN.









    @214[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.214
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Speichern>:
    Die Funktion <Objekt><Speichern> kann sowohl direkt aus dem entsprechenden
    Editor als auch aus allen Ebenen der GDM aufgerufen werden:

    * Task-Ebene oder Editor (PL2000/AWL/KOP/DAT/usw.):
      Das MARKIERTE OBJEKT wird nach Abschluá der @213[Vorbereitungsschritte] in den
      festlegten nichtflchtigen Speicher (siehe Objekt-Parameter) bertragen.
    * Prozessor-Ebene:
      JEDES eingetragene OBJEKT des MARKIERTEN PROZESSOR-SYMBOLS durchl„uft
      als erstes die @213[Vorbereitungsschritte]. Erst nach dem FEHLERFREIEN Ab-
      schluá dieser Vorbereitungen werden die lauff„higen Module in den fest-
      legten nichtflchtigen Speicher (siehe Objekt-Parameter) bertragen.
    * RPS-Ebene:
      JEDES eingetragene OBJEKT des MARKIERTEN RPS-SYMBOLS durchl„uft als
      erstes die @213[Vorbereitungsschritte]. Erst nach dem FEHLERFREIEN Abschluá
      dieser Vorbereitungen werden die lauff„higen Module in den festlegten
      nichtflchtigen Speicher (siehe Objekt-Parameter) bertragen.


                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.215
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Entfernen>:
    Diese Funktion kann aus allen Ebenen der GDM aufgerufen werden!
    In einer Dialogbox k”nnen Sie den Umfang des L”schvorgangs bestimmen:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍEntfernenÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    ºL”schen der Module auf der RPS : Ja  º
    ºL”schen des Objektes in der GDM: Ja  º
    ºL”schen von src-und br-Modulen : Neinº
    º                                      º
    º      [    OK    ]  [  Cancel  ]      º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    L”schen der Module auf der RPS:
    Ja ..... Alle B&R-Module (z.B. compilierte Tasks) des angew„hlten Objektes,
             die sich auf der RPS befinden, werden auf der RPS gel”scht.
             Das L”schen ist mit einer Rckfrage abgesichert.
    Nein ... Befinden sich B&R-Module des angew„hlten Objektes auf der RPS, so
             werden diese nicht gel”scht.

    @216[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.216
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Entfernen>:

    L”schen des Objektes in der GDM:
    Ja ..... Das angew„hlte Objekt wird in der GDM gel”scht. Das L”schen ist
             mit einer Rckfrage abgesichert. Existieren unter der aktiven
             Ebene noch weitere Ebenen (sogenannte Child-Ebenen), erscheint
             in einer Dialogbox eine weitere Sicherheitsabfrage mit der
             Warnung, daá diese ebenfalls gel”scht werden.
    Nein ... Das angew„hlte Objekt-Symbol wird nicht gel”scht.

    L”schen von src-und br-Modulen: 
    Ja ..... Alle Quelldateien (*.SRC-Dateien) und B&R-Module (*.BR-Dateien)
             des angew„hlten Objektes werden gel”scht. Das L”schen ist mit
             einer Rckfrage abgesichert.
    Nein ... Die Quelldateien (*.SRC-Dateien) und B&R-Module (*.BR-Dateien)
             des angew„hlten Objektes werden nicht gel”scht.



                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.217
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Monitor>:              <F11>
    Mit Hilfe dieser Funktion k”nnen Sie den PV-MONITOR bzw. den KOP-MONITOR
    fr das markierte Objekt (aktive Fenster) aktivieren.
    Im PV-Monitor k”nnen Werte von taskspezifischen PVs angezeigt, PV-Werte
    auf die RPS geschrieben oder geforct werden. Eine detaillierte Beschreibung
    des PV-Monitors finden Sie im Abschnitt @850[< P V - MONITOR >].
    Mit dem KOP-Monitor besteht die M”glichkeit, die PVs eines auf der RPS
    laufenden KOPs direkt in der Kontaktplan-Maske anzuzeigen. Der KOP-Monitor
    ist im Abschnitt @660[< K O P - MONITOR >] beschrieben.

    <OBJEKT><Debugger>:             <F11>
    Mit Hilfe dieser Funktion wird der SOURCE-LEVEL-DEBUGGER fr PL2000- und
    AWL-Programme aktiviert. Der Debugger ist ein Werkzeug zur Fehleranalyse
    und erm”glicht es, die genauen Taskabl„ufe zu verschiedenen Zeitpunkten
    anhand des Source-Codes und der PV-Werte genau zu untersuchen.
    Eine detaillierte Beschreibung des Debuggers finden Sie im Abschnitt
    @460[< P L 2 0 0 0 - DEBUGGER >] bzw. @560[< A W L - DEBUGGER >].
 

                                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.219
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><G D M>:               <F12>
    Die GRAPHISCHE DESIGN METHODE (kurz GDM genannt) ist ein Hilfsmittel zur
    Projektierung und Projektanalyse von Automatisierungsaufgaben. Mit Hilfe
    der GDM k”nnen Sie eine Automatisierungsaufgabe in Teilaufgaben unterteilen
    und die Beziehung dieser Teilaufgaben zueinander festlegen. Die grafische
    Darstellung eines Projektes erh”ht die šbersichtlichkeit und erleichtert
    die Wartung.

    Die GDM bietet:
    - Symbolbezogenes Bearbeiten der Objektparameter und Variablen.
    - Symbolbezogenes Compilieren / Ex- oder Importieren von Objekten.
    - Symbolbezogenes Aufrufen des entsprechenden Source-Codes.
    - Dokumentationshilfen: Listen der verwendeten PVs, Tasks, usw.
    - Die grafische Darstellung erm”glicht eine schnelle Zuordnung der Tasks
      zur entsprechenden RPS und zum richtigen Prozessor der RPS.
    - usw.

    Bedienung und Syntax der GDM sind im Abschnitt @325[< GDM-EDITOR >] beschrieben.

                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.220
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Vergleich>:
    Durch diese Funktion werden das aktuelle PG2000-Projekt und das RPS-
    Projekt (Projekt, das auf der RPS l„uft) Task fr Task miteinander
    verglichen. Dabei werden folgende Unterschiede gemeldet:

    - Unterschiede bei den B&R-Modulen (compilierte Tasks, Datenmodule,
      Systemmodule, usw.)
    - Unterschiede in der Variablendeklaration

    Die festgestellten Unterschiede werden im Meldungsfenster angezeigt.

    Das Ergebnis dieses Vergleichs hilft Ihnen auch beim Festlegen des šber-
    tragungsmodus (siehe auch Objektparameter "@116[Transfer Mode]" bzw. Compiler-
    Einstellung "@291[Transfer Mode]") fr die zyklischen Tasks.
    




                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.221
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Abgleich>:
    Mit dieser Funktion k”nnen Sie ...
    ... ein komplettes Projekt aus der RPS in die Task-Ebene der GDM laden.
    ... das aktive Projekt mit einem Verzeichnis (B&R-Module) am PC abgleichen.

    ÉÍÍÍÍÍÍÍÍÍÍÍProjekt AbgleichÍÍÍÍÍÍÍÍÍÍÍ»
    ºACHTUNG : Vor dem Abgleich wird ein   º
    ºNeu-Init der Datenbank durchgefhrt.  º
    ºNachfolgend bitte Projekt generieren !º
    º                                      º
    ºRekonstruktion der Datenbank : @222[Ja    ]º ... PV-Offsets bernehmen (Ja/Nein)
    ºRekonstruktion der Sourcen   : @222[Ja    ]º ... Sourcen k”nnen ge„ndert/nicht
    º                                      º     ge„ndert werden
    ºAbgleich durchfhren mit: @223[RPS Online ]º ... Abgleich mit RPS/Verzeichnis
    ºVerzeichnis: @224[                        ]º ... Verzeichnis, mit dem das
    º                                      º     aktive Projekt abgeglichen
    º        [   OK   ]  [ Cancel ]        º     werden soll
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.222
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Abgleich>:
    Fr den Projekt-Abgleich stehen verschiedene M”glichkeiten zur Verfgung:

    - Rekonstruktion der Datenbank : Ja
      Rekonstruktion der Sourcen   : Ja
      Bedeutung: Ver„nderbares Projekt, mit fixen ursprnglichen PV-Offsets

    - Rekonstruktion der Datenbank : Ja
      Rekonstruktion der Sourcen   : Nein
      Bedeutung: Erweiterbares Projekt, mit fixen ursprnglichen PV-Offsets

    - Rekonstruktion der Datenbank : Nein
      Rekonstruktion der Sourcen   : Ja
      Bedeutung: Ver„nderbares Projekt, mit neuorganisierten PV-Offsets

    - Rekonstruktion der Datenbank : Nein
      Rekonstruktion der Sourcen   : Nein
      Bedeutung: Erweiterbares Projekt, mit neuorganisierten PV-Offsets

    @223[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.223
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Abgleich>:
    Fr den Abgleich mit einer RPS ("Abgleich durchfhren mit: RPS Online")
    gilt folgendes:
    * Alle B&R-Module (*.BR-Modul = lauff„higes Modul), die sich im Anwender-
      ROM, Anwender-RAM oder FIX-RAM der RPS befinden, jedoch nicht in der GDM
      eingetragen sind, werden von der RPS in die GDM zurckgeladen und in Form
      von Symbolen dargestellt.
    * Bei allen Modulen, die in doppelter oder unterschiedlicher Form (PG/RPS)
      vorliegen, erscheint eine Dialogbox zur Auswahl des gewnschten Vorgangs.
    * Objekte (Tasks), die mit Rebuild Info (Taskparameter "Rebuild Info: Ein")
      bersetzt wurden, werden beim Projekt-Abgleich mit der Einstellung
      "Rekonstruktion der Sourcen: Ja" automatisch rckbersetzt. D.h. die
      komplette Quellinformation des B&R-Moduls (Source, Initialisierungs-
      Unterprogramm, Taskparameter, Variablendeklaration, usw.) wird wieder-
      hergestellt. Anschlieáend kann die Task ge„ndert werden.
    * Bei Objekten ohne Rebuild Info (Taskparameter "Rebuild Info: Aus") ist
      keine Rckbersetzung m”glich. In diesem Fall wird fr das entsprechende
      Modul ein B&R-Symbol in die GDM eingetragen.

    @224[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.224
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Abgleich>:
    Mit der Einstellung "Abgleich durchfhren mit: Verzeichnis" ist es m”glich,
    das aktive Projekt mit einem Verzeichnis (B&R-Module) am PC abgleichen.

    ACHTUNG: Nach dem Projekt-Abgleich muá das Projekt generiert (compiliert)
             werden!













    @221[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.225
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datentyp>:
    Mit der Funktion <Projekt><Datentyp> k”nnen PROJEKTSPEZIFISCHE DATENTYPEN
    erstellt, gel”scht und bearbeitet werden.
    Diese Funktion bietet dieselben M”glichkeiten wie die Funktion "typedef"
    der VARIABLENDEKLARATION, sie ist jedoch unabh„ngig vom ge”ffneten Editor.
    Eine detaillierte Beschreibung des Funktionsumfangs finden Sie im Abschnitt
    @843[< VARIABLENDEKLARATION >< STRUKTURDEFINTION >].












                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.226
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Konstanten>:
    Durch diese Funktion wird ein Tabelleneditor zum Definieren und Bearbeiten
    PROJEKTSPEZIFISCHER KONSTANTEN ge”ffnet:

    ÄÄÄÄÄÄÄÄ Konstanten ÄÄÄÄÄÄÄÄ[][]¿
    ³ÉÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍ»³
    ³º Name     ³ Datentyp ³ Wert     º°
    ³ÌÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍ¹°
    ³ºTRUE      ³BIT       ³1         º°
    ³ºGLOB_NAME ³STRING    ³abcdefghijº°
       :          :          :
    <Name> ....... NAME der Konstante (im gesamten Projekt gltig).
    <Datentyp> ... DATENTYP der Konstante. Folgende Datentypen sind m”glich:
                   BIT, INT8, INT16, INT32, BYTE, WORD, LONG, FLOAT, STRING.
    <Wert> ....... WERT der Konstante. Dieser Wert wird bei der Referenzierung
                   der Konstante im entsprechenden Source-Code eingesetzt.

    Mit ^<BACKSPACE> k”nnen Sie eine Konstante aus der Tabelle L™SCHEN.

                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.227
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Netzwerk>:
    Mit dieser Funktion k”nnen Sie ein NETZWERK (seriell, CAN) konfigurieren.
    Dadurch wird die PG-Kommunikation im Netzwerk erm”glicht. D.h. Sie k”nnen
    jede RPS im Netzwerk programmieren, ohne eine Online-Verbindung zu dieser
    RPS herstellen zu mssen.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Netz Tabelle ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³ÉÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍ»³
    ³º@228[Segment    ]³@228[RPS Name   ]³@228[Knoten Nr]³@228[Slot     ]³@228[Subslot  ]³@228[Interface  ]º°
    ³ÌÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍ¹°
    ³ºNetz 1     ³2003_1     ³01       ³00       ³00       ³CAN        º°
    ³ºNetz 1     ³2003_2     ³02       ³00       ³00       ³CAN        º°
       :           :           :         :         :         :






                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.228
    PG2000  HAUPTMENš FUNKTIONEN

    <Segment> ..... Name des Netzwerk-Segments.
    <RPS Name> .... Prozessor-Name. Der Prozessor-Name wird in der System-
                    konfiguration festgelegt (siehe Eingabefeld <RPS Name>
                    der @912[Systemkonfiguration]).
    <Knoten Nr> ... Bei einem CAN-NETZWERK wird hier die Knotennummer der CAN-
                    Station eingestellt.
                    Im SERIELLEN NETZWERK muá fr die Knotennummer immer der
                    Wert "00" eingestellt werden.
    <Slot> ........ Wird derzeit noch nicht untersttzt und muá daher immer
                    "0" sein!
    <Subslot> ..... Wird derzeit noch nicht untersttzt und muá daher immer
                    "0" sein!
    <Interface> ... CAN: Kommunikation ber die CAN-Schnittstelle
                    IF1: Kommunikation ber die (serielle) Schnittstelle IF1
                    IF2: Kommunikation ber die (serielle) Schnittstelle IF2
                    IF3: Kommunikation ber die (serielle) Schnittstelle IF3
                    IF4: Kommunikation ber die (serielle) Schnittstelle IF4


                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.229
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datenbank>:
    In der Projekt-Datenbank (DBK) werden alle Projekt-Informationen, die
    nicht direkt in den Quelldateien gespeichert sind (Tasks, Datentypen,
    Prozeávariablen, usw.), abgelegt. Die DBK ist die Informationsquelle
    fr die Erstellung von Querverweis-Listen (Crossreferenz-Informationen),
    Datentyp-Listen, Funktions-Listen, usw.

    Nach Aufruf der Funktion <Projekt><Datenbank> werden in einem Popup-Men
    folgende PROJEKTSPEZIFISCHE Datenbank-Funktionen angeboten:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@230[ Crossreferenz    ]³ ... Liste aller Prozeávariablen (PVs) im Projekt
    ³@236[ RIO-Konfig       ]³ ... Konfiguration des Remote-IO (RIO)
    ³@239[ FUB-Liste        ]³ ... Liste aller Standard-Funktionen und Anwender-FUBs
    ³@242[ Datentyp-Liste   ]³ ... Liste aller Datentypen
    ³@245[ Library L”schen  ]³ ... Library aus der Projekt-Datenbank l”schen
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ



                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.230
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datenbank><Crossreferenz>:
    Nach Aufruf dieser Funktion werden alle in der aktuellen Projekt-Datenbank
    vorhandenen Prozeávariablen, die nicht nur in einer Task gltig sind (PVs
    mit dem Gltigkeitsbereich tk_global, proc_global oder rps_global), aufge-
    listet. Diese Liste wird auch als Querverweis-Liste bezeichnet:

    ÉÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍ»
    º@231[ Nr.]³@231[PV-Name   ]³@231[Kennung   ]³@232[RPSIO-Typ           ]³@232[Taskname  ]³@232[TC  ]º
    ÌÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍ¹
    º    ³          ³          ³                    ³          ³    º









    @229[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.231
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @232[< WEITER >]
    Spalte "Nr.":
    Fortlaufende Numerierung der Prozeávariablen (PVs) der aktuellen Projekt-
    Datenbank.

    Spalte "PV-Name":
    PV-Namen (Prozeávariablen-Namen) aller in der aktuellen Projekt-Datenbank
    vorhandenen Prozeávariablen (in alphabetischer Reihenfolge).
    Prozeávariablen mit den Datentypen lokal oder dynamisch (Prozeávariablen,
    die nur innerhalb einer Task gltig sind) werden in der Querverweis-Liste
    nicht angezeigt.

    Spalte "Kennung":
    In dieser Spalte wird der Gltigkeitsbereich der einzelnen Prozeávariablen
    (tk_global, proc_global oder rps_global) angezeigt.
    Prozeávariablen mit den Datentypen lokal oder dynamisch (Prozeávariablen,
    die nur innerhalb einer Task gltig sind) werden in der Querverweis-Liste
    nicht angezeigt.


                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.232
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @230[< WEITER >]
    Spalte "RPSIO-Typ":
    I/O-Typ (MERKER, 2003-I/O, 2005-I/O, 2010-I/O, REMOTE-I/O, CAN-I/O, IP,
    MP/PP oder Referenz) der einzelnen Prozeávariablen.
    Wurde mit der Men-Funktion <Einstellungen><Editor><Variable> die Option
    "I/O-Krzel: Ja" eingestellt, wird an Stelle des I/O-Typs die Hardware-
    Zuordnung (HW-Zuordnung) der einzelnen Prozeávariablen angezeigt (weitere
    Informationen dazu finden Sie im Abschnitt @829[< VARIABLENDEKLARATION >]).

    Spalte "Taskname":
    Name der Tasks, in denen die entsprechende Prozeávariable verwendet wird.

    Spalte "TC":
    Taskklasse der Tasks, in denen die entsprechende Prozeávariable verwendet
    wird.





                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.236
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datenbank><RIO-Konfig>:
    Mit Hilfe dieser Funktion kann das REMOTE-IO (RIO) entsprechend den
    Anforderungen KONFIGURIERT werden.

    Die Konfiguration fr den Digital- und Analogbereich wird getrennt
    durchgefhrt:

    RIO Konfiguration: @237[< Digital I/O >]
    RIO Konfiguration: @238[< Analog I/O  >]


    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Damit die ge„nderten Einstellungen wirksam werden, muá die Datenbank   º
    º neu organisiert werden. Anschlieáend mssen die Tasks neu compiliert   º
    º werden. Rufen Sie dazu die Funktion <Objekt><Optimieren> auf.          º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼



    @229[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.237
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @238[< WEITER >]
    <RIO-Konfiguration Digital I/O>:

    <Blockgr”áe(Byte)>:
    - Die Blockgr”áe wird fr sowohl den IN- und OUT-Bereich als auch fr jede
      Taskklasse getrennt festgelegt.
    - Gr”áe:             1 Byte -> entspricht  8 digitalen KANLEN (pro Block)
      Standard:          8 Byte -> entspricht 64 digitalen KANLEN (pro Block)
    - Maximale Gr”áe: 2048 Byte -> entspricht 16384 Kan„len

    <Blockanzahl(max.)>:
    - Die Blockgr”áe entspricht bei zeitoptimierter Konfiguration der maxi-
      malen Slave-Anzahl. Bei der speicheroptimierten Konfiguration werden
      mehr Bl”cke konfiguriert, als Slaves vorhanden sind.
    - Wenn eine Slave-Station die konfigurierte KANAL-ANZAHL berschreitet,
      wird automatisch ein WEITERER BLOCK fr diese Slave-Station bentzt
      (speicheroptimierte Konfiguration). Da immer nur ein Block nach dem
      anderen mit der CPU ausgetauscht wird, sinkt in diesem Fall die Leistung
      fr das entsprechende RIO.

                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.238
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @236[< WEITER >]
    <RIO-Konfiguration Analog I/O>:

    <Blockgr”áe(Byte)>:
    - Die Blockgr”áe wird fr sowohl den IN- und OUT-Bereich als auch fr jede
      Taskklasse getrennt festgelegt.
    - Gr”áe:              2 Byte -> entspricht  1 analogen KANAL (pro Block)
      Standard:          64 Byte -> entspricht 32 analogen KANLEN (pro Block)
    - Maximale Gr”áe: 12800 Byte -> entspricht 6400 Kan„len

    <Blockanzahl(max.)>:
    - Die Blockgr”áe entspricht bei zeitoptimierter Konfiguration der maxi-
      malen Slave-Anzahl. Bei der speicheroptimierten Konfiguration werden
      mehr Bl”cke konfiguriert, als Slaves vorhanden sind.
    - Wenn eine Slave-Station die konfigurierte KANAL-ANZAHL berschreitet,
      wird automatisch ein WEITERER BLOCK fr diese Slave-Station bentzt
      (speicheroptimierte Konfiguration). Da immer nur ein Block nach dem
      anderen mit der CPU ausgetauscht wird, sinkt in diesem Fall die Leistung
      fr das entsprechende RIO.

                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.239
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datenbank><FUB-Liste>:
    Nach Aufruf dieser Funktion werden alle in der aktuellen Projekt-Datenbank
    eingebundenen Standard-Funktionen und Anwender-FUBs (vom Anwender selbst
    erstellte Funktionsbl”cke) in Form einer Tabelle angezeigt:

    ÉÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍ»
    º@240[ Nr.]³@240[ FUB/UP   ]³   ³@240[Kennung]³@240[Bit/Byt]³@241[ In-Param ]³@241[ Typ   ]³@241[ Out-Param]³@241[ Typ   ]º
    ÌÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍ¹
    º    ³          ³   ³       ³       ³          ³       ³          ³       º










    @229[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.240
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @241[< WEITER >]
    Spalte "Nr.":
    Fortlaufende Numerierung der Funktionen der aktuellen Projekt-Datenbank.

    Spalte "FUB/UP":
    Namen aller in der aktuellen Projekt-Datenbank vorhandenen Funktionen
    (in alphabetischer Reihenfolge).

    Spalte "Kennung":
    In dieser Spalte wird die Art der Funktionen angezeigt:
       Inline ... Inline-Funktion
       Lokal  ... Lokaler Funktionsblock
       AVT    ... AVT-Funktionsblock
    Der Unterschied zwischen lokalen Funktionsbl”cken und AVT-Funktionsbl”cken
    ist im "B&R SYSTEM 2000 Programmiersprachen Anwenderhandbuch" beschrieben.

    Spalte "Bit/Byt":
    Funktionsinterner Speicherbedarf in Bit/Byte.


                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.241
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @239[< WEITER >]
    Spalte "In-Param":
    In dieser Spalte sind die Eingangsparameter (bzw. Ein-/Ausgangsparameter)
    fr jede einzelne Funktion aufgelistet.

    Spalte "Typ":
    Datentyp (BIT, INT8, INT16, INT32, BYTE, WORD, LONG oder FLOAT) der
    einzelnen Eingangsparameter. Bei Eingangsparametern, denen selbst eine
    Struktur (benutzerspezifischer Datentyp) zugeordnet ist, wird in dieser
    Spalte der Name der entsprechenden Struktur (Strukturname) angezeigt.

    Spalte "Out-Param":
    In dieser Spalte sind die Ausgangsparameter fr jede einzelne Funktion auf-
    gelistet.

    Spalte "Typ":
    Datentyp der einzelnen Ausgangsparameter. Bei Ausgangsparametern, denen
    selbst eine Struktur (benutzerspezifischer Datentyp) zugeordnet ist, wird
    in dieser Spalte der Name der entsprechenden Struktur angezeigt.

                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.242
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datenbank><Datentyp-Liste>:
    Nach Aufruf dieser Funktion werden alle im aktuellen Projekt definierten
    Strukturen (Struktur = benutzerspezifischer Datentyp) in Form einer Tabelle
    angezeigt:

    ÉÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍ»
    º@243[ Nr.]³@243[Name      ]³@243[Bit/Byte ]³@244[Element   ]³@244[Typ       ]³@244[Len ]º
    ÌÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍ¹
    º    ³          ³         ³          ³          ³    º

    Das Erzeugen von benutzerspezifischen Datentypen (Strukturen) ist im Ab-
    schnitt @843[< VARIABLENDEKLARATION >< STRUKTURDEFINTION >] beschrieben.







    @229[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.243
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @244[< WEITER >]
    Spalte "Nr.":
    Durchgehende Numerierung aller in der aktuellen Projekt-Datenbank vor-
    handenen Strukturen.

    Spalte "Name":
    In dieser Spalte sind die Strukturnamen (Datentyp-Namen) aller im aktuellen
    Projekt definierten Strukturen in alphabetischer Reihenfolge aufgelistet.

    Spalte "Bit/Byte":
    In dieser Spalte wird der Speicherbedarf der einzelnen benutzerspezifischen
    Datentypen in Bits und Bytes angezeigt. D.h. dieser Spalte k”nnen Sie ent-
    nehmen, wieviel Speicherplatz eine Variable mit dem Datentyp <Strukturname>
    im PV-Speicher (DPR) belegt.
    Bei den Angaben in der Spalte "Bit/Byte" ist die WORD-Ausrichtung fr
    Strukturen bereits bercksichtigt!




                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.244
    PG2000  HAUPTMENš FUNKTIONEN
                                                                      @242[< WEITER >]
    Spalte "Element":
    In dieser Spalte sind die Elemente fr jede einzelne Struktur aufgelistet.

    Spalte "Typ":
    Datentyp (INT8, INT16, INT32, BYTE, WORD, LONG oder FLOAT) der einzelnen
    Strukturelemente. Bei Elementen, denen selbst eine Struktur (benutzer-
    spezifischer Datentyp) zugeordnet ist, wird in dieser Spalte der Name der
    entsprechenden Struktur (Strukturname) angezeigt.

    Spalte "Len":
    Wird in dieser Spalte ein Wert gr”áer 1 angezeigt, so handelt es sich bei
    dem entsprechenden Strukturelement um ein Feld, das aus der angezeigten
    Anzahl von Feldelementen besteht. D.h. in der Spalte "Len" wird die Anzahl
    der Feldelemente angezeigt, die Sie mittels eines Index unter dem
    symbolischen Namen des Strukturelements ansprechen k”nnen. 




                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.245
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Datenbank><Library L”schen>:
    Zum Entfernen einer Library aus der Datenbank des aktuellen Projektes
    w„hlen Sie die Funktion <Projekt><Datenbank><Library L”schen>. Dadurch
    wird eine Auswahlliste aller in der aktuellen Datenbank eingebundenen
    Libraries eingeblendet. Nach Auswahl der gewnschten Library wird diese
    aus der Datenbank des aktuellen Projektes gel”scht:

    - Alle Funktionen, die diese Library enth„lt, werden aus der Datenbank
      (DBK) gel”scht.
    - Die Library-Datei (*.SLB-Datei) wird aus dem aktuellen Projektverzeichnis
      gel”scht.

    Das L”schen ist mit einer Rckfrage abgesichert.






    @229[ Fortsetzung ]                    @053[ HM-Projekt ]                    @001[ šbersicht ]
.246
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><S„ubern>:
    Zum L”schen aller Backup-Dateien (*.BAK und *.?_?) des aktuellen Projektes
    w„hlen Sie die Funktion <Projekt><S„ubern>. Nach Anwahl dieser Funktion er-
    scheint folgende Sicherheitsabfrage am Bildschirm:

    ÚÄÄÄÄÄÄÄÄÄÄS„ubernÄÄÄÄÄÄÄÄÄÄÄ¿
    ³L”schen aller Backup-Files ?³
    ³                            ³
    ³    [  Ja   ]  [ Nein  ]    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Nach der Best„tigung dieser Nachfrage mit <Ja> wird das aktuelle Projekt
    ges„ubert.
    Das Programmiersystem legt beim Sichern von Source-Dateien automatisch
    Backup-Dateien an (maximal werden drei Backup-Dateien angelegt, Beispiel:
    TASK.SRC => TASK.S_0, TASK.S_1 und TASK.S_2).



                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.247
    PG2000  HAUPTMENš FUNKTIONEN

    <PROJEKT><Schlieáen>:
    Durch die Men-Funktion <Projekt><Schlieáen> wird das aktuelle Projekt ge-
    schlossen, die PG2000 Programmiersoftware (Programmierumgebung) verlassen
    und die Projektverwaltung aufgerufen.
    In der Projektverwaltung kann eines der bestehenden Projekte ge”ffnet oder
    ein neues Projekt erzeugt werden (siehe Abschnitt @004[< PROJEKTVERWALTUNG >]).













                                     @053[ HM-Projekt ]                    @001[ šbersicht ]
.250
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Monitor>:
    Mit Hilfe dieser Funktion k”nnen Sie den SYSTEM-MONITOR (auch "RPS-MONITOR"
    genannt) aktivieren. Im RPS-Monitor k”nnen alle RPS-Objekte angezeigt und
    manipuliert werden (Start/Stop, Download/Upload, Entfernen/Speichern).
    Eine detaillierte Beschreibung des RPS-Monitors finden Sie im Abschnitt
    @850[< R P S - MONITOR >].













                                      @054[ HM-System ]                    @001[ šbersicht ]
.251
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Analyse>:
    Alle schwerwiegenden Fehler, die im Rahmen einer Applikation auftreten
    (RPSSW-Fehler wie Zykluszeitverletzungen, Installationsfehler, usw.),
    werden vom Betriebssystem mit Fehlerzeitpunkt und Zusatzinformationen
    in ein Fehlerlogbuch eingetragen.
    Die max. Anzahl der Fehlerlogbucheintr„ge kann in der Systemkonfiguration
    festgelegt werden. Dazu rufen Sie die Funktion <Objekt><Parameter> aus der
    Task-Ebene der GDM auf. Der Cursor muá sich dabei auf dem "sysconf"-Symbol
    befinden. Anschlieáend kann durch Anwahl der Funktion <Module> aus dem
    Popup-Men des Feldes <Konfiguriere> die maximale Anzahl der Logbuch-
    eintr„ge ver„ndert werden.








    @252[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.252
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Analyse>:
    Mit Hilfe der Funktion <System><Analyse> k”nnen Sie die Eintr„ge des
    Fehlerlogbuchs auslesen. Dazu wird folgende Dialogbox ge”ffnet:

    ÚÄÄÄÄÄÄÄÄAnalyseÄÄÄÄÄÄÄÄÄ¿
    ³ Eintrag   :    0       ³ 
    ³ Anzahl    :   15       ³
    ³                        ³
    ³ [   OK   ]  [ Cancel ] ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Eintrag ... Um den jngsten Eintrag aus dem Fehlerlogbuch auszulesen,
                mssen Sie die Nummer "0" eingeben.
    Anzahl .... Anzahl der anzuzeigenden Fehlerlogbucheintr„ge.





    @253[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.253
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Analyse>:
    Nach dem Schlieáen der Dialogbox durch Anwahl des Best„tigungsfeldes <OK>
    wird ein Fenster mit den gewnschten Fehlerlogbucheintr„gen eingeblendet:

          ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Fehlernummer
          ³           ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Detailfehler
          ³           ³               ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Taskklasse der Task,
          ³           ³               ³                  die den Fehler ver-
          ³           ³               ³                  ursacht hat
          ³           ³               ³             ÚÄÄÄ Fehlerzeitpunkt
          ³           ³               ³             ³
    Code: xxxx  Info: yyyyyyyy  Task: zzzz    Time: DD.MM.YY HH:MM:SS,ss
    Fehlertext in Worten

    Beispiel fr einen Fehlerlogbucheintrag:
    Code: 3915  Info: 00C12B3C  Task: syss    Time: 23.01.97 13:52:25,11
    EPROM kann nicht gebrannt werden, Schreibschutzschalter ?


                                      @054[ HM-System ]                    @001[ šbersicht ]
.254
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Info>:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄInfoÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³System:                               ³
    ³  @255[Hardware     ]:  2010 / 16.67 MHz    ³
    ³  @255[Revision     ]:  V1.005              ³
    ³  @255[Hochlaufgrund]:  Warmstart           ³
    ³Speicher:                             ³
    ³  @255[RPS I/O]:   128 kByte [@256[ Forcen aus ]] ³
    ³  @255[System ]:   256 kByte [@256[  Belegung  ]] ³
    ³AWS:                                  ³
    ³  @257[Type    ]: ME913      @257[Puff. CPU]: OK  ³
    ³  @257[Speicher]: FLASH/RAM  @257[Puff. AWS]: OK  ³
    ³  @257[RPSSW   ]: V1.91                     ³
    ³RTC:                                  ³
    ³  @258[Datum]: 10.3.1997                    ³
    ³  @258[Zeit ]: 14.12.21      [@258[   Setzen   ]] ³
    ³@258[Knotennummer]:  00                     ³
    ³            [     OK     ]            ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
                                      @054[ HM-System ]                    @001[ šbersicht ]
.255
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Info><System>:
    Hardware ........ RPS-System (B&R 2003, B&R 2005 oder B&R 2010)
    Revision ........ Hardware-Revision der CPU
    Hochlaufgrund ... Grund des letzten Boot-Vorgangs (Kaltstart, Warmstart,
                      Reset (entspricht "RPS Halt"), Error oder Diagnose)

    <SYSTEM><Info><Speicher>:
    RPS I/O ........ Gr”áe des Dual Ported RAMs (DPR) der CPU
    System ......... Gr”áe des System-RAMs der CPU










    @254[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.256
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Info><Speicher>:
    <Forcen aus> ... Durch Anwahl dieses Feldes k”nnen Sie die Force-Funktion
                     aller PVs der RPS, die mit der Funktion <Force> (siehe PV-
                     Monitor) auf einen definierten Wert gesetzt wurden, de-
                     aktivieren. Nach erfolgreicher Ausfhrung der <Force aus>-
                     Funktion kann die RPS den Wert dieser PVs wieder „ndern.
    <Belegung> ..... Durch Anwahl dieses Feldes wird eine Dialogbox ge”ffnet:

    ÚÄÄÄÄÄÄÄSpeicherbelegungÄÄÄÄÄÄÄ¿
    ³           belegt frei        ³ Speicher (belegt/frei) im bzw. auf der ...
    ³System   : xxxxxx xxxxxx Byte ³ ... System-RAM
    ³SYS  ROM : xxxxxx xxxxxx Byte ³ ... System-ROM
    ³User ROM : xxxxxx xxxxxx Byte ³ ... Anwender-ROM
    ³User RAM : xxxxxx xxxxxx Byte ³ ... Anwender-RAM
    ³FIX  RAM : xxxxxx xxxxxx Byte ³ ... FIX-RAM
    ³Tempor„r : xxxxxx xxxxxx Byte ³ ... tempor„ren Bereich des System-RAMs
    ³Mem Card : xxxxxx xxxxxx Byte ³ ... MEM-Card (PCMCIA-Speicherkarte)
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @254[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.257
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Info><AWS>:
    Type ........... Bezeichnung des verwendeten Anwenderspeichers
    Speicher ....... Art des nicht flchtigen Speichers im Anwenderspeicher
                     (EPROM oder FlashPROM)
    RPSSW .......... Version des Betriebssystems RPSSW
    Puff. CPU ...... Zustand der Lithium-Batterie im CPU (OK/Leer)
    Puff. AWS ...... Zustand der Lithium-Batterie im AWS (OK/Leer)












    @254[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.258
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Info><RTC>:
    Datum .......... Hier wird das aktuelle Datum (Echtzeituhr der RPS) an-
                     gezeigt.
    Zeit ........... Hier wird die aktuelle Uhrzeit (Echtzeituhr der RPS) an-
                     gezeigt.
    <Setzen> ....... Durch Anwahl dieses Feldes wird eine Dialogbox ge”ffnet.
                     In dieser Dialogbox kann die Echtzeituhr der RPS auf
                     einen vom Anwender gewnschten Wert gesetzt werden.
                     Fr Datum und Zeit werden standardm„áig die aktuellen
                     Werte angezeigt.

    <SYSTEM><Info><Knotennummer>:
    An dieser Stelle wird die am Modul eingestelle CAN-Knotennummer angezeigt.






    @254[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.259
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><RPS Halt>:
    Mit dieser Funktion k”nnen Sie die RPS anhalten (ursprnglich wurde fr
    "RPS Halt" die Bezeichnung "Reset" verwendet).
    Beim Boot-Modus RPS Halt bleiben (analog zum Boot-Modus Warmstart) alle
    batteriegepufferten RAM-Daten (alle Tasks im Anwender-RAM, Werte der Ein-
    g„nge, Ausg„nge und Merker, usw.) erhalten. Nach Aufruf der Funktion
    <System><RPS Halt> geht die RPS jedoch in den Service-Modus, d.h. die
    im Anwender-ROM, Anwender-RAM oder FIX-RAM abgelegten Anwenderprogramme
    werden nicht gestartet. Im Service-Modus l„uft nur die RPSSW, alle
    Anwenderprogramme sind inaktiv.









                                      @054[ HM-System ]                    @001[ šbersicht ]
.260
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Warmstart>:
    Mit dieser Funktion k”nnen Sie auf der Steuerung einen "Warmstart" aus-
    l”sen.
    Bei einem Warmstart bleiben alle batteriegepufferten RAM-Daten (alle Tasks
    im Anwender-RAM, Werte der Eing„nge, Ausg„nge und Merker, usw.) erhalten.
    Dadurch haben nach dem Warmstart alle Tasks denselben Zustand (Task aktiv
    oder Task inaktiv) wie vor dem Warmstart. Neu gesteckte Systemmodule
    (Remote Master, Netzwerkmodul, usw.) werden bei einem Warmstart nicht
    erkannt. Zum Initialisieren von neuen Systemmodulen muá ein Kaltstart
    ausgel”st werden.
    Nach einem Warmstart werden alle Tasks, die vor dem Warmstart aktiv waren,
    gestartet. Dabei werden alle vorhandenen INIT-UPs (auch die INIT-UPs in-
    aktiver Tasks) ausgefhrt.






                                      @054[ HM-System ]                    @001[ šbersicht ]
.261
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service>:
    Nach Anwahl der Funktion <System><Service> erscheint ein Popup-Men, in dem
    folgende Funktionen zur Auswahl stehen:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ @262[Kaltstart             ]  ³ ... Totalinitialisierung der RPS
    ³ @263[Diagnose              ]  ³ ... In den Diagnose-Modus schalten
    ³ @266[FIXRAM l”schen        ]  ³ ... FIX-RAM l”schen
    ³ @267[FLASH l”schen         ]  ³ ... FLASH-Anwenderspeicher l”schen
    ³ @268[Memcard l”schen       ]  ³ ... MEM-Card l”schen
    ³ @269[Perm. Speicher l”schen]  ³ ... Permanenten Speicher l”schen
    ³ @270[RPSSW Update          ]  ³ ... RPSSW-Update (Betriebssystem-Update) 
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ






                                      @054[ HM-System ]                    @001[ šbersicht ]
.262
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><Kaltstart>:
    Der Boot-Modus "Kaltstart" (alte Bezeichnung: "TOTALINIT") entspricht einer
    ERST-Initialisierung der CPU. Dabei wird das Betriebssystem v”llig neu ge-
    startet, allokierter Speicher wird freigegeben und die Systemstrukturen
    (alle Informationen des Betriebssystems fr eine ordnungsgem„áe Funktion
    der CPU) werden neu angelegt. Bei einem Kaltstart werden alle Systemmodule
    (Remote Master, Netzwerkmodul, usw.) erkannt und initialisiert. Zus„tzlich
    werden alle im Anwender-RAM gespeicherten Module gel”scht (auáer die im
    FIX-RAM hinterlegten Module) und alle Prozeávariablen (Eing„nge, Ausg„nge
    und Merker) mit dem Wert Null initialisiert.
    Nach einem Kaltstart werden alle im Anwender-ROM oder FIX-RAM abgelegten
    Tasks gestartet (Voraussetzung: Taskparameter <Installation> und <Start>
    auf "Default" eingestellt). Dabei werden alle vorhandenen Initialisierungs-
    Unterprogramme (INIT-UPs) ausgefhrt.





    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.263
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><Diagnose>:
    Wenn ein defektes, im Anwender-ROM oder FIX-RAM gespeichertes B&R-Modul auf
    der RPS einen fatalen Fehler ausl”st, k”nnen Sie mit der Funktion <System>
    <Service><Diagnose> die RPS im Diagnose-Modus hochfahren, das defekte Modul
    ermitteln und (falls erforderlich) l”schen. Dazu wird beim Aufruf dieser
    Funktion automatisch das Diagnose-Modul (DIAGNOSE.BR) auf die RPS geladen.
    Das Diagnose-Modul durchsucht sowohl das Anwender-ROM als auch das FIX-RAM
    nach B&R-Modulen. Nach Abschluá der Untersuchung erscheint folgende Diaog-
    box:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄDiagnoseÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Modul :@264[°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°]ÃÄÄÄÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³                                                 ³   ³ Popup-Fenster mit ³
    ³                                                 ³   ³ einer Liste aller ³
    ³          [  L”schen  ]  [  Cancel   ]           ³   ³ vorhandenen B&R-  ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ   ³ Module            ³
                                                          ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


    @264[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.264
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><Diagnose>:
    Aus der Dialogbox k”nnen Sie ein Popup-Fenster aufrufen. In diesem Popup-
    Fenster werden alle (im Anwender-ROM und FIX-RAM) vorhandenen B&R-Module
    sowie deren Status ("OK" oder "defekt") aufgelistet:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³  0 00.exermo     OK        FLASH  V1.30    ³
    ³         :                        :         ³
    ³  n 00.io_lib     OK        FLASH  V1.50    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
       ³ ³             ³         ³      ÀÄÄÄ Versionsnummer des Moduls
       ³ ³             ³         ÀÄÄÄÄÄÄÄÄÄÄ Speicher des Moduls:
       ³ ³             ³                        EPROM .... EPROM
       ³ ³             ³                        FLASH .... FlashPROM
       ³ ³             ³                        FIXRAM ... FIX-RAM
       ³ ³             ³                        MEMCRD ... MEM-Card
       ³ ³             ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Status des Moduls (OK oder defekt)
       ³ ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Name des Moduls
       ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Laufende Nummer

    @265[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.265
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><Diagnose>:
    Wenn eines der angezeigten B&R-Module als defekt gekennzeichnet ist, k”nnen
    Sie dieses Modul im Popup-Fenster selektieren und anschlieáend durch Anwahl
    des Feldes <L”schen> aus dem Speicher (EPROM, FlashPROM, FIX-RAM bzw. MEM-
    CARD) l”schen.
    Nach einem Hochlauf im Diagnose-Modus kann die RPS nur durch einen Kalt-
    start gestartet werden (Warmstart ist nicht m”glich). Deshalb wird bereits
    beim Aufruf der Funktion <System><Service><Diagnose> darauf hingewiesen,
    daá durch diese Funktion alle Daten im Anwender-RAM verloren gehen!

    Die Funktion <System><Service><Diagnose> wird auch verwendet, wenn ein
    defektes (im Anwender-ROM oder FIX-RAM gespeichertes) B&R-Modul einen
    korrekten Hochlauf verhindert. Um in diesem Fall ein L”schen des ganzen
    Anwender-ROMs bzw. FIX-RAMs zu umgehen, mssen Sie die RPS im Diagnose-
    Modus hochfahren. Beim Boot-Modus Diagnose l„uft die RPS nur mit der
    Betriebssystem-Software hoch, d.h. alle B&R-Module im Anwender-RAM und
    -ROM werden ignoriert und sind somit inaktiv. Dadurch wird sichergestellt,
    daá auch das defekte B&R-Modul nicht referenziert wird.

    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.266
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><FIXRAM l”schen>:
    Mit dieser Funktion k”nnen Sie den gesamten Inhalt des FIX-RAMs l”schen.
    Nach dem L”schen des FIX-RAMs wird automatisch ein Kaltstart durchgefhrt.
    Aus diesem Grund wird bereits beim Aufruf der Funktion <System><Service>
    <FIXRAM l”schen> darauf hingewiesen, daá durch diese Funktion alle Daten
    im Anwender-RAM verloren gehen!

    Das FIX-RAM ist jener Teil des Anwender-RAMs, der sich beim Kaltstart wie
    das Anwender-ROM verh„lt. D.h. B&R-Module, die im FIX-RAM gespeichert sind,
    werden bei einem Kaltstart nicht gel”scht.









    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.267
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><FLASH l”schen>:
    Mit der Funktion <System><Service><FLASH l”schen> k”nnen Sie den gesamten
    Inhalt des FlashPROMs (Anwender-ROMs) l”schen.
    Nach dem L”schen des FlashPROMs wird automatisch ein Kaltstart durch-
    gefhrt. Aus diesem Grund wird bereits beim Aufruf der Funktion <System>
    <Service><FLASH l”schen> darauf hingewiesen, daá durch diese Funktion alle
    Daten im Anwender-RAM verloren gehen!












    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.268
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><Memcard l”schen>:
    Mit der Funktion <System><Service><Memcard l”schen> k”nnen Sie alle auf der
    PCMCIA-Speicherkarte (auch "MEM-Card" genannt) gespeicherten Daten l”schen.
    Nach dem L”schen der MEM-Card wird automatisch ein Kaltstart durchgefhrt.
    Aus diesem Grund wird bereits beim Aufruf der Funktion <System><Service>
    <Memcard l”schen> darauf hingewiesen, daá durch diese Funktion alle Daten
    im Anwender-RAM verloren gehen!

    Auf der PCMCIA-Speicherkarte k”nnen Anwenderprogramme (zyklische und nicht-
    zyklische Tasks, Datenmodule, etc.) nullspannungssicher gespeichert werden.
    Die PCMCIA-Speicherkarte kann wie der FlashPROM-Anwenderspeicher verwendet
    werden.







    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.269
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><Perm. Speicher l”schen>:
    Ab der Version 1.91 der RPSSW kann auf der RPS ein Speicherblock definiert
    werden, welcher auch bei einem Kaltstart nicht gel”scht wird (permanenter
    Speicher). Mit der Funktion <System><Service><Perm. Speicher l”schen>
    k”nnen Sie diesen permanenten Speicher l”schen. 
    Nach dem L”schen des permanenten Speichers wird automatisch ein Kaltstart
    durchgefhrt. Aus diesem Grund wird bereits beim Aufruf der Funktion
    <System><Service><Perm. Speicher l”schen> darauf hingewiesen, daá durch
    diese Funktion alle Daten im Anwender-RAM verloren gehen!

    Die Gr”áe des permanenten Speichers kann in der Systemkonfiguration fest-
    gelegt werden. Dazu rufen Sie die Funktion <Objekt><Parameter> aus der
    Task-Ebene der GDM auf. Der Cursor muá sich dabei auf dem "sysconf"-Symbol
    befinden. Anschlieáend kann durch Anwahl der Funktion <Speicher> aus dem
    Popup-Men des Feldes <Konfiguriere> die Gr”áe des permanenten Speichers
    ("Permanente Merker") festgelegt werden.
    Die entsprechenden Eingabefelder sind erst ab Version 1.91 der RPSSW frei-
    gegeben!

    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.270
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><RPSSW Update>:
    Mit dieser Funktion kann das Betriebssystem RPSSW in den AWS bertragen
    oder ein Betriebssystem-Update durchgefhrt werden.

    VORBEREITENDE TTIGKEITEN durch den Anwender:
    1. Der PROGRAMMIERSCHUTZSCHALTER auf der AWS-Front muá sich in Schreib-
       position befinden.
    2. Kontrolle der BOOTMAN-SCHALTERSTELLUNG hinter dem AWS-Seitenfenster:
       - Bei bereits programmiertem FlashPROM-AWS (Betriebssystem-Update) muá
         sich der Bootman-Schalter in der Stellung "ERASE" befinden!
       - Bei leerem FlashPROM-AWS (Betriebssystem-Download) ist auch die
         Bootman-Schalterstellung "OK" m”glich!
    3. Update:   Aufruf der Funktion <System><Service><RPSSW Update>.
       Download: Nach dem Stecken eines neuen (leeren) FlashPROM-AWS wird der
                 Download-Vorgang im PG2000 automatisch gestartet.
    Wenn diese Schritte nicht eingehalten werden, kommt keine PG-/Bootman-
    Verbindung zustande. In diesem Fall ist kein Betriebssystem-Update bzw.
    Betriebssystem-Download m”glich!

    @271[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.271
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Service><RPSSW Update>:
    Beim Betriebssystem-Update wird nach Aufruf der Funktion <RPSSW Update>
    eine Dialogbox eingeblendet, in der Sie die šbertragungsrate (Baudrate)
    und die PC-Schnittstelle fr den Update festlegen k”nnen:

    ÚÄÄÄÄÄÄÄÄÄ System Download ÄÄÄÄÄÄÄ¿
    ³Baudrate: 9600   Port: COM1    ³ Baudrate: 9600/19200/38400/57600/115200
    ³                                 ³ Port:     COM1/COM2
    ³     [   OK   ]  [ Cancel ]      ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Durch Anwahl des Feldes <OK> wird eine weitere Dialogbox eingeblendet. In
    dieser Dialogbox kann die Version des Betriebssystems angew„hlt werden.
    Nach dem Schlieáen dieser Dialogbox mit <Ja> wird der Betriebssystem-Update
    bzw. -Download gestartet. Der Update-/Download-Fortschritt wird in der
    Meldungszeile angezeigt. Um das Booten des Betriebssystemes zu erm”glichen,
    mssen Sie die RPS nach der šbertragung neu starten (Power OFF/ON) und den
    Bootman-Schalter wieder auf "OK" zurckstellen!

    @261[ Fortsetzung ]                     @054[ HM-System ]                    @001[ šbersicht ]
.272
    PG2000  HAUPTMENš FUNKTIONEN

    <SYSTEM><Modem>:
    Mit Hilfe der MODEM-Funktionen kann eine Modem-Verbindung aufgebaut werden.
    šber diese Verbindung stehen (wie fr jede andere Online-Verbindung zu
    einer RPS) alle RPS-Funktionen zur Verfgung.
 
    Nach Aufruf der Funktion <System><Modem> erscheint ein Popup-Men mit den
    folgenden MODEM-Funktionen:

    ÚÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ W„hlen    ³ ... Modem-Verbindung AUFBAUEN
    ³ Auflegen  ³ ... Modem-Verbindung ABBAUEN
    ³ Befehl    ³ ... Modemspezifischen DIENST absetzen
    ³ Sichern   ³ ... Modem-Einstellungen SICHERN
    ÀÄÄÄÄÄÄÄÄÄÄÄÙ





                                      @054[ HM-System ]                    @001[ šbersicht ]
.273
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor>:
    Mit Hilfe dieser Funktion k”nnen die STANDARDEINSTELLUNGEN fr folgende
    Editoren festgelegt werden:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@274[ PL2000\AWL\Text  ]³ ... fr die textuellen Editoren
    ³@278[ Kontaktplan      ]³ ... fr den KOP-Editor
    ³@279[ Variable         ]³ ... fr die Variablendeklaration
    ³@282[ Bereichseditor   ]³ ... fr den Bereichs-Editor
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    ACHTUNG:  Eine dauerhafte šbernahme dieser Einstellungen erfolgt erst
              nach dem Aufruf der HM-Funktion <Einstellungen><Sichern>!
              Andernfalls gelten die ver„nderten Einstellungen nur w„hrend
              der aktuellen PG-Sitzung.





                                  @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.274
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><PL2000\AWL\Text>:   
    Mit dieser Funktion k”nnen Sie die Standardeinstellungen fr die textuellen
    Editoren festlegen:

    ÚÄÄÄÄÄÄÄÄÄEinstellungenÄÄÄÄÄÄÄÄÄÄ¿  
    ³@275[Blockmodus     ]: Zeile         ³ ... Zeile/Spalte
    ³@275[Zeilenbindung  ]: Ein           ³ ... Ein/Aus
    ³@276[Cursorbewegung ]: Folgen        ³ ... Folgen/Verlassen
    ³@276[Labelfeld berspringen ] : Ja   ³ ... Ja/Nein
    ³@277[Tabulator      ]: 8              ³ ... Tabulatorabstand
    ³                                ³
    ³  [    OK    ]  [  Cancel  ]    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Eine dauerhafte šbernahme dieser Einstellungen erfolgt erst nach dem Aufruf
    der HM-Funktion <Einstellungen><Sichern>.



    @273[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.275
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><PL2000\AWL\Text>:   * ==> Default-Einstellung

    <Blockmodus>:
    * Zeile:     Nur ZEILENWEISES MARKIEREN (Kopieren, ...) m”glich.
    - Spalte:    BELIEBIGES BLOCKMARKIEREN (Kopieren, ...) m”glich. Dieser
                 Modus kann nur gew„hlt werden, wenn die Zeilenbindung
                 ausgeschaltet ist.

    <Zeilenbindung>:
    * Ein:       Zeilen k”nnen mit <Enter> NICHT GETRENNT werden.
                 In der PL2000 wird bei aktiver Zeilenbindung auch ein Online-
                 Syntax-Check durchgefhrt.
    - Aus:       In diesem Modus kann an jeder Cursor-Position mit <Enter>
                 ein ZEILENUMBRUCH eingefgt werden (Trennen einer Zeile).





    @274[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.276
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><PL2000\AWL\Text>:   * ==> Default-Einstellung

    <Cursorbewegung>:
    - Folgen:    Der Cursor kann NUR im EINGEGEBENEN TEXTBEREICH positioniert
                 werden. Die Grenzen sind das Zeilen- und Dokument-Ende.
    * Verlassen: Der Cursor kann an jeder BELIEBIGEN POSITION, auch auáerhalb
                 des EINGEGEBENEN TEXTBEREICHES (die Grenzen des Textbereiches
                 sind das Zeilen- und Dokument-Ende) positioniert werden.
                 Wenn sich der Cursor auáerhalb des eingegebenen Textes be-
                 findet, wird dies durch eine GENDERTE CURSOR-FORM und -FARBE
                 angezeigt.

    <Labelfeld berspringen>:
    * Ja:        Der Cursor wird nach einem <Enter> auf die Position des ersten
                 Tabulators in der n„chsten Zeile gestellt.
    - Nein:      Der Cursor wird nach einem <Enter> auf die erste Spalte der
                 n„chsten Zeile gestellt.


    @274[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.277
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><PL2000\AWL\Text>:   * ==> Default-Einstellung

    <Tabulator>: Hier k”nnen Sie den Raster fr die Tabulatoren einstellen.
                 Mit jeder Bet„tigung der <TAB>-Taste wird der Cursor auf die
                 n„chste Postion des Rasters bewegt.














    @274[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.278
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><Kontaktplan>:

    ÚÄÄÄÄÄÄÄEinstellungenÄÄÄÄÄÄÄ¿        ÚÄÄÄÄÄÄÄ¿
    ³Langtext  :    aus       ÄÅÄÄÄÄ¿   ³ ein   ³
    ³I/O-Krzel:    aus       ÄÅÄÄÄÄÁÄÄÄ´ aus   ³
    ³                           ³        ÀÄÄÄÄÄÄÄÙ
    ³  [   OK   ]  [ Cancel ]   ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Das I/O-Krzel und der Langname k”nnen optional im unteren Fensterrahmen
    angezeigt werden. Wenn die Einstellungen eingeschaltet sind und fr den
    angew„hlten Kontakt ein I/O-Krzel oder ein Langname existiert, werden
    diese im unteren Fensterrahmen angezeigt:

    ÀÄZeile x Spalte y / I/O-Krzel / Langname ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ

    Eine dauerhafte šbernahme dieser Einstellungen erfolgt erst nach dem
    Aufruf der HM-Funktion <Einstellungen><Sichern>.

    @273[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.279
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><Variable>:
                                         ÚÄÄÄÄÄÄÄ¿
    ÚÄÄÄÄÄÄÄEinstellungenÄÄÄÄÄÄÄ¿        ³ Ja    ³
    ³@280[Langtext  ]:    Nein      ÄÅÄÄÄÄÂÄÄÄ´ Nein  ³
    ³@281[I/O-Krzel]:    Ja        ÄÅÄÄÄÄ´   ÀÄÄÄÄÄÄÄÙ
    ³@280[Trennzeile]:    Nein      ÄÅÄÄÄÄ´   
    ³@280[Init-Werte]:    Nein      ÄÅÄÄÄÄÙ
    ³                           ³
    ³  [   OK   ]  [ Cancel ]   ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


    Standardeinstellungen der VARIABLENDEKLARATION festlegen.

    Eine dauerhafte šbernahme dieser Einstellungen erfolgt erst nach dem
    Aufruf der HM-Funktion <Einstellungen><Sichern>.



    @273[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.280
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><Variable>:     * ==> Default-Einstellung

    <Langtext>:
    * Ja:   ANZEIGE des Eingabefeldes "Langtext" im Tabelleneditor der VD
            bzw. ERM™GLICHEN einer Langtext-EINGABE.
    - Nein: Das Eingabefeld Langtext wird im TABELLENEDITOR der VD NICHT
            ANGEZEIGT.

    <Trennzeile>:
    - Ja:   Zwischen den PV-Eintr„gen wird eine Trenn-Linie "ÄÄÄÄ" ausgegeben.
    * Nein: Alle PV-Eintr„ge stehen DIREKT UNTEREINANDER.

    <Init-Werte>:
    - Ja:   ANZEIGE des Eingabefeldes "Init-Wert" im Tabelleneditor der VD
            bzw. ERM™GLICHEN einer INITWERT-EINGABE. (Fr die Initialisierung
            der PVs muá die RPS-SW V1.10 und mind. BUGFIX_V115 gebrannt sein!)
    * Nein: Keine Definition von PV-INITWERTEN.


    @279[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.281
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><Variable>:     * ==> Default-Einstellung

    <I/O-Krzel>:
    * Nein: Textuelle Anzeige des Modultyps und Eingabe ber POPUP-Men
            in Form von 2005-I/O, 2010-I/O, IP-I/O, ...

    - Ja:   Anzeige der HW-Zuordnung.
            Die HW-Zuordnung kann DIREKT im Eingabefeld "I/O-Typ" eingegeben
            werden, ohne das POPUP-Men zu bentzen.

     ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ  Modul-Typ (I ... Eingangsmodul; Q ... Ausgangsmodul)
     ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ  Bus-Typ (P ... I/O-Bus; R ... Remote-Bus)
     ³³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ  B&R-Hardware-Typ (1 ... 2010; 5 ... 2005; 3 ... 2003)
     ³³³ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ  genaue Adresse (3 oder 4 Ziffern je nach Typ)
     mBx.y1.y2.y3.y4




    @279[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.282
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Editor><Bereichseditor>:
                                        ÚÄÄÄÄÄÄÄÄÄÄ¿
                                        ³ Hex      ³
    ÚÄÄÄÄÄÄ Einstellungen ÄÄÄÄÄÄ¿   ÚÄÄÄ´ Dezimal  ³         ÚÄÄÄÄÄÄÄ¿
    ³Anzeige    :   Dezimal   ÄÅÄÄÄÙ   ÀÄÄÄÄÄÄÄÄÄÄÙ         ³ Ja    ³
    ³Offsetcheck:   Ja        ÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´ Nein  ³
    ³                           ³                            ÀÄÄÄÄÄÄÄÙ
    ³  [   OK   ]  [ Cancel ]   ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    <Anzeige>:
    - Hex:     Anzeige/Eingabe des Offsets als HEX-ZAHL.
    - Dezimal: Anzeige/Eingabe des Offsets als DEZIMAL-ZAHL.

    <Offsetcheck>:
    - Ja:   OFFSETCHECK wird beim Sichern des Bereichseditors DURCHGEFšHRT.
    - Nein: Es wird KEIN OFFSETCHECK durchgefhrt.


    @273[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.283
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄCompilerÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿   ACHTUNG:
    ³Allgemein:                          ³   Eine dauerhafte šbernahme dieser
    ³  @285[Generieren    ]  :  Make          ³   Einstellungen erfolgt erst nach
    ³  @286[ASCII Import  ]  :  Nachfragen    ³   dem Aufruf der HM-Funktion
    ³I/O:                                ³   <Einstellungen><Sichern>!
    ³  @287[I/O-Tabellen  ]  :  standard      ³   Andernfalls gelten die ver„nderten
    ³  @288[I/O-Emulation ]  :  optimiert     ³   Einstellungen nur w„hrend der
    ³  @289[Bit Behandlung]  :  optimiert     ³   aktuellen PG-Sitzung.
    ³Runtime:                            ³
    ³  @290[Range Check   ]  :  aus           ³
    ³  @290[Check adr(0)  ]  :  aus           ³
    ³Install:                            ³
    ³  @291[Transfer Mode ]  :  Exchange      ³
    ³  @294[Spoolen       ]  :  bei Download  ³
    ³  @295[Rebuild Info  ]  :  ein           ³
    ³                 ...                ³

    @284[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.284
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:

    ³                ...                 ³
    ³Init-Werte:                         ³
    ³  @296[Merker        ]  :  remanent      ³
    ³  @296[Output        ]  :  remanent      ³
    ³                                    ³
    ³      [   OK   ]  [ Cancel ]        ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ










                                  @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.285
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Generieren>:
    * Make:     Beim Compilieren werden nur die zum šbersetzen des angew„hlten
                Objektes notwendigen Vorg„nge durchgefhrt:
                - Nur wenn der PL2000-Source-Code jngeren Datums als das
                  lauff„hige Modul (B&R-Modul) ist, wird der ZEILENPARSER
                  aktiviert.
                - Nur wenn eine Programm„nderung erfolgte (Speichern, PV ge-
                  „ndert) wird der CODEGENERATOR fr die PL2000/AWL/KOP-Syntax
                  aufgerufen.
                - Trifft keiner der oben angefhrten Punkte zu, wird nur der
                  TASKBUILDER aufgerufen.
    - Compiler: Der CODEGENERATOR wird JEDESMAL beim Compilieren AUFGERUFEN.
    - Parser:   Der ZEILENPARSER und der CODEGENERATOR werden JEDESMAL beim
                Compilieren AUFGERUFEN.



    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.286
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <ASCII Import>:
    Beim Compilieren eines Projektes besteht die M”glichkeit, verschiedene
    Arten von "ASCII->Editor"-Imports durchzufhrten.
    VORAUSSETZUNG: Eine gltige ASCII-Importdatei (*.brt/*.awt) muá ein
    "jngeres" Datum als die entsprechende Projekt-Source-Datei (*.src) haben.
    Folgende M”glichkeiten stehen zur Auswahl:

    * Nachfrage:   Wahlweiser Import "neuerer" ASCII-Importdateien. Vor jedem
                   Import werden Sie aufgefordert, den Import zu best„tigen.
    - Automatisch: Die "neuere" ASCII-Importdatei wird automatisch vor dem
                   Compilieren importiert (und als Source gespeichert).
    - Kein Import: Vorhandene ASCII-Importdateien werden NICHT importiert.





    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.287
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <I/O-Tabellen>:
    Beim Compilieren eines Projektes besteht mit dieser Compiler-Option die
    M”glichkeit, die I/O-Schaufeltabellen zu optimieren:

    - optimiert: Es wird nur EINE OPTIMIERTE SCHAUFELTABELLE PRO TASKKLASSE an-
                 gelegt. Dies ist bei zeitkritischen Anwendungen vorteilhaft,
                 da die I/O-Variablen nur einmal pro Taskklasse gelesen werden.
                 Die compilierten Tasks sind KEINE LAUFFHIGEN MODULE
                 (Schaufeltabelle extra) und drfen daher nur mit der Funktion
                 <Objekt Liste><RUN> (Popup-Men der RPS-Ebene) auf die RPS
                 bertragen werden!
    * standard:  Compilieren anhand der eingestellten Compiler-Optionen, wobei
                 jede compilierte Task aus einem lauff„higen Modul und einer
                 eigenen PV-Schaufeltabelle besteht.



    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.288
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <I/O-Emulation>:
    Beim Steuerungssystem B&R 2005 sind System- und I/O-Bus der Basisrckwand
    nicht getrennt. Das bedeutet, daá auf der Basisrckwand kein I/O-Prozessor
    arbeitet. Die Funktion des I/O-Prozessors wird bei diesem System durch die
    Software emuliert (I/O-Emulation).
    Ab Version 1.92 des Betriebssystems RPSSW ist eine zweite (optimierte) Form
    der I/O-Emulation m”glich. Bei der optimierten I/O-Emulation wird fr die
    Basisrckwand des Systems B&R 2005 anstelle der I/O-Schaufeltabelle ein
    Code zum Kopieren der I/O-Punkte erzeugt. Dieser Code wird nicht vom PG,
    sondern vom Betriebssystem generiert (w„hrend der Installation der Task).
    Die optimierte Form der I/O-Emulation verbraucht weniger CPU-Zeit als die
    herk”mmliche I/O-Emulation (I/O-Emulation mit I/O-Schaufeltabelle).

    * optimiert: Die optimierte I/O-Emulation wird verwendet.
    - standard:  Die herk”mmliche I/O-Emulation wird verwendet.


    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.289
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Bit Behandlung>:
    Bei den Zentraleinheiten des Systems B&R 2000 (CP1xx, CP2xx, usw.) ist die
    Bit/Byte-Logik per Hardware als ASIC realisiert. Damit ist eine optimierte
    Bit-Behandlung m”glich (das gew”hnlich praktizierte zeitraubende Maskieren
    durch die Zentraleinheit entf„llt).
    Bei anderen Prozessoren (C200, usw.) muá die Bit/Byte-Logik durch die
    Software realisiert werden. Dadurch ist die Bit-Behandlung bei diesen
    Prozessoren viel zeitaufwendiger als die optimierte Bit-Behandlung bei
    den Zentraleinheiten des Systems B&R 2000.

    * optimiert: Optimierte Bit-Behandlung (wenn die Bit/Byte-Logik durch die
                 Hardware realisiert ist).
    - standard:  Standardm„áige Bit-Behandlung (wenn die Bit/Byte-Logik durch
                 die Software realisiert ist).



    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.290
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Range Check>:
    - ein:   Bei Tasks, die mit der Option <Range Check>=ein compiliert wurden,
             wird w„hrend der Laufzeit jeder Feld-Zugriff berwacht. Erkennt
             die CPU einen Zugriff auf ein nichtexistierendes Element einer
             Feldvariable, wird die Exception "Range Overflow" ausgel”st.
    * aus:   Feld-Zugriffe werden NICHT berwacht.


    <Check adr(0)>:
    - ein:   Bei Tasks, die mit der Option <Check adr(0)>=ein compiliert wurden,
             wird w„hrend der Laufzeit jeder Zeiger-Zugriff berwacht. Erkennt
             die CPU einen Zugriff auf einen nichtinitialisierten Zeiger
             (Adresse=0), wird die Exception "Null Pointer" ausgel”st.
    * aus:   Zeiger-Zugriffe werden NICHT berwacht.



    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.291
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Transfer Mode>:
    Mit dem Parameter Transfer Mode definiert der Anwender, wie die Task auf
    die RPS bertragen wird. Wird anstelle von Exchange Update gew„hlt, k”nnen
    nur mehr nderungen compiliert werden, die das šbertragen im Modus Update
    erm”glichen.
    * @292[Exchange]: Tasks, die bereits auf der RPS vorhanden sind, werden zun„chst
                auf der RPS gel”scht und erst dann bertragen. 
    - @293[Update  ]: Tasks, die bereits auf der RPS vorhanden sind, werden beim
                šbertragen stoáfrei ausgetauscht. D.h. die Task wird nicht
                gel”scht, sondern einfach berschrieben. Der šbertragungsmodus
                Update ist jedoch nicht immer m”glich. Wurden z.B. nderungen
                in der Speicherbelegung der Task vorgenommen, kann die šber-
                tragung nicht im Modus Update durchgefhrt werden. In diesem
                Fall wird der šbertragungsvorgang abgebrochen und es erscheint
                eine entsprechende Meldung im Meldungsfenster. Die šbertragung
                kann dann nur im Modus Exchange durchgefhrt werden.

    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.292
    PG2000  HAUPTMENš FUNKTIONEN

    Exchange:
               ³
    System-    ³     10 ms
    zyklus     ³<------------>³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
               ³   (3)        ³               ³               ³           Zeit
               ³<------->³    ³               ³               ³
    Zyklisches ÃÄÄÄÄÄÂÄÄÄ´    ÃÄÄÄÄÄ¿         ÃÄÄÄÄÄÂÄÄÄ¿     ÃÄÄÄÄÄÂÄÄÄ¿
    System     ³     ³(1)³    ³     ³         ³     ³(2)³     ³     ³(2)³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÅÄÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÁÄÄÁÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÁÄÄÄÄ>
               ³         ³    ³     ³                 ³
               ³         ÃÄÂÄÄ´     ÃÄÄÄÄÄ¿           ³
    Restzeit   ³         ³ ³°°³     ³°°°°°ÃÄÄÄÄÄÄÄÄÄÄÄÙ
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÁÅÁÅÄÅÄÄÄÄÄÁÅÄÄÄÄÁÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
                          ³ ³        ³
    (1) ... alte Task     ³ ÀÄÄÄÄÄÄÄÄÁÄ šbertragung der neuen Task auf die RPS
    (2) ... neue Task     ÀÄÄÄÄÄÄÄÄÄÄÄÄ L”schen der alten Task       
    (3) ... zyklische Tasks

    @291[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.293
    PG2000  HAUPTMENš FUNKTIONEN

    Update: (stoáfreier Austausch einer Task)
               ³
    System-    ³     10 ms
    zyklus     ³<------------>³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
               ³   (3)        ³               ³               ³           Zeit
               ³<------->³    ³               ³               ³
    Zyklisches ÃÄÄÄÄÄÂÄÄÄ´    ÃÄÄÄÄÄÂÄÄÄ¿     ÃÄÄÄÄÄÂÄÄÄ¿     ÃÄÄÄÄÄÂÄÄÄ¿
    System     ³     ³(1)³    ³     ³(1)³     ³     ³(2)³     ³     ³(2)³
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÅÄÄÄÄÅÄÄÄÄÄÁÄÄÄÅÄÄÄÄÄÅÄÄÄÄÄÁÄÄÁÄÄÄÄÄÅÄÄÄÄÄÁÄÄÄÁÄÄÄÄ>
               ³         ³    ³         ³             ³
               ³         ÃÄÄÄÄ´         ÃÄÄÄ¿         ³
    Restzeit   ³         ³°°°°³         ³°°°ÃÄÄÄÄÄÄÄÄÄÙ
    ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÁÄÅÄÄÅÄÄÄÄÄÄÄÄÄÁÄÅÄÁÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄ>
                           ³              ³
    (1) ... alte Task      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄ šbertragung der neuen
    (2) ... neue Task                          Task auf die RPS
    (3) ... zyklische Tasks

    @291[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.294
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Spoolen>:
    * bei Download:  Das compilierte Objekt wird SOFORT nach dem šbertragen
                     (Download) zum spezifizierten IO-Prozessor (Steckplatz
                     bekannt) gespoolt.
    - bei Warmstart: Das compilierte Objekt wird zun„chst in die CPU ber-
                     tragen. Der Spoolvorgang von der CPU an den IO-Prozessor
                     erfolgt erst nach einem Warmstart (siehe dazu Funktion
                     <System><Warmstart>).
    - nie Spoolen:   Bei dieser Einstellung wird das Objekt zwar als nicht zu
                     installieren gekennzeichnet, allerdings wird das Objekt
                     weder nach dem šbertragen (Download) noch bei einem Warm-
                     start zum IO-Prozessor gespoolt. Damit ist es m”glich,
                     ausschlieálich mit den Funktionen der I/O-Prozessor-
                     Library (Spool-Library) zu arbeiten.



    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.295
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Rebuild Info>:
    * ein:   Aus einem lauff„higen Modul (*.BR-Module), das mit dieser Ein-
             stellung erzeugt wurde, kann durch Rckbersetzen mit der Funktion
             <Rebuild> (siehe Popup-Men der Task-Ebene) oder durch einen
             <Projekt><Abgleich> die komplette Quellinformation (Source-Code,
             INIT-UP, Taskparameter, VD, usw.) wiedergewonnen werden.
    - aus:   Beim Compilieren wird im lauff„higen Modul (*.BR-Module) keine
             Rckbersetzungsinformation abgelegt, d.h. die mit dieser Ein-
             stellung erzeugten Module k”nnen nicht rckbersetzt werden.








    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.296
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Compiler>:     * ==> Default-Einstellung

    <Init-Werte><Merker>:
    * remanent:  Fr Merker ist standardm„áig KEIN Initialwert festgelegt.
    - 0:         Standardm„áig wird fr alle Merker der Initialwert 0 verwendet.

    <Init-Werte><Output>:
    * remanent:  Fr digitale und analoge Ausg„nge ist standardm„áig KEIN
                 Initialwert festgelegt.
    - 0:         Standardm„áig wird fr alle digitalen und analogen Ausg„nge
                 der Initialwert 0 verwendet.

    In der Variablendeklaration kann fr jede PV ein beliebiger INITIALWERT
    definiert werden. Erfolgt KEINE SPEZIELLE DEKLARATION in der VD, werden die
    Werte von <Merker> und <Output> standardm„áig noch vor der Durchfhrung der
    Init-Programme auf die entsprechenden PVs kopiert !!!

    Voraussetzung: RPS-SW V1.10 und mind. BUGFIX_V115 muá gebrannt sein!

    @283[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.297
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><On-Line>:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄOn-LineÄÄÄÄÄÄÄÄÄÄÄÄÄ¿   ACHTUNG:
    ³Allgemein                        ³   Eine dauerhafte šbernahme dieser
    ³  @298[Variablen Update  ] :     990 ms³   Einstellungen erfolgt erst nach
    ³PV Anzeige                       ³   dem Aufruf der HM-Funktion
    ³  @298[Format            ] :Dezimal   ³   <Einstellungen><Sichern>.
    ³  @298[TC-Info           ] :Nein      ³   Andernfalls gelten die ver„nderten
    ³Sicherheit                       ³   Einstellungen nur w„hrend der
    ³  @299[Stoppen best„tigen] :Ja        ³   aktuellen PG-Sitzung.
    ³  @299[L”schen best„tigen] :Ja        ³
    ³Speichern Monitor                ³
    ³  @299[Objektparameter   ] :Ja        ³
    ³RPSSW Download                   ³
    ³  @300[Baudrate          ] :57600     ³
    ³                                 ³
    ³      [   OK   ]  [ Cancel ]     ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


                                  @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.298
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><On-Line>:     * ==> Default-Einstellung

    <Variablen Update>:
    Mit dieser Funktion k”nnen Sie die Aktualisierungsrate (REFRESH RATE)
    fr den PV-Monitor und den KOP-Monitor (in Millisekunden) einstellen.

    <Format>:
    * Dezimal: Der Wert der PV wird (im PV-Monitor) als dezimale Zahl an-
               gezeigt.
    - Hex:     Der Wert der PV wird (im PV-Monitor) als hexadezimale Zahl
               angezeigt.

    <TC-Info>:
    - Ja:      Im PV-Monitor wird die Spalte "gltig" (Gltigkeitsbereich
               der PV) angezeigt.
    * Nein:    Im PV-Monitor wird die Spalte "gltig" (Gltigkeitsbereich
               der PV) ausgeblendet.


    @297[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.299
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><On-Line>:     * ==> Default-Einstellung

    <Stoppen best„tigen>:
    * Ja:      Das Stoppen von Tasks ist mit einer Rckfrage abgesichert.
    - Nein:    Keine Rckfrage beim Stoppen einer Task.

    <L”schen best„tigen>:
    * Ja:      Das L”schen von Tasks ist mit einer Rckfrage abgesichert.
    - Nein:    Keine Rckfrage beim L”schen einer Task.

    <Objektparameter>:
    * Ja:      Beim Aufruf der Funktion <Speichern> des RPS-Monitors kann der
               gewnschte Zielspeicher (Anwender-ROM, FIX-RAM, MEM-Card oder
               System-ROM) angegeben werden. D.h. der in den Objekt-Parametern
               eingestellte Zielspeicher wird ignoriert.
    - Nein:    Bei dieser Einstellung wird fr die Funktion <Speichern> der in
               den Objekt-Parametern eingestellte Zielspeicher verwendet (siehe
               dazu Funktion <Objekt><Parameter>).

    @297[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.300
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><On-Line>:     * ==> Default-Einstellung

    <Baudrate>:
    Mit dieser Funktion k”nnen Sie die Baudrate fr den Betriebssystem-Download
    festlegen. Nach dem Stecken eines neuen (leeren) FlashPROM-AWS wird im
    PG2000 der Download-Vorgang automatisch gestartet.













    @297[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.301
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Verbindung>:
                        ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                        ³ AUSWAHL der Verbindungen ber deren Verbindungs-   ³
                        ³ namen (OFFLINE/COM1/COM2/...) und ANZEIGE und des  ³
                        ³ aktuellen Verbindungsstatus:                       ³
                        ³     - Ein .... AKTIVE Verbindung                   ³
    ÚÄÄVerbindungÄÄÄ¿   ³     - Init ... Verbindung wird gerade aufgebaut    ³
    ³ 1 COM1   EinÄÅÄÄÄ´     - Aus .... KEINE Verbindung                    ³
    ³               ³   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    ³ [    OK    ]  ³  ... Angezeigte Verbindung best„tigen/aktivieren
    ³ [@303[  Konfig  ]]  ³  ... Konfigurationsmodul ausw„hlen/wechseln
    ³ [  Cancel  ]  ³  ... Abbrechen ohne Aktion
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    - Wenn eine Verbindung (COM1/COM2/PROFIBUS-VB/...) angew„hlt ist, wird
      diese vom PG aufrecht erhalten und steht daher immer zur Verfgung (wenn
      ein Verbindungsaufbau m”glich war). Kann kein Verbindungsaufbau erfolgen
      (z.B. Kabel abgesteckt), wird der Verbindungsaufbau zyklisch vom PG
      durchgefhrt, bis der Verbindungsaufbau erfolgt.

    @302[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.302
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Verbindung>:
    - Bei der Verbindung <OFFLINE> wird KEINE Verbindung zur RPS aufgebaut.
      Wenn ein Verbindungsaufbau erfolgen soll, muá die gewnschte Verbindung
      ausgew„hlt und mit <OK> best„tigt werden.
    - Wenn die angew„hlte Verbindung AUFGEBAUT werden konnte, wird deren
      Verbindungsstatus in der @908[Meldungszeile] angezeigt.













    @303[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.303
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Verbindung><Konfig>:

    - šber eine Dateiauswahlbox kann das gewnschte NW-Konfigurationsmodul
      (Netzwerk-Konfigurationsmodul) ausgew„hlt werden.
    - Standardm„áig werden die NW-Konfigurationsmodule aus dem PG-Verzeichnis
      NW_CONFIG angezeigt. Beispiele:

        nw_p1_19.br
        nw_p1_57.br
        nw_p2_19.br
        nw_p2_57.br
            ³ ÀÄÄÄÄÄÄÄ Baudrate:             57 ... 57600 Baud
            ³                                19 ... 19800 Baud
            ÀÄÄÄÄÄÄÄÄÄ Schnittstellennummer:  1 ... serielle Schnittstelle COM1
                                              2 ... serielle Schnittstelle COM2

    - Nach dem Ausw„hlen und Best„tigen des neuen NW-Konfigurationsmoduls kann
      in der Verbindungs-Dialogbox die gewnschte Verbindung ausgew„hlt werden.

    @301[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.304
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Dokumentation>:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄDruckenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@305[Drucker   ]:HP Laser Jet                   ³
    ³@305[Vorspann  ]:                               ³
    ³@306[Kopfzeile ]:Project: @@p  @@n  Author:        ³
    ³@306[Fuázeile  ]:@@d B&R Perfection in Automation ³
    ³-------------------------------------------³
    ³Allgemein @307[Ausgabe  ]:LPT1                  ³
    ³          @307[Vorschub ]:Nach                  ³
    ³Seite     @308[Zeilen   ]:      80               ³
    ³          @308[Spalten  ]:      72               ³
    ³R„nder    @309[Oben     ]:       0               ³
    ³          @309[Unten    ]:       0               ³
    ³          @309[Links    ]:       0               ³
    ³          @309[Rechts   ]:       0               ³
    ³Abst„nde  @309[Kopfzeile]:       1               ³
    ³          @309[Fuázeile ]:       1               ³
    ³           [   OK   ]  [ Cancel ]          ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
                                  @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.305
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Dokumentation>:

    <Drucker>:
    šber dieses Eingabefeld k”nnen Sie ein Popup-Fenster ”ffnen, in dem Sie den
    gewnschten Druckertyp anw„hlen k”nnen.

    <Vorspann>:
    ASCII-String fr die Druckerinitialisierung.











    @304[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.306
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Dokumentation>:

    <Kopfzeile> und <Fuázeile>:
    Mit diesem beiden Eintr„gen k”nnen Sie die Kopf- und Fuázeile definieren.
    Folgende Variablen k”nnen fr Kopf- oder Fuázeilen verwendet werden:

    Variable:      Bedeutung:
    --------------------------------------------------------------
    @@s             Seitennummer
    @@p             Projektname
    @@n             Name der Datei, Task, CPU oder RPS
    @@d             Datum und Zeit der letzten nderung der Datei
    @@a             Autor der letzten nderung der Datei

    Sind in der Kopf- oder Fuázeile ZWEI oder MEHR LEERZEICHEN eingetragen,
    werden beim Ausdruck an diesen Stellen soviele Leerzeichen eingefgt,
    daá die VOLLE SEITENBREITE erreicht wird.


    @304[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.307
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Dokumentation>:

    <Allgemein><Ausgabe>:
    Mit diesem Eintrag legen Sie fest, welcher Anschluá vom Drucker belegt
    wird. Neben den Anschlssen LPT1, LPT2, LPT3 und LPT4 kann auch ein Ver-
    zeichnis oder eine Datei als Anschluá angeben werden. In diesem Fall
    wird die Ausgabe in ein Verzeichnis bzw. eine Datei umgelenkt. Bei Angabe
    eines Verzeichnisses muá bei jedem Druckauftrag der Name der Druck-Datei
    festgelegt werden.

    <Allgemein><Vorschub>:
    - Keine:  Kein Seitenvorschub (Endlospapier).
    - Vor:    Seitenvorschub vor jeder Seite (Nadeldrucker mit Papiereinzug).
    - Nach:   Seitenvorschub nach jeder Seite (Laserdrucker).





    @304[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.308
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Dokumentation>:

    <Seite>:
    Mit den Eintr„gen <Zeilen> und <Spalten> k”nnen Sie die Gr”áe einer Seite
    definieren:
    <Zeilen> .... Papierbreite in Zeichen pro Zeile.
    <Spalten> ... Papierl„nge in Zeilen pro Seite.
    Die Papierbreite/-l„nge ist INKLUSIVE der R„nder, Abst„nde, Kopf- und Fuá-
    zeilen anzugeben.










    @304[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.309
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Dokumentation>:

    <R„nder>:
    Um bestimmte Bereiche einer Seite vor dem Bedrucken zu schtzen, k”nnen Sie
    mit diesen Eintr„gen Seitenr„nder definieren:
    <Oben> ..... Oberer freier Rand in Zeilen.
    <Unten> .... Unterer freier Rand in Zeilen.
    <Links> .... Linker freier Rand in Zeichen.
    <Rechts> ... Rechter freier Rand in Zeichen.
    Es muá sichergestellt sein, daá die angegebenen R„nder nicht mehr als die
    Papierbreite/-l„nge ausmachen und auch fr die Kopf- und Fuázeilen noch
    gengend Platz vorhanden ist.

    <Abst„nde>:
    Um die Kopf- und Fuázeile besser hervorzuheben, kann ein bestimmter Abstand
    zum Textk”rper definiert werden:
    <Kopfzeile> ... Abstand zwischen Textk”rper und Kopfzeile in Zeilen.
    <Fuázeile> .... Abstand zwischen Textk”rper und Fuázeile in Zeilen.

    @304[ Fortsetzung ]                 @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.310
    PG2000  HAUPTMENš FUNKTIONEN

    <EINSTELLUNGEN><Sichern>:
    ALLE EINSTELLUNGEN, die im MENš <Einstellungen> gemacht wurden, werden mit
    dieser Funktion DAUERHAFT GESICHERT. D.h., auch nach einem neuerlichen PG-
    Einstieg werden die ge„nderten Einstellungen verwendet.

    - Die mit dieser Funktion gesicherten Einstellungen werden in der Datei
      PG.INI DAUERHAFT gespeichert.

    - Die gespeicherten Einstellungen gelten fr das aktuelle Projekt. Ein
      anderes Projekt kann durchaus andere Einstellungen besitzen, die ebenso
      DAUERHAFT gespeichert werden k”nnen.








                                  @055[ HM-Einstellungen ]                 @001[ šbersicht ]
.311
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Fensterliste>:
    Durch die Funktion <Tools><Fensterliste> wird ein Popup-Fenster mit einer
    Liste ALLER gerade GE™FFNETEN FENSTER eingeblendet (Fensterliste). Das ge-
    wnschte Fenster wird durch Anw„hlen des entsprechenden Eintrags in der
    Fensterliste aktiviert.
    Beispiel fr eine Fensterliste:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ Nr.
    ³ Meldungen       ³ 1 ... Meldungsfenster (wird immer angezeigt)
    ³ TEST_RPS        ³ 2 ... GDM-Editor (RPS-Ebene)
    ³ source01.src    ³ 3 ... PL2000-Editor mit Task source01.src
    ³ source02.src    ³ 4 ... AWL-Editor mit Task source02.src
    ³ source03.src    ³ 5 ... KOP-Editor mit Task source03.src
    ³ source01.pvm    ³ 6 ... PV-MONITOR fr PL2000-Task source01.src
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Mit der Fensterliste k”nnen Sie schnell das gewnschte Fenster aktivieren.
    In der Liste werden die Fenster in der Reihenfolge angezeigt, in der sie
    ge”ffnet wurden.
                                      @056[ HM-Tools ]                     @001[ šbersicht ]
.312
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Datei kopieren>:
    Nach der Auswahl der Quelldatei mittels einer Dateiauswahlbox wird
    folgende Dialogbox zur Definition der Zieldatei (Verzeichnis) eingeblendet:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄKopierenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Quelle : c:\pg\pgm\filename.ext       ³ <- Quelldatei
    ³Ziel   : c:\pg\pgm\                   ³ <- Zieldatei
    ³                                      ³
    ³        [   OK   ]  [ Cancel ]        ³ <- BESTTIGUNGSFELDER:
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <OK>     .. Datei kopieren
                                                <Cancel> .. Dialogbox schlieáen
    Quelle: Die Quelldatei wird mit Hilfe einer Dateiauswahlbox ausgew„hlt.
    Ziel:   - Neuer Pfad und neuer Name: Die Datei wird mit dem neuen Namen in
              das angegebene Verzeichnis kopiert.
            - Neuer Pfad und alter Name: Die Datei wird mit dem ursprnglichen
              Namen in das angegebene Verzeichnis kopiert.
            - Bei Angabe einer Laufwerkskennung (z.B.: D:) wird die Datei in
              das aktuelle Verzeichnis dieses Laufwerkes kopiert.

                                      @056[ HM-Tools ]                     @001[ šbersicht ]
.313
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Datei umbenennen>:
    Nach der Auswahl der Quelldatei mittels einer Dateiauswahlbox wird
    folgende Dialogbox zur Definition des neuen Dateinamens eingeblendet:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄUmbenennenÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Quelle : c:\pg\pgm\filexxxx.ext       ³ <- alter Name
    ³Ziel   : c:\pg\pgm\                   ³ <- neuer Name
    ³                                      ³
    ³        [   OK   ]  [ Cancel ]        ³ <- BESTTIGUNGSFELDER:
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <OK>     .. Datei umbenennen
                                                <Cancel> .. Dialogbox schlieáen
    Quelle: Die Quelldatei wird mit Hilfe einer Dateiauswahlbox ausgew„hlt.
    Ziel:   - Neuer Pfad und neuer Name: Die Datei wird in das angegebene
              Verzeichnis verschoben und umbenannt.
            - Neuer Pfad und alter Name: Die Datei wird in das angegebene
              Verzeichnis verschoben.
            - Alter Pfad und neuer Name: Die Datei wird umbenannt.


                                      @056[ HM-Tools ]                     @001[ šbersicht ]
.314
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Wiederherstellen>:
    Nach der Anwahl dieser Funktion wird anhand einer Dateiauswahlbox die ent-
    sprechende Task (Task, von der eine „ltere Version geladen werden soll)
    ausgew„hlt. Nach der Best„tigung von z.B. "ladder.src" erscheint eine Liste
    der automatisch angelegten Backup-Dateien:

    ÚÄÄÄÄÄÄÄÄÄÄÄWiederherstellenÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿³<- Backup-Dateien der Source-Datei,
    ³³  ladder.s_0   11.01.1997 09:40:46  ³³   die repariert werden soll (die
    ³³  ladder.s_1   11.01.1997 09:50:40  ³³   angezeigten Backup-Dateien werden
    ³³  ladder.s_2   11.01.1997 09:50:28  ³³   vom PG beim Speichern angelegt).
    ³ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ³
    ³                                      ³<- BESTTIGUNGSFELDER:
    ³         [   OK   ]  [ Cancel ]       ³   <OK>     .. Datei wieder-
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ               herstellen
                                               <Cancel> .. Dialogbox schlieáen



    @315[ Fortsetzung ]                     @056[ HM-Tools ]                     @001[ šbersicht ]
.315
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Wiederherstellen>:
    Das Programmiersystem legt beim Sichern von Source-Dateien automatisch
    Backup-Dateien an (maximal werden drei Backup-Dateien angelegt, Beispiel:
    TASK.SRC => TASK.S_0, TASK.S_1 und TASK.S_2). Die Numerierung der Backup-
    Dateien gibt keinen Aufschluá ber das Alter der Dateien bzw. ber die
    Reihenfolge der Erstellung.













                                      @056[ HM-Tools ]                     @001[ šbersicht ]
.316
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Logfile>:
    In der LOG-Datei des Programmiersystems PG2000 werden alle PG-AKTIVITTEN,
    die im MELDUNGSFENSTER angezeigt werden, aufgezeichnet.

    - Fr jedes PROJEKT wird eine LOG-Datei (Datei "PG.LOG") im entsprechenden
      Projektverzeichnis gespeichert!

    - Mit Hilfe der LOG-Datei k”nnen Abl„ufe aus der aktuellen PG-SITZUNG oder
      von frheren PG-SITZUNGEN genau ANALYSIERT werden. Das kann vor allem bei
      FEHLERMELDUNGEN von Compilierungsabl„ufen oder bei den PROJEKT-Diensten
      von Vorteil sein.








    @317[ Fortsetzung ]                     @056[ HM-Tools ]                     @001[ šbersicht ]
.317
    PG2000  HAUPTMENš FUNKTIONEN

    <TOOLS><Logfile>:
    Nach Aufruf dieser Funktion wird eine Dialogbox mit Informationen zur LOG-
    Datei eingeblendet. Weiters besteht mit der Funktion <Tools><Logfile> die
    M”glichkeit, die LOG-Datei zu l”schen:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄLogfileÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Erzeugt : 20.01.1997  13:35:26       ³ <- Informationen zur LOG-Datei
    ³Gr”áe   : 34657 Byte                 ³
    ³                                     ³
    ³[   OK    ]  [ L”schen ]  [ Cancel  ]³ <- BESTTIGUNGSFELDER:
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <OK>      .. Infos best„tigen
                                               <L”schen> .. LOG-Datei l”schen
                                               <Cancel>  .. Dialogbox schlieáen






                                      @056[ HM-Tools ]                     @001[ šbersicht ]
.318
    PG2000  HAUPTMENš FUNKTIONEN

    <HILFE><Index>:
    Durch Aufruf dieser Funktion wird ein Popup-Men mit einer Auswahl zus„tz-
    licher Hilfethemen eingeblendet. Auch der Anwender hat die M”glichkeit,
    Hilfedateien zu erstellen und in diese Liste einzubinden (siehe dazu Hilfe-
    thema "Wie erstellt man eine Helpdatei").














                                      @057[ HM-Hilfe ]                     @001[ šbersicht ]
.319
    PG2000  HAUPTMENš FUNKTIONEN

    <HILFE><Libraries>:
    Die Library-Hilfe umfaát alle in der aktuellen Projekt-Datenbank vor-
    handenen Libraries (importierte Libraries), alle Standard-Funktionen
    (Funktionsbl”cke und Inline-Funktionen) und die vom Anwender selbst
    erstellten Funktionsbl”cke. Die integrierten Info-, Hilfe- und Such-
    Funktionen steigern die šbersichtlichkeit und untersttzen den Anwender
    bei seiner Suche nach den gewnschten Informationen.

    Durch Aufruf der Funktion <Hilfe><Libraries> wird eine Dialogbox mit
    einer Auswahlliste aller in der aktuellen Projekt-Datenbank vorhandenen
    Funktions-Libraries eingeblendet.








    @320[ Fortsetzung ]                     @057[ HM-Hilfe ]                     @001[ šbersicht ]
.320
    PG2000  HAUPTMENš FUNKTIONEN

    <HILFE><Libraries>:    Auswahlliste der Libraries
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄLibrariesÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³          Name    : *.*              ³ <- SUCHKRITERIUM eingeben
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ³
    ³³  user          ³ ³  FRM_clos      ³ ³ <- Anzeige in 2 Auswahllisten:
    ³³  burtrap.SLB   ° ³  FRM_ctrl      ° ³    Links:  LIBRARY-šbersicht
    ³³  can_lib.SLB   ° ³  FRM_gbuf      ° ³    Rechts: FUNKTIONEN der aus-
    ³³  frame.SLB     ° ³  FRM_open      ° ³            gew„hlten LIBRARY
    ³³  iec_1131.SLB  ° ³  FRM_rbuf      ° ³
    ³³  io_lib.SLB    ° ³  FRM_read      ° ³ BESTTIGUNGSFELDER:
    ³³  mathtrap.SLB  ² ³  FRM_robu      ° ³ <OK> .......... Infos zur ange-
    ³³  net2000.SLB    ³  FRM_writ      ° ³                 w„hlten FUNKTION
    ³ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³                ° ³ <Cancel>  ..... Dialogbox
    ³  [     OK     ]   ³                ° ³                 schlieáen
    ³  [   Cancel   ]   ³                ° ³ <Lib-Info> .... LIBRARY-Version
    ³  [  Lib-Info  ]   ³                ³ ³                 anzeigen
    ³  [ Lib-Hilfe  ]   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³ <Lib-Hilfe> ... LIBRARY-Hilfe
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ                 aktivieren

                                      @057[ HM-Hilfe ]                     @001[ šbersicht ]
.325
    GRAPHISCHE DESIGN METHODE:  GDM - EDITOR

    Die GRAPHISCHE DESIGN METHODE - GDM - dient der bersichtlichen grafischen
    Darstellung, Planung und Verwaltung der einzelnen Projekte.

    Durch die transparente Abbildung der Applikations-Struktur ist ein effekti-
    veres Erstellen und Warten der Projekt-Sourcen und Parameter m”glich.


    @370[< SYNTAX-Beschreibung >]         - GDM Ebenen
                                    - GDM Symbole, Datenbaum

    @326[< EDITOR-Bedienung    >]         - GDM Fuázeilenmen, Popup-Mens
                                    - GDM Tastaturkommandos

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men




                                                                     @001[ šbersicht ]
.326
    G D M - BEDIENUNG

    Die Bedienung des GDM-Editors wird ber Funktionen der Fuázeile, der
    Popup-Mens und funktionellen Tastenkombinationen durchgefhrt.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄFenstertitelÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³ ANZEIGE DER AKTUELLEN EINSTIEGSEBENE (Name als Fenstertitel)       ²
    ³                                                                    ²
    ³ Bedienung mittels TASTENKOMBINATIONEN oder ber MAUS:              ²
    ³                                                                    ²
    ³ @329[< GDM - Popup Mens       >]   - Ebenen- und Symbol-sensitive       ²
    ³                                 Funktionen zum EDITIEREN einer GDM ²
    ³ @355[< GDM - Fuázeilenmen     >]   - Einfgen neuer Symbole             ²
    ³                               - WECHSELN zu anderen EBENEN         ²
    ³ @327[< GDM - Tastaturkommandos >]   - Weitere Tastenkombinationen        ²
    ³                                 fr die Editor- Bedienung          ²
    ³                                                                    ³
    ÀÄEBENE: Zeile x Spalte yÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ


    @370[ GDM-Syntax ]                     @325[ GDM-Editor ]                    @001[ šbersicht ]
.327
    G D M - BEDIENUNG

    GDM-EDITIERUNG       :  Mit der Maus kann der gewnschte Editor-Bereich
                            direkt markiert werden.
                            Tastenkombinationen fr GDM-Editierung:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Tastenkombination      ³ Beschreibung                                  º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º <DEL>                  ³ L”schen des markierten GDM-Symbols            º
    º <INS>                  ³ Einfgen eines neuen GDM-Symbols              º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º <CURSOR-TASTEN>       ³ Setzen der Textmarke, auch ber mehrere GDM-  º
    º                        ³ Symbole und Datenb„ume                        º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º ^F3                    ³ Aufrufen eines Ebenen- und Symbol-sensitiven  º
    º                        ³ Popup-Mens mit GDM-Funktionen                º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
    º                        ³ Aussteigen aus einer angew„hlten Funktion     º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @328[ Fortsetzung ]                 @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.328
    G D M - BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit Hilfe der
                            Bildlaufleiste und einem Mausklick durchgefhrt
                            werden, oder ber folgende Tastenkombinationen:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Tastenkombination      ³ Beschreibung                                  º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º <LEFT>/<RIGHT>         ³ Ein Zeichen/Symbol nach links/rechts          º
    º <UP>/<DOWN>            ³ Eine Zeile/Symbol aufw„rts/abw„rts            º
    º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
    º <TAB>                  ³ Vorw„rts Anw„hlen des n„chsten GDM-Symbols    º
    º <TAB>                 ³ Rckw„rts Anw„hlen des n„chsten GDM-Symbols   º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
    º 2x<HOME>               ³ Cursor auf Anfang der GDM                     º
    º 2x<END>                ³ Cursor ans Ende der GDM                       º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


                                  @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.329
    G D M - POPUP MENšS
   ÄÄÄÄÄÄÄÄÄÄÄÄÄÄ ALPHA ÄÄÄÄÄÄÄÄÄÄÄÄÄ¿  @334[POPUP der <RPS-EBENE>]
   ³ ÉÍÍÍÍÍÍÍÍÍÍ»                     ³
   ³ º TEST_RPS º                     ³
   ³ ÈÍÍÍÍÍÍÍÍÍÍ¼                     ³
   ³    ÄÄÄÄÄÄÄÄÄÄÄÄÄ TEST_RPS ÄÄÄÄÄÄÁÄÄÄÄ¿  @332[POPUP der <PROZESSOR-EBENE>]
   ³    ³ ÉÍÍÍÍÍÍCPUÍ»  ÉÍÍÍÍÍÍÍMPÍ»       ³
   ÀÄÄÄÄ´ º MAIN_CPU º  º MP_VISU  º       ³  <GDM-POPUP-MENšS> sind:
        ³ ÈÍÍÍÍÍÍÍÍÍÍ¼  ÈÍÍÍÍÍÍÍÍÍÍ¼       ³  Ebenen- und Objekt-sensitiv
        ³                                  ³
        ³    ÄÄÄÄÄÄÄÄ TEST_RPS.MAIN_CPU ÄÄÁÄÄÄÄ¿  @330[POPUP der <TASK-EBENE>]
        ³    ³ ÚÄÄÄÄÄÄTC#1¿    ÚÄÄÄÄÄÄTC#2¿     ³
        ÀÄÄÄÄ´ ³ source00 ³    ³ source01 ³     ³
             ³ ÀÄÄÄÄÄÄÄÄÄÄÙ    ÀÄÄÄÄÄÄÄÄÄÄÙ     ³
             ³      º                º          ³
             ³      ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼          ³  @336[POPUP ÍÍ<DATENBAUM>ÍÍ]
             ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    Aktivierung der POPUP-MENšS ber:     Ausfhren einer POPUP-Funktion:
    - DOPPELKLICK der MAUS                - mittels MAUSKLICK
    - TASTENKOMBINATION: ^F3              - mittels CURSOR-TASTEN und <ENTER>
                                          - Bet„tigen der ANFANGSBUCHSTABEN
                                  @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.330
    G D M - POPUP MENš - TASK-EBENE
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿     @329[<POPUP-šbersicht>]
    ³@340[ GDM Prfen             ]³ ... gemeinsame PVs und Datenb„ume berprfen
    ³@331[ Objekt Liste          ]³ ... Popup mit Objekt-Diensten (Compile, RUN, ...)
    ³@347[ FUB Liste             ]³ ... FUB-Liste erstellen/compilieren
    ³@341[ Crossreferenz          ]³ ... Anzeigen gemeinsamer PVs von 2 Sourcen
    ³@342[ Einstiegsebene         ]³ ... Definition der Standard-Einstiegsebene
    ³@342[ Umbenennen             ]³ ... ndern des Symbol-Namens (Objekt-Name)
    ³@342[ Verschieben            ]³ ... Verschieben des markierten GDM-Symbols
    ³@343[ šbertragen          F9]³ ... šbertragen des markierten OBJEKTES
    ³@344[ Rebuild                ]³ ... Rebuild des markierten OBJEKTES
    ³@345[ On-Line                ]³ ... Objekt-Status auf RPS und Task-Dienste
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    Die Bezeichnung "GDM-Symbol" umfaát in der Task-Ebene alle definierten
    Task-Symbole (Projekt-Sourcen verschiedener Editoren, Datenmodule, ...).

    - Da die POPUP-Mens Ebenen- und Symbol-sensitiv sind, k”nnen nur ausfhr-
      bare Dienste ausgew„hlt werden. Dienste die auf einem "Symbol" nicht aus-
      fhrbar sind, unterscheiden sich in der farblichen Darstellung im Popup.


                                  @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.331
    G D M - POPUP MENš - TASK-EBENE
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@330[ Objekt Liste          ]Å¿ .... Die OBJEKTLISTE besteht aus ALLEN OBJEKTEN
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ³      der AKTUELLEN TASK-EBENE
      ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁ¿
      ³@337[ Erstellen              ]³ ... Erstellen einer OBJEKTLISTE
      ³ Compile                ³ ... Compilieren aller Objekte der Objektliste
      ³ šbertragen             ³ ... šbertragen  aller Objekte der Objektliste
      ³ RUN                    ³ ... RUN         aller Objekte der Objektliste
      ³ Speichern              ³ ... Speichern   aller Objekte der Objektliste
      ³ Drucken                ³ ... Drucken     aller Objekte der Objektliste
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    - Die Objektliste kann beim Erstellen unter einem speziellen Namen
      gespeichert werden. Standardm„áig wird "task.prj" vorgeschlagen.
      <== LISTE-ERSTELLEN IST EBENEN UND SYMBOL-BEZOGEN !!

    - Beim Ausfhren einer der Objektlisten-Dienste (RUN, Speichern, ...) werden
      alle Objekte aus der ausgew„hlten Liste mitbercksichtigt. D.h., wurde
      eine Objektliste in der RPS-Ebene erzeugt, dann wird das ganze Projekt bei
      den Diensten mitbercksichtigt. <== DIENSTE SIND LISTENBEZOGEN !!

                                   @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.332
    G D M - POPUP MENš - PROZESSOR-EBENE
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿     @329[<POPUP-šbersicht>]
    ³@340[ GDM Prfen             ]³ ... gemeinsame PVs und Datenb„ume berprfen
    ³@333[ Objekt Liste          ]³ ... Popup mit Objekt-Diensten (Compile,RUN,...)
    ³@347[ FUB Liste             ]³ ... FUB-Liste erstellen/compilieren
    ³@341[ Crossreferenz          ]³ ... Anzeigen gemeinsamer PVs von 2 Sourcen
    ³@342[ Einstiegsebene         ]³ ... Definition der Standard-Einstiegsebene
    ³@342[ Umbenennen             ]³ ... ndern des Symbol-Namen (Objekt-Name)
    ³@342[ Verschieben            ]³ ... Verschieben des markierten GDM-Symbols
    ³@343[ šbertragen          F9]³ ... šbertragen ALLER PROZESSOR-OBJEKTE
    ³@348[ Ebene entfernen        ]³ ... Angew„hlte Symbol-Ebene l”schen
    ³@348[ Ebene einziehen        ]³ ... Neue Symbol-Ebene einziehen
    ³@350[ Bereichseditor         ]³ ... Vergeben fixer Variablen-Offsets
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    Die Bezeichnung "GDM-Symbol" umfaát in der Prozessor-Ebene alle
    definierten Prozessor-Symbole (CPU, MP, IP, ...).

    - Da die POPUP-Mens Ebenen- und Symbol-sensitiv sind, k”nnen nur ausfhr-
      bare Dienste ausgew„hlt werden. Dienste die auf einem "Symbol" nicht aus-
      fhrbar sind, unterscheiden sich in der farblichen Darstellung im Popup.

                                  @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.333
    G D M - POPUP MENš - PROZESSOR-EBENE
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@332[ Objekt Liste          ]Å¿ .... Die OBJEKTLISTE besteht aus ALLEN OBJEKTEN
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ³      der PROZESSOR-EBENE (CPU,MP,.. EINER RPS)
      ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁ¿
      ³@338[ Erstellen              ]³ ... Erstellen einer OBJEKTLISTE
      ³ Compile                ³ ... Compilieren aller Objekte der Objektliste
      ³ šbertragen             ³ ... šbertragen  aller Objekte der Objektliste
      ³ RUN                    ³ ... RUN         aller Objekte der Objektliste
      ³ Speichern              ³ ... Speichern   aller Objekte der Objektliste
      ³ Drucken                ³ ... Drucken     aller Objekte der Objektliste
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    - Die Objektliste kann beim Erstellen unter einem speziellen Namen
      gespeichert werden. Standardm„áig wird "task.prj" vorgeschlagen.
      <== LISTE-ERSTELLEN IST EBENEN UND SYMBOL-BEZOGEN !!

    - Beim Ausfhren einer der Objektlisten-Dienste (RUN, Speichern, ...) werden
      alle Objekte aus der ausgew„hlten Liste mitbercksichtigt. D.h., wurde
      eine Objektliste in der Task-Ebene erzeugt, dann werden NUR die Tasks der
      entspr. Task-Ebene mitbercksichtigt. <== DIENSTE SIND LISTENBEZOGEN !!

                                   @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.334
    G D M - POPUP MENš - RPS-EBENE
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿     @329[<POPUP-šbersicht>]
    ³@340[ GDM Prfen             ]³ ... gemeinsame PVs und Datenb„ume berprfen
    ³@335[ Objekt Liste          ]³ ... Popup mit Objekt-Diensten (Compile, RUN, ...)
    ³@347[ FUB Liste             ]³ ... FUB-Liste erstellen/compilieren
    ³@341[ Crossreferenz          ]³ ... Anzeigen gemeinsamer PVs von 2 Sourcen
    ³@342[ Einstiegsebene         ]³ ... Definition der Standard-Einstiegsebene
    ³@342[ Umbenennen             ]³ ... ndern des Symbol-Namen (Objekt-Name)
    ³@342[ Verschieben            ]³ ... Verschieben des markierten GDM-Symbols
    ³@343[ šbertragen          F9]³ ... šbertragen ALLER RPS-OBJEKTE
    ³@348[ Ebene entfernen        ]³ ... Angew„hlte Symbol-Ebene l”schen
    ³@348[ Ebene einziehen        ]³ ... Neue Symbol-Ebene einziehen
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Die Bezeichnung "GDM-Symbol" umfaát in der RPS-Ebene alle definierten
    RPS-Symbole (beteiligte RPSen).

    - Da die POPUP-Mens Ebenen- und Symbol-sensitiv sind, k”nnen nur ausfhr-
      bare Dienste ausgew„hlt werden. Dienste die auf einem "Symbol" nicht aus-
      fhrbar sind, unterscheiden sich in der farblichen Darstellung im Popup.

                                  @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.335
    G D M - POPUP MENš - RPS-EBENE
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@334[ Objekt Liste          ]Å¿ .... Die OBJEKTLISTE besteht aus ALLEN OBJEKTEN
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ³      des GESAMTEN PROJEKTES (ALLE RPSen)
      ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁ¿
      ³@339[ Erstellen              ]³ ... Erstellen einer OBJEKTLISTE
      ³ Compile                ³ ... Compilieren aller Objekte der Objektliste
      ³ šbertragen             ³ ... šbertragen  aller Objekte der Objektliste
      ³ RUN                    ³ ... RUN         aller Objekte der Objektliste
      ³ Speichern              ³ ... Speichern   aller Objekte der Objektliste
      ³ Drucken                ³ ... Drucken     aller Objekte der Objektliste
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    - Die Objektliste kann beim Erstellen unter einem speziellen Namen
      gespeichert werden, standardm„áig wird "task.prj" vorgeschlagen.
      <== LISTE-ERSTELLEN IST EBENEN UND SYMBOL-BEZOGEN !!

    - Beim Ausfhren einer der Objektlisten-Dienste (RUN, Speichern, ...) werden
      alle Objekte aus der ausgew„hlten Liste mitbercksichtigt. D.h., wurde
      eine Objektliste in der Task-Ebene erzeugt, dann werden NUR die Tasks der
      entspr. Task-Ebene mitbercksichtigt. <== DIENSTE SIND LISTENBEZOGEN !!

                                   @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.336
    G D M - POPUP MENš - DATENBAUM

    Beim Aufrufen eines Popups ber einen Datenbaum werden die GEMEINSAMEN
    PVs der zwei Symbole, die ber diesen Datenbaum symbolisch verbunden sind,
    in einer DIALOGBOX angezeigt:

    ÚÄÄÄÄÄVLD-File erzeugenÄÄÄÄ¿   - Mit Hilfe der BILDLAUFLEISTE kann die
    ³      ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿     ³     Liste mit der MAUS durchsucht werden.
    ³      ³ Anzeige          ³
    ³      ³ der         ±     ³   - mit den CURSORTASTEN oder mittels
    ³      ³ verwendeten ±     ³     ANFANGSBUCHSTABEN von PVs kann die
    ³      ³ PVs              ³     Liste durchsucht werden.
    ³      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     ³
    ³                          ³   - Mit <OK> kann die Liste als ASCII-Datei
    ³  [   OK   ]  [ Cancel ]  ³     (*.vld) gespeichert werden, und in Folge
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     als Variablenliste fr den PV-Monitor
                                     verwendet werden.




    @329[ POPUP-šbersicht ]             @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.337
    G D M - POPUP FUNKTIONEN - TASK-EBENE

    <Objekt Liste><Erstellen>:  <== LISTE BEINHALTET OBJEKTE der TASK-EBENE

       Die OBJEKTLISTE besteht aus ALLEN OBJEKTEN der AKTUELLEN TASK-EBENE.
       - Die Objektliste kann beim Erstellen unter einem speziellen Namen
         gespeichert werden, standardm„áig wird "task.prj" vorgeschlagen.

       Die TASKLISTE wird mit folgenden Informationen als ASCII-Datei
       gespeichert:
       - PG-Version und Generierungs-Datum der Objektliste
       - Taskname mit Numerierung der Tasks
       - Zuordnung der Task-RPS (RPS-Name der RPS-Ebene)
       - Zuordnung des Task-Prozessors (Prozessor-Name der Prozessor-Ebene)
       - Source-Typ der Task (SRC, KOP, DAT, EXC, ...)
       - Taskklasse der Task (TC#1=1, TC#2=2, TC#3=3, TC#4=4, HS#1=17, ...)
       - Source-Typ des Init-UP, falls vorhanden




    @331[ Objekt Liste]                  @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.338
    G D M - POPUP FUNKTIONEN - PROZESSOR-EBENE

    <Objekt Liste><Erstellen>:  <== LISTE BEINHALTET OBJEKTE der PROZESSOR-EBENE

       Die OBJEKTLISTE besteht aus ALLEN OBJEKTEN der PROZESSOR-EBENE
       (CPU, MP, ... ALLE OBJEKTE EINER RPS)
       - Die Objektliste kann beim Erstellen unter einem speziellen Namen
         gespeichert werden, standardm„áig wird "task.prj" vorgeschlagen.

       Die TASKLISTE wird mit folgenden Informationen als ASCII-Datei
       gespeichert:
       - PG-Version und Generierungs-Datum der Objektliste
       - Taskname mit Numerierung der Tasks
       - Zuordnung der Task-RPS (RPS-Name der RPS-Ebene)
       - Zuordnung des Task-Prozessors (Prozessor-Name der Prozessor-Ebene)
       - Source-Typ der Task (SRC, KOP, DAT, EXC, ...)
       - Taskklasse der Task (TC#1=1, TC#2=2, TC#3=3, TC#4=4, HS#1=17, ...)
       - Source-Typ des Init-UP, falls vorhanden



    @333[ Objekt Liste]                  @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.339
    G D M - POPUP FUNKTIONEN - RPS-EBENE

    <Objekt Liste><Erstellen>:  <== LISTE BEINHALTET OBJEKTE der RPS-EBENE

       Die OBJEKTLISTE besteht aus ALLEN OBJEKTEN des GESAMTEN PROJEKTES
       (ALLE OBJEKTE ALLER RPSen)
       - Die Objektliste kann beim Erstellen unter einem speziellen Namen
         gespeichert werden. Standardm„áig wird "task.prj" vorgeschlagen.

       Die TASKLISTE wird mit folgenden Informationen als ASCII-Datei
       gespeichert:
       - PG-Version und Generierungs-Datum der Objektliste
       - Taskname mit Numerierung der Tasks
       - Zuordnung der Task-RPS (RPS-Name der RPS-Ebene)
       - Zuordnung des Task-Prozessors (Prozessor-Name der Prozessor-Ebene)
       - Source-Typ der Task (SRC, KOP, DAT, EXC, ...)
       - Taskklasse der Task (TC#1=1, TC#2=2, TC#3=3, TC#4=4, HS#1=17, ...)
       - Source-Typ des Init-UP, falls vorhanden



    @335[ Objekt Liste]                  @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.340
    G D M - POPUP FUNKTIONEN

    <GDM Prfen>:  Die GESAMTE GDM wird auf folgende Bedingungen berprft:

       - Ist zu jedem Task eine QUELLDATEI vorhanden ?
         Fehlermeldung : NV-Liste zu "<Task-Name>" nicht vorhanden ...
         Fehlerbehebung: Task muá erst codiert werden (Quelldatei erstellen)

       - Sind alle TASKPARAMETER definiert ?
         Fehlermeldung : Task "<Task-Name>" nicht in der Datenbank
         Fehlerbehebung: Taskparameter definieren

       - Gibt es LEERE DATENBUME ?
         Fehlermeldung : <Task-Name><-><Task-Name>: Leerer Datenbaum ...
         Fehlerbehebung: Entsprechenden Datenbaum l”schen

       - Gibt es GEMEINSAME GLOBALE VARIABLEN in Tasks, die nicht durch einen
         Datenbaum verbunden sind ?
         Fehlermeldung : <Task-Name><-><Task-Name>: Gemeinsame PVs, aber ...
         Fehlerbehebung: Entsprechenden Datenbaum zeichnen

    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.341
    G D M - POPUP FUNKTIONEN

    <Crossreferenz>:

       Die Funktion Crossreferenz (Querverweis) liefert einen šberblick ber
       die GEMEINSAMEN PVs von ZWEI AUSWHLBAREN TASKS einer Ebene.

       - die Auswahl der zu berprfenden Tasks, und das Starten der
         šberprfung, wird mit Hilfe einer Dialogbox durchgefhrt.

       - die gemeinsamen PVs werden in einer Dialogbox angezeigt und k”nnen
         wahlweise in einer ASCII-Datei (*.vld) gespeichert werden.

       Hinweis: Da die erforderlichen Crossreferenz-Infomationen erst beim
                Compilieren in die DBK eingetragen werden, liefert diese
                Funktion nur dann korrekte Informationen, wenn die zu ber-
                prfenden Tasks bereits compiliert sind.




    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.342
    G D M - POPUP FUNKTIONEN

    <Einstiegsebene>:

       Mit dieser Funktion wird die aktuelle GDM-Ebene als GDM-Einstiegsebene
       abgespeichert. Beim n„chsten Aufruf der GDM dieses Projektes wird sofort
       die gespeicherte GDM-Einstiegsebene ge”ffnet.

    <Umbenennen>:

       Mit Hilfe dieser Funktion kann der NAME DES ANGEWHLTEN Symbols
       VERNDERT werden.

    <Verschieben>:

       Mit Hilfe dieser Funktion kann das angew„hlte Symbol mit den <Cursor>-
       Tasten auf eine ANDERE POSITION VERSCHOBEN werden. Mit der <Enter>-Taste
       oder mittels Mausklick an der gewnschten Position wird das Symbol dort-
       hin verschoben (nur wenn gengend Platz zur Verfgung steht).


    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.343
    G D M - POPUP FUNKTIONEN

    <Objekt šbertragen>:

       Alle Tasks des angew„hlten Objektes (RPS/PROZESSOR/TASK) werden ins
       Anwender-RAM der entsprechenden RPS bertragen, ohne den MAKE-Mechanis-
       mus zu durchlaufen. Die Tasks mssen daher bereits compiliert sein.














    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.344
    G D M - POPUP FUNKTIONEN

    <Rebuild>:

       Befindet sich der Curser auf einem OBJ-Symbol (lauff„higes *.br Programm-
       Modul), kann dieses rckbersetzt werden, wenn das Programm-Modul ur-
       sprnglich mit der Compiler-Option Rckbersetzung=EIN (RB-Info=Ein)
       erzeugt wurde.

       - Beim Objekt-Rebuild wird die Quelldatei entsprechend dem ursprnglichen
         Quelltyp (KOP, AWL, PL2000) wiederhergestellt, und der dabei entste-
         hende Task in die Projekt-DBK eingetragen.

       - Weiters wird das OBJ-Symbol dabei automatisch durch das entsprechende
         Task-Symbol ersetzt.:

         ÚÄÄÄÄÄÄÄÄOBJÄ¿     ÚÄÄÄÄÄÄÄTC#1Ä¿
         ³  source00  ³ ==> ³  source00  ³
         ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ


    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.345
    G D M - POPUP FUNKTIONEN

    @346[<On-Line>]:   FUNKTIONSUMFANG

       Diese Funktionen k”nnen nur ONLINE zur RPS durchgefhrt werden !!
       Alle Funktionen beziehen sich auf das MARKIERTE TASK-Symbol !!

       Bei einer AKTIVEN VERBINDUNG zur RPS steht folgender Funktionsumfang
       zur Verfgung:
                                             FUNKTIONSUMFANG:
       STATUSANZEIGE:     ÚÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´ Download   ³ ... šbertragen des angew„hlten Task
    ³  Task-Zustand: RUN  ³ Overload   ³ ... Deinstall und šbertragen der Task
    ³  Speicher-Typ: RAM  ³ Start      ³ ... Starten der gestoppten Task
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´ Stop       ³ ... Stoppen der laufenden Task
                          ³ Install    ³ ... Installieren der Task
                          ³ Neustart   ³ ... Starten der installierten Task
                          ³ Speichern  ³ ... Task auf USERROM/FIX-RAM Speichern
                          ³ Entfernen  ³ ... Entfernen der gebrannten Task
                          ³ Cancel     ³ ... Abbrechen aus Popup-Funktion
                          ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ
    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.346
    G D M - POPUP FUNKTIONEN

    @345[<On-Line>]:   TASK-STATUSANZEIGE

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³  Task-Zustand: RUN  ³   Der entsprechende RPS Task-Status wird in
    ³  Speicher-Typ: RAM  ³   einer INFO-Box angezeigt:
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

       M”gliche Task-Zust„nde:
          NON ... Task nicht auf RPS vorhanden
          RUN ... Task l„uft (Task AKTIV)
          STP ... Task ist gestoppt (Task INAKTIV)
          RDY ... Task auf RPS vorhanden, jedoch nicht installiert (INAKTIV)
          IDL ... Task auf RPS installiert, jedoch nicht gestartet (INAKTIV)

       Speicher-Typen:
          NON ... Task nicht auf RPS vorhanden
          RAM ... Task ist im User-RAM (oder FIX-RAM) gespeichert
          EPR ... Task ist auf User-PROM gebrannt

                                   @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.347
    G D M - POPUP FUNKTIONEN

    <FUB Liste>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ FUB Liste             Å¿ .... In die FUB-Liste werden ALLE Anwender-FUBs
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ³      (vom Anwender selbst erstellte FUBs) des
                              ³      GESAMTEN PROJEKTES aufgenommen.
           ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁ¿
           ³ Erstellen         ³ ... Erstellen einer FUB-Liste
           ³ Compile           ³ ... Compilieren aller FUBs der FUB-Liste
           ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    - Die FUB-Liste kann beim Erstellen unter einem speziellen Namen
      gespeichert werden, standardm„áig wird "fubs.prf" vorgeschlagen.

    - Beim Compilieren einer FUB-Liste werden alle FUBs in der ausgew„hlten
      Liste mitbercksichtigt.




    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.348
    G D M - POPUP FUNKTIONEN

    <Ebene entfernen>:

       ALLE CHILD-EBENEN des angew„hlten Symbols (alle untergeordneten Ebenen,
       mit deren Symbolen und Quelltexten) werden nach Best„tigung der Sicher-
       heitsabfrage gel”scht !!

       Bsp.: Das L”schen der Prozessor-Ebene mit allen Task-Ebenen wird von
             der RPS-Ebene aus durchgefhrt.


    <Ebene einziehen>:

       Mit dieser Funktion kann man eine NEUE EBENE ZWISCHEN der aktuellen
       Ebene und der Child-Ebene einfgen.

       Bsp.: Da standardm„áig keine PROZESSOR-Ebene zwischen der RPS-Ebene und
             der TASK-Ebene projektiert ist, muá die PROZESSOR-Ebene bei Bedarf
             einmal als neue Ebene ZWISCHEN DEN BEIDEN eingezogen werden.

    [BACKSPACE]==>POPUP            @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.350
    G D M - BEREICHSEDITOR

    <Bereichseditor>:

       Mit Hilfe des Bereichseditors k”nnen FIXE VARIABLEN-OFFSETS im DPR
       fr Projekt-PVs vergeben werden.
       Der Vorteil besteht darin, daá sich die VARIABLEN-OFFSETS der im
       Bereichseditor definierten PVs durch das Projektieren von zus„tzlichen
       PVs oder aufgrund der Compilierungs-Reihenfolge NICHT MEHR NDERN.

       @351[< Bereichs-Editor >]    - Beschreibung der Eingabefelder

       @352[< Fuázeilenmen   >]    - Bedienung des Bereichseditors

       - es k”nnen nur rps_globale PVs im DPR FIXIERT werden
       - Nach dem Aufruf der Funktion kann durch eine Auswahl aus einem Popup-
         Men der "Digitale" oder "Analoge" Merkerbereich festgelegt werden.
       - Vor allem dient der Bereichseditor zur Festlegung von PVs im DPR
         von MP/PP/IPs, die an CPU-PVs angekoppelt werden.


    @332[ Symbol-POPUP ]                 @329[ POPUP-šbersicht ]                 @001[ šbersicht ]
.351
    BEREICHSEDITOR
                                                                      @352[< WEITER >]
    EINGABE mittels Tabelleneditor:
                            ÚÄÄÄ Aktueller Bereich (Dig/Ana) und Prozessor-Name
                            ³                ÚÄÄÄ Definierter Bereich
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍBereich PROZESSOR_NAME (0-x)ÍÍÍÍÍÍÍÍÍ»
    º Name     º Datentyp ³ L„nge  ³ Bytes   ³ Offset    º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍ¹
    º ANZEIGE  º EINGABE  ³EINGABE ³ ANZEIGE ³ EINGABE   º
    º und      º ber     ³Feld-   ³ Gesamt- ³ oder      º
    º EINGABE  º POPUP    ³Anzahl  ³ L„nge   ³ BERECHNEN º
    ÈÍ1/yÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍ¼
       ÀÄÄÄ Aktueller Tabelleneintrag/Anzahl der Tabelleneintr„ge

    <Name> ....... ANZEIGE oder EINGABE des PV-Namens
    <Datentyp> ... DATENTYP der PV (Struktur-DEFINITION, BIT, BYTE, LONG, ...)
    <L„nge> ...... ANZAHL der Datentypen (L„nge ungleich 1 => PV ist ein Feld)
    <Bytes> ...... ANZEIGE der tats„chlichen PV-L„nge
    <Offset> ..... EINGABE des PV-Offsets, oder automatisches Ermitteln aller
                   Offsets mit der Funktion <Berechnen>.

                                   @350[ Bereichseditor ]                  @002[ DEBUGGING ]
.352
    BEREICHSEDITOR - FUSSZEILENMENš

ÉÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍ»
º1 Berechnen³2 Kontrolle³3 Sortieren³4 PV einf.³5 PV entf.³6    ³7 Bereich³    º
ÈÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍ¼

   <F1> <Berechnen> :  Automatisches BERECHNEN und EINTRAGEN der PV-Offsets.
                       (Offsetvergabe anhand der Eingabe-Reihenfolge)
   <F2> <Kontrolle> :  KONTROLLIEREN der eingetragenen PV-Offsets auf
                       BEREICHSšBERSCHNEIDUNGEN
   <F3> <Sortieren> :  SORTIEREN der Tabelleneintr„ge in aufsteigender
                       Reihenfolge der PV-Offsets.
   <F4> <PV einf.>  :  Tabelleneintrag EINFšGEN
   <F5> <PV entf.>  :  angew„hlten Tabelleneintrag L™SCHEN
   <F7> <Bereich>   :  Anzeige des vergebenen Offset-Bereich (entspricht dem
                       n„chsten Tabelleneintrags-Offset)


    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick ausgew„hlt werden.

                                   @350[ Bereichseditor ]                  @002[ DEBUGGING ]
.355
    G D M - FUSSZEILENMENš

    Das FUSSZEILENMENš steht in JEDER GDM-EBENE zur Verfgung.
    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick angew„hlt werden:

 ÉÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍ»
 º@356[1 Symbol]³@362[2 Datenbaum]³@364[3 Kommentar]³4 Zeile+³5 Zeile-³6    ³7 Ebene³8 Ebeneº
 ÈÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍ¼

    <F1> <Symbol>   :  SETZEN eines NEUEN GDM-Symbols
    <F2> <Datenbaum>:  DATENBAUM editieren (Zeichnen, L”schen, PV-Liste, ...)
    <F3> <Kommentar>:  Eingeben oder Editieren eines KOMMENTAR-FELDES
    <F4> <Zeile+>   :  EINFšGEN einer NEUEN Zeile: GDM-Symbole werden mitver-
                                schoben. Datenbaum-Abschnitte werden erweitert.
    <F5> <Zeile->   :  L™SCHEN  der aktuellen Zeile: GDM-Symbole blockieren
                                die Ausfhrung. Datenbaum-Abschnitte werden mit
                                der Zeile mitgel”scht.
    <F7> <Ebene>   :  WECHSELN zur TIEFEREN EBENE (Symbol-sensitiv)
    <F8> <Ebene>   :  WECHSELN auf die n„chst H™HERE EBENE (immer m”glich)

    @327[ Tastenkombinationen ]         @326[ Editor-Bedienung ]                 @001[ šbersicht ]
.356
    G D M - FUSSZEILENMENš FUNKTIONEN
    <F1 Symbol>:   Cursor auf freier Stelle:  SETZEN EINES NEUEN SYMBOLS
                   Cursor auf GDM-Symbol   :  AKTIVIEREN DES POPUP-MENšs

       Wenn die Funktion auf einer leeren GDM-Position durchgefhrt wird,
       erscheint ein SYMBOL-CURSOR, der mittels Mausklick oder den <CURSOR>-
       Tasten, auf die endgltige Position gesetzt werden kann.

       BESTTIGUNG DER POSITION ber folgende Tastenkombinationen:

       <ENTER> ... Je nach Ebene mssen verschiedene ANGABEN ZUM neuen
                   GDM-OBJEKT (Symbol) gemacht werden:
                   @357[<TASK-Ebene>]   @357[<PROZESSOR-Ebene>]   @357[<RPS-Ebene>]:

       <SPACE> ... In der <TASK-Ebene> erscheint eine Auswahlliste aller Tasks
                   des aktuellen Projektes, die noch nicht in der GDM erfaát
                   sind, von denen aber bereits Quelldateien existieren. Nach
                   der Auswahl der gewnschten Task wird eine Dialogbox zum
                   Einstellen der TASKPARAMETER eingeblendet.


                                  @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.357
    G D M - FUSSZEILENMENš FUNKTIONEN
    <Symbol>:   SYMBOLTYP AUF TASK-EBENE

     - Beim Einfgen eines Symbols erscheint ein Popup-Men zur Auswahl des
       SYMBOLTYPS:
     ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³ @380[Zyklische Task] ³ ... Task-Symbol fr normale und superschnelle Tasks
     ³ @382[Interrupt Task] ³ ... Task-Symbol fr Interrupttasks
     ³ @382[Exception Task] ³ ... Task-Symbol fr Exceptiontasks
     ³ @381[Textdatei     ] ³ ... Text-Symbol
     ³ @381[Datenmodul    ] ³ ... Datenmodul-Symbol
     ³ @383[B&R Modul     ] ³ ... Symbol fr ein B&R-Modul (z.B: Anwender *.br Module)
     ³ @383[Systemmodul   ] ³ ... Symbol fr ein B&R-Modul (z.B: System *.br Module)
     ³ @384[Konfiguration ] ³ ... Setzen/Laden der RPS-Konfiguration
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
     - In einer Dialogbox k”nnen Sie den SYMBOL-NAMEN eingeben.
     - Nach der Best„tigung des SYMBOL-Namens k”nnen die erforderlichen
       OBJEKT-Parameter in einer Dialogbox eingegeben werden.
     - Jeder Symboltyp erfordert andere Parameter. Diese sind bei der HM-
       Funktion <Objekt><Parameter> ausfhrlich beschrieben.

    @359[ Symbol ]                      @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.359
    G D M - FUSSZEILENMENš FUNKTIONEN
    <Symbol>:   SYMBOLTYP AUF PROZESSOR-EBENE

     - Wenn keine CPU definiert ist, wird standardm„áig der SYMBOLTYP CPU
       ausgew„hlt.
     - Sonst erscheint beim Einfgen eines Symbols ein Popup-Men zur Auswahl
       des SYMBOLTYPS:
     ÚÄÄÄÄÄÄ¿
     ³ MP   ³ .... Setzen eines Multi-Prozessor-Symbols
     ³      ³             Modulnr. :    ÚÄÄÄÄÄÄÄÄÄÄÄ¿
     ³      ³             Modulmode: ÄÄÄ³ autark    ³
     ³      ³             Modultyp :    ³ asynchron ³
     ³      ³                           ÀÄÄÄÄÄÄÄÄÄÄÄÙ
     ³      ³
     ³ IP   ³ .... Setzen eines Intelligenten Peripheriemodul-Symbols
     ³      ³             Rckwand   :
     ³      ³             Steckplatz :
     ÀÄÄÄÄÄÄÙ             Zykluszeit : in æs

     - Weiters erscheint eine Dialogbox zur Eingabe des SYMBOL-NAMENS

    @360[ Symbol ]                      @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.360
    G D M - FUSSZEILENMENš FUNKTIONEN
    <Symbol>:   SYMBOLTYP AUF RPS-EBENE

     - Beim Einfgen eines Symbols erscheint eine Dialogbox zur Eingabe
       des SYMBOL-NAMENS (RPS Name).

       - Falls eine Prozessor-Ebene erforderlich ist, kann diese mit der
         Popup-Funktion "Ebene einziehen" jederzeit generiert werden.

       - Fr jedes definierte RPS-Symbol gibt es dementsprechend eine
         Prozessor- oder Task-Ebene, die mit der Fuázeilen-Funktion "Ebene "
         zu erreichen ist.









                                  @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.362
    G D M - FUSSZEILENMENš FUNKTIONEN
    <Datenbaum>:   EDITIEREN EINES DATENBAUMES

       Nach der Auswahl der Funktion erscheint ein Popup-Men, in der Sie eine
       der Datenbaum-Funktionen ausw„hlen k”nnen:
     ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³ Zeichnen              ³ .... Zeichnen eines Datenbaumes
     ³ L”schen               ³ .... L”schen von Datenbaum-Zeichen
     ³ Zeichnen/L”schen aus  ³ .... Ausschalten der Fkt. Zeichnen/L”schen
     ³ Prozessvariablen      ³ .... Auflisten von gemeinsamen PVs, der mit
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ      dem Datenbaum verbundenen Symbole.

      <Zeichnen>/<L”schen> : AB DER AKTUELLEN Cursorposition kann mit den
                             <CURSOR>-Tasten oder BIS ZUR Position eines MAUS-
                             Klicks, ein Datenbaum gezeichnet/gel”scht werden.

      <Prozessvariablen>   : Diese Funktion wird nur AUF EINEM DATENBAUM aus-
                             gefhrt. Die angezeigten PVs k”nnen als ASCII-
                             Datei gespeichert werden. (*.vld)


    @363[ Datenbaum ]                   @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.363
    G D M - FUSSZEILENMENš FUNKTIONEN
    <Datenbaum>   :  Auf Taskebene erfolgt die Zuordnung von gemeinsamen PVs
                     automatisch ber den Namen. Auf h”heren Ebenen muá die
                     Zuordnung manuell festgelegt werden.

                     ÚÄÄÄÄÄÄÄTC#1Ä¿     ÚÄÄÄÄÄÄÄTC#1Ä¿     ÚÄÄÄÄÄÄÄTC#4Ä¿
                     ³  source00  ³ÍÍÍÍÍ³  source01  ³ÍÍÍÍÍ³  source02  ³
                     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ
          ebenfalls   <---- º                                     º
          mind. 2 ZEICHEN   ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


     - Ein Datenbaum-Abschnitt muá immer mind. ZWEI ZEICHEN LANG sein.
       (Der Abstand zwischen den zu verbindenden Symbolen muá daher auch
       ZWEI ZEICHEN betragen, um einen Datenbaum zeichnen zu k”nnen.)

     - Datenb„ume werden nur von einem Symbol zum n„chsten Symbol ausgewertet
       (z.B: source00<->source01; source01<->source02). Da die Verbindung
       durch jedes Symbol unterbrochen wird, muá der Datenbaum im Bedarfsfall
       ber einen freien Bereich gefhrt werden (z.B: source00<->source02).

                                  @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.364
    G D M - FUSSZEILENMENš FUNKTIONEN
    <Kommentar>:   EINGEBEN, EDITIEREN EINES KOMMENTARS

       Der Kommentar wird mit Hilfe einer Dialogbox eingegeben:

       - Die Gr”áe des Kommentar-Bereiches h„ngt von den Platzverh„ltnissen in
         der GDM ab.
         Kommentar-Bereich: AB DER CURSOR-Position bis zum Zeilen-Ende oder
                            EINSCHRNKUNG durch ein GDM-SYMBOL.

       - Der Kommentar-Bereich wird ebenfalls als GESCHšTZTER SYMBOL-Bereich
         der GDM gewertet.

       - Aufruf der Funktion auf einem KOMMENTAR-Bereich bewirkt das Editieren
         des bestehenden KOMMENTAR-Bereiches.






                                  @355[ GDM-Fuázeilenmen ]                @001[ šbersicht ]
.370
    G D M  SYNTAX-BESCHREIBUNG

    Die Strukturierung eines Projektes wird durch die SYMBOLISCHE DARSTELLUNG
    der Projekt-Sourcen und Parameter optimal untersttzt.
    Die Aufteilung in verschiedene Ebenen erm”glicht eine bersichtliche
    Darstellung von Multi-Prozessor Projekten.


    @374[< GDM Ebenen         >]     - RPS-Ebene, Prozessor-Ebene, Task-Ebene


    @378[< GDM Symbole        >]     - Symbole der verschiedenen Ebenen
                               - Datenbaum


    @371[< GDM Grundbegriffe  >]     - Erkl„rung der Grundbegriffe





    @326[ Editor-Bedienung ]               @325[ GDM-Editor ]                    @001[ šbersicht ]
.371
    G D M - GRUNDBEGRIFFE

    <Ebenen>:          Die GDM wird in DREI Arten von EBENEN unterteilt: die
                       RPS-Ebene, Prozessor-Ebene und Task-Ebene.
                       Je nach Anzahl der RPSen und Prozessoren kann die GDM
                       mehrere Prozessor-Ebenen und Task-Ebenen umfassen.
                       Zwischen den Ebenen kann mit:
                       Ebene : RPS-EBENE  -> (PROZESSOR) -> TASK-EBENE -> TASK
                       Ebene : TASK-EBENE -> (PROZESSOR) -> RPS-EBENE
                       verzweigt werden.

    <OBJEKTE/Symbole>: Objekte werden in Form von SYMBOLEN in der GDM darge-
                       stellt. (z.B.: RPSen, Prozessoren, Tasks, BR-Module, ...)

                       Die Bezeichnung "Objekt" hat in jeder Ebene eine andere
                       Bedeutung. (In der RPS-Ebene ist je eine RPS ein GDM-
                       Objekt. In der Prozessor-Ebene stellt jeder definierte
                       Prozessor (CPU, MP, IP) ein Objekt dar, und in der
                       Task-Ebene sind die einzelnen Projekt-Sourcen und Daten-
                       module, ... eigene Objekte.)

    @372[ Grundbegriffe ]                  @370[ GDM-Syntax ]                    @001[ šbersicht ]
.372
    G D M - GRUNDBEGRIFFE

    <Datenbaum>:      Der Datenbaum ist die symbolische Darstellung von
                      GEMEINSAMEN DATENPUNKTEN zweier oder mehrerer Objekte
                      der gleichen Ebene. Die gemeinsamen Datenpunkte dieser
                      Objekte mssen mit einem entsprechenden Speicherattribut
                      deklariert sein (TK_global, RPS_global, ...).

    <Projektname>:    Der Projektname entspricht dem aktuell ge”ffnetem Projekt.

    <Fenstertitel>:   Als Fenstertitel einer Ebene wird zus„tzlich der Name der
                      n„chsth”heren Ebene im Fensterrahmen angezeigt.
                      Als Fenstertitel der RPS-Ebene (h”chste Ebene) wird der
                      Projektname angezeigt.







    @374[ GDM-Ebenen ]                     @370[ GDM-Syntax ]                    @001[ šbersicht ]
.374
    GRAPHISCHE DESIGN METHODE:  GDM-EBENEN
   ÄÄÄÄÄÄÄÄÄÄÄÄÄÄ ALPHA ÄÄÄÄÄÄÄÄÄÄÄÄÄ¿  @375[<RPS-EBENE>]
   ³ ÉÍÍÍÍÍÍÍÍÍÍ»                     ³       šbersicht aller RPSen des
   ³ º TEST_RPS º                     ³       Projektes "ALPHA"
   ³ ÈÍÍÍÍÍÍÍÍÍÍ¼                     ³
   ³                                  ³
   ³    ÄÄÄÄÄÄÄÄÄÄÄÄÄ TEST_RPS ÄÄÄÄÄÄÁÄÄÄÄ¿  @376[<PROZESSOR-EBENE>]
   ³    ³ ÉÍÍÍÍÍÍCPUÍ»  ÉÍÍÍÍÍÍÍMPÍ»       ³       šbersicht aller Prozessoren
   ÀÄÄÄÄ´ º MAIN_CPU º  º MP_VISU  º       ³       der RPS "TEST_RPS"
        ³ ÈÍÍÍÍÍÍÍÍÍÍ¼  ÈÍÍÍÍÍÍÍÍÍÍ¼       ³
        ³                                  ³
        ³    ÄÄÄÄÄÄÄÄ TEST_RPS.MAIN_CPU ÄÄÁÄÄÄÄ¿  @377[<TASK-EBENE>] der "MAIN_CPU"
        ³    ³ ÚÄÄÄÄÄÄTC#1¿ ÚÄÄÄÄÄÄTC#2¿        ³      šbersicht aller Projekt-
        ÀÄÄÄÄ´ ³ source00 ³ ³ source01 ³        ³      Sourcen und Datenmodule
             ³ ÀÄÄÄÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÄÄÄÙ        ³
 ÚÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄ source00.src ÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
 ³ ; Aus der GDM kann direkt in den entsprechenden Source-Code ³  <EDITOR>
 ³ ; Editor des angew„hlten Objektes gewechselt werden.        ³
 ÀÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
             ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

                                     @370[ GDM-Syntax ]                    @001[ šbersicht ]
.375
    G D M - EBENEN und SYMBOLE

    <RPS-EBENE>      :  Die RPS-Ebene ist die H™CHSTE Ebene der GDM, und sollte
                        eine šbersicht der beteiligten RPSen EINES PROJEKTES
                        umfassen.

                        Mit "EBENE " wird SYMBOL-SENSITIV in die entsprechende
                        PROZESSOR- oder TASK-Ebene verzweigt.


    *<RPS-Symbol>     : Fr jede RPS wird ein SYMBOL eingegeben.

      ÉÍÍÍÍÍÍÍÍÍÍÍÍ»   - Nach der Definition des RPS-Namen, wird dieser im
      º RPS_NAME   º     Symbol angezeigt.
      ÈÍÍÍÍÍÍÍÍÍÍÍÍ¼   - Das automatisch mit dem "Projekt-Erzeugen" angelegte
                         RPS-Symbol erh„lt den projektierten RPS-Namen.





    @376[ Fortsetzung ]                    @374[ GDM-Ebenen ]                    @001[ šbersicht ]
.376
    G D M - EBENEN und SYMBOLE

    <PROZESSOR-EBENE>  : Die Prozessor-Ebene sollte eine šbersicht ber alle
                         Prozessoren (Zentraleinheit, Multi-Prozessoren,
                         Intelligente Peripherie) EINER RPS umfassen.
                         Wenn nur die CPU in einer RPS vorhanden ist, kann auf
                         diese Ebene verzichtet werden.

                         Mit "EBENE " wird SYMBOL-SENSITIV in die entsprechende
                         TASK-Ebene verzweigt.

    *<Prozessor-Symbol>: Fr jeden PROZESSOR wird ein SYMBOL eingegeben. Nach
                         der Definition des Prozessor-Namen, wird dieser im
      ÉÍÍÍÍÍÍÍÍ###Í»     Symbol angezeigt.
      º PROZ_NAME  º
      ÈÍÍÍÍÍÍÍÍÍÍÍÍ¼     ###:   Anzeige des Prozessortyps:
                          CPU.. Zentraleinheit
                          MP... Multi-Prozesor
                          IP... Intelligente Peripherie


    @377[ Fortsetzung ]                    @374[ GDM-Ebenen ]                    @001[ šbersicht ]
.377
    G D M - EBENEN und SYMBOLE

    <TASK-EBENE>     :  Die Task-Ebene ist die TIEFSTE Ebene der GDM, und
                        sollte eine šbersicht der verwendeten Projekt-Sourcen
                        und Datenmodule EINES PROZESSORS beinhalten.

                        Mit "EBENE " wird SYMBOL-SENSITIV der entsprechende
                        EDITOR mit dem Quellcode ge”ffnet.

    *<Symbole>        : Fr alle ben”tigten Module eines Prozessors wird ein
                        SYMBOL eingegeben.
                        ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ÚÄÄÄÄÄÄÄÄ###Ä¿    ³@380[ Task-Symbol ]³ .... Kennzeichnung TC#1-TC#4/HS#1-HS#4
      ³ NAME       ³ => ³@381[ DAT-Symbol  ]³ .... Kennzeichnung DAT
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ    ³@381[ Text-Symbol ]³ .... Kennzeichnung TXT
                        ³@382[ IRQ-Symbol  ]³ .... Kennzeichnung IRQ
                        ³@382[ EXC-Symbol  ]³ .... Kennzeichnung EXC
                        ³@383[ OBJ-Symbol  ]³ .... Kennzeichnung OBJ
                        ³@383[ SYS-Symbol  ]³ .... Kennzeichnung SYS
                        ³@384[ CFG-Symbol  ]³ .... Kennzeichnung CFG
                        ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
                                     @374[ GDM-Ebenen ]                    @001[ šbersicht ]
.378
    G D M - SYMBOLE
    Je nach der ANGEWHLTEN EBENE stehen folgende SYMBOLE zur Verfgung:

     - RPS-Symbol :                    - Prozessor-Symbol :
       ÉÍÍÍÍÍÍÍÍÍÍÍÍ»                    ÉÍÍÍÍÍÍÍÍ###Í»
       º RPS_NAME   º                    º PROZ_NAME  º
       ÈÍÍÍÍÍÍÍÍÍÍÍÍ¼                    ÈÍÍÍÍÍÍÍÍÍÍÍÍ¼
        ### :  Kennzeichnet den Typ des Prozessors (CPU..HauptCPU,
               MP..Multiprozessor, IP..intelligentes Peripheriemodul);

     - Symbol der Taskebene :
                        ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ÚÄÄÄÄÄÄÄÄ###Ä¿    ³@380[ Task-Symbol ]³ .... Kennzeichnung TC#1-TC#4/HS#1-HS#4
      ³ NAME       ³ => ³@381[ DAT-Symbol  ]³ .... Kennzeichnung DAT
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ    ³@381[ Text-Symbol ]³ .... Kennzeichnung TXT
                        ³@382[ IRQ-Symbol  ]³ .... Kennzeichnung IRQ
                        ³@382[ EXC-Symbol  ]³ .... Kennzeichnung EXC
                        ³@383[ OBJ-Symbol  ]³ .... Kennzeichnung OBJ
                        ³@383[ SYS-Symbol  ]³ .... Kennzeichnung SYS
                        ³@384[ CFG-Symbol  ]³ .... Kennzeichnung CFG
                        ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    @379[ GDM-Symbole ]                    @370[ GDM-Syntax ]                    @001[ šbersicht ]
.379
    G D M - SYMBOLE
    <Datenbaum>   :  Der Datenbaum ist die symbolische Darstellung von
                     GEMEINSAMEN DATENPUNKTEN von ZWEI Objekten der gleichen
                     Ebene. Auf Taskebene erfolgt die Zuordnung von gemein-
                     samen PVs ber den Namen, auf h”heren Ebenen muá die
                     Zuordnung manuell festgelegt werden.

                     ÚÄÄÄÄÄÄÄTC#1Ä¿     ÚÄÄÄÄÄÄÄTC#1Ä¿     ÚÄÄÄÄÄÄÄTC#4Ä¿
                     ³  source00  ³ÍÍÍÍÍ³  source01  ³ÍÍÍÍÍ³  source02  ³
                     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ     ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ
          ebenfalls   <---- º                                     º
          mind. 2 ZEICHEN   ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

     - Um einen Datenbaum zeichnen zu k”nnen, mssen mindestens ZWEI ZEICHEN
       Abstand zwischen den zu verbindenden Symbolen sein.

     - Datenb„ume werden nur von einem Symbol zum n„chsten Symbol ausgewertet
       (z.B: source00<->source01; source01<->source02). Da die Verbindung
       durch jedes Symbol unterbrochen wird, muá der Datenbaum im Bedarfsfall
       ber einen freien Bereich gefhrt werden (z.B: source00<->source02).

    @371[ Grundbegriffe ]                  @370[ GDM-Syntax ]                    @001[ šbersicht ]
.380
    G D M - SYMBOLE

    <TASK-Symbol>:
     - Am oberen rechten Rand des Symbols wird die Taskklasse des Tasks ange-
       zeigt. (z.B. fr normale Tasklassen TC#1 = Taskklasse 1; oder fr super-
       schnelle HS#1 = HS-Taskklasse 1)
     - Innerhalb einer Task-Ebene drfen Task-Namen nur einmal vorkommen.

     - Standard-Konfigurierung der Taskklassen:
ÚÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³ Taskklasse³Zykluszeit (std.)³erlaubte Zykluszeitverletzung³ verfgbar       ³
ÃÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
³   TC#1    ³       10ms      ³              20ms           ³ B&R2010/B&R2005 ³
³   TC#2    ³       50ms      ³              50ms           ³ B&R2010/B&R2005 ³
³   TC#3    ³      100ms      ³             100ms           ³ B&R2010/B&R2005 ³
³   TC#4    ³       10ms      ³            3000ms           ³ B&R2010/B&R2005 ³
³   HS#1    ³      3000æs     ³             KEINE           ³ B&R2010/B&R2005 ³
³   HS#2    ³      5000æs     ³             KEINE           ³ B&R2010         ³
³   HS#3    ³      7000æs     ³             KEINE           ³ B&R2010         ³
³   HS#4    ³      9000æs     ³             KEINE           ³ B&R2010         ³
ÀÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    [BACKSPACE]==>zurck             @370[ GDM-Syntax ]                    @001[ šbersicht ]
.381
    G D M - SYMBOLE

    <DAT-Symbol>:
     - Kennzeichnet ein Datenmodul. Wobei mit "Ebene " direkt der Datenmodul-
       Editor mit dem entsprechenden Datenmodul ge”ffnet wird.


    <TEXT-Symbol>:
     - Kennzeichnet eine ASCII-Textdatei. Wobei mit "Ebene " direkt der Text-
       Editor mit dem entsprechenden Textdatei ge”ffnet wird.







  !!  Innerhalb einer Task-Ebene drfen Symbol-Namen gleicher SYMBOLTYPEN
      nur einmal vorkommen  !!


    [BACKSPACE]==>zurck             @370[ GDM-Syntax ]                    @001[ šbersicht ]
.382
    G D M - SYMBOLE

    <IRQ-Symbol>:
     - Kennzeichnet einen Interrupt-Task, der beim Auftreten des parametrierten
       IRQs, auf der RPS gestartet wird. Mit "Ebene " wird direkt der entspr.
       Source-Code-Editor, mit dem entspr. Quellcode, ge”ffnet.


    <EXC-Symbol>:
     - Kennzeichnet einen Exception-Task, der beim Auftreten der parametrierten
       Exception, auf der RPS gestartet wird. Mit "Ebene " wird direkt der
       entspr. Source-Code-Editor, mit dem entspr. Quellcode, ge”ffnet.





  !!  Innerhalb einer Task-Ebene drfen Symbol-Namen gleicher SYMBOLTYPEN
      nur einmal vorkommen  !!


    [BACKSPACE]==>zurck             @370[ GDM-Syntax ]                    @001[ šbersicht ]
.383
    G D M - SYMBOLE

    <B&R Modul>:
      Kennzeichnet ein BR-Modul (z.B.: compilierte Task, Runtime-Module der
      Libraries, Exceptionhandler, ...). Dieses Symbol wird z.B. beim Abgleichen
      der GDM eingefgt. Wenn das BR-Modul des OBJ-Symbols mit RB-Info=Ein
      bersetzt wurde, kann der Quellcode (PVs, ...) mit REBUILD rckgewonnen
      werden. Dabei wird das OBJ-Symbol in der GDM, auf ein Task-Symbol umge-
      wandelt.

    <Systemmodul>:
      Kennzeichnet ein B&R-Modul (z.B.: Bugfix, Runtime-Module der Libraries,
      Exceptionhandler, ...). Nach dem Einfgen dieses Symbols wird ein POPUP-
      Men mit einer Liste aller im System installierten Systemmodule, die in
      der GDM noch nicht eingetragen sind, aufgelistet. Nach der Auswahl eines
      Listeneintrages wird das entsprechende BR-Modul in das Modulverzeichnis
      der aktuellen CPU (oder Prozessor) kopiert.

  !!  Innerhalb einer Task-Ebene drfen Symbol-Namen gleicher SYMBOLTYPEN
      nur einmal vorkommen !!

    [BACKSPACE]==>zurck             @370[ GDM-Syntax ]                    @001[ šbersicht ]
.384
    G D M - SYMBOLE

    <CFG-Symbol>:
     - Nach dem Setzen dieses Symbols wird die Systemkonfiguration von der
       RPS geladen und ein Objekt mit dem Namen "sysconf", in der GDM einge-
       tragen.
     - Mit Hilfe der <OBJEKT><PARAMETER> dieses Symbols k”nnen zumindest die
       wichtigsten Parameter der RPS-Systemkonfiguration ver„ndert werden, und
       dann auf die RPS bertragen werden.












    [BACKSPACE]==>zurck             @370[ GDM-Syntax ]                    @001[ šbersicht ]
.400
    PL2000 - SPRACHEDITOR

    Der PL2000 Spracheditor dient zur ERSTELLUNG und BEARBEITUNG von Programmen
    in der Programmiersprache PL2000.
    Der PL2000 Spracheditor ist ein SYNTAXSENSITIVER Editor, der nach
    jedem Bearbeiten einer Zeile diese vor dem Verlassen auf RICHTIGKEIT
    DER SYNTAX berprft.

    @420[< SYNTAX-Beschreibung >]         - Operatoren und Kontrollkonstrukte
                                    - Prozeávariablen und Datentypen
                                    - Konstanten und Kommentar

    @402[< EDITOR-Bedienung    >]         - PL2000 Tastaturkommandos
                                    - Zeichen/Zeile einfgen, l”schen
                                    - Bereich markieren, kopieren

    @460[< PL2000-Debugger     >]         - PL2000 Source-Level DEBUGGER

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men

                                                                     @001[ šbersicht ]
.402
    PL2000 BEDIENUNG

    Eine neu er”ffnete Datei erh„lt den Namen <sourcexx.src>, wobei "xx"
    eine fortlaufende Nummer darstellt.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄsourcexx.src [ PL2000 ]ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³ ; Bereich fr PL2000-Syntax                                        ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³ ; @403[< Tastaturkommandos >]      - PL2000 Tastaturkommandos            ²
    ³ ;                            - Zeichen/Zeile einfgen, l”schen     ²
    ³ ;                            - Bereich markieren, kopieren         ²
    ³                                                                    ³
    ÀÄZeile xÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ
      ³
      ÀÄÄÄÄÄÄÄÄ Anzeige der aktuellen CURSOR-Position

    @420[ PL2000-Syntax ]                 @400[ PL2000-Editor ]                  @001[ šbersicht ]
.403
    PL2000 BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Bedienung des PL2000 Editors kann wahlweise
                            mittels TASTENKOMBINATIONEN oder MAUS-Bedienung
                            erfolgen:


  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Aussteigen aus einer angew„hlten Funktion     º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ENTER>                ³ Abschluá einer Zeile mit SYNTAXPRšFUNG, UND   º
  º                        ³ im Einfge-Modus:                             º
  º                        ³    - Leerzeile nach aktueller Zeile einfgen  º
  º                        ³    - Cursor auf Leerzeile stellen             º
  º                        ³ im šberschreibmodus:                          º
  º                        ³    - Cursor in die n„chste Zeile stellen      º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @404[ Fortsetzung ]                   @400[ PL2000-Editor ]                  @001[ šbersicht ]
.404
    PL2000 BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit Hilfe der
                            Bildlaufleiste und einem Mausklick durchgefhrt
                            werden, oder ber folgende Tastenkombinationen:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein Zeichen nach links/rechts                 º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  º <TAB>                  ³ im Einfge-Modus: Einfgen von Leerzeichen    º
  º                        ³    bis zur n„chsten TAB-Position              º
  º                        ³ im šberschreibmodus: Cursor auf n„chste       º
  º                        ³    TAB-Position stellen                       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º ^<HOME>/ 2x<HOME>      ³ Cursor zum Beginn des Textes                  º
  º ^<END>/ 2x<END>        ³ Cursor zum Ende des Textes                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @405[ Fortsetzung ]                   @400[ PL2000-Editor ]                  @001[ šbersicht ]
.405
    PL2000 BEDIENUNG

    TEXT- BEARBEITUNG    :  Mit Hilfe der Maus kann der gewnschte Editor-
                            Bereich direkt markiert werden.
                            Tastenkombinationen fr Textbearbeitung:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^<BACKSPACE>           ³ L”schen der aktuellen Zeile                   º
  º <DEL>/<BACKSPACE>      ³ Zeichen unter/vor dem Cursor l”schen          º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>                ³ Setzen der Textmarke bis Zeilenanfang         º
  º <END>                 ³ Setzen der Textmarke bis Zeilenende           º
  º <CURSOR-TASTEN>       ³ Setzen der Textmarke                          º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <INS>                  ³ Wechselweises Umschalten zwischen             º
  º                        ³ EINFšGE-/šBERSCHREIB-Modus                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @402[ Fortsetzung ]                   @400[ PL2000-Editor ]                  @001[ šbersicht ]
.420
    PL2000  SYNTAX-BESCHREIBUNG

    Nach jedem Bearbeiten einer Zeile wird diese vor dem Verlassen
    auf RICHTIGKEIT DER SYNTAX berprft.

    @421[< PL2000 Operatoren   >]     - Arithmetische und logische Ausdrcke
                                - Priorit„t der Operatoren
    @435[< PL2000 Anweisungen  >]     - Kontrollkonstrukte: if/loop/select/case/goto
                                - Weitere Anweisungen: adr/access/...
    @490[< FUB-Einbindung      >]     - Aufruf von FUBs und Funktionen

    @426[< Prozeávariablen     >]     - Deklaration von Prozeávariablen

    @424[< Konstante           >]     - Deklaration von Konstanten

    @425[< Kommentar           >]     - Eingeben eines Kommentars

    @428[< Datentyp-Umwandlung >]     - Datentypen (Standard, Array, Struktur)
                                - Implizite/Explizite Konvertierung


    @402[ Editor-Bedienung ]              @400[ PL2000-Editor ]                  @001[ šbersicht ]
.421
    PL2000 Syntax:  Operatoren in ARITHMETISCHEN Ausdrcken

    UNRE Operatoren brauchen nur EINEN Operanden, w„hrend BINRE Operatoren
    zwischen ZWEI Operanden (PV oder Konstante) stehen mssen.

    ÉÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º SYM ³ Beschreibung                                                  º
    ÌÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º -   ³ un„res Minus                                                  º
    º not ³ un„re Negation                                                º
    º +   ³ Addition                                                      º
    º -   ³ Subtraktion                                                   º
    º *   ³ Multiplikation                                                º
    º /   ³ Division                                                      º
    º mod ³ Rest einer ganzzahligen Division                              º
    º and ³ logisches UND (bitweise gebildet)                             º
    º or  ³ logisches ODER (bitweise gebildet)                            º
    º xor ³ logisches EXCLusives ODER (bitweise gebildet)                 º
    ÈÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @422[ Logische Op. ]                  @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.422
    PL2000 Syntax:  Operatoren in LOGISCHEN Ausdrcken

    Das Boolsche Zwischenergebnis von logischen Ausdrcken kann den Wert
    WAHR (=1) bei Erfllung der Bedingung oder den Wert FALSCH (=0) annehmen.

  ÉÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º SYM ³ Beschreibung   ³ WAHR-Bedingung der Boolschen Funktion             º
  ÌÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º  =  ³ Gleichheit     ³ Der Wert rechts entspricht dem Wert links         º
  º  <> ³ Ungleichheit   ³ Der Wert rechts ist ungleich dem Wert links       º
  º  >  ³ Gr”áer         ³ Der Wert links ist gr”áer als der Wert rechts     º
  º  >= ³ Gr”áer gleich  ³ Wert links ist gr”áer_als/gleich_dem Wert rechts  º
  º  <  ³ Kleiner        ³ Der Wert links ist kleiner als der Wert rechts    º
  º  <= ³ Kleiner gleich ³ Wert links ist kleiner_als/gleich_dem Wert rechts º
  ÈÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    'rechts' bzw. 'links' beziehen auf den logischen Ausdruck rechts bzw. links
    des Operators.



    @423[ Priorit„t ]                     @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.423
    PL2000 Syntax:  PRIORITT der Operatoren

    Die PRIORITT der Operatoren bestimmt die REIHENFOLGE DER AUFL™SUNG
    eines Ausdruckes in der PL2000-SPRACHE.

    ÉÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º PRIO ³ OPERATOREN                                                   º
    ÌÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º   7  ³ not, - (un„re Operatoren)           ³ H™CHSTE Priorit„t      º
    º   6  ³ *, /, mod                           ³                        º
    º   5  ³ +, -                                ³                        º
    º   4  ³ and                                 ³                        º
    º   3  ³ xor                                 ³                        º
    º   2  ³ or                                  ³                        º
    º   1  ³ =, <>, <=, >=, <, >                 ³ NIEDRIGSTE Priorit„t   º
    ÈÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼





    @421[ Arithmet. Op. ]                 @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.424
    PL2000 Syntax:  DEKLARATION VON KONSTANTEN

    Eine Konstante beschreibt einen Wert, der sich ber die Laufzeit des
    Programms nicht „ndert:
    ÉÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  SYM  ³ KONSTANTE              ³  Beispiele                           º
    ÌÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º  zz   ³ DEZIMALE Konstante     ³  [+|-]zzz                            º
    º       ³                        ³  23, -13876, ...                     º
    º  zz.  ³ FLOAT Konstante        ³  [+|-]zzz.[zzz] [E|e [+|-]zz]        º
    º       ³                        ³  100. ,-47.23 ,+6.17e-01 ,...        º
    º  %%zz ³ BINRE Konstante       ³  %%11011110, ...                     º
    º  $zz  ³ HEXADEZIMALE Konstante ³  $9, $a3, $FF, ...                   º
    º "sss" ³ STRING Konstante       ³  "a b c d", "String-Konstante", ...  º
    ÈÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    - Eine KONSTANTE wird immer als KLEINSTM™GLICHER DATENTYP interpretiert.
    - Bin„r- und Hex-Konstanten sind immer UNSIGNED, sonst optional [+|-].
    - STRINGKONSTANTEN drfen bis zu 255 ZEICHEN lang sein, und bestehen aus
      einer Folge von darstellbaren Zeichen einschlieálich Leerzeichen, die
      zwischen Anfhrungszeichen gesetzt werden.

                                    @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.425
    PL2000 Syntax:  KOMMENTAR

    ÉÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  SYM  ³ Bezeichnung            ³  Beispiel                          º
    ÌÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º   ;   ³ KOMMENTAR              ³  ; An allen Positionen einer Zeile º
    ÈÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    Der Kommentar wird vom Programm durch einen ";" getrennt. Die Zeichen
    ab einem ";" bis zum Zeilenende werden als Kommentar interpretiert.

    Aussagekr„ftige Kommentare, gezielt ins Programm eingesetzt, erleich-
    tern die Orientierung in einem l„ngeren Programm.








                                    @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.426
    PL2000 Syntax:  PROZESSVARIABLEN-NAMEN

    Als PV-Namen werden in der PL2000-Syntax SYMBOLISCHE NAMEN verwendet,
    die nach den folgenden Regeln zusammengesetzt sein drfen:
    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  Buchstaben          ³  Zeichen             ³  Ziffern              º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º 'A'bis'Z', 'a'bis'z' ³       '_', '$'       ³       '0'bis'9'       º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

     - Alle Anwender-PVs MšSSEN mit einem BUCHSTABEN beginnen !
       (Ausnahme: nur bereitgestellte Systemvariablen beginnen mit einem "_")
     - Die Groá-/Kleinschreibung eines PV-Namens ist SIGNIFIKANT.
     - Der PV-Name muá aus einer DURCHGEHENDEN ZEICHENKETTE bestehen.
     - Die PV-Namensl„nge ist nicht beschr„nkt, SIGNIFIKANT sind 10 ZEICHEN.
     - PV-Namen drfen NICHT wie SCHLšSSELW™RTER der Programmiersprachen heiáen.
     - Die Trennung zwischen einem PV-Namen und einem Schlsselwort der
       Sprach-Syntax erfolgt durch mindestens 1 Leerzeichen (" ").
     - Zwischen einer PV und einem Operator ist kein Leerzeichen notwendig.
     - Die DEFINITION erfolgt anhand des Namens in der VARIABLENDEKLARATION.

                                    @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.428
    PL2000 Syntax:  ANALOGE UND DIGITALE DATENTYPEN
    DIGITALE DATENTYPEN:  Der Standardtyp fr ein digitales I/O ist das BIT.
     ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
     º Datentyp º Bitbereich ³ Minimalwert   Maximalwert ³ Speicherbedarf º
     ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
     º  BIT     º   1 Bit    ³      0      ³      1      ³  niedrigster   º
     ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    ANALOGE DATENTYPEN :  šbersicht ber die analogen Datentypen:
     ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
     º          º            ³    Darstellbare Zahlen    ³     H”he       º
     º Datentyp º Bitbereich ³ Minimalwert   Maximalwert ³ Speicherbedarf º
     ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
     º  BYTE    º   8 Bit    ³      0      ³     255     ³  niedrigster   º
     º  WORD    º  16 Bit    ³      0      ³    65535    ³  mittlerer     º
     º  LONG    º  32 Bit    ³      0      ³ 4294967295  ³  h”chster      º
     º  INT8    º   7 Bit+VZ ³    -128     ³    +127     ³  niedrigster   º
     º  INT16   º  15 Bit+VZ ³   -32768    ³   +32767    ³  mittlerer     º
     º  INT32   º  31 Bit+VZ ³ -2147483648 ³ +2147483647 ³  h”chster      º
     º  FLOAT   º  32 Bit    ³1.18E-38(abs)³3.4 E+38(abs)³  h”chster      º
     ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @429[ Weitere Datentypen ]            @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.429
    PL2000 Syntax:  KOMPLEXE DATENTYPEN

    <FELD (Array)>:          Feldname[Elementindex]

      Ein Feld besteht aus einer definierten Anzahl von Elementen des GLEICHEN
      DATENTYPES. Der Datentyp der Elemente eines Feldes kann jeder Datentyp
      einschlieálich einer Struktur sein.
      Einzelne Elemente eines Feldes werden durch die Angabe des FELDNAMENS
      und des ELEMENTINDEX [in Eckiger Klammer] angegeben.


    <STRUKTUR>    :          Strukturname.Komponentenname

      Eine Struktur ist eine Ansammlung von Elementen. Die Elemente k”nnen
      BELIEBIGE ANALOGE DATENTYPEN, FELDER oder wiederum STRUKTUREN sein.
      Ein Element einer Struktur wird durch den gemeinsamen STRUKTURNAMEN
      (Variablennamen) und dem KOMPONENTENNAMEN, die durch einen Punkt mit-
      einander verbunden sind, beschrieben.
      (z.B. auch: Strukturname.Feldname[Elementindex])


    @430[ Datentypumwandlung ]            @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.430
    PL2000 Syntax:  IMPLIZITE DATENTYPKONVERTIERUNG
    Die DATENTYPKONVERTIERUNGSREGELN auf einen Blick:
     ÚÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³Datentyp³  BIT   BYTE   WORD   LONG   INT8   INT16   INT32   FLOAT³
     ÃÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
     ³ BIT    ³          *      *      *      *      *       *       -  ³
     ³ BYTE   ³   *           WORD   LONG   BYTE   INT16   INT32     -  ³
     ³ WORD   ³   *    WORD          LONG   WORD   WORD    INT32     -  ³
     ³ LONG   ³   *    LONG   LONG          LONG   LONG    LONG      -  ³
     ³ INT8   ³   *    BYTE   WORD   LONG          INT16   INT32     -  ³
     ³ INT16  ³   *    INT16  WORD   LONG   INT16          INT32     -  ³
     ³ INT32  ³   *    INT32  INT32  LONG   INT32  INT32             -  ³
     ³ FLOAT  ³   -      -      -      -      -      -       -          ³
     ÀÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
       */- ...... je nach Operation explizite Konvertierung erforderlich
    Treffen bei einer LOGISCHEN oder ARITHMETISCHEN OPERATION unterschiedliche
    Datentypen zusammen, so wird vom Compiler eine AUTOMATISCHE (implizite)
    DATENTYPUMWANDLUNG durchgefhrt. Bei ZUWEISUNGEN wird nur dann eine 
    implizite Datentypkonvertierung ausgefhrt, wenn die linke Seite der
    Zuweisung einen h”heren Datentyp als den h”chsten Datentyp der rechten
    Seite aufweist.
    @431[ Datentypumwandlung ]            @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.431
    PL2000 Syntax:  EXPLIZITE DATENTYPKONVERTIERUNG
    ANWEISUNGEN fr EXPLIZITE DATENTYPKONVERTIERUNG
     ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³ ZIEL-DATENTYP ³ QUELL-DATENTYPEN: qdata ³ QUELL-DATENTYP: fdata  ³
     ³               ³ (BIT/BYTE/WORD/LONG     ³ (FLOAT-Variable)       ³
     ³               ³  INT8/INT16/INT32)      ³                        ³
     ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
     ³ BIT           ³ BIT(qdata)              ³ BIT(F_TO_L(fdata))     ³
     ³ BYTE          ³ BYTE(qdata)             ³ BYTE(F_TO_L(fdata))    ³
     ³ WORD          ³ WORD(qdata)             ³ WORD(F_TO_L(fdata))    ³
     ³ LONG          ³ LONG(qdata)             ³ F_TO_L(fdata)          ³
     ³ INT8          ³ INT8(qdata)             ³ INT8(F_TO_I32(fdata))  ³
     ³ INT16         ³ INT16(qdata)            ³ INT16(F_TO_I32(fdata)) ³
     ³ INT32         ³ INT32(qdata)            ³ F_TO_I32(fdata)        ³
     ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
     ³ FLOAT         ³ L_TO_F(long_var) oder   ³                        ³
     ³               ³ I32_TO_F(int32_var)  ** ³                        ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
       ** ... Quell-Datentypen ungleich LONG oder INT32 mssen vorher auf
              LONG(qdata) oder INT32(qdata) umgewandelt werden.

    @428[ Einfache Datentypen ]           @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.435
    PL2000 SYNTAX:  ANWEISUNGEN
                                                                      @436[< WEITER >]
    Zur Steuerung des Programmflusses werden folgende KONTROLLKONSTRUKTE
    angeboten:
    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Bezeichnung      ³  Schlsselw”rter          ³    B e d e u t u n g    º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º@440[ if-Anweisung     ]³ if, then, else, endif     ³ Bedingung (Abfrage)     º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º@442[ loop-Anweisung   ]³ loop, endloop,            ³ Wiederholung (Schleife) º
    º                  ³ to, downto, exitif        ³                         º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º@444[ select-Anweisung ]³ select, next, endselect   ³ Zustandsbl”cke und      º
    º                  ³ state, when, do           ³ šbergangsbedingungen    º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º@447[ case-Anweisung   ]³ case, of                  ³ Auswahl einer Aktion    º
    º                  ³ action, elseaction        ³ ber Sprungverteiler    º
    º                  ³ endaction,                ³                         º
    º                  ³ endcase                   ³                         º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

                                    @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.436
    PL2000 SYNTAX:  ANWEISUNGEN
                                                                      @435[< WEITER >]
    Weiters stehen folgende ANWEISUNGEN zur Verfgung:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Bezeichnung      ³ Schlsselw”rter           ³ B e d e u t u n g       º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º@449[ Sprunganweisung  ]³ goto                      ³ Programmablauf„nderung  º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º@451[ access-Anweisung ]³ access                    ³ Adreázuweisung          º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º@451[ adr - Anweisung  ]³ adr()                     ³ Adresse ermitteln       º
    ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º@454[ sizeof-Anweisung ]³ sizeof()                  ³ PV-L„nge ermitteln      º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼






                                    @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.440
    PL2000 Kontrollkonstrukte:  IF - ANWEISUNG

    Schlsselw”rter IF- ANWEISUNG:

         IF ... THEN          Bedingung
         ELSE IF ... THEN     Alternativ-Bedingung
         ELSE                 Bedingung(en) nicht erfllt
         ENDIF                Ende-Kennung UNBEDINGT erforderlich


    Die 'if'-Anweisung erm”glicht es in Abh„ngigkeit der mit den Schlssel-
    w”rtern 'if'/'else if'/'else' definierten Bedingungen in verschiedene
    Programmabschnitte zu verzweigen.

    Zwischen den Bedingungen 'if' und 'else' k”nnen beliebig viele 'else if'
    Bedingungen definiert werden.
    Sobald eine 'if' oder 'else if' Bedingung erfllt war, sind alle weiteren
    'else if' Bedingungen, sowie die 'else' Bedingung, automatisch nicht
    mehr zu erfllen, also fr den aktuellen Programmzyklus hinf„llig.


    @441[ if-Anweisung ]                 @442[ loop-Anweisung ]          @435[ Kontrollkonstruke ]
.441
    PL2000 Kontrollkonstrukte:  Pseudocode IF - ANWEISUNG

    IF Bedingung  THEN         ; Schlsselwort - Abfrage - Schlsselwort
       Anweisung(en)1          ; Programmteil fr Bedingung=WAHR
    [ELSE                      ; Schlsselwort fr Alternativprogramm
       Anweisung(en)3 ]        ; Programmteil fr Bedingung=FALSCH
    ENDIF                      ; Schlsselwort fr Abfrageblock - Ende

    Verkettung mehrerer Abfragebedingungen:
    IF Bedingung  THEN         ; Schlsselwort - Abfrage - Schlsselwort
       Anweisung(en)1          ; Programmteil fr Bedingung1=WAHR
    ELSE IF Bedingung2 THEN    ; Schlsselwort fr Alternativprogramm
            Anweisung(en)2     ;
    [ELSE                      ; Schlsselwort fr Alternativprogramm
       Anweisung(en)3 ]        ; Programmteil fr Bedingung=FALSCH
    ENDIF                      ; Schlsselwort fr Abfrageblock - Ende

    Falls bei Bedingung=FALSCH keine Anweisungen auszufhren sind, entf„llt
    der Programmteil Anweisung(en)3 einschlieálich dem Schlsselwort ELSE.


    @440[ if-Anweisung ]                 @442[ loop-Anweisung ]          @435[ Kontrollkonstruke ]
.442
    PL2000 Kontrollkonstrukte:  LOOP - ANWEISUNG

    Schlsselw”rter LOOP - ANWEISUNG:
         LOOP .. TO .. DO     Loop bedingt: STARTWERT-ZUWEISUNG kleiner ENDWERT
         ENDLOOP              Ende-Kennung UNBEDINGT erforderlich
                              => Loopdurchl„ufe = (Endwert - Startwert + 1)

         LOOP .. DOWNTO .. DO Loop bedingt: STARTWERT-ZUWEISUNG gr”áer ENDWERT
         ENDLOOP              Ende-Kennung UNBEDINGT erforderlich
                              => Loopdurchl„ufe = (Startwert - Endwert + 1)

         LOOP                 BEENDEN der Loop-Schleife durch Erfllen einer
         EXITIF Bedingung     definierten AUSSTIEGSBEDINGUNG
         ENDLOOP              Ende-Kennung UNBEDINGT erforderlich
    Die 'loop'-Anweisung erm”glicht es, Anweisungen beliebig oft durchzufhren
    solange bis die Ausstiegsbedingung erfllt ist. Start- und Endwert geben
    eine feste Anzahl von Durchl„ufen vor; 'exitif' beendet die Schleife, wenn
    eine bestimmte Bedingung erfllt ist. Eine bedingte Loop-Anweisung (Start-
    und Endwert) kann ebenfalls vorzeitig mit einer Ausstiegsbedingung 'exitif'
    abgebrochen werden.

    @443[ loop-Anweisung ]              @444[ select-Anweisung ]         @435[ Kontrollkonstruke ]
.443
    PL2000 Kontrollkonstrukte:  Pseudocode LOOP - ANWEISUNG

    LOOP
        Anweisung(en)          ; Anweisungen vor der Ausstiegskontrolle
    EXITIF logischer Ausdruck  ; Ausstiegsbedingung der Loop-Schleife
        [Anweisung(en)]        ; Anweisungen - Ausstiegsbedingung nicht erfllt
    ENDLOOP

  ACHTUNG: Der logische Ausdruck nach 'exitif' ist so zu formulieren, daá er im
     Laufe der Abarbeitung des Programms logisch WAHR wird, andernfalls fhrt
     dies zu einer Zykluszeitverletzung (==> "Endlosschleife") .

    Aufbau einer BEDINGTEN loop - Anweisung:

    LOOP  Start-Zuweisung TO Endwert DO   ; (z.B: loop startval=0 to 200 do)
        Anweisung(en)          ; Anweisungen der Loop-Schleife
    [EXITIF logischer Ausdruck ; wahlweise vorzeitige Ausstiegsbedingung
        Anweisung(en)]         ;
    ENDLOOP                    ; Loop endet mit Erreichen des Endwertes 200
                               ; => Loopdurchl„ufe = (200 - 0 + 1) = 201

    @442[ loop-Anweisung ]              @444[ select-Anweisung ]         @435[ Kontrollkonstruke ]
.444
    PL2000 Kontrollkonstrukte:  SELECT - ANWEISUNG

    Schlsselw”rter SELECT - ANWEISUNG:

         SELECT               Beginn des Select-Programmteiles
            STATE ...         Markierung eines ZUSTANDBLOCKES
            WHEN ... DO       šbergangsbedingung auf NCHSTEN Zustandsblock
                NEXT ...      Definition des NCHSTEN Zustandsblockes
         ENDSELECT            Ende-Kennung UNBEDINGT erforderlich

    Mit Hilfe der 'select'-Anweisung kann in Abh„ngigkeit verschiedener
    Zust„nde (definiert durch šbergangsbedingungen) zwischen verschiedenen
    Programmteilen (Zustandsbl”cken) beliebig gewechselt werden.

    Es wird immer nur ein Zustandsblock pro Programmzyklus abgearbeitet.

    Standardm„áig wird der erste definierte Zustandsblock solange durch-
    laufen, bis mittels einer erfllten šbergangsbedingung in einen anderen
    Zustandsblock verzweigt wird.


    @445[ select-Anweisung ]             @447[ case-Anweisung ]          @435[ Kontrollkonstruke ]
.445
    PL2000 Kontrollkonstrukte:  SELECT - ANWEISUNG

    Zustandsbl”cke und šbergangsbedingungen:

         STATE manu           Markierung des ZUSTANDBLOCKES 'manu'
            Anweisungen_manu  Programmteil des Zustandsblockes 'manu'
         WHEN auto_fl=1 DO    šbergangsbedingung auf Zustandsblock 'auto'
            NEXT auto         Verzweigung auf den Zustandsblock 'auto'
         STATE auto           Markierung des ZUSTANDBLOCKES 'auto'
            :
    Jeder Zustandsblock beginnt mit 'state'. Die šbergangsbedingung wird mit
    'when' eingeleitet, und muá mit dem Schlsselwort 'next' auf einen mit
    'state' definierten Zustandsblock verzweigen.

    Der Wechsel auf einen anderen Zustandsblock erfolgt erst nach der Erfllung
    einer šbergangsbedingung. Wenn in einem Zustandsblock keine šbergangsbedin-
    gung erfllt ist, wird im n„chsten Zyklus wieder derselbe Zustandsblock ab-
    gearbeitet.

    Mittels 'goto' kann NICHT auf Zustandsbl”cke verzweigt werden !

    @446[ select-Anweisung ]             @447[ case-Anweisung ]          @435[ Kontrollkonstruke ]
.446
    PL2000 Kontrollkonstrukte:  Pseudocode SELECT - ANWEISUNG

  SELECT                         ; Schlsselwort Beginn select-Anweisung
      STATE Statename(1)         ; Markierung des 1. Zustandsblockes
            Anweisung(en)1       ; Programmteil des 1. Zustandblockes

      WHEN  Bedingung(1_1) DO    ; 1. šbergangsbedingung
            [Anweisung(en)11]    ; wahlw. Programmteil f. Bedingung(1_1)=WAHR
            NEXT Statename(x)    ; Ziel-Zustandsblock fr Bedingung(1_1)=WAHR
  [   WHEN  Bedingung(1_2) DO    ; 2. šbergangsbedingung optional
            [Anweisung(en)12]    ; wahlw. Programmteil f. Bedingung(1_2)=WAHR
            NEXT Statename(y)    ; Ziel-Zustandsblock fr Bedingung(1_2)=WAHR
          . . .                  ; beliebig viele šbergangsbedingungen
          . . .                  ; mit wahlweise Programmteilen
          . . .               ]  ; und Verzweigung auf Ziel-Zustandsbl”cken

      STATE Statename(2)         ; Markierung des 2. Zustandsblockes
        . . .                    ; beliebig viele Zustandsbl”cke

  ENDSELECT                      ; Schlsselwort Ende select-Anweisung

    @444[ select-Anweisung ]             @447[ case-Anweisung ]          @435[ Kontrollkonstruke ]
.447
    PL2000 Kontrollkonstrukte:  CASE - ANWEISUNG

    Schlsselw”rter CASE - ANWEISUNG:

         CASE ... OF             Auswertung des definierten Ausdrucks, der im-
                                 plizit in den Datentyp WORD konvertiert wird.
         ACTION value:           SELECTION ber vorzeichenlose Konstante
         ELSEACTION:             Definition einer DEFAULTAKTION; diese
                                 wird ausgefhrt, wenn der 'case-Ausdruck'
                                 auf keinen 'ACTION-value' zutrifft.
         ENDACTION               UNBEDINGTE Ende-Kennung eines Aktion-Blocks
         ENDCASE                 UNBEDINGTE Ende-Kennung des Case-Blocks


    Mit Hilfe der 'case'-Anweisung kann anhand von verschiedenen Werten oder
    Bereichen eines Ausdrucks auf verschiedene Aktionen verzweigt werden.

    Die Definition einer Aktion ber einen Wertebereich erfolgt durch zwei
    Punkte '..' (action startval..endval: ; z.B: action 10..15: ).


    @448[ case-Anweisung ]                @440[ if-Anweisung ]           @435[ Kontrollkonstruke ]
.448
    PL2000 Kontrollkonstrukte:  Beispiel CASE - ANWEISUNG

  CASE word1 OF       ; Entsprechend word1 wird zu einer Aktion verzweigt
    ACTION 22:        ;
    ACTION 25..33:    ;
       Anweisung(en)  ; Diese Anweisungen werden ausgefhrt, wenn word1 = 22
                      ; oder im Bereich von 25 bis 33 liegt.
    ENDACTION         ; Ende dieser Aktion; es muá eine weitere Aktion oder
                      ; das Schlsselwort 'endcase' folgen
    ACTION %%111..$f:  ; Bereich von 7 bis 15; es folgt eine leere Aktion
    ENDACTION         ; Leere Aktion wird abgeschlossen
    ELSEACTION:       ; diese Aktion wird ausgefhrt, wenn keine andere
       Anweisung(en)  ; Aktionsnummer zutrifft.
    ENDACTION         ; Auch die Elseaktion muá mit 'endaction' abgeschlossen
                      ; werden.
  ENDCASE             ; Abschluá des Case-Konstrukts

  ACHTUNG: Es drfen maximal 512 Aktionsnummernvorkommen, einschlieálich aller
           Zahlen der Bereiche (Begrenzung der Sprungtabelle). Im obigen Fall
           wurden 19 Zahlen verwendet.

    @447[ case-Anweisung ]                @440[ if-Anweisung ]           @435[ Kontrollkonstruke ]
.449
    PL2000 Kontrollkonstrukte:  GOTO - ANWEISUNG

    Schlsselw”rter GOTO - ANWEISUNG:

         GOTO ...             Verzweigung auf eine SPRUNGMARKE

         ...:                 Definition der SPRUNGMARKE mit DOPPELPUNKT


     Die 'goto'-Anweisung bewirkt die Umleitung des Programmflusses auf Anwei-
     sungen nach der definierten SPRUNGMARKE.
     'goto'-Anweisungen sollten m”glichst berlegt eingesetzt werden. Sie
     beschr„nken die Optimierungsm”glichkeiten des PL2000 Sprach-Compilers.

     Am Ende des SPRUNGMARKENNAMENS muá immer ein ':' stehen. Weiters gelten
     die gleichen Regeln wie fr die Zusammensetzung von PV-Namen.

     Verzweigungen in einen Block oder zwischen Bl”cke von Kontrollkonstrukten
     ('if', 'loop', 'select', ...) mssen auf alle F„lle vermieden werden.


    @450[ goto-Anweisung ]               @451[ adr-Anweisung  ]                @436[ Anweisungen ]
.450
    PL2000 Kontrollkonstrukte:  Pseudocode GOTO - ANWEISUNG

       Anweisung(en)1
    GOTO Sprungmarke              ; Verzweigung auf Sprungmarke
       Anweisung(en)2             ; die Anweisung(en)2 werden in diesem Fall NIE
                                  ; ausgefhrt, da die GOTO-Verzweigung IMMER ab
                                  ; der Sprungmarke (Anweisung(en)3) fortsetzt.

    Sprungmarke:                  ; Definition der Sprungmarke
       Anweisung(en)3


     Nach der Abarbeitung der Anweisung(en)1 wird durch die 'goto'-Anweisung der
     Programmfluá umgeleitet und bei Anweisung(en)3 fortgesetzt.







    @449[ goto-Anweisung ]               @451[ adr-Anweisung  ]                @436[ Anweisungen ]
.451
    PL2000 Anweisungen:  ACCESS/ADR - ANWEISUNG

    Schlsselw”rter ACCESS/ADR - ANWEISUNG:

         ACCESS               Adreázuweisung auf eine DYNAMISCHE VARIABLE
         ADR()                Adreáermittlung einer STATISCHEN VARIABLEN

    Die 'access'-Anweisung erm”glicht es, die mit 'adr()' ermittelte Adresse der
    STATISCHEN VARIABLE, einer DYNAMISCHEN VARIABLE zuzuweisen.

    - Eine ADRESSZUWEISUNG mittels 'access' sollte NUR EINMAL im Programm
      vorkommen. Am bersichtlichsten sind solche Zuweisungen im INIT-
      Unterprogramm.
    - Der VORTEIL bei der Verwendung von dynamischen Variablen liegt in der
      UNABHNGIGKEIT des Datentyp-Zugriffs der dynamischen Variable zu der
      eingentlichen statischen Variable.
    - Die Verwendung von dynamischen Variablen erfordert jedoch eine "saubere"
      Programmierung, da GLEICHE STATISCHE Variablen ber deren Adresse von
      VERSCHIEDENEN DYNAMISCHEN Variablen bentzt werden k”nnten. Achten Sie
      darauf, daá die eigentliche BEREICHSLNGE nicht šBERSCHRITTEN wird !!

    @452[ adr-Anweisung ]               @454[ sizeof-Anweisung ]               @436[ Anweisungen ]
.452
    PL2000 Anweisungen:  ACCESS/ADR - ANWEISUNG

    ZUSAMMENHANG STATISCHE - DYNAMISCHE Variablen:
    - DYNAMISCHE Variblen wird kein reservierter Speicherplatz zugewiesen. Erst
      nach einer erfolgten Adreázuweisung mit 'adr()' kann diese dynamische
      Variable benutzt werden.
    - Die Variable, deren Adresse mit 'adr()' zugewiesen wird, darf nicht den
      Gltigkeitsbereich dynamisch besitzen. Sie darf jeden beliebigen Datentyp
      (BYTE, LONG, Struktur, ...) besitzen.
    - Eine mit 'adr()' definierte dynamische PV kann mit 'access' auf eine ande-
      re dynamische PV zugewiesen werden, zeigt aber immer auf die statische PV.
    - Es gibt KEINE KONTROLLE der L„nge der dynamischen und der eigentlichen
      L„nge der statischen Variable! Eine BEREICHSšBERSCHREITUNG wird dadurch
      m”glich !!
    ADRESSERMITTLUNG einer statischen Variable:  ADR()
    - Als Adresse k”nnen nicht nur Anfangs-Adressen von statische PVs ange-
      geben werden, sondern es stehen mehrere M”glichkeiten zur Verfgung:
      - Normalfall - Adresse einer PV:  ADR(statPV)
      - Adresse einer PV + Offset:      ADR(statPV + 100)
                                        ADR(statPV + 2 + sizeof(statPV_2))
      - Adresse eines Elementes:        ADR(array[5]) oder ADR(struct.item_1)
    @453[ adr-Anweisung ]               @454[ sizeof-Anweisung ]               @436[ Anweisungen ]
.453
    PL2000 Anweisungen:  Pseudocode ACCESS/ADR - ANWEISUNG

    dynPV ACCESS ADR(statPV)      ; Adreázuweisung von statPV auf dynPV
    dynPV = dynPV + 1             ; mit dieser Anweisung wird die statische
                                  ; Variable statPV um 1 erh”ht

    dyn_array ACCESS ADR(statPV)  ; Adreázuweisung von statPV auf dynPV
    dyn_array[2] = $0F0F          ; das 3. ARRAY-Element von statPV wird mit
                                  ; $0F0F beschrieben (Elementgr”áe wird durch
                                  ; die VD von dyn_array bestimmt)

    dyn_struct ACCESS ADR(statPV) ; Adreázuweisung von statPV auf dynPV
    dyn_struct.item_1 = 146       ; die statische Variable statPV wird ent-
    dyn_struct.item_2 = 234       ; sprechend dem Strukur-Offset beschrieben

    - aus den angefhrten Beispielen kann man deutlich erkennen, daá eine
      MEHRFACHVERWENDUNG einer statischen Variable m”glich w„re !
    - die links neben ACCESS stehende Variable wird in der VARIABLENDEKLARATION
      AUTOMATISCH mit dem Gltigkeitsbereich DYNAMISCH vorgemerkt.


    @451[ adr-Anweisung ]               @454[ sizeof-Anweisung ]               @436[ Anweisungen ]
.454
    PL2000 Anweisungen:  SIZEOF - ANWEISUNG

    Schlsselw”rter SIZEOF - ANWEISUNG:

         SIZEOF()             Ermitteln der PV-L„nge (Speicherbedarf BIT/BYTE)

    Die 'sizeof()'-Anweisung erm”glicht es, in einem RPS-Programm die L„nge
    (Speicherbedarf) einer beliegigen PV zu ermitteln.

    - Wird die PV-L„nge eines analogen Datentyps (BYTE, LONG, Struktur, ...)
      abgefragt, liefert die Funktion die Gr”áe der PV in BYTE. Bei einem
      Funktionsaufruf SIZEOF(PV mit BIT-Datentyp) erh„lt man die Gr”áe in BIT.
    - Eine Aufw„rtskonvertierung des Datentyps der BIT/BYTE-Anzahl erfolgt
      automatisch (wie bei "Konstanten").







    @455[ sizeof-Anweisung ]             @449[ goto-Anweisung ]                @436[ Anweisungen ]
.455
    PL2000 Anweisungen:  Beispiele SIZEOF - ANWEISUNG

    PV-Deklarationen:   PV_by         (Datentyp: BYTE)
                        PV_lo         (          LONG)
                        PV_st         (          Struktur, 100-Byte L„nge)
                        bit_arr[16]   (          BIT)

    Beispiele mit den genannten PV-Deklarationen:       ³Ergebnis:
    ÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ
    ; Speicherbedarf von ANALOGEN Datentypen:           ³
    len = sizeof(PV_lo)                                 ³len = 4
    len = sizeof(PV_by) + sizeof(PV_lo) + sizeof(PV_st) ³len = 1 + 4 + 100 = 105
    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ
    ; Speicherbedarf von BIT-Datentypen:                ³
    len = sizeof(bit_arr)                               ³len = 16
    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ
    ; Achtung: Mischen von ANALOGEN                     ³
    ; und BIT-Datentypen nicht sinnvoll                 ³
    len = sizeof(PV_lo) + sizeof(bit_arr)               ³len = 4 + 16 = 20


    @454[ sizeof-Anweisung ]             @449[ goto-Anweisung ]                @436[ Anweisungen ]
.460
    PL2000 - SOURCE LEVEL DEBUGGER

    Der SOURCE LEVEL DEBUGGER ist ein HILFSMITTEL fr die FEHLERSUCHE
    (Debugging) in Programmen (fr zeilenorientierte Programmiersprachen).
    Der Debugger l„át ein Programm (Task) zeilenweise oder in einzelnen Zyklen
    ablaufen, wobei Unterbrechungspunkte (Haltepunkte/Breakpoints) gesetzt und
    die aktuellen Werte der PVs in einem eigenen Fenster (PV-Monitor) angezeigt
    werden k”nnen.


    @461[< DEBUGGER-Aufruf    >]     - AKTIVIERUNG des Debuggers fr PL2000-Task


    @564[< DEBUGGER-Bedienung >]     - BEDIENUNG des Source-Level Debuggers


    @402[< PL2000-Editor      >]     - PL2000 Syntax und Editor-Bedienung




                                                                     @002[ DEBUGGING ]
.461
    PL2000 - SOURCE LEVEL DEBUGGER

    <AKTIVIERUNG> des PL2000 Source-Level Debuggers:

    ==> - MARKIEREN Sie in der GDM das TASK-SYMBOL, fr den der Debugger aufge-
          rufen werden soll.
        - Mit der HM-Funktion <Objekt><Debugger> oder mit der Tastenkombination
          <F11> wird der Debugger aufgerufen.

    * Beim Starten des Debugvorgangs wird zuerst der DEBUGGER auf die RPS
      bertragen, danach der entsprechende DEBUG-TASK und zus„tzlich der PV-
      MONITOR aktiviert.
    * Der DEBUG-TASK wird ANHAND der eingestellten TASKPARAMETER INSTALLIERT
      und GESTARTET.

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Existiert auf der RPS ein Task mit dem gleichen Namen, der jedoch nicht  º
  º kompatibel zum DEBUG-Task ist, wird eine Meldung angezeigt, die Sie be-  º
  º st„tigen mssen. Anschlieáend wird der Task in der RPS korrigiert !!     º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @460[ Fortsetzung ]                                                    @002[ DEBUGGING ]
.490
    PL2000 Syntax:  EINBINDEN VON FUNKTIONSBL™CKEN und FUNKTIONEN

    @491[<FUNKTIONSBL™CKE>]       Zwei M”glichkeiten zum Aufruf von FUBs:

        * Vollst„ndige      ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
          Parameterliste:   º fubname(eing_1, eing_2, ...., ergebnis)º
                            ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
        * ALIAS-Aufruf:     ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
                            º aliasname FUB fubname()    º
                            ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
          Die Parameter k”nnen als Strukturelement der Aliasstruktur mit
          "aliasname.paramname"  angesprochen werden


    @492[<FUNKTIONEN/Inline-FUBs>]

        * Vollst„ndige      ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
          Parameterliste:   º ergebnis = funktion(eing_1, eing_2)    º
                            ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
          Der FUB-Ausgang wird als <RETURN>-Wert "ergebnis" bergeben.

                                    @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.491
    PL2000 Syntax:  EINBINDEN VON FUNKTIONSBL™CKEN

    Alle Ein- und Ausgangsparameter eines FUBs mssen entweder beim FUB-Aufruf
    in Klammern () oder als Parameter einer ALIAS-Struktur bergeben werden.
    FUNKTIONSBL™CKE haben KEINEN RETURNWERT als Rckgabeparameter.
    Vom Anwender k”nnen zur Zeit nur FUNKTIONSBL™CKE erstellt werden.

    - <FUB-AUFRUF> mit vollst„ndiger IN/OUT-Parameterliste:
      fubx(in1, in2,..., out_1, out_2)  ; 'fubx' ist der Name des FUB
                                        ; in1,in2, ... Eingangs-Parameter
                                        ; out_1, ..... Ausgangs-Parameter

    - <FUB ALIAS-Aufruf>:
      Die Parameter des FUBs k”nnen als Strukturkomponenten der Alias-Struktur
      mit "aliasname.paramname" angesprochen werden:
      alias_n.in1 = 1                   ; in1 ist Eingangs-Parameter von fubx
      alias_n.in2 = 0                   ; in2 ist Eingangs-Parameter von fubx
      :                                 ; 'alias_n': Name der FUB-Struktur
      alias_n  FUB fubx()               ; 'fubx' ist der Name des FUB
      ergebnis = alias_n.out1           ; out1 ist Ausgangs-Parameter von fubx

    @490[ FUB-Einbindung ]                @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.492
    PL2000 Syntax:  EINBINDEN VON FUNKTIONEN

    Alle Standard-Library FUNKTIONEN k”nnen ber den folgenden Funktions-
    Aufruf im Anwenderprogramm gentzt werden.
    FUNKTIONEN haben NUR EINEN Ausgabeparameter. Dieser wird als RETURNWERT
    von der Funktion zurckgegeben.

    - <FUNKTIONS-Aufruf> mit vollst„ndiger IN-Parameterliste:

      ergebnis = funkt(in1, in2,...) ; 'fubx' ist der Name des FUB
                                     ; in1, in2, ... Eingangs-Parameter
                                     ; ergebnis .... enth„lt nach dem Funktions-
                                                     Aufruf Ausgangs-Parameter








    @490[ FUB-Einbindung ]                @420[ PL2000-Syntax ]                  @001[ šbersicht ]
.500
    AWL - SPRACHEDITOR

    Der ANWEISUNGSLISTEN-Spracheditor dient zur ERSTELLUNG und BEARBEITUNG
    von Programmen in AWL-Programmiersprache (grundlegend nach IEC-1131).

    Die RICHTIGKEIT DER SYNTAX wird erst beim Speichern der AWL-Quelldatei
    berprft.

    @520[< SYNTAX-Beschreibung >]         - Aufbau einer AWL-Zeile
                                    - Befehlsbersicht und FUB-Einbindung
                                    - Konstanten und Prozeávariablen

    @502[< EDITOR-Bedienung    >]         - AWL Tastenkombinationen
                                    - Zeichen/Zeile einfgen, l”schen
                                    - Bereich markieren, kopieren

    @560[< AWL-Debugger        >]         - AWL Source-Level DEBUGGER

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men

                                                                     @001[ šbersicht ]
.502
    AWL EDITOR-BEDIENUNG

    Eine neu er”ffnete Datei erh„lt den Namen <sourcexx.src>, wobei "xx"
    eine fortlaufende Nummer darstellt.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄsourcexx.src [ AWL ]ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³ (* Bereich fr AWL-Syntax                                       *) ²
    ³                                                                    ²
    ³  Label:      Operator    Operand        (* Kommentar            *) ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³ (* @503[< Tastaturkommandos >]     - AWL Tastaturkommandos            *) ²
    ³ (*                           - Zeichen/Zeile einfgen, l”schen  *) ²
    ³ (*                           - Bereich markieren, kopieren      *) ²
    ³                                                                    ³
    ÀÄZeile x Spalte yÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ
      ³
      ÀÄÄÄÄÄÄÄÄ Anzeige der aktuellen CURSOR-Position

    @520[ AWL-Syntax ]                     @500[ AWL-Editor ]                    @001[ šbersicht ]
.503
    AWL EDITOR-BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Bedienung des AWL Editors kann wahlweise mit
                            TASTENKOMBINATIONEN oder MAUS-Bedienung erfolgen:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen einer angew„hlten Funktion          º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ENTER>                ³ im Einfge-Modus:                             º
  º                        ³    - Leere Zeile einfgen und Anweisung ab    º
  º                        ³      aktueller Cursor-Position in eingefgte  º
  º                        ³      Zeile verschieben.                       º
  º                        ³    - Cursor in n„chste Zeile stellen          º
  º                        ³ im šberschreibmodus:                          º
  º                        ³    - Cursor in die n„chste Zeile stellen      º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @504[ Fortsetzung ]                    @500[ AWL-Editor ]                    @001[ šbersicht ]
.504
    AWL EDITOR-BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit Hilfe der Bild-
                            laufleiste und einem Mausklick oder ber folgende
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein Zeichen nach links/rechts                 º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  º <TAB>                  ³ im Einfge-Modus: Einfgen von Leerzeichen    º
  º                        ³    bis zur n„chsten TAB-Position              º
  º                        ³ im šberschreibmodus: Cursor auf n„chste       º
  º                        ³    TAB-Position stellen                       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º ^<HOME>/ 2x<HOME>      ³ Cursor zum Beginn des Textes                  º
  º ^<END>/ 2x<END>        ³ Cursor zum Ende des Textes                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @505[ Fortsetzung ]                    @500[ AWL-Editor ]                    @001[ šbersicht ]
.505
    AWL EDITOR-BEDIENUNG

    TEXT- BEARBEITUNG    :  Der Text kann, solange die <SHIFT>-Taste gedrckt
                            bleibt, mit <HOME>/<END>/<CURSOR> markiert werden.
                            Mit Hilfe der Maus kann der gewnschte Editor-
                            Bereich direkt markiert werden.

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^<BACKSPACE>           ³ L”schen der aktuellen Zeile                   º
  º <DEL>/<BACKSPACE>      ³ Zeichen unter/vor dem Cursor l”schen          º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>                ³ Setzen der Textmarke bis Zeilenanfang         º
  º <END>                 ³ Setzen der Textmarke bis Zeilenende           º
  º <CURSOR-TASTEN>       ³ Setzen einer beliebigen Textmarke             º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <INS>                  ³ Wechselweises Umschalten zwischen             º
  º                        ³ EINFšGE- und šBERSCHREIB-Modus                º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @502[ Fortsetzung ]                    @500[ AWL-Editor ]                    @001[ šbersicht ]
.520
    AWL  SYNTAX-BESCHREIBUNG

    Bei der AWL-Programmierung wird soweit wie m”glich die Norm IEC-1131
    eingehalten.
    Unterschiede zur Norm ergeben sich jedoch aus dem globalen Konzept der
    B&R-Programmiersprachen (Variablendeklaration, FUB-Einbindung, Datentyp-
    Umwandlung).

    @525[< AWL-Zeile           >]     - AUFBAU einer AWL-Zeile:
                                  Label / Operator / Operand / Kommentar
    @528[< AWL-Befehle         >]     - šBERSICHT der AWL-Befehle (Operatoren)
                                  Laden, Speichern, Sprnge, ...
    @540[< FUB-Einbindung      >]     - Aufruf von FUBs und Funktionen

    @544[< Prozeávariablen     >]     - Deklaration von Prozeávariablen

    @546[< Konstante           >]     - Deklaration von Konstanten

    @548[< Datentyp-Umwandlung >]     - Implizite/Explizite Konvertierung


    @502[ Editor-Bedienung ]               @500[ AWL-Editor ]                    @001[ šbersicht ]
.525
    AWL - ZEILE

    Eine AWL-Zeile besteht aus VIER M™GLICHEN FELDERN, die an einer beliebigen
    Position einer Zeile stehen k”nnen:

    Kommentar  ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    Operand    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿            ³
    Operator   ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿    ³            ³
    Label      ÄÄÄÄÄÄÄ¿          ³    ³            ³
                    ÄÅÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÅÄÄÄName.awlÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
                    ³ ³          ³    ³            ³                       ³
                    ³ label:     ld   variable     (* Kommentar *)         ²
                    ³            st   variable     (* Kommentar *)         ²
                    :                                                      :
                    ÀÄZeile/SpalteÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ

    - Die FELDER mssen durch mindestens EIN LEERZEICHEN getrennt sein.
    - Die REIHENFOLGE der Felder ist UNBEDINGT EINZUHALTEN.
    - Das KOMMENTARFELD ist OPTIONAL.
    - LEERZEILEN sind erlaubt.

    @526[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.526
    AWL - ZEILE
            ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
            ³ Label       ³ Operator    ³ Operanden   ³ Kommentar   ³
            ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    <LABELFELD    >: Der LABELNAME gefolgt von einem ":" bezeichnet einen LABEL,
                     der einer Zeile vorangestellt ist. Label sind optional und
                     k”nnen auch alleine in einer Zeile stehen.

    <OPERATORFELD >: Ein OPERATOR kann entweder ein SCHLšSSELWORT (AWL-Befehl)
                     oder ein FUNKTIONSNAME sein.

    <OPERANDENFELD>: Einem Operator k”nnen EIN oder MEHRERE OPERANDEN folgen.
                     Grundoperationen k”nnen nur einen Operanden haben.

    <KOMMENTARFELD>: Kommentare sind zwischen "(*" und "*)" eingefaáte Texte,
                     die berall erlaubt sind, wo auch Leerzeichen stehen drfen
                     (auch zwischen Feldern).
                     Kommentare drfen nicht ber meherere Zeilen gehen.


                                     @520[ AWL-Syntax ]                    @001[ šbersicht ]
.528
    AWL - BEFEHLE

    AWL-Befehle sind SCHLšSSELW™RTER der ANWEISUNGSLISTEN-Programmierung
    und werden im OPERATORFELD eingegeben.

    Syntax-šberprfung und Compilierung:
    - Die AWL-SYNTAX wird erst BEIM SPEICHERN vom AWL-Parser šBERPRšFT.
      Aufgetretene Fehler und Warnungen werden im Meldungsfenster ausgegeben.
    - Ein COMPILER-Lauf fr die AWL besteht aus Parser, Code-Generator und
      Taskbuilder. Die Variablen sind der Namensverwaltung nach dem Parser-
      Lauf bekannt und k”nnen anschlieáend deklariert werden.

    Folgende MERKMALE der AWL-Programmierung sind ZU BEACHTEN:
    - In AWL steht nur EIN REGISTER => AKKU zur Verfgung (keine Adreáregister
      oder erweiterten Adressierungsarten).
    - Die ADRESSIERUNG in der AWL erfolgt ber VARIABLEN und KONSTANTEN.
    - Das aktuelle ERGEBNIS einer Anweisungszeile wird im AKKU abgelegt.
    - Die Groá- und Kleinschreibung der Schlsselw”rter ist NICHT SIGNIFIKANT.
    - FUNKTIONSBL™CKE k”nnen auf verschiedene Arten eingebunden werden.


    @531[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.529
    AWL - BEFEHLE
    AUFBAU der šbersicht:
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator º  N,C,(  ³   V/K   ³ AKKU-Zuweisung und Bedeutung                  º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    N .... Der Operand wird vor Ausfhrung der Operation NEGIERT
           z.B.: ANDN Operand --> AKKU= AKKU UND NOT Operand

    C .... Die Operation wird nur ausgefhrt, wenn das Ergebnis der vorher-
           gehenden Operation "boolsch 1" (AKKU ungleich 0) ist.
           z.B.: JMP   label  --> Springe auf Label (unbedingter Sprung)
                 JMPC  label  --> Springe auf Label, wenn der "AKKU <> 0" ist
                 JMPCN label  --> Springe auf Label, wenn der "AKKU  = 0" ist

    ( .... Der Befehl wird erst ausgefhrt, nachdem die Anweisungen innerhalb
           der Klammern ausgefhrt worden sind.
           z.B.: LD     a
                 AND (  b
                 OR     c
                 )            --> AKKU= a UND (b ODER c)
    @530[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.530
    AWL - BEFEHLE
    AUFBAU der šbersicht:
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator º  N,C,(  ³   V/K   ³ AKKU-Zuweisung und Bedeutung                  º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    V/K .. VARIABLE oder KONSTANTE (automatische Ermittlung des Datentyps)
           - die Operation wird mit dem jeweiligen Datentyp des Operanden
             bzw. AKKU durchgefhrt. (IMPLIZITE Datentypkonvertierung auf den
             Datentyp des "gr”áeren" Operanden.)
           - Bei Vergleichsoperationen ist der Ergebnisdatentyp BIT (jedoch ist
             EXPLIZITE Datentypkonvertierung erforderlich)

    Bedeutung: Zur Erkl„rung des Operations-Ablaufes wird folgende Syntax
           verwendet:
           NOT ... un„re NEGATION
           >= .... gr”áer_gleich
           <= .... keiner_gleich
           <> .... ungleich


    @531[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.531
    AWL - BEFEHLE
    ZUWEISUNGEN
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  LD      º   N     ³   V/K   ³ AKKU= Operand  (Laden)                        º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  ST      º   N     ³    V    ³ Operand= AKKU  (Speichern)                    º
º  S       º         ³  BIT V  ³ Wenn AKKU <> 0:  Operand= 1  (Setzen)         º
º  R       º         ³  BIT V  ³ Wenn AKKU <> 0:  Operand= 0  (Rcksetzen)     º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    z.B.: LD  Operand  --> AKKU = Operand
          LDN Operand  --> AKKU = NOT Operand
          STN Operand  --> Operand = NOT AKKU (AKKU invertiert in der Variable
                           speichern. Der AKKU-Inhalt wird dabei ver„ndert!)
                
    @532[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.532
    AWL - BEFEHLE
    BOOLSCHE VERKNšPFUNGEN
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  AND     º   N,(   ³   V/K   ³ AKKU= AKKU UND Operand                        º
º  &       º   N,(   ³   V/K   ³ AKKU= AKKU UND Operand                        º
º  OR      º   N,(   ³   V/K   ³ AKKU= AKKU ODER Operand                       º
º  XOR     º   N,(   ³   V/K   ³ AKKU= AKKU EXKLUSIV-ODER Operand              º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    ARITHMETISCHE VERKNšPFUNGEN
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  ADD     º    (    ³   V/K   ³ AKKU= AKKU + Operand                          º
º  SUB     º    (    ³   V/K   ³ AKKU= AKKU - Operand                          º
º  MUL     º    (    ³   V/K   ³ AKKU= AKKU * Operand                          º
º  DIV     º    (    ³   V/K   ³ AKKU= AKKU / Operand                          º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @533[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.533
    AWL - BEFEHLE
    VERGLEICHS-OPERATOREN
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  GT      º    (    ³   V/K   ³ Wenn AKKU >  Operand: AKKU= 1; sonst AKKU= 0  º
º  GE      º    (    ³   V/K   ³ Wenn AKKU >= Operand: AKKU= 1; sonst AKKU= 0  º
º  EQ      º    (    ³   V/K   ³ Wenn AKKU  = Operand: AKKU= 1; sonst AKKU= 0  º
º  LE      º    (    ³   V/K   ³ Wenn AKKU <= Operand: AKKU= 1; sonst AKKU= 0  º
º  LT      º    (    ³   V/K   ³ Wenn AKKU <  Operand: AKKU= 1; sonst AKKU= 0  º
º  NE      º    (    ³   V/K   ³ Wenn AKKU <> Operand: AKKU= 1; sonst AKKU= 0  º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    SPRUNGANWEISUNGEN
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  JMP     º         ³  LABEL  ³ Unbedingter Sprung auf Label                  º
º  JMPC    º   C     ³  LABEL  ³ Springe wenn AKKU <> 0 auf Label              º
º  JMPCN   º   CN    ³  LABEL  ³ Springe wenn AKKU  = 0 auf Label              º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @534[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.534
    AWL - BEFEHLE
    SONSTIGE ANWEISUNGEN
ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Operator ºModifiers³ Operand ³ Bedeutung                                     º
ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º  CAL     º         ³  NAME   ³ Aufruf des FUBs 'NAME'                        º
ÇÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º  )       º         ³         ³ Aufl”sung Klammer-Operation von "(" bis ")"   º
ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼












    @529[ Fortsetzung ]                    @520[ AWL-Syntax ]                    @001[ šbersicht ]
.540
    AWL - SYNTAX:  FUNKTIONSBL™CKE UND FUNKTIONEN

    @541[<FUNKTIONSBL™CKE>]       Zwei M”glichkeiten zum Aufruf von FUBs:

        * Vollst„ndige      ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
          Parameterliste:   º CAL  fubname ( param1, param2, .....)  º
                            ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
        * ALIAS-Aufruf:     ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
                            º aliasname FUB fubname()    º
                            ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
          Die Parameter k”nnen als Strukturelement der Aliasstruktur mit
          "aliasname.paramname"  angesprochen werden

    @542[<FUNKTIONEN/Inline-FUBs>]

        * Der ERSTE PARAMETER ist im AKKU, und wird deshalb nicht angefhrt.
          Aufruf durch:     ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
                            º funktionsname parmeter2, parameter3, ... º
                            ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
          Nach dem Aufruf befindet sich im AKKU der Returnwert der Funktion.

                                     @520[ AWL-Syntax ]                    @001[ šbersicht ]
.541
    AWL - SYNTAX:  EINBINDEN von FUNKTIONSBL™CKEN

    Alle FUB Ein- und Ausgangsparameter mssen entweder beim Aufruf dem FUB oder
    als Parameter einer ALIAS-Struktur bergeben werden.
    Ein FUNKTIONSBLOCK-AUFRUF „ndert den AKKU nicht (kein RETURNWERT) !

    - <FUB-AUFRUF> mit vollst„ndiger IN/OUT-Parameterliste:
       CAL  fubx(in1, in2,..., out_1)   (* 'fubx' ist der Name des FUBs;    *)
                                        (* in1,in2,.. Inparameter von fubx   *)
                                        (* out_1,.... Outparameter von fubx  *)
    - <FUB ALIAS-Aufruf>:
       Die Parameter des FUBs k”nnen als Strukturkomponenten der Alias-Struktur
       mit "aliasname.paramname" angesprochen werden
       LD       1
       ST       alias_n.in1             (* in1 ist Inparameter von fubx      *)
       LD       0
       ST       alias_n.in2             (* in2 ist Inparameter von fubx      *)
       :                                (* 'alias_n': Name der FUB-Struktur  *)
       alias_n  FUB fubx()              (* 'fubx' ist der Name des FUB;      *)
       LD       alias_n.out1            (* out1 ist Outparameter von fubx    *)

    @540[ FUB-Einbindung ]                 @520[ AWL-Syntax ]                    @001[ šbersicht ]
.542
    AWL - SYNTAX:  EINBINDEN von FUNKTIONEN

    Alle Standard-Library FUNKTIONEN k”nnen ber den folgenden Funktions-
    Aufruf im Anwenderprogramm gentzt werden.
    FUNKTIONEN haben NUR EINEN Ausgabeparameter, und den bergeben sie als
    RETURNWERT im AKKU zurck.

    <FUNKTIONEN / Inline-FUBs>:

     - Beim Aufruf von FUNKTIONEN wird der AKKU als ERSTER PARAMETER bergeben.
       Optionale weitere Parameter werden OHNE KLAMMER durch KOMMAS getrennt
       eingegeben. Nach dem Aufruf befindet sich das ERGEBNIS im AKKU.

       LD       pv1               (* 1. IN-Parameter im AKKU               *)
       funct_x  pv2, pv3, pv4
       ST       ergebnis          (* AKKU als Ergebnis speichern           *)


       Diese drei Zeilen wrde man in PL2000-Syntax folgendermaáen schreiben:
       ergebnis = funct_x(pv1, pv2, pv3, pv4)

    @540[ FUB-Einbindung ]                 @520[ AWL-Syntax ]                    @001[ šbersicht ]
.544
    AWL - SYNTAX:  PROZESSVARIABLEN-NAMEN

    Als PV-Namen werden in der AWL-Syntax SYMBOLISCHE NAMEN verwendet, die nach
    den folgenden Regeln zusammengesetzt sein drfen:
    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  Buchstaben          ³  Zeichen             ³  Ziffern              º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º 'A'bis'Z', 'a'bis'z' ³       '_', '$'       ³       '0'bis'9'       º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

     - Alle Anwender-PVs MšSSEN mit einem BUCHSTABEN beginnen !
       (Ausnahme: nur bereitgestellte Systemvariablen beginnen mit einem "_")
     - Die Groá-/Kleinschreibung eines PV-Namens ist SIGNIFIKANT.
     - Der PV-Name muá aus einer DURCHGEHENDEN ZEICHENKETTE bestehen.
     - Die PV-Namensl„nge ist nicht beschr„nkt; SIGNIFIKANT sind 10 ZEICHEN.
     - Die DEFINITION erfolgt anhand des Namens in der VARIABLENDEKLARATION.

     - PV-Namen drfen NICHT wie ein SCHLšSSELWORT der AWL-Syntax heiáen.
     - Die Trennung zwischen einem PV-Namen und einem AWL-Operator erfolgt
       durch mindestens 1 Leerzeichen (" ").

                                     @520[ AWL-Syntax ]                    @001[ šbersicht ]
.546
    AWL - SYNTAX:  DEKLARATION VON KONSTANTEN

    Eine Konstante ist ein Wert, der sich ber die gesamte Laufzeit des
    Programms nicht „ndert:
    ÉÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  SYM  ³ KONSTANTE              ³  Beispiele                         º
    ÌÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º  zz   ³ DEZIMALE Konstante     ³  [+|-]zzz                          º
    º       ³                        ³  23, -13876, ...                   º
    º  zz.  ³ FLOAT Konstante        ³  [+|-]zzz.[zzz] [E|e [+|-]zz]      º
    º       ³                        ³  100. ,-47.23 ,+6.17e-01 ,...      º
    º  %%zz ³ BINRE Konstante       ³  %%11011110, ...                   º
    º  $zz  ³ HEXADEZIMALE Konstante ³  $9, $a3, $FF, ...                 º
    º "sss" ³ STRING Konstante       ³  "a b c d", "String-Konstante",... º
    ÈÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    - Eine KONSTANTE wird immer als KLEINSTM™GLICHER DATENTYP interpretiert.
    - Bin„r- und Hex-Konstanten sind immer UNSIGNED ansonsten optional [+|-].
    - STRINGKONSTANTEN drfen bis zu 255 ZEICHEN lang sein, und bestehen aus
      einer Folge von darstellbaren Zeichen einschlieálich Leerzeichen, die
      zwischen Anfhrungszeichen gesetzt werden.

                                     @520[ AWL-Syntax ]                    @001[ šbersicht ]
.548
    AWL - SYNTAX:  ANALOGE UND DIGITALE DATENTYPEN
    DIGITALE DATENTYPEN:  Der Standardtyp fr ein digitales I/O ist das BIT.
     ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
     º Datentyp º Bitbereich ³ Minimalwert   Maximalwert ³ Speicherbedarf º
     ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
     º  BIT     º   1 Bit    ³      0      ³      1      ³  niedrigster   º
     ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    ANALOGE DATENTYPEN :  šbersicht ber die analogen Datentypen:
     ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
     º          º            ³    Darstellbare Zahlen    ³     H”he       º
     º Datentyp º Bitbereich ³ Minimalwert   Maximalwert ³ Speicherbedarf º
     ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
     º  BYTE    º   8 Bit    ³      0      ³     255     ³  niedrigster   º
     º  WORD    º  16 Bit    ³      0      ³    65535    ³  mittlerer     º
     º  LONG    º  32 Bit    ³      0      ³ 4294967295  ³  h”chster      º
     º  INT8    º   7 Bit+VZ ³    -128     ³    +127     ³  niedrigster   º
     º  INT16   º  15 Bit+VZ ³   -32768    ³   +32767    ³  mittlerer     º
     º  INT32   º  31 Bit+VZ ³ -2147483648 ³ +2147483647 ³  h”chster      º
     º  FLOAT   º  32 Bit    ³1.18E-38(abs)³3.4 E+38(abs)³  h”chster      º
     ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @549[ Weitere Datentypen ]             @520[ AWL-Syntax ]                    @001[ šbersicht ]
.549
    AWL - SYNTAX:  KOMPLEXE DATENTYPEN

    <FELD (Array)>:          Feldname[Elementindex]

      Ein Feld besteht aus einer definierten Anzahl von Elementen des GLEICHEN
      DATENTYPs. Der Datentyp der Elemente eines Feldes kann jeder Datentyp
      einschlieálich einer Struktur sein.
      Einzelne Elemente eines Feldes werden eindeutig durch den FELDNAMEN und
      den ELEMENTINDEX [in Eckiger Klammer] adressiert und angesprochen.


    <STRUKTUR>    :          Strukturname.Komponentenname

      Eine Struktur ist eine Ansammlung von Elementen. Die Elemente k”nnen
      BELIEBIGE ANALOGE DATENTYPEN, FELDER oder wiederum STRUKTUREN sein.
      Ein einzelnes Element einer Struktur wird durch den gemeinsamen STRUKTUR-
      NAMEN (Variablennamen) und den KOMPONENTENNAMEN eindeutig bestimmt. Um ein
      einzelnes Element zu adressieren, geben Sie Struktur- und Komponentennamen
      getrennt durch einen Punkt '.' ein.
      (z.B. auch: Strukturname.Feldname[Elementindex])

    @550[ Datentypumwandlung ]             @520[ AWL-Syntax ]                    @001[ šbersicht ]
.550
    AWL - SYNTAX:  IMPLIZITE DATENTYPKONVERTIERUNG
    Die REGELN der DATENTYPKONVERTIERUNG:
     ÚÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³Datentyp³  BIT   BYTE   WORD   LONG   INT8   INT16   INT32   FLOAT³
     ÃÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
     ³ BIT    ³          *      *      *      *      *       *       -  ³
     ³ BYTE   ³   *           WORD   LONG   BYTE   INT16   INT32     -  ³
     ³ WORD   ³   *    WORD          LONG   WORD   WORD    INT32     -  ³
     ³ LONG   ³   *    LONG   LONG          LONG   LONG    LONG      -  ³
     ³ INT8   ³   *    BYTE   WORD   LONG          INT16   INT32     -  ³
     ³ INT16  ³   *    INT16  WORD   LONG   INT16          INT32     -  ³
     ³ INT32  ³   *    INT32  INT32  LONG   INT32  INT32             -  ³
     ³ FLOAT  ³   -      -      -      -      -      -       -          ³
     ÀÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
       */- ...... je nach Operation explizite Konvertierung erforderlich

  Treffen bei einer LOGISCHEN oder ARITHMETISCHEN OPERATION unterschiedliche
  Datentypen zusammen, so wird vom Compiler eine AUTOMATISCHE (implizite)
  DATENTYPUMWANDLUNG durchgefhrt. Bei einer ZUWEISUNG wird eine implizite
  Konvertierung nur dann durchgefhrt, wenn die linke Seite der Zuweisung einen
  h”heren Datentyp als den h”chsten der rechten Seite aufweist.
    @551[ Datentypumwandlung ]             @520[ AWL-Syntax ]                    @001[ šbersicht ]
.551
    AWL - SYNTAX:  EXPLIZITE DATENTYPKONVERTIERUNG
    ANWEISUNGEN fr EXPLIZITE DATENTYPKONVERTIERUNG
     ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
     ³ ZIEL-DATENTYP ³ QUELL-DATENTYPEN: qdata ³ QUELL-DATENTYP: fdata  ³
     ³               ³ (BIT/BYTE/WORD/LONG     ³ (FLOAT-Variable)       ³
     ³               ³  INT8/INT16/INT32)      ³                        ³
     ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
     ³ BIT           ³ Funktion BIT            ³ Funktion F_TO_L      * ³
     ³ BYTE          ³ Funktion BYTE           ³ Funktion F_TO_L      * ³
     ³ WORD          ³ Funktion WORD           ³ Funktion F_TO_L      * ³
     ³ LONG          ³ Funktion LONG           ³ Funktion F_TO_L        ³
     ³ INT8          ³ Funktion INT8           ³ Funktion F_TO_I32    * ³
     ³ INT16         ³ Funktion INT16          ³ Funktion F_TO_I32    * ³
     ³ INT32         ³ Funktion INT32          ³ Funktion F_TO_I32      ³
     ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
     ³ FLOAT         ³ Funktion L_TO_F  oder   ³                        ³
     ³               ³ Funktion I32_TO_F    ** ³                        ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
        * ... weitere Umwandlung in Ziel-Datentyp erforderlich
       ** ... Quell-Datentypen ungleich LONG oder INT32 mssen zuvor in
              LONG- oder INT32-Datentypen umgewandelt werden.
    @552[ Beispiel ]                       @520[ AWL-Syntax ]                    @001[ šbersicht ]
.552
    AWL - SYNTAX:  EXPLIZITE DATENTYPKONVERTIERUNG
    BEISPIEL fr Explizite Datentypkonvertierung:

    - Umwandeln einer WORD-Variable in BYTE-Variable:

       LD       word_pv1          (* Laden einer WORD-Variable             *)
       BYTE                       (* Aufruf des Inline-Fub BYTE()          *)
       ST       byte_pv1          (* Speichern in BYTE-Variable            *)


    - Umwandeln einer FLOAT-Variable in WORD-Variable:

       LD       float_pv          (* Laden einer FLOAT-Variable            *)
       F_TO_L                     (* Aufruf des Inline-Fub F_TO_L()        *)
       WORD                       (* Aufruf des Inline-Fub WORD()          *)
       ST       word_pv1          (* Speichern in WORD-Variable            *)





    @548[ Einfache Datentypen ]            @520[ AWL-Syntax ]                    @001[ šbersicht ]
.560
    AWL - SOURCE LEVEL DEBUGGER

    Der SOURCE LEVEL DEBUGGER ist ein HILFSMITTEL fr die FEHLERSUCHE
    (Debugging) in Programmen (fr zeilenorientierte Programmiersprachen).
    Der Debugger l„át ein Programm (Task) zeilenweise oder in einzelnen Zyklen
    ablaufen, wobei Unterbrechungspunkte (Haltepunkte/Breakpoints) gesetzt und
    die aktuellen Werte der PVs in einem eigenen Fenster (PV-Monitor) angezeigt
    werden k”nnen.


    @561[< DEBUGGER-Aufruf    >]     - AKTIVIERUNG des Debuggers fr AWL-Task


    @564[< DEBUGGER-Bedienung >]     - BEDIENUNG des Source-Level Debuggers


    @502[< AWL-Editor         >]     - AWL Syntax und Editor-Bedienung




                                                                     @002[ DEBUGGING ]
.561
    AWL - SOURCE LEVEL DEBUGGER

    <AKTIVIERUNG> des AWL Source-Level Debuggers:

    ==> - MARKIEREN Sie in der GDM das TASK-SYMBOL, fr den der Debugger aufge-
          rufen werden soll.
        - Mit der HM-Funktion <Objekt><Debugger> oder mit der Tastenkombination
          <F11> wird der Debugger aufgerufen.

    * Beim Starten des Debugvorgangs wird zuerst der DEBUGGER auf die RPS
      bertragen, danach der entsprechende DEBUG-TASK und zus„tzlich der PV-
      MONITOR aktiviert.
    * Der DEBUG-TASK wird ANHAND der eingestellten TASKPARAMETER INSTALLIERT
      und GESTARTET.

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Existiert auf der RPS ein Task mit dem gleichen Namen, der jedoch nicht  º
  º kompatibel zum DEBUG-Task ist, wird eine Meldung angezeigt, die Sie be-  º
  º st„tigen mssen. Anschlieáend wird der Task in der RPS korrigiert !!     º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @560[ Fortsetzung ]                                                    @002[ DEBUGGING ]
.564
    SOURCE LEVEL DEBUGGER

    Bei der AKTIVIERUNG des SOURCE-LEVEL-DEBUGGERS wird automatisch der
    PV-MONITOR mit aktiviert:

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿   ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                              ²   ³                              ²
    ³   S O U R C E - L E V E L    ²   ³  P V - M O N I T O R         ²
    ³       D E B U G G E R        ²   ³                              ²
    ³                              ²   ³                              ²
    ³                              ²   ³                              ²
    ³       @565[<  Bedienung  >]        ²   ³        @850[<  Bedienung  >]       ²
    ³                              ²   ³                              ²
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ4ÄÙ



  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Der PL2000- und AWL- Source-Level Debugger haben die gleiche OBERFLCHE! º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @460[ PL2000-Debugger ]               @560[ AWL-Debugger ]                   @002[ DEBUGGING ]
.565
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    Der NAME des TASK, fr den der Debugger aufgerufen wurde, wird im Fenster-
    rahmen angezeigt.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄtaskname.SRCÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                     ³
    ³ Die BEDIENUNG des Debuggers wird ber Funktionen der Fuázeile       ²
    ³ und funktionelle Tastenkombinationen durchgefhrt:                  ²
    ³                                                                     ²
    ³ @570[< Fuázeilenmen     >]     - Optimal fr TASTATUR-Bedienung          ²
    ³                                                                     ²
    ³ @566[< Hotkey-Tasten     >]     - SONDER-TASTENBELEGUNG des Debuggers     ²
    ³ @567[< Tastaturkommandos >]     - Allgemeine Editor-Bedienung             ²
    ³                                                                     ³
    ÀÄZ: x  S: y  STOP Input OutputÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ
      ÀÄÄÄÄ Anzeige der Zeilen- und Spaltenposition und des TASK-Zustandes

    - Das Zeichen '>' am linken Rand des Debugger-Fensters bezeichnet
      die Zeile, die als n„chstes ausgefhrt werden wird (Codepointer).

                                @564[ Source-Level-Debugger ]              @002[ DEBUGGING ]
.566
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    HOTKEY-TASTEN        :  AUSFšHREN einer FUNKTION anhand von SONDERTASTEN:


  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Hotkey                 ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <g>                    ³ ZEILENNUMMER direkt anw„hlen                  º
  º                        ³ (entspricht Funktion <Zeile><Zeile>)          º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼










    @567[ Fortsetzung ]                 @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.567
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein Zeichen nach links/rechts                 º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º ^<HOME>/ 2x<HOME>      ³ Cursor auf Beginn des Debug-Tasks             º
  º ^<END>/ 2x<END>        ³ Cursor ans Ende des Debug-Tasks               º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼




    @568[ Fortsetzung ]                 @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.568
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Debugger-Bedienung kann wahlweise mittels
                            TASTENKOMBINATIONEN oder MAUS-Bedienung erfolgen:


  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^F3                    ³ Aufrufen eines Symbol-sensitiven Popup-Mens  º
  º                        ³ mit DEBUGGER-Funktionen                       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼






                                  @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.570
    SOURCE LEVEL DEBUGGER - FUSSZEILENMENš
                                                                      @566[< WEITER >]
ÉÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÑÍÍÍÍÍÍÑÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º@575[1 Start/Stop]³@575[2 Zyklus]³@575[3 Step]³@576[4 Halt]³@577[5 I/O ]³@576[6 L”schen]³7 zum BZ³                º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÏÍÍÍÍÍÍÏÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

   <F1> <Start/Stop>:  STARTEN/STOPPEN des Debug-Tasks
   <F2> <Zyklus>    :  ZYKLUSWEISES Abarbeiten des Debug-Tasks
   <F3> <Step>      :  ZEILENWEISES Abarbeiten des Debug-Tasks
   <F4> <Halt>      :  SETZEN/L™SCHEN eines HALTEPUNKTES
   <F5> <I/O>       :  EIN-/AUSSCHALTEN der I/O-KOPIERFUNKTIONEN
   <F6> <L”schen>   :  ALLE HALTEPUNKTE des Debug-Tasks L™SCHEN
   <F7> <zum BZ>    :  CURSOR in gekennzeichnete Codepointer Zeile ">" stellen


    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick angew„hlt werden.




                                  @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.575
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    Funktionen zur STEUERUNG der DEBUG-TASK:

    <Start> :  Das Programm wird gestartet und l„uft bis zum n„chsten
               Haltepunkt (Breakpoint), oder bis die Stop-Taste gedrckt wird.

    <Stop>  :  Ein laufendes Programm wird sofort angehalten.

    <Step>  :  Eine Zeile des Programms wird ausgefhrt. Anschlieáend werden
               die Variablen neu angezeigt.

    <Zyklus>:  Das Programm wird EINMAL BIS ZUM ENDE ausgefhrt.


    - Das Zeichen '>' am linken Rand des Debugger-Fensters bezeichnet
      die Zeile, die als n„chstes ausgefhrt werden wird (Codepointer).

    - Am unteren Rand des Debugger-Fensters ist ersichtlich, ob das
      Programm gerade l„uft (RUN) oder steht (STOP).

    @570[ Fuázeilen-Men ]              @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.576
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    Befehle zum Einrichten von HALTEPUNKTEN:

    <Halt>   :  SETZT oder L™SCHT einen BREAKPOINT an der Stelle im Programm,
                an der der Cursor gerade steht.

    <L”schen>:  L™SCHT ALLE Breakpoints.


    - BREAKPOINTS werden durch das Zeichen '=' am linken Rand des Debugger-
      Fensters markiert.
    - Wurde der Task an eiem BREAKPOINT angehalten, wird die AKTUELLE CODEZEILE
      mit '>>' gekennzeichnet.







    @570[ Fuázeilen-Men ]              @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.577
    SOURCE LEVEL DEBUGGER - BEDIENUNG

    Der Debug-Task kann mittels den folgenden KOPIER-OPTIONEN V™LLIG von der
    RPS-Hardware (von den tats„chlichen I/O-Zust„nden) ENTKOPPELT werden !!

    <Input>  :  Schaltet das AUTOMATISCHE KOPIEREN der Input-Daten am
                BEGINN JEDES ZYKLUS EIN bzw. AUS.
    <Output> :  Schaltet das AUTOMATISCHE KOPIEREN der Output-Daten am
                ENDE JEDES ZYKLUS EIN bzw. AUS.
    <Manuell>:  (Diese Funktion ist zur Zeit noch nicht realisiert.)


    - Im Fenster-Rahmen des Debugger-Fensters wird der aktuelle ZUSTAND der
      KOPIER-OPTIONEN angezeigt:
      :                                              :
      ÀÄZ: x  S: y  STOP Input OutputÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... INPUT- und OUTPUT-
      ÀÄZ: x  S: y  STOP InputÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... NUR die INPUT-DATEN
      ÀÄZ: x  S: y  STOP OutputÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... NUR die OUTPUT-DATEN
                                                           werden kopiert


    @570[ Fuázeilen-Men ]              @565[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.600
    KONTAKTPLAN  -  KOP-EDITOR

    Im KONTAKTPLAN-Editor k”nnen Programme mit Hilfe einer grafischen Program-
    miersprache (KOP Symbole) erstellt werden.
    Diese Art der Programmierung ist vergleichbar mit dem Zeichnen eines Strom-
    laufplanes jedoch mit erweiterten Funktionalit„ten (Einbinden von Funktions-
    bl”cken oder die automatische šberprfung von KOP-Konventionen beim Setzen
    von Symbolen).


    @630[< SYNTAX-Beschreibung >]         - Konventionen
                                    - KOP Kontakte

    @602[< EDITOR-Bedienung    >]         - KOP Fuázeilenmen, Popup- Mens
                                    - KOP Tastaturkommandos

    @660[< KOP-Monitor         >]         - KOP-Monitor

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men

                                                                     @001[ šbersicht ]
.602
    K O P  -  BEDIENUNG

    Eine neu erstellte Datei erh„lt den Namen <sourcexx.src>, wobei "xx"
    eine fortlaufende Nummer darstellt.
    Die Bedienung des KOP-Editors wird ber Funktionen der Fuázeile,
    der Popup-Mens und funktionellen Tastenkombinationen durchgefhrt.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄsourcexx.src [ KOP ]ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³ Bedienung mittels TASTENKOMBINATIONEN oder ber MAUS:              ²
    ³                                                                    ²
    ³ @620[< KOP - Popup Mens       >]     - Symbol-Sensitive Funktionen      ²
    ³                                   zum EDITIEREN eines KOP          ²
    ³ @610[< KOP - Fuázeilenmen     >]     - NEUEINGABEN von Symbolen         ²
    ³                                 - Eingeben von KOMMENTAR           ²
    ³ @606[< KOP - Hotkey-Tasten     >]     - Sonder-Tastenbelegung            ²
    ³                                   fr die Editor-Bedienung         ²
    ³ @603[< KOP - Tastaturkommandos >]     - Weitere Tastenkombinationen      ²
    ³                                   fr die Editor-Bedienung         ³
    ÀÄZeile x Spalte yÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ

    @630[ KOP-Syntax ]                     @600[ KOP-Editor ]                    @001[ šbersicht ]
.603
    K O P  -  BEDIENUNG

    KOP- EDITIERUNG      :  Mit Hilfe der Maus kann der gewnschte Editor-
                            Bereich direkt markiert werden.
                            Tastenkombinationen fr KOP-Editierung:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <DEL>                  ³ L”schen des angew„hlten KOP-Symbols           º
  º <INS>                  ³ Setzen eines neuen Kontaktes oder einer Linie º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <SPACE>                ³ Aktuelles SYMBOL wird GEL™SCHT oder auf       º
  º                        ³ einem leeren Feld eine LINIE GEZEICHNET       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <CURSOR-TASTEN>       ³ Setzen der Textmarke auch ber mehrere KOP-   º
  º                        ³ Symbole (Kontakte, Linien, FUBs, ...)         º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼



    @604[ Fortsetzung ]                 @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.604
    K O P  -  BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit der Bildlauf-
                            leiste und einem Mausklick oder mit folgenden
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein KOP-Raster nach links/rechts              º
  º <UP>/<DOWN>            ³ Einen KOP-Raster aufw„rts/abw„rts             º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º 2x<HOME>               ³ Cursor auf KOP-Anfang                         º
  º 2x<END>                ³ Cursor auf KOP-Ende                           º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼




    @605[ Fortsetzung ]                 @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.605
    K O P  -  BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Bedienung des KOP Editors kann wahlweise
                            mittels TASTENKOMBINATIONEN oder MAUS-Bedienung
                            erfolgen:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^F3                    ³ Aufrufen eines Symbol-sensitiven Popup-Mens  º
  º                        ³ mit KOP-Funktionen                            º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ENTER>                ³ Anfang der folgenden KOP-Rasterzeile wird zum º
  º                        ³ aktuellen KOP-Raster                          º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼



                                  @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.606
    K O P  -  BEDIENUNG

    HOTKEY: šber BUCHSTABEN k”nnen FOLGENDE FUNKTIONEN aktiviert werden (bei
            Bet„tigung einer 'funktionslosen' Taste erfolgt ein Warnton):

  ÉÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Hotkey ³ Beschreibung                                                  º
  ÌÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <n>    ³ Der DIGITALE Kontakt, auf dessen Feld der Cursor steht,       º
  º        ³ wird NEGIERT.                                                 º
  ÇÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <->    ³ Der angew„hlte Kontakt, kann in alle anderen Kontaktarten     º
  º        ³ verwandelt werden:                                            º
  º        ³ Wechseln: *  Ä´ ÃÄ, Ä´/ÃÄ, Ä´+ÃÄ, Ä´-ÃÄ, Ä´&ÃÄ                º
  º        ³           *  ÄÄ( ), ÄÄ(/), ÄÄ(+), ÄÄ(-), ÄÄ(&), ÄÄ(S), ÄÄ(R)  º
  ÈÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼





    @603[ Tastenkombinationen ]         @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.610
    K O P  -  FUSSZEILENMENš

    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick ausgew„hlt werden:

ÉÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍ»
º@611[1 Kontakt]³@611[2 Funktionen]³@612[3 Kommentar]³4 Zeile+³5 Zeile-³@612[6³links]³@612[7rechts³]³        º
ÈÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍ¼

   <F1> <Kontakt>   :  SETZEN eines KOP-Kontaktes
   <F2> <Funktionen>:  SETZEN eines FUNKTIONSBLOCKES
   <F3> <Kommentar> :  Eingeben oder Editieren eines KOMMENTAR-FELDES
   <F4> <Zeile+>    :  EINFšGEN einer NEUEN Zeile: KOP-Symbole werden mitver-
                                schoben. Datenbaum-Abschnitte werden erweitert.
   <F5> <Zeile->    :  L™SCHEN  der aktuellen Zeile: KOP-Symbole blockieren
                                die Ausfhrung. Datenbaum-Abschnitte werden mit
                                der Zeile mitgel”scht.
   <F6> <³ links>   :  ZEICHNEN einer senkrechten Linie LINKS von akt. Position
   <F7> <rechts ³>  :  ZEICHNEN einer senkrechten Linie RECHTS von akt. Position


    @606[ Hotkey-Tasten ]               @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.611
    K O P  -  FUSSZEILENMENš FUNKTIONEN

    <Kontakt>:     SETZEN EINES KOP-KONTAKTES

       In einem Auswahlfenster werden Ihnen alle zur Verfgung stehenden Kon-
       takte angeobten. W„hlen Sie das gewnschte Symbol mit den Cursor-Tasten
       oder der Maus aus. Sie k”nnen Kontakte auch ohne Auswahlfenster durch
       bestimmte Tasten einfgen (siehe @640[KOP Symbole]). Beim Einfgen eines Kon-
       taktes erscheint ein Fenster, in dem Sie entweder einen konstanten Wert
       oder einen Variablennamen (max. 10 Zeichen lang) fr den Kontakt ein-
       geben. Mit <Enter> best„tigen Sie Ihre Eingabe. Der Kontakt wird an der
       Postion des Cursors eingefgt (auch auf Feldern, die Linien enthalten).

    <Funktionen>:  SETZEN EINES FUNKTIONSBLOCKES

       In einem Auswahlfenster werden alle in der Datenbank vorhandenen Funk-
       tionsbl”cke angezeigt. W„hlen Sie den gewnschten FUB aus. Der FUB wird,
       wenn dies m”glich ist, immer so in den KOP eingefgt, daá die linke 
       obere Ecke des FUBs mit der aktuellen Cursor-Position identisch ist.


                                  @610[ KOP-Fuázeilenmen ]                @001[ šbersicht ]
.612
    K O P  -  FUSSZEILENMENš FUNKTIONEN

    <Kommentar>:   EINGEBEN ODER EDITIEREN EINES KOMMENTAR-FELDES

       Entsprechend dem zur Verfgung stehenden Platz erscheint ein Fenster, in
       dem der Kommentar eingegeben werden kann. Ein Kommentar wird gel”scht,
       indem er markiert und anschlieáend die <Del> Taste gedrckt wird.


    <³ links>/<rechts ³>:  ZEICHNEN EINER SENKRECHTEN LINIE

       Zeichnen einer senkrechten Linie nach oben links/rechts vom Cursor bis
       zum n„chsten 'Hindernis'. Wenn der Cursor auf einer Linie oder einem
       Kontakt steht, wird links/rechts vom Cursor senkrecht nach oben eine 
       Verbindung bis zum n„chsten Kontakt oder zur n„chsten Linie hergestellt.
       Ist dies nicht m”glich, wird der Befehl ignoriert. Wenn schon eine
       Linie vorhanden ist, wird sie gel”scht.




                                  @610[ KOP-Fuázeilenmen ]                @001[ šbersicht ]
.620
    K O P  -  POPUP MENšS

    Mit Hilfe der Popup-Mens werden SYMBOL-SPEZIFISCHE FUNKTIONEN (Symbole
    editieren, Aliasname, ...) effizient untersttzt.
    Folgende Popups stehen zur Verfgung:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º  Cursor auf:       º                                 º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º  KONTAKT           º        @621[  Symbol-Popup  ]         º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º  LINIE             º        @622[  Symbol-Popup  ]         º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º  FUNKTIONSBLOCK    º        @623[  Symbol-Popup  ]         º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    Aktivierung der POPUP-MENšS ber:     Ausfhren einer POPUP-Funktion mit
    - DOPPELKLICK der MAUS                - MAUSKLICK
    - TASTENKOMBINATION: ^F3              - CURSOR-TASTEN und <ENTER>
                                          - ANFANGSBUCHSTABEN

                                  @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.621
    K O P  -  POPUP MENš

    Cursor auf :  KONTAKT                                 @622[<   n„chstes Popup   >]

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@625[ Name                   ]³ ... Namen des Kontaktes „ndern
    ³@625[ Einfgen               ]³ ... Neuen Kontakt oder Linie EINFšGEN
    ³@625[ L”schen                ]³ ... Angew„hltes Symbol L™SCHEN
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ









    Die Bezeichnung "Symbol" umfaát alle Arten von Konakten und Linien.


    @620[ KOP-Popups ]                  @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.622
    K O P  -  POPUP MENš

    Cursor auf :  LINIE                                   @623[<   n„chstes Popup   >]

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Name                   ³
    ³@625[ Einfgen               ]³ ... Neuen Kontakt oder Linie EINFšGEN
    ³@625[ L”schen                ]³ ... Angew„hltes Symbol L™SCHEN
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ









    Die Bezeichnung "Symbol" umfaát alle Arten von Konakten und Linien.


    @620[ KOP-Popups ]                  @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.623
    K O P  -  POPUP MENš

    Cursor auf :  FUNKTIONSBLOCK                          @621[<   n„chstes Popup   >]

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@627[ Aliasname              ]³ ... Vergeben eines ALIASNAMEN fr FUB
    ³@627[ L”schen                ]³ ... Angew„hlten Funktionsblock L™SCHEN
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ









    Dieses Popup kann nur auf der LINKEN OBEREN ECKE eines Funktionsblockes
    aufgerufen werden.


    @620[ KOP-Popups ]                  @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.625
    K O P  -  POPUP FUNKTIONEN

    <Name>:

       Mit Hilfe dieser Funktion kann ein Kontaktname nachtr„glich DEFINIERT
       oder GENDERT werden.

    <Einfgen>:

       Nach dem Anw„hlen dieser Funktion erscheint die DIALOGBOX zur Auswahl
       der KOP-Kontakte. Der ausgew„hlte KONTAKT oder eine LINIE wird an der
       aktuellen Position eingefgt und der Rest der Zeile wird um eine
       Position NACH RECHTS verschoben.
       Die Funktion wird nur ausgefhrt wenn GENšGEND PLATZ vorhanden ist!

    <L”schen>:

       ENTFERNEN des angew„hlten Symbols. Bei KEINER šBERSCHNEIDUNG mit
       geschtzen Bereichen (Fub,Kontakte), werden die von dieser Linie
       betroffenen Zeilen um eine Raster NACH LINKS verschoben.

    @620[ KOP-Popups ]                                                     @001[ šbersicht ]
.627
    K O P  -  POPUP FUNKTIONEN

    <Aliasname>:

       Mit Hilfe dieser Funktion kann ein ALIASNAME fr den Funktionsblock ver-
       geben werden.

    <L”schen>:

       ENTFERNEN des angew„hlten FUNKTIONSBLOCKES.











    @620[ KOP-Popups ]                                                     @001[ šbersicht ]
.630
    K O P  -  SYNTAX-BESCHREIBUNG

    Der Vorteil der symbolischen Programmierung im KONTAKTPLAN-Editor ist die
    šbersichtlichkeit von Programmabl„ufen und logischen Verknpfungen.
    Durch eindeutige Kontakt-Symbole kann eine Zuordnung von Eing„ngen und
    Ausg„ngen (analog oder digital) leicht getroffen werden.


    @640[< KOP Symbole        >]     - Kontakte und Linien
                               - Funktionsbl”cke


    @635[< KOP Konsante       >]     - Deklaration von Konstanten


    @632[< KOP Konventionen   >]     - Allgemeine Konventionen





    @600[ KOP-Editor ]                  @602[ Editor-Bedienung ]                 @001[ šbersicht ]
.632
    K O P  -  KONVENTIONEN

    Bei der KONTAKTPLANERSTELLUNG werden folgende Konventionen berprft
    und bei Nichteinhaltung gemeldet:

    - Einschr„nkungen zur Kontaktplazierung auf Cursorposition:
      - Ausg„nge nicht in der ersten Spalte
      - Analoge Eing„nge nur in erster Spalte

    - Einschr„nkungen zur FUB-Plazierung (Cursorposition= Ecke links oben):
      - FUB hat keinen Platz (wrde z.B.andere KOP-Teile berdecken).
      - FUBs nicht in Spalte 1 (keine Eing„nge anschlieábar).
      - FUBs nicht in vorletzter Spalte (keine Ausg„nge anschlieábar).

    Fehlerberprfung vor dem ABSPEICHERN des Kontaktplanes:

    - fehlende Ausg„nge
    - offene Linien (Ausgang fehlt)
    - namenlose Kontakte
    - Funktionsblock ohne Ein- oder Ausg„nge

                                     @630[ KOP-Syntax ]                    @001[ šbersicht ]
.635
    K O P  -  KONSTANTEN

    Eine Konstante beschreibt einen Wert, der sich w„hrend der Laufzeit eines
    Programms nicht „ndert:
    ÉÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  SYM  ³ KONSTANTE              ³  Beispiele                         º
    ÌÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º  #zz  ³ DEZIMALE Konstante     ³  #[+|-]zzz                         º
    º       ³                        ³  #23, #-13876, ...                 º
    º  #zz. ³ FLOAT Konstante        ³  #[+|-]zzz.[zzz] [E|e [+|-]zz]     º
    º       ³                        ³  #100. ,#-47.23 ,#+6.17e-01 ,...   º
    º  %%zz  ³ BINRE Konstante       ³  %%11011110, ...                    º
    º  $zz  ³ HEXADEZIMALE Konstante ³  $9, $a3, $FF, ...                 º
    ÇÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
    º "sss" ³ STRING Konstante       ³  "abcdef", "ASCII_Name", ...       º
    ÈÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    - Bin„r- und Hex-Konstanten sind immer unsigned ansonsten optional [+|-].
    - KONSTANTEN und KONTAKTNAMEN k”nnen maximal 10 ZEICHEN lang sein.


                                     @630[ KOP-Syntax ]                    @001[ šbersicht ]
.640
    K O P  -  SYMBOLE
        Nummer       Symbol         Bedeutung
                 ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
          1      ³   ÄÄ´ ÃÄÄ   ³    digitaler Eingang
          2      ³   ÄÄ´/ÃÄÄ   ³    digitaler Eingang negiert
          3      ³     º ÇÄÄ   ³    analoger Eingang
          4      ³   ÄÄÄ( )    ³    digitaler Ausgang
          5      ³   ÄÄÄ(/)    ³    digitaler Ausgang negiert
          6      ³   ÄÄÄ(( ))  ³    analoger Ausgang
          7      ³   ÄÄ´+ÃÄÄ   ³    positive Flanke
          8      ³   ÄÄ´-ÃÄÄ   ³    negative Flanke
          9      ³   ÄÄ´&ÃÄÄ   ³    beide Flanken
          0      ³   ÄÄÄ(S)    ³    Set
          q      ³   ÄÄÄ(R)    ³    Reset
          w      ³   ÄÄÄ(+)    ³    positiver Flankenausgang
          t      ³   ÄÄÄ(-)    ³    negativer Flankenausgang
          u      ³   ÄÄÄ(&)    ³    Flankenausgang
          o      ³     ºAÇÄÄ   ³    Adreáoperator
                 ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
         Mit 'Nummer' kann der zu setzende Kontakt direkt in den KOP eingefgt
         werden, ohne das Popup-Men aufzurufen.
                                     @630[ KOP-Syntax ]                    @001[ šbersicht ]
.660
    K O P  -  MONITOR

    Mit dem KOP-MONITOR besteht die M”glichkeit, VARIABLEN eines auf der RPS
    laufenden KOPs direkt in der KONTAKTPLAN-MASKE ANZUZEIGEN. Mit der einge-
    stellten Abtastrate werden die PVs AKTUALISIERT und BESCHRIEBEN.

 ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄKOP-MonitorÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
 ³                                                   ²
 ³  dig_in1  dig_in2                   dig_out1      ²=> VARIABLEN-NAME
 ³ÍÍÍµ ÃÄÄÄÄÄÄ´ ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ( )         ²=> KOP-Syntax
 ³    1        0                          0          ²=> AKTUELLER PV-WERT
 ³                                                   ²   (drei Anzeige-FORMATE)
 ³                                                   ²
 ÀÄ< taskname : Run TK1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ
      ÀÄÄÄÄÄÄÄÄ Anzeige von Taskname, Zustand und Taskklasse

    @661[< Aufruf des KOP-Monitors    >]  - Allgemeines und AKTIVIERUNG

    @665[< Bedienung des KOP-Monitors >]  - BEDIENUNG des KOP-Monitor
                                    - Funktionen und Tastenkombinationen

                                                                     @002[ DEBUGGING ]
.661
    K O P  -  MONITOR

    REIHENFOLGE der KOP-MONITOR <AKTIVIERUNG>:

    ==> - MARKIEREN Sie in der GDM das gewnschte KOP-SYMBOL
    ==> - oder LADEN Sie den gewnschten KOP in den KOP-Editor
        - šBERTRAGEN des KOPs auf die RPS !!
        - Mit der HM-Funktion <Objekt><Monitor> oder mit der Tastenkombination
          <F11> wird der KOP-Monitor aufgerufen.

    * Beim Starten des KOP-MONITORs wird die LISTE der ben”tigten
      KOP-Variablen automatisch erstellt.
    * Das ZYKLISCHE LESEN der Variablen MUSS VOM ANWENDER GESTARTET
      WERDEN (Funktion F1: "Start/Stop") !

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Existiert auf der RPS ein Task mit dem gleichen Namen, der jedoch nicht  º
  º kompatibel zum DEBUG-Task ist, wird eine Meldung angezeigt, die Sie be-  º
  º st„tigen mssen. Anschlieáend wird der Task in der RPS korrigiert !!     º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

                                     @662[ Allgemeines ]                   @002[ DEBUGGING ]
.662
    K O P  -  MONITOR

    WERTANZEIGE:           Zustand
                         "logisch 0"
     dig_in1  dig_in2        ³           dig_out1     ... VARIABLEN-NAME
    ÍÍÍµ ÆÍÍÍÍÍÍµ ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ( )        ... KOP-Syntax
     ³  1        0                          0         ... AKTUELLER PV-WERT
     ³                                                    im eingestellten
     ÀÄÄÄÄÄÄ Ein DOPPELSTRICH kennzeichnet einen          FORMAT:
             Zustand der "logisch 1" entspricht           "$"... hexadezimal
                                                          "%%"... bin„re Anzeige

    Beispiel:

     dig_in1  dig_in2                    dig_out1     ... VARIABLEN-NAME
    ÍÍÍµ ÆÍÍÑÍÍÍµ ÆÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ( )        ... KOP-Syntax
        1   ³    1                          1         ... AKTUELLER PV-WERT
     dig_in3³
    ÄÄÄ´ ÃÄÄÙ
        0

    @660[ Fortsetzung ]                                                    @002[ DEBUGGING ]
.665
    K O P  -  MONITOR BEDIENUNG

    Der entsprechende KONTAKTPLAN-NAME wird in der unteren, linken Ecke
    des Fensterrahmens angezeigt.
    Ein ZYKLISCHES LESEN von Variablen-Werten wird mit einem "" in der
    unteren, linken Ecke des Fensterrahmens angezeigt.
    Als Fenstertitel wird immer "KOP-Monitor" angezeigt.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄKOP-MonitorÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                     ³
    ³ Die BEDIENUNG des KOP-Monitors wird ber Funktionen                 ²
    ³ der FUSSZEILE und funktionellen HOTKEYS durchgefhrt:               ²
    ³                                                                     ²
    ³                                                                     ²
    ³ @670[< Fuázeilenmen     >]     - Optimal fr TASTATUR-Bedienung          ²
    ³                                                                     ²
    ³ @667[< Hotkey-Tasten     >]     - KOP-Monitor SONDER-TASTENBELEGUNG       ²
    ³ @668[< Tastaturkommandos >]     - Allgemeine Editor-Bedienung             ²
    ³                                                                     ³
    ÀÄ< KOP_name : Run TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ

                                     @660[ KOP-Monitor ]                   @002[ DEBUGGING ]
.667
    K O P  -  MONITOR BEDIENUNG

    HOTKEY-TASTEN        :  AUSFšHREN einer FUNKTION anhand von SONDERTASTEN:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Hotkey                 ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <c>                    ³ FORCEN der Variable AKTIVIEREN/DEAKTIVIEREN   º
  º                        ³ (entspricht Funktion <Force>)                 º
  º <s>                    ³ ANZAHL der TASK-ZYKLEN eingeben               º
  º                        ³ (entspricht Funktion <Task><Zyklusanzahl>)    º
  º <w>                    ³ BESCHREIBEN der angew„hlten Variable          º
  º                        ³ (entspricht Funktion <Write>)                 º
  º <y>                    ³ UMSCHALTEN auf KOP-EDITOR-Darstellung         º
  º                        ³ (entspricht Funktion <Editor>)                º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <TAB>                  ³ ZYKLUSWEISES Abarbeiten der Task (<s> Anzahl) º
  º                        ³ (entspricht Funktion <Task><Zyklus>)          º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @668[ Fortsetzung ]                 @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.668
    K O P  -  MONITOR BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein KOP-Raster nach links/rechts              º
  º <UP>/<DOWN>            ³ Einen KOP-Raster aufw„rts/abw„rts             º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º 2x<HOME>               ³ Cursor auf KOP-Anfang                         º
  º 2x<END>                ³ Cursor auf KOP-Ende                           º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Aussteigen aus einer angew„hlten Funktion     º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

                                  @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.670
    K O P  -  MONITOR FUSSZEILENMENš
                                                                      @667[< WEITER >]
ÉÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍ»
º@671[1 Start/Stop]³@675[2 Task]³@672[3 Write]³@673[4 Force]³@674[5 Format]³6       ³7     ³@676[8 Editor]º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍ¼

   <F1> <Start/Stop>:  STARTEN/STOPPEN der PV-Listen AKTUALISIERUNG
   <F2> <Task>      :  TASK-Funktionen (Start/Stop/Zykus)
   <F3> <Write>     :  BESCHREIBEN der ausgew„hlten Variable (Hotkey: <w>)
   <F4> <Force>     :  FORCEN der ausgew„hlen Variable AKTIVIEREN/DEAKTIVIEREN
   <F5> <Format>    :  ANZEIGE-FORMAT der ausgew„hlten Variable (dez./hex/bin„r)
   <F8> <Editor>    :  UMSCHALTEN auf KOP-EDITOR-Darstellung zum KOP-Editieren


    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick angew„hlt werden.





                                  @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.671
    K O P  -  MONITOR BEDIENUNG

    <Start/Stop>:

    - Der KOP-Monitor wird ABWECHSELD GESTARTET oder GESTOPPT.


    - GESTARTETER KOP-MONITOR:
      - Die angezeigten VARIABLEN-WERTE werden zyklisch mit der eingestellten
        Abtastrate AKTUALISIERT (HM-Funktion <Einstellungen><On-Line>).
      - Die n„chste Bet„tigung der <Start/Stop>-Taste bewirkt ein STOPPEN.
      :                                               :
      ÀÄ<  taskname : Run TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... Kennzeichnung: ""


    - GESTOPPTER KOP-MONITOR:
      - Die VARIABLEN-WERTE der Liste werden NICHT AKTUALISIERT.
      - Die n„chste Bet„tigung der <Start/Stop>-Taste bewirkt ein STARTEN.
      :                                               :
      ÀÄ< taskname : Run TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... KEINE Kennzeichnung

    @670[ Fuázeilen-Men ]              @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.672
    K O P  -  MONITOR BEDIENUNG

    <Write>:      (Hotkey: <w>)

    - Die Variable, auf der sich der Cursor befindet, wird mit einem beliebigen
      Wert beschrieben.

    - Der WERT wird in einem EINGABE-FENSTER eingegeben.
      "%%"  ........ einem BINREN Wert ist das Zeichen "%%" voranzustellen
      "$"  ........ einem HEXADEZIMALEN Wert das Zeichen "$" voranzustellen
      "FLOAT.0" ... GANZSTELLIGE FLOAT-Werte mssen mit einem KOMMA ("wert.0")
                    eingegeben werden.

    - Dieser Dienst kann nur bei GESTARTETEM KOP-Monitor durchgefhrt werden !







    @670[ Fuázeilen-Men ]              @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.673
    K O P  -  MONITOR BEDIENUNG

    <Force EIN>/<Force AUS>:    (Hotkey: <c>)

    EIN:  - Das Forcen ist ein ZWINGENDES BESCHREIBEN einer Variable mit dem
            angegebenen WERT. Die Variable wird dabei gezwungen genau diesen
            Wert beizubehalten. Beschreibt ein Task eine geforcte PV, dann 
            bleibt der ge„nderte Wert NUR WHREND des aktuellen TASK-DURCH-
            LAUFES ERHALTEN. Anschlieáend wird die PV automatisch mit dem 
            FORCE-Wert šBERSCHRIEBEN.
          - Der FORCE-WERT wird mittels EINGABE-FENSTER eigegeben.
            (gewnschtes Zahlenformat bercksichtigen "%%","$","wert.0")
          - Der FORCE-WERT wird als Variablen-Wert ANGEZEIGT.

    AUS:  - Das FORCEN der ANGEWHLTEN VARIABLE wird auf der RPS DEAKTIVIERT.

    - ALLE FORCE-AUFTRGE von allen RPS-Variablen k”nnen mit der HM-Funktion
      <System><Info><Forcen aus> DEAKTIVIERT werden.

  ==> Sobald eine PV auf der RPS geforct wird, leuchtet die FORCE-LED der CPU.

    @670[ Fuázeilen-Men ]              @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.674
    K O P  -  MONITOR BEDIENUNG

    <Format>:

    - In einem POPUP-Men w„hlen Sie das ANZEIGEFORMAT der ausgew„hlten
      Variable:
    ÚÄÄÄÄÄÄÄÄÄÄ¿
    ³ dezimal  ³ ... DEZIMALE Anzeige des PV-Wertes
    ³ hex      ³ ... HEXADEZIMALE Anzeige des PV-Wertes, Kennzeichnung: "$"
    ³ bin„r    ³ ... BINRE Anzeige des PV-Wertes, Kennzeichnung: "%%"
    ÀÄÄÄÄÄÄÄÄÄÄÙ

    - Der Variablen-Wert wird im eingestellten FORMAT angezeigt.

    - Alle EINGABEN von Schreib- oder Force-Werten sind im EINGESTELLTEN
      FORMAT zu t„tigen.





    @670[ Fuázeilen-Men ]              @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.675
    K O P  -  MONITOR BEDIENUNG

    <Task>:

    - In einem POPUP-Men weden Ihnen verschiedene Task-Funktionen angeboten:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Start        ³ ... STARTEN einer gestoppten Task
    ³ Stop         ³ ... STOPPEN eines laufenden Task
    ³ Zyklus       ³ ... ZYKLUSWEISES Abarbeiten der Task (Hotkey: <TAB>)
    ³ Zyklusanzahl ³ ... ANZAHL der TASK-ZYKLEN eingeben (Hotkey: <s>)
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    <Zyklus>: Zyklusweises ABARBEITEN der Task. Die Task wird eine ANZAHL von
              Zyklen, die bei Zyklusanzahl definiert wurde, durchlaufen.


    - Der aktuelle TASK-ZUSTAND und ZYKLUSANZAHL wird am linken, unteren
      Fensterrand angezeigt (Run, Stop, Zyklusanzahl).



    @670[ Fuázeilen-Men ]              @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.676
    K O P  -  MONITOR BEDIENUNG

    <Editor>:

    - UMSCHALTEN des KOP-Monitorfensters in die Darstellung des KOP-Editors.
      Der KOP kann gegebenenfalls im KOP-Editor ge„ndert werden. Nach einer
      nderung muá dieser NEU COMPILIERT und in die RPS šBERTRAGEN werden.

    - Das ZURšCKSCHALTEN in den KOP-Monitor, erfolgt ber die HM-Funktion
      <Objekt><Monitor> oder direkt ber die Funktionstasten <F11>.











    @670[ Fuázeilen-Men ]              @665[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.700
    FUB - EDITOR

    In Form von FUNKTIONSBL™CKEN steht eine umfangreiche Auswahl an Library-
    Funktionen zur Verfgung. Beginnend von einfachen Vergleichsfunktionen ber
    Mathematikfunktionen (sin, cos, ...) bis hin zu komplexeren L”sungen von
    Standardanwendungen.
    Mit Hilfe des FUB-Editors k”nnen PROJEKTSPEZIFISCHE FUNKTIONEN vom
    ANWENDER in PL2000-, AWL- oder KOP-SYNTAX erstellt werden.


    @730[< SYNTAX-Beschreibung >]         - FUB-Erstellung: FUB-Source und Maske
                                      Prozeávariablen und Dokumentation

    @702[< EDITOR-Bedienung    >]         - Bedienung und Aufruf des FUB-Editors
                                    - Hotkey und Tastenkombinationen

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men



                                                                     @001[ šbersicht ]
.702
    FUB - EDITOR BEDIENUNG

    Eine neue Datei erh„lt den Namen <funct_xx.src>, wobei "xx" eine fort-
    laufende Nummer ist und "[ yyy ]" den Typ des Source-Code-Editors anzeigt:

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄfunct_xx.src [ yyy ]ÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³ @707[< FUB-SOURCE        >]     - in PL2000-, AWL- oder KOP- SYNTAX      ³
    ³                                                                    ²
    ³  ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄfunct_xx.fubÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³  ³ @708[< FUB-MASKE         >]     - AUFRUF und Erstellen der FUB-MASKE     ³
    ³  ³                                                                    ²
    ³  ³ @715[< Fuázeilenmen     >]     - NEUEINGABEN von Symbolen               ²
    ³  ³                           - Eingeben von KOMMENTAR                 ²
    ³  ³ @705[< Hotkey-Tasten     >]     - Sonder-Tastenbelegung fr die          ²
    ³  ³                             Editor- Bedienung                      ²
    ³  ³ @703[< Tastaturkommandos >]     - Weitere Tastenkombinationen fr die    ²
    ³  ³                             Editor- Bedienung                      ³
    ³  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂ3ÄÙ
    ³                                                                    ³
    ÀÄZeile x Spalte yÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ

    @730[ FUB-Syntax ]                     @700[ FUB-Editor ]                    @001[ šbersicht ]
.703
    FUB - EDITOR BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung wird mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende
                            Tastenkombinationen durchgefhrt:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Wechseln zwischen E/A-Anschluá der Zeile      º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼








    @704[ Fortsetzung ]                    @700[ FUB-Editor ]                    @001[ šbersicht ]
.704
    FUB - EDITOR BEDIENUNG

    EDITOR-BEDIENUNG     :  Die CURSOR-Positionierung wird mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende
                            Tastenkombinationen durchgefhrt:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼









    @702[ Fortsetzung ]                    @700[ FUB-Editor ]                    @001[ šbersicht ]
.705
    FUB - EDITOR BEDIENUNG

    HOTKEY-TASTEN:          Folgende Hotkey-Tasten stehen im FUB-Editor zur
                            Verfgung:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <INS>                  ³ SETZEN eines neuen FUB-ANSCHLUSSES. Auswahl   º
  º                        ³ der Anschlsse aus PV-Namens-Liste.           º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <DEL>                  ³ L™SCHEN des angew„hlten FUB-ANSCHLUSSES, die  º
  º                        ³ Anschluázeile bleibt erhalten.                º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼







    @703[ Tastenkombinationen ]         @702[ Editor-Bedienung ]                 @001[ šbersicht ]
.707
    FUB - EDITOR:  FUB-SOURCE

    Die <FUB-SOURCE NEUERSTELLUNG> muá ber die Hauptmen-Funktion <DATEI>
    <NEU><Funktionsbaustein> durchgefhrt werden, um die FUB-MASKE mit der
    Hauptmen-Funktion <OBJEKT><PARAMETER> ERSTELLEN oder EDITIEREN zu k”nnen:


    <DATEI><NEU>:           - Die FUB-SOURCE ERSTELLUNG erfolgt in der
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿    ausgew„hlten Syntax:
    ³ :                  ³  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Funktionsbaustein ÅÄÄ´@600[ Kontaktplan      ]³  KOP-Syntax
    ³ :                  ³  ³@400[ PL2000           ]³  PL2000-Syntax
    ³ :                  ³  ³@500[ Anweisungsliste  ]³  AWL-Syntax
    ³ :                  ³  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


    Das EDITIEREN eines FUBs (Source, Maske) erfolgt ber die Hauptmen-
    Funktion <DATEI><™FFNEN>. Die FUB-Source wird automatisch in den richtigen
    Editor geladen. Darauf basierend kann die FUB-Maske editiert werden.

                                  @702[ Editor-Bedienung ]                 @001[ šbersicht ]
.708
    FUB - EDITOR:  FUB-MASKE

    <NEUERSTELLEN oder EDITIEREN> der FUB-MASKE:
    - Das ENTSPRECHENDE FUB-SOURCE-FENSTER MUSS AKTIV sein !!
    - Mit der Hauptmen-Funktion <OBJEKT><PARAMETER> wird die zum FUB-Source
      geh”rende FUB-MASKE angezeigt.
      Als Fenstertitel wird der Name <funct_xx.fub> angezeigt, wobei "funct_xx"
      dem Namen der FUB-Source-Datei entspricht.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄfunct_xx.fubÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³                   ÛÛÛÛÛÛÛÛÛÛÛÄÄÄÄÄÄÄÄÄÄÄ¿                          ²
    ³ <MINIMALGR™SSE>   ³             funct_xx³                          ²
    ³  eines FUBs       ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´                          ²
    ³                   ³                     ³ ... keine E/A-Anschlsse ²
    ³                   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ                          ²
    ³                                                                    ²
    ³  ÛÛÛÛÛÛÛÛÛÛÛ .... <CURSOR>; kann nur auf E/A-Bereiche oder den     ²
    ³                    obersten FUB-Rahmen gestellt werden             ²
    ³                                                                    ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ
    @709[ Fortsetzung ]                 @702[ Editor-Bedienung ]                 @001[ šbersicht ]
.709
    FUB - EDITOR:  FUB-MASKE

    šBERSICHT EDITOR-BEDIENUNG:

    * <E/A-Parameter anschlieáen/umbenennen>:
      - mit Fuázeilen-Funktion <F1><Ein-/Ausg.>
      - Hotkey-Funktion <INS> (nur auf freiem Anschluá)

    * <E/A-Parameter l”schen>:
      - Hauptmen-Funktion <BEARBEITEN><L™SCHEN>
      - Hotkey-Funktion <DEL> l”scht den markierten Ein- bzw. Ausgang

    * <E/A-Bereich erweitern>:
      - mit Fuázeilen-Funktion <F4><Zeile+>

    * <E/A-Bereich minimieren>:
      - mit Fuázeilen-Funktion <F5><Zeile-> kann der FUB beliebig bis zur
        MINIMALGR™SSE reduziert werden.



                                  @702[ Editor-Bedienung ]                 @001[ šbersicht ]
.715
    FUB - EDITOR:  FUSSZEILENMENš

    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick ausgew„hlt werden:

ÉÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º@716[1 Ein-/Ausg.]³@716[2 Bewegen]³@717[3 Kommentar]³@717[4 Zeile+]³@717[5 Zeile-]³                         º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

   <F1> <Ein-/Ausg.>:  SETZEN oder UMBENENNEN eines FUB-ANSCHLUSSES.
                       Anschluá wird aus PV Namens-Liste ausgew„hlt.
   <F2> <Bewegen>   :  VERSCHIEBEN des angew„hlten FUB-ANSCHLUSSES
   <F3> <Kommentar> :  Eingeben oder Editieren des KOMMENTAR-FELDES
   <F4> <Zeile+>    :  EINFšGEN einer NEUEN Anschluá-Zeile. FUB-E/As werden
                       mitverschoben.
   <F5> <Zeile->    :  L™SCHEN der aktuellen Anschluá-Zeile. FUB-E/As werden
                       mitgel”scht.




    @705[ Hotkey-Tasten ]               @702[ Editor-Bedienung ]                 @001[ šbersicht ]
.716
    FUB - EDITOR:  FUSSZEILENMENš FUNKTIONEN

    <Ein-/Ausg.>:  SETZEN ODER UMBENENNEN EINES FUB-ANSCHLUSSES

       - An aktueller Curserposition wird ein FUB Ein- bzw. Ausgang gesetzt.
         Dieser ist aus einer PV Namens-Liste auszuw„hlen, wobei bereits
         fr Ein-/Ausg„nge verwendete PVs nicht mehr angezeigt werden.
       - Falls sich an der Einfgestelle schon ein Anschluá befindet, wird der
         Ein- bzw. Ausgang an der aktuellen Curserposition umbenannt. Der neue
         Name ist wieder der PV-Namens-Liste zu entnehmen.


    <Bewegen>:     VERSCHIEBEN DES ANGEWHLTEN FUB-ANSCHLUSSES

       - Der Ein- bzw. Ausgang an der aktuellen Curserposition wird markiert.
         Nach Positionieren des Cursors kann der Anschluá mit <ENTER> oder mit
         Mausklick an die gewnschte FREIE POSITION verschoben werden.
       - Eing„nge befinden sich immer auf der linken und Ausg„nge immer auf
         der rechten Seite eines FUBs.


                                  @715[ FUB-Fuázeilenmen ]                @001[ šbersicht ]
.717
    FUB - EDITOR:  FUSSZEILENMENš FUNKTIONEN

    <Kommentar>:   EINGEBEN ODER EDITIEREN DES KOMMENTAR-FELDES

       In einer Dialogbox k”nnen Sie einen Kommentar mit max. 60 Zeichen
       eingeben bzw. bearbeiten. Dieser Text wird zusammen mit dem FUB
       gespeichert und dient als Kurzbeschreibung der Funktion.


    <Zeile+>:      EINFšGEN EINER NEUEN ANSCHLUSS-ZEILE:

       An aktueller Cursorposition wird eine Zeile eingefgt. Falls sich an der
       Einfgeposition ein Anschluá befindet, wird dieser nach unten verschoben.


    <Zeile->:      L™SCHEN  DER AKTUELLEN ANSCHLUSS-ZEILE:

       An aktueller Cursorposition wird eine Zeile gel”scht. Anschlsse werden
       ohne Warnung mitgel”scht.


                                  @715[ FUB-Fuázeilenmen ]                @001[ šbersicht ]
.730
    FUB - SYNTAX BESCHREIBUNG

    Die eigentliche FUB-SYNTAX ist ein FIX VORGEGEBENES FORMAT der FUB-Maske,
    das vom FUB-Editor AUTOMATISCH EINGEHALTEN wird. Die SYNTAX des FUB-Sources
    h„ngt vom entsprechenden Source-Code-Editor ab.


    @731[< FUB-Erstellung      >]    - REIHENFOLGE bei der FUB-Erstellung:
                                 FUB-Source erstellen (Syntax: PL2000/KOP/AWL)
                                 Variablendeklaration (PVs lokal, dynamisch)
                                 FUB-Maske definieren (E/A-Anschlsse)
                                 Compilieren

    @735[< FUB-Maske           >]    - FORMAT der FUB-Maske (E/A-Anschlsse)
                               - Erstellung ber FUB-EDITOR

    @740[< Dokumentation       >]    - Kommentar und Dokumentation




    @702[ Editor-Bedienung ]               @700[ FUB-Editor ]                    @001[ šbersicht ]
.731
    FUB - ERSTELLUNG

    Bei der FUB-NEUERSTELLUNG ist ein BESTIMMTER ABLAUF von einzelnen
    Schritten einzuhalten:

    1.) FUB-SOURCE ERSTELLEN:    Syntax: PL2000, AWL oder KOP

        Nach dem Laden oder Neuerstellen wird direkt der entsprechende
        Source-Code-Editor aufgerufen.
        Die FUB-Source Erstellung wird anhand der entsprechenden Syntax der
        ausgew„hlten Programmiersprache durchgefhrt.


    2.) VARIABLENDEKLARATION:   Glitgkeitsbereich: LOKAL oder DYNAMISCH

        Nur definierte PVs der Variablendeklaration k”nnen als Ein- oder
        Ausg„nge in der FUB-Maske angeschlossen werden.
        Die definierten PVs des FUBs sind immer V™LLIG ENTKOPPELT zu allen
        anderen Projekt-Variablen.


    @732[ Fortsetzung ]                    @730[ FUB-Syntax ]                    @001[ šbersicht ]
.732
    FUB - ERSTELLUNG

    3.) FUB-MASKE IM FUB-EDITOR DEFINIEREN:    E/A-Anschlsse festlegen
        - Aufruf des entsprechenden FUB-EDITORS fr den ANGEWHLTEN FUB-Source
          AUFRUF:  ber Hauptmen-Funktion <OBJEKT><PARAMETER>
        - Nur in der VD definierte PVs k”nnen als Ein- oder Ausg„nge verwendet
          werden.

    4.) COMPILIEREN:
        - Beim COMPILER-Aufruf muá die entsprechende FUB-Source angew„hlt sein.
        - Entsprechende Fehlermeldungen und Warnungen werden im Meldungsfenster
          angezeigt.

    5.) Der fertig-compilierte Funktionsblock kann in PL2000-, AWL-Programmen
        oder im KOP verwendet werden.
        Erst nach der Compilierung der Programme, in denen der entsprechende
        FUB verwendet wird, werden durchgefhrte FUB-nderungen bercksichtigt.




                                     @730[ FUB-Syntax ]                    @001[ šbersicht ]
.735
    FUB - MASKE

    1) ANZEIGE nach erstem Aufruf:        FUB-CURSOR:
       (MINIMAL-Gr”áe eines FUBs)
         ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿              ÛÛÛÛÛÛÛÛÛÛÛ ... kann nur auf E/A-
         ³           FUB-NAME³                              Bereiche oder den
         ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´                              obersten FUB-Rahmen
   ÉÍÍÍ> ³                   ³ <ÍÍÍ»                        positioniert werden
   º     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     º
 EINGANGS-Seite          AUSGANGS-Seite .... Der E/A-Bereich (Anschluá-Anzahl)
                                             kann erweitert/verkleinert werden


    2) ANZEIGE nach Setzen des            3) ANZEIGE nach Setzen des
       FUB-Einganges "input_1":              FUB-Ausganges "output_1":
         ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿                 ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
         ³           FUB-NAME³                 ³           FUB-NAME³
         ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´                 ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
    ÍÍÍ> ´input_1            ³                 ´input_1    output_1Ã <ÍÍÍ
         ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ                 ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @736[ FUB-Editor ]                     @730[ FUB-Syntax ]                    @001[ šbersicht ]
.736
    FUB - MASKE

    - Mit Hilfe der FUB-Maske wird die EINGANGS-/AUSGANGS-ZUWEISUNG der
      FUB-INTERNEN VARIABLEN getroffen.

    - Es k”nnen NUR PVs als Ein- bzw. Ausg„nge verwendet werden, die in der VD
      deklariert wurden. Diese PVs werden in einer Auswahlliste beim Einfgen
      eines FUB-Anschlusses angezeigt. (Bereits angeschlossene PVs werden in
      der Auswahlliste nicht mehr angezeigt.)

    - Die FUB-INTERNEN VARIABLEN sind V™LLIG ENTKOPPELT von den Variablen, die
      sp„ter an den FUB angeschlossen werden.

    - Die REIHENFOLGE der sp„ter an den FUB bergebenen PVs h„ngt von der
      Anschluáreihenfolge (von OBEN nach UNTEN) der FUB-Parameter ab.

    - Die FUB-Maske wird im FUB-Editor definiert.

    - Die SYNTAX wird vom FUB-Editor automatisch eingehalten. Der FUB-Rahmen,
      und der Anschluábereich wird beim Einfgen und L”schen automatisch
      erweitert bzw. verkleinert.
                                     @730[ FUB-Syntax ]                    @001[ šbersicht ]
.740
    FUB - DOKUMENTATION

    <STANDARD FUBs>

    - Die Hilfe zu den einzelnen Standard-FUNKTIONSBL™CKEN beinhaltet folgenden
      Informationsumfang:   - FUB-Aufruf und FUB-Maske
                            - FUB-Anschlsse (verbale Beschreibung)
                            - Allgemeine FUB-Beschreibung

    <PROJEKT FUBs>

    - Im FUB-Editor kann ein bis zu 60 Zeichen groáes KOMMENTAR-Feld eingegeben
      werden. Dieser Kommentar dient als Kurzbeschreibung und wird zusammen mit
      dem FUB gespeichert.

    - Bei Bedarf kann eine ASCII Hilfe-Datei erstellt werden. Der Name dieser
      Hilfe-Datei wird in der Datei <hlp-049.lst> in einer definierten Form, 
      eingetragen. Diese selbsterstellt Hilfe kann dann mit der Hauptmen-
      Funktion <Hilfe><Index> aufgerufen werden.


                                     @730[ FUB-Syntax ]                    @001[ šbersicht ]
.750
    TEXT-EDITOR

    Mit dem TEXT-Editor k”nnen BELIEBIGE ASCII-Dateien GELADEN, ERSTELLT
    und BEARBEITET werden.

    Im TEXT-Editor gibt es KEINE SYNTAX, da der Text beliebig angeordnet und
    aufgebaut sein kann.



    @752[< EDITOR-Bedienung    >]         - TEXT-Editor Tastenkombinationen
                                    - Zeichen/Zeile einfgen, l”schen
                                    - Bereich markieren, kopieren

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men





                                                                     @001[ šbersicht ]
.752
    TEXT-EDITOR BEDIENUNG

    Eine neue Datei erh„lt den Namen <nonamexx.txt>, wobei "xx" eine fort-
    laufende Nummer ist.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄnonamexx.txt [ TEXT ]ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³ Bereich fr ASCII-Text Eingabe                                     ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³ @753[< Tastaturkommandos >]     - TEXT-Editor Tastaturkommandos          ²
    ³                           - Zeichen/Zeile einfgen, l”schen        ²
    ³                           - Bereich markieren, kopieren            ²
    ³                                                                    ³
    ÀÄZeile x Spalte yÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ
      ³
      ÀÄÄÄÄÄÄÄÄ Anzeige der aktuellen CURSOR-Position

                                     @750[ TEXT-Editor ]                   @001[ šbersicht ]
.753
    TEXT-EDITOR BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Bedienung des TEXT-Editors erfolgt entweder
                            mit TASTENKOMBINATIONEN oder mit der MAUS:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ENTER>                ³ im Einfge-Modus:                             º
  º                        ³    - Leere Zeile einfgen und Text ab         º
  º                        ³      aktueller Cursor-Position in eingefgte  º
  º                        ³      Zeile verschieben.                       º
  º                        ³    - Cursor in n„chste Zeile stellen          º
  º                        ³ im šberschreibmodus:                          º
  º                        ³    - Cursor in die n„chste Zeile stellen      º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @754[ Fortsetzung ]                    @750[ TEXT-Editor ]                   @001[ šbersicht ]
.754
    TEXT-EDITOR BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung wird mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende 
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein Zeichen nach links/rechts                 º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  º <TAB>                  ³ im Einfge-Modus: Einfgen von Leerzeichen    º
  º                        ³    bis zur n„chsten TAB-Position              º
  º                        ³ im šberschreibmodus: Cursor auf n„chste       º
  º                        ³    TAB-Position stellen                       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º ^<HOME>/ 2x<HOME>      ³ Cursor zum Beginn des Textes                  º
  º ^<END>/ 2x<END>        ³ Cursor zum Ende des Textes                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @755[ Fortsetzung ]                    @750[ TEXT-Editor ]                   @001[ šbersicht ]
.755
    TEXT-EDITOR BEDIENUNG

    TEXT- BEARBEITUNG    :  Das markieren eines Textbereiches erfolgt mit der
                            Tastenkombination <SHIFT>+<HOME>/<END>/<CURSOR> 
                            oder mit der Maus (Festhalten der linken Maustaste
                            und Ziehen der Maus).

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^<BACKSPACE>           ³ L”schen der aktuellen Zeile                   º
  º <DEL>/<BACKSPACE>      ³ Zeichen unter/vor dem Cursor l”schen          º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>                ³ Setzen der Textmarke bis Zeilenanfang         º
  º <END>                 ³ Setzen der Textmarke bis Zeilenende           º
  º <CURSOR-TASTEN>       ³ Setzen einer beliebigen Textmarke             º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <INS>                  ³ Wechselweises Umschalten zwischen             º
  º                        ³ EINFšGE-/šBERSCHREIB-Modus                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @752[ Fortsetzung ]                    @750[ TEXT-Editor ]                   @001[ šbersicht ]
.770
    DATENMODUL - EDITOR

    Mit dem DATENMODUL-Editor k”nnen DATENMODULE ERSTELLT und BEARBEITET
    werden. Das compilierte Datenmodul kann auf der RPS von Anwendertasks
    als DATEN-TABELLE oder als DATENSPEICHER bentzt werden.
    In der B&R-Library stehen "DA_...()" Funktionen fr die Verwendung von
    Datenmodulen zur Verfgung (die B&R-Library muá extra importiert werden).


    @780[< DATENMODUL-Syntax   >]         - DATENMODUL-Format

    @785[< DATENMODUL-Aufruf   >]         - Verwendung von DATENMODULEN in
                                      ANWENDER-Tasks (mit Beispiel)

    @772[< EDITOR-Bedienung    >]         - DATENMODUL-Editor Tastenkombinationen
                                    - Zeichen/Zeile einfgen, l”schen
                                    - Bereich markieren, kopieren

    @880[< PG-Bedienung        >]         - Hauptmen, System-Men
                                    - Bedienung Fuázeile, Popup-Men

                                                                     @001[ šbersicht ]
.772
    DATENMODUL-EDITOR BEDIENUNG

    Eine neue Datei erh„lt den Namen <data__xx.dat>, wobei "xx" eine fort-
    laufende Nummer ist.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄdata__xx.dat [ DAT ]ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³                                                                    ³
    ³ Bereich fr DATENMODUL Eingabe                                     ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³                                                                    ²
    ³ @773[< Tastaturkommandos >]     - DATENMODUL-Editor Tastaturkommandos    ²
    ³                           - Zeichen/Zeile einfgen, l”schen        ²
    ³                           - Bereich markieren, kopieren            ²
    ³                                                                    ³
    ÀÄZeile x Spalte yÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ
      ³
      ÀÄÄÄÄÄÄÄÄ Anzeige der aktuellen CURSOR-Position

                                  @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.773
    DATENMODUL-EDITOR BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Bedienung des DATENMODUL-Editors erfolgt ent-
                            weder mit TASTENKOMBINATIONEN oder mit der MAUS:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ENTER>                ³ im Einfge-Modus:                             º
  º                        ³    - Leere Zeile einfgen und Text ab         º
  º                        ³      aktueller Cursor-Position in eingefgte  º
  º                        ³      Zeile verschieben.                       º
  º                        ³    - Cursor in n„chste Zeile stellen          º
  º                        ³ im šberschreibmodus:                          º
  º                        ³    - Cursor in die n„chste Zeile stellen      º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @774[ Fortsetzung ]                 @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.774
    DATENMODUL-EDITOR BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung wird mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende 
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein Zeichen nach links/rechts                 º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  º <TAB>                  ³ im Einfge-Modus: Einfgen von Leerzeichen    º
  º                        ³    bis zur n„chsten TAB-Position              º
  º                        ³ im šberschreibmodus: Cursor auf n„chste       º
  º                        ³    TAB-Position stellen                       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º ^<HOME>/ 2x<HOME>      ³ Cursor zum Beginn des Textes                  º
  º ^<END>/ 2x<END>        ³ Cursor zum Ende des Textes                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @775[ Fortsetzung ]                 @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.775
    DATENMODUL-EDITOR BEDIENUNG

    TEXT-BEARBEITUNG     :  Das markieren eines Bereiches erfolgt mit der
                            Tastenkombination <SHIFT>+<HOME>/<END>/<CURSOR> 
                            oder mit der Maus (Festhalten der linken Maustaste
                            und Ziehen der Maus).

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^<BACKSPACE>           ³ L”schen der aktuellen Zeile                   º
  º <DEL>/<BACKSPACE>      ³ Zeichen unter/vor dem Cursor l”schen          º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>                ³ Setzen der Textmarke bis Zeilenanfang         º
  º <END>                 ³ Setzen der Textmarke bis Zeilenende           º
  º <CURSOR-TASTEN>       ³ Setzen einer beliebigen Textmarke             º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <INS>                  ³ Wechselweises Umschalten zwischen             º
  º                        ³ EINFšGE-/šBERSCHREIB-Modus                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @772[ Fortsetzung ]                 @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.780
    DATENMODUL - SYNTAX

    < ALLGEMEINE Syntax >:
      ',' ... trennt die EINZELNEN EINTRGE
      ';' ... Kennzeichnet den Beginn eines KOMMENTARS
      " " ... Kennzeichnet eine STRING-EINGABE MIT NULL-TERMINIERUNG "string\0"
      ' ' ... Kennzeichnet eine STRING-EINGABE OHNE NULL-Terminierung 'string'


    @781[< Syntax fr ZAHLENEINGABEN >:]
    - Als EINGABEFORMATE stehen dezimal/hex/bin„r/float Eingaben zur Auswahl
    - Der DATENTYP des Eintrags resultiert aus LNGE und dem WERT DER EINGABE.
      

    @782[< Syntax fr SONDERZEICHEN  >:]
    - Sonderzeichen in Strings k”nnen in "C-Format" eingegeben werden.


    Beispiel:   ; Datenmodul - Beispiel (Kommentarzeile)
                2,15,"Das ist ein String",$1234, %%10011010

                                  @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.781
    DATENMODUL - SYNTAX

     Syntax der Zahleneingabe im Datenmodul
     * DEZIMAL : (-)dddd       d ... 0 - 9
          BYTE : Stellenanzahl <= 3, Wert < 256   bzw. > -128
          WORD : Stellenanzahl <= 5, Wert < 65536 bzw, > -32268
          LONG : bei šberschreitung der WORD-Grenzen

     * HEX     : $xxxx         x ... 0 - 9, A - F (2,4 oder 8-stellig)
          BYTE : Stellenanzahl 2
          WORD : Stellenanzahl 4
          LONG : Stellenanzahl 8

     * BINR   : %%bbbbbbbb      b ... 0,1  (8, 16 oder 32-stellig !!)
          BYTE : Stellenanzahl  8
          WORD : Stellenanzahl 16
          LONG : Stellenanzahl 32

     * FLOAT   :   ff.ff          f ... 0 - 9
            Floatwerte werden durch den Dezimalpunkt gekennzeichnet.

    @780[ Fortsetzung ]                 @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.782
    DATENMODUL - SYNTAX

    Sonderzeichen in Strings k”nnen in C-Format eingegeben werden:

    Sonderzeichen                  ASCII-Wert
    --------------------------------------------------------------
      \a    ... Bell (Alert)         7
      \b    ... Backspace            8
      \e    ... Escape              27
      \f    ... Form Feed           12
      \n    ... New line            10
      \r    ... Carriage return     13
      \t    ... Tabulator            9
      \\    ... Backslash
      \ddd  ... Oktalziffer (d..0-7)
      \xhhh ... Hex-Ziffer  (h..0-9, A-F)





    @780[ Fortsetzung ]                 @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.785
    DATENMODUL - VERWENDUNG

    Datenmodule k”nnen sowohl fr die RPS-CPU (Spooladr.=0) als auch fr
    intelligente Regler (Spooladr=Position des Reglers) erzeugt werden.
    (Verwendung: DA_info()...dynamische Variable auf Datenmodul zuweisen, ...)

    FUNKTIONSšBERSICHT:
    status = DA_create(adr(name),grp,spadr,dlng,adr(data),adr(modat),adr(ident))
    status = DA_write(ident,adr(data),dlng,mo_offs)
    status = DA_ident(adr(name),grp,adr(ident))
    status = DA_delete(ident)
    status = DA_burn(ident)
    status = DA_info(ident,adr(dadr),adr(dlen),adr(mem_typ))

    šBERGABEPARAMETER
      name...BYTE-Feld / "Modulname"    data.....BYTE-Stream  Datenbereich
      grp....BYTE  Gruppennummer        modat....LONG  Adr. der Moduldaten
      spadr..WORD  Spooladresse         ident....LONG  Identifier
      dlng...LONG  Datenl„nge           mo_offs..LONG  Offset im Datenbereich
      dadr...LONG  Adr. Datenbereich    mem_typ..BYTE  (1=RAM,2=EPROM)

    @786[ Beispiel ]                    @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.786
    DATENMODUL - VERWENDUNG
    VD:     NAME       gltig      Datentyp     L„nge   I/O-Typ
            da_ptr     dynamisch   da_struct    10      Referenz
            da_adr     lokal       LONG         1       MERKER
            :          :           :            :       :
    TASK:   ;---------------------- INIT-TEIL: ------------------------
            status = DA_ident("modulname",0,adr(ident))
            if status = 0 then                 ; Datenmodul gefunden ?
                status = DA_info(ident,adr(da_adr),adr(lng),adr(memtyp))
                if status <> 0 then
                    da_adr = 0                 ; ==> KEIN Datenmodul-Zugriff
                else
                    da_ptr access da_adr       ; ==> KORREKTE ADRESS-Zuweisung
            else
                da_adr = 0                     ; ==> KEIN Datenmodul-Zugriff
            endif
            ;-------------------- ZYKLISCHER TEIL: --------------------
            if da_adr <> 0 then                ; Datenmodul-Zugriff erlaubt ?
                ; LESEN aus dem Datenmodul mittels da_ptr[index].element
                ; SCHREIBEN in das Datenmodul mit der Funktion DA_write()
            endif
    @785[ Fortsetzung ]                 @770[ DATENMODUL-Editor ]                @001[ šbersicht ]
.800
    V D - VARIABLENDEKLARATION

 ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
 º@801[ Name     ]º@802[ gltig  ]³@803[ Datentyp ]³@804[ L„nge ]³@805[ I/O Typ  ]³@808[Init-Wert ]³@807[ Langname     ]º
 ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
 º ANZEIGE  º@834[ POPUP-  ]³@835[ POPUP-   ]³Anzahl-³@836[ POPUP-   ]³Optionale ³ Optionale    º
 º          º EINGABE ³ EINGABE  ³EINGABE³ EINGABE  ³Eingabe   ³ Eingabe      º
 ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    <Name> ........ ANZEIGE des PV-Namens (verschiedene Anzeige-Kriterien)
    <gltig> ...... GšLTIGKEITSBEREICH der PV (global, lokal, dynamisch, ...)
    <Datentyp> .... DATENTYP der PV (Struktur-DEFINITION, BIT, BYTE, LONG, ...)
    <L„nge> ....... ANZAHL der Datentypen (L„nge ungleich 1 => PV ist ein Feld)
    <I/O-Typ> ..... HARDWARE-ZUORDNUNG festlegen (Merker, Eingang, Ausgang,..)
    <Init-Wert> ... Optionale ZUORDNUNG von PV-spezifischen INIT-WERTEN
    <Langname> .... Optionale Angabe eines Langnamens der PV

    @837[< ALLGEMEINES         >]       - AUFRUF und KONVENTIONEN
    @841[< Fuázeilenmen       >]       - FUNKTIONEN und Bedienung
    @839[< Tastaturkommandos   >]       - WECHSELN zwischen den EINGABEFELDERN

    @843[< STRUKTURDEFINITION  >]       - AUFRUF, FUNKTIONEN und Bedienung
                                                                     @001[ šbersicht ]
.801
    V D - VARIABLENDEKLARATION
                                                                      @802[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º NAME     º@802[ gltig  ]³@803[ Datentyp ]³@804[ L„nge ]³@805[ I/O Typ  ]³@807[ Langname         ]º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º@809[ pv_name  ]º

    - Alle PV-NAMEN, die das mit <F1 Filter> eingestellte ANZEIGE-KRITERIUM
      erfllen, werden in dieser Spalte ANGEZEIGT.
      Standardm„áig werden entweder alle PVs des aktuellen Objektes oder nur 
      jene PVs, die noch nicht deklariert wurden, angezeigt.

    - Wenn bereits EINGABEN zu einer PV durchgefhrt wurden, werden diese nach
      einem erneuten Einstieg in die VD ANGEZEIGT. Diese k”nnen jederzeit
      wieder ge„ndert werden.

    - Es werden nur die 10 SIGNIFIKANTEN ZEICHEN eines PV-Namens angezeigt.

    - Der PV-Name kann in der VD NICHT EDITIERT werden !


                                     @800[     V D     ]                   @001[ šbersicht ]
.802
    V D - VARIABLENDEKLARATION
                                                                      @803[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º@801[ Name     ]º GšLTIG  ³@803[ Datentyp ]³@804[ L„nge ]³@805[ I/O Typ  ]³@807[ Langname         ]º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    - Der GšLTIGKEITSBEREICH wird mit Hilfe eines POPUP-Fensters oder dem
      ANFANGSBUCHSTABEN des gewnschten Gltigkeitsbereichs definiert.

    - Je nach PROZESSOR-TYP werden nur die entsprechenden Gltigkeitsbereiche,
      die mit "*" oder mit "STD" (vorgeschlagene Standardeinstellung) gekenn-
      zeichnet sind, angezeigt:
                                                              CPU ³ MP ³ IP
      ÚÄÄ gltig ÄÄÄ¿                                        ÄÄÄÄÄÅÄÄÄÄÅÄÄÄÄÄ
      ³@810[ tk_global   ]³  ...  Taskklassenglobale Variable        STD³ STD³ STD
      ³@810[ rps_global  ]³  ...  RPS-globale Variable               *  ³ *  ³
      ³@810[ proc_global ]³  ...  Prozessor-globale Variable            ³ *  ³
      ³@810[ dynamisch   ]³  ...  bei Adreáoperationen               STD³ STD³ STD
      ³@810[ lokal       ]³  ...  Lokale Variable                    *  ³ *  ³ *
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @811[ CPU/MP/PP/IP ]                   @800[     V D     ]                   @001[ šbersicht ]
.803
    V D - VARIABLENDEKLARATION
                                                                      @804[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º@801[ Name     ]º@802[ gltig  ]³ DATENTYP ³@804[ L„nge ]³@805[ I/O Typ  ]³@807[ Langname         ]º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    - Der DATENTYP ist grunds„tzlich entscheidend fr die LNGE EINER PV,
      und wird mit Hilfe eines POPUP-Fensters oder dem ANFANGSBUCHSTABEN
      des gewnschten Datentyps definiert:
      Ú Datentyp ¿
      ³ BIT      ³  1 Bit   , Variable mit dem Wert 0 oder 1.
      ³ INT8     ³  7 Bit+VZ, Werte von        -128 bis        +127.
      ³ INT16    ³ 15 Bit+VZ, Werte von      -32768 bis      +32767.
      ³ INT32    ³ 31 Bit+VZ, Werte von -2147483648 bis +2147483647.
      ³ BYTE     ³  8 Bit   , Werte von           0 bis         255.
      ³ WORD     ³ 16 Bit   , Werte von           0 bis       65535.
      ³ LONG     ³ 32 Bit   , Werte von           0 bis  4294967295.
      ³ FLOAT    ³ 32 Bit   , Werte von 1,18E-38(abs) bis 3,4E+38(abs)
      ³@843[ typedef  ]³ Benutzerdefinierte Datentypen:  STRUKTUREN
      ³@816[ FUBAlias ]³ Zugriff auf FUB Ein- bzw. Ausg„nge ber einen Strukturtyp
      ÀÄÄÄÄÄÄÄÄÄÄÙ
                                     @800[     V D     ]                   @001[ šbersicht ]
.804
    V D - VARIABLENDEKLARATION
                                                                      @805[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º@801[ Name     ]º@802[ gltig  ]³@803[ Datentyp ]³ LNGE ³@805[ I/O Typ  ]³@807[ Langname         ]º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    - Die vorgeschlagene STANDARDEINSTELLUNG ist "1". Das bedeutet:
      - Die EIGENTLICHE LNGE der PV entspricht der DATENTYP-L„nge.
      - Die PV ist eine Einfach-Variable, wenn ein GRUND-DATENTYP (BIT, 
        BYTE, ...) ausgew„hlt wurde, oder die PV ist eine STRUKTUR mit dem
        bei Datentyp angezeigten Struktur-Typ.

    - Bei einer Angabe "UNGLEICH 1" ist die zu definierende PV automatisch
      eine FELD-VARIABLE (ARRAY), die aus der in <LNGE> definierten ANZAHL
      VON FELDELEMENTEN besteht.
      Ein FELDELEMENT entspricht dem eingestellten DATENTYP und kann daher
      auch eine Struktur sein.




                                     @800[     V D     ]                   @001[ šbersicht ]
.805
    V D - VARIABLENDEKLARATION
                                                                      @800[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º@801[ Name     ]º@802[ gltig  ]³@803[ Datentyp ]³@804[ L„nge ]³ I/O TYP  ³@807[ Langname         ]º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    - @829[<ANZEIGEM™GLICHKEITEN>] und DIREKTEINGABE des I/O-Krzels im Eingabefeld!
    - @817[<PRšFKRITEREN>]         automatische Prfkriterien bei I/O-TYP AUSWAHL!

      Da der VD anhand der DATENBANK (DBK) alle Informationen zur Verfgung
      stehen (Prozessortyp, Gltigkeitsbereich, Datentyp, ...), werden nur
      dementsprechend KORREKTE AUSWAHLM™GLICHKEITEN von I/O-Typen angezeigt.

      ÚÄÄ I/O Typ ÄÄ¿
      ³ MERKER      ³  ... STANDARDEINSTELLUNG: Zugriff auf SPEICHERSTELLE
      ³@819[ MP/PP       ]³  ... Zugriff auf rps_globale MP/PP-Speicherstelle
      ³@820[ 2003-I/O    ]³  ... 2003 Eingangs-/Ausgangskarte
      ³@821[ 2005-I/O    ]³  ... 2005 Eingangs-/Ausgangskarte
      ³@822[ 2010-I/O    ]³  ... 2010 Eingangs-/Ausgangskarte
      ³ IP          ³  ... Intelligentes Peripherie-Modul
      ³     ...     ³
                                     @806[ Fortsetzung ]                   @001[ šbersicht ]
.806
    V D - VARIABLENDEKLARATION
                                                                      @800[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º@801[ Name     ]º@802[ gltig  ]³@803[ Datentyp ]³@804[ L„nge ]³ I/O TYP  ³@807[ Langname         ]º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

      ³     ...     ³
      ³@823[ REMOTE-I/O  ]³  ... REMOTE Eingangs-/Ausgangskarte
      ³@825[ CAN-I/O     ]³  ... CAN Eingangs-/Ausgangskarte
      ³ Referenz    ³  ... nicht anw„hlbar: fr dynamischen Gltigkeitsbereich
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ










    @811[ CPU/MP/PP/IP ]                   @800[     V D     ]                   @001[ šbersicht ]
.807
    V D - VARIABLENDEKLARATION
                                                                      @800[< WEITER >]
    ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º@801[ Name     ]º@802[ gltig  ]³@803[ Datentyp ]³@804[ L„nge ]³@805[ I/O Typ  ]³ LANGNAME         º
    ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
                                                       ³@833[ langname         ]º

    - Der <LANGNAME> ist eine OPTIONALE Eingabe und hat NUR die Aufgabe, die
      Lesbarkeit des Programms zu erh”hen (Dokumentation). Es mssen trotzdem
      einige Vorschriften eingehalten werden, die unter <langname> beschrieben
      sind.

    - Anwendungsbeispiel: Bei Vergabe von HW-nahen Bezeichnungen als PV-Name
      (vm_2_21) kann in Form des Langnamens (Presse2_Mengenventil) eine Art
      Kommentar eingegeben werden.

    - Die ANZEIGE des Langnamens kann ber <Einstellungen><Editor><Variable>
      AUS- bzw. EINGESCHALTET werden.

    - Es werden nur 18 von maximal 35 ZEICHEN eines Langnamens angezeigt.

                                     @800[     V D     ]                   @001[ šbersicht ]
.808
    V D - VARIABLENDEKLARATION
                                                                      @800[< WEITER >]
 ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
 º@801[ Name     ]º@802[ gltig  ]³@803[ Datentyp ]³@804[ L„nge ]³@805[ I/O Typ  ]³Init-Wert ³@807[ Langname     ]º
 ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹

    - Die Eingabe von <INIT-WERTEN> ist OPTIONAL. Es k”nnen fr jede PV appli-
      kationsspezifische Init-Werte vergeben werden. Die PV-Initialisierung
      wird nach jedem INIT oder KALTSTART noch vor der Ausfhrung der Init-UPs
      durchgefhrt.

    - Die ANZEIGE der INIT-WERTE kann ber <Einstellungen><Editor><Variable>
      AUS- bzw. EINGESCHALTET werden.

    ACHTUNG :
    - Standardm„áig (keine Definition von Init-Werten durchgefhrt) werden
      jedoch die eingestellten Init-Werte von <Einstellungen><Compiler> fr
      die PV-Initialisierung verwendet!
    - Fr eine DURCHFšHRUNG der PV-Initialisierung muá mindestens die Version
      V1.10 des Betriessystems und BUGFIX_V115 gebrannt sein!

                                     @800[     V D     ]                   @001[ šbersicht ]
.809
    V D - VARIABLENDEKLARATION

    Als <PV-NAMEN> werden SYMBOLISCHE NAMEN verwendet, die nach den folgenden
    Regeln aus alphanumerischen Zeichen und Ziffern zusammengesetzt sein
    drfen:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  Buchstaben          ³  Zeichen             ³  Ziffern              º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º 'A'bis'Z', 'a'bis'z' ³       '_', '$'       ³       '0'bis'9'       º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

     - Alle Anwender-PVs MšSSEN mit einem BUCHSTABEN beginnen!
       (Ausnahme: nur bereitgestellte Systemvariablen beginnen mit einem "_")
     - Die Groá-/Kleinschreibung eines PV-Namens ist SIGNIFIKANT.
     - Der PV-Name muá aus einer DURCHGEHENDEN ZEICHENKETTE bestehen.
     - Die PV-Namensl„nge ist nicht beschr„nkt, SIGNIFIKANT sind 10 ZEICHEN.
     - SCHLšSSELWORTER der Programmierspachen als PV-Namen sind nicht erlaubt.
     - Die Trennung zwischen einem PV-Namen und einem Schlsselwort erfolgt
       durch mindestens 1 Leerzeichen (" ").

    @801[ VD-Name ]                        @800[     V D     ]                   @001[ šbersicht ]
.810
    V D - VARIABLENDEKLARATION

    <tk_global> :  Die entsprechende Variable ist fr ALLE TASKS DERSELBEN
                   TASKKLASSE gltig.

    <rps_global>:  Die entsprechende Variable ist fr ALLE TASKS ALLER TASK-
                   KLASSEN und MPs derselben RPS gltig.

    <proc_global>: Die entsprechende Variable ist fr ALLE TASKS ALLER TASK-
                   KLASSEN desselben MPs gltig.

    <dynamisch> :  Die Variable hat KEINEN EIGENEN GšLTIGKEITSBEREICH, und
                   muá w„hrend Laufzeit erst ber die Zuweisung einer Adresse
                   definiert werden.

    <lokal>     :  Die entsprechende Variable ist NUR FšR EINE TASK (FUB)
                   gltig. Eine PV mit dem gleichen Namen in einer ANDEREN
                   TASK (FUB) ist eine unabh„ngige ANDERE VARIABLE.



    @811[ CPU/MP/PP/IP ]                   @800[     V D     ]                   @001[ šbersicht ]
.811
    V D - VARIABLENDEKLARATION
    SPEICHERAUFTEILUNG der einzelnen GšLTIGKEITSBEREICHE:
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º PROZESSOR    º rps_global (DPR) ³ proc_global (DPR) ³ tk_global           º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º CPU          º Merker, SP-DPR   ³ ----              ³ Merker,I/O,IP-DPR   º
  º MP-syn       º Merker, SP-DPR   ³ Merker (itself)   ³ Merker,I/O,IP-DPR   º
  º MP-asyn      º Merker, SP-DPR   ³ Merker (itself)   ³ Merker              º
  º PP/MP-autark º ----             ³ Merker (itself)   ³ Merker              º
  º IP           º ----             ³ Merker (itself)   ³ Merker I/O (itself) º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    SP...Systemprozessoren(MP/PP)     IP...Intelligente Peripherie

    rps_global .... Variablen im MERKERBEREICH der CPU oder SP-DPR:
                    Die Verbindung von Variablen der CPU-Seite auf die SP-Seite
                    wird in der GDM mit Hilfe der "Crossreferenz" oder mittels
                    des eingezeichneten "Datenbaum" zwischen CPU/SP durchgefhrt
    proc_global ... Variablen im MERKERBEREICH des SP/IP-DPR
    tk_global ..... Pro Taskklasse ein eigener Variablenbereich. Weiters k”nnen
                    IP-Variablen CPU-seitig definiert werden.

    @802[ VD-gltig ]                      @800[     V D     ]                   @001[ šbersicht ]
.812
    V D - VARIABLENDEKLARATION

    <STRUKTURDEKLARATION>:

    - Falls bereits DATENTYPEN definiert wurden, kann mit Hilfe einer Dialogbox
      der gewnschte Datentyp ausgew„hlt oder ein neuer DATENTYP definiert
      werden.

      Als TABELLENTITEL erscheint der vollst„ndige Komponentenname oder bei
      sehr langen Namen die gerade bearbeitete Komponente "...comp_name".
      Die Einzelbezeichnungen der Strukturebenen sind durch PUNKTE getrennt.
                              ÉÍÍÍÍÍÍÍÍÍ< comp_name1 >ÍÍÍÍÍÍÍÍ»
                             ÉÍÍÍ< comp_name1.comp_name2 >ÍÍÍ»º
                             º KOMPONENTE ³ Datentyp ³ L„nge ºº
                             ÌÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍ¹º
                                   :            :        :
    <AUFRUF>    - Wird in der VD der Datentyp <typedef> ausgew„hlt, erscheint
                  eine "Dialogbox" fr die Definition des Datentyps
                - Projektglobale Datentypen k”nnen mit der Hauptmen-Funktion
                  <Projekt><Datentyp> definiert werden.

    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.813
    V D - VARIABLENDEKLARATION

    <STRUKTUR><KOMPONENTE>

    - In dieser Spalte werden die BEN™TIGTEN oder bereits DEFINIERTEN
      STRUKTURELEMENTE angezeigt.
    - Vom 1. STRUKTURELEMENT kann in max. 4 WEITERE UNTER-STRUKTUREN
      VERZWEIGT werden.
    - Ein NEUES STRUKTURELEMENT kann immer in der 1. FREIEN ZEILE definiert
      werden.
    - Zum Editieren bereits DEFINIERTER STRUKTURELEMENTE stehen folgende
      Funktionen in einem POPUP-MENš zur Verfgung:
      ÚÄÄÄÄÄÄÄÄÄÄÄ¿
      ³ Einfgen  ³ ... Einfgen eines NEUEN Strukturelementes
      ³ L”schen   ³ ... L”schen eines ANGEZEIGTEN Strukturelementes
      ³ Bewegen   ³ ... Verschieben eines Strukturelementes
      ³ Name      ³ ... ndern des STRUKTURELEMENT-NAMENS
      ÀÄÄÄÄÄÄÄÄÄÄÄÙ
                        AKTIVIERUNG mittels:  ^F3 oder DOPPEL-KLICK der Maus


    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.814
    V D - VARIABLENDEKLARATION

    <STRUKTUR><DATENTYP>

    - Der DATENTYP ist grunds„tzlich entscheidend fr die LNGE EINES
      STRUKTURELEMENTES und wird mit Hilfe eines POPUP-Fensters oder dem
      ANFANGSBUCHSTABEN des gewnschten Datentyps definiert:

      Ú Datentyp ¿
      ³ BIT      ³ NICHT aktivierbar ==> NUR ANALOGE Datentypen m”glich.
      ³ INT8     ³  7 Bit+VZ, Werte von        -128 bis        +127
      ³ INT16    ³ 15 Bit+VZ, Werte von      -32768 bis      +32767
      ³ INT32    ³ 31 Bit+VZ, Werte von -2147483648 bis +2147483647
      ³ BYTE     ³  8 Bit   , Werte von           0 bis         255
      ³ WORD     ³ 16 Bit   , Werte von           0 bis       65535
      ³ LONG     ³ 32 Bit   , Werte von           0 bis  4294967295
      ³ FLOAT    ³ 32 Bit   , Werte von 1,18E-38(abs) bis 3,4E+38(abs)
      ³ typedef  ³ ANLEGEN einer UNTERSTRUKTUR (maximal 5 ELEMENT-EBENEN)
      ÀÄÄÄÄÄÄÄÄÄÄÙ


    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.815
    V D - VARIABLENDEKLARATION

    <STRUKTUR><LNGE>

    In dieser Tabellenposition steht die ANZAHL DER FELDELEMENTE EINES
    STRUKTURELEMENTS.

    - Die vorgeschlagene STANDARDEINSTELLUNG ist "1". Das bedeutet:
      - Die LNGE des STRUKTURELEMENTS entspricht der DATENTYP-L„nge, wobei
        der angew„hlte Datentyp wieder eine Struktur sein kann.

    - Bei einer Eingabe "UNGLEICH 1" ist das zu definierende STRUKTURELEMENT
      ein FELD (ARRAY), das aus der in <LNGE> definierten ANZAHL VON FELD-
      ELEMENTEN besteht. Ein FELDELEMENT entspricht dem eingestellten DATENTYP,
      und kann daher auch eine Struktur sein.






    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.816
    V D - VARIABLENDEKLARATION

    <FUBAlias>

    - Fr jeden definierten FUNKTIONSBLOCK wird automatisch ein ALIAS-
      STRUKTURTYP angelegt, der folgenden Namen erh„lt:

      _$FUBNAME     _$ ........ Kennzeichnung eines Alias-Strukturtyps
                    FUBNAME ... identisch mit FUB-NAME

    - šber eine FUBAlias-Auswahlliste kann der zum FUB gewnschte Alias-
      Strukturtyp ausgew„hlt werden und somit der gewnschten Struktur
      (vom Anwender definierte PV) zugeordnet werden.

    - Die Alias-Struktur-Komponenten k”nnen ber <Datentyp> und Auswahl
      des angezeigten Alias-Strukturtyp-Namen (oberste Zeile=_$FUBNAME)
      angezeigt werden. Wird der Alias-Strukturtyp-Name in der obersten Zeile
      nicht angezeigt, mssen mittels <Datentyp><typdef> und durch Eingabe des
      Alias-Strukturtyp-Namens (_$FUBNAME) die Komponenten angezeigt werden.


    @803[ VD-Datentyp ]                    @800[     V D     ]                   @001[ šbersicht ]
.817
    V D - VARIABLENDEKLARATION

    - Standardm„áig wird der I/O-Typ <MERKER> vorgeschlagen. Beim dynamischen
      Gltigkeitsbereich wird automatisch <Referenz> parametriert, da beliebige
      Adreázuordnungen erst w„hrend der Laufzeit des Programms erfolgen.

    - Bei einer nachtr„glichen nderung des Gltigkeitsbereiches oder des
      Datentyps wird eine PRšFUNG des definierten I/O-Types durchgefhrt
      und gegebenenfalls eine entsprechende FEHLERMELDUNG in der LETZTEN
      Bildschirm-ZEILE ausgegeben:


    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Bei einer nachtr„glichen NICHT KOMPATIBLEN nderung von VD-Eingaben   º
    º wird eine entsprechende FEHLERMELDUNG oder WARNUNG in der letzten     º
    º Bildschirmzeile ausgegeben.                                           º
    º FATALE FEHLER k”nnen eine Korrektur der Eingabe erzwingen, da andern- º
    º falls die Eingabezeile NICHT VERLASSEN werden kann.                   º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    @818[ Fortsetzung ]                    @800[     V D     ]                   @001[ šbersicht ]
.818
    V D - VARIABLENDEKLARATION
    <PRšFKRITERIEN>:
      ÉÍÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍ»
      º    I/O-Typ --> Digital  ³  Analog  ³  Transp.  ³  Status  º
      ÌÍÍÍÍÑÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍ¹
      º D  ³ BIT    º   O.K.    ³   (*)    ³   (*)     ³   (*)    º
      º a  ³ INT8   º   (*)     ³   (*)    ³   O.K.    ³   O.K.   º
      º t  ³ INT16  º   (*)     ³   O.K.   ³   O.K.    ³   O.K.   º
      º e  ³ INT32  º   (*)     ³   (*)    ³   O.K.    ³   O.K.   º
      º n  ³ BYTE   º   (*)     ³   (*)    ³   O.K.    ³   O.K.   º
      º t  ³ WORD   º   (*)     ³   O.K.   ³   O.K.    ³   O.K.   º
      º y  ³ LONG   º   (*)     ³   (*)    ³   O.K.    ³   O.K.   º
      º p  ³ FLOAT  º   (*)     ³   (*)    ³   O.K.    ³   O.K.   º
      º    ³ typedefº   (*)     ³   (*)    ³   (*)     ³   (*)    º
      ÈÍÍÍÍÏÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍ¼

      O.K. ....... sinnvolle Kombination ohne Meldungsausgabe.
      (*)  ....... FEHLERMELDUNG oder WARNUNG erscheint !!
                   (z.B. wenn der Datentyp und der I/O-Typ nicht zusammen-
                   passen oder bei Mehrfachzugriff auf Digitalkan„le, ...)

    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.819
    V D - VARIABLENDEKLARATION

    I/O-Typ <MP/PP>:

    - nur bei Gltigkeitsbereich "rps_global" ausw„hlbar.
    - der gewnschte MP/PP wird ber ein POPUP-Fenster ausgew„hlt (Es stehen
      alle in der GDM definierten MP/PPs zur Auswahl).
    - Die CPU-Variable wird ber die Zuordnung des I/O-Typs "MP/PP" fr die
      KOPPLUNG an die MP/PP-Variable VORGEMERKT.

      Die eigentliche Verbindung von Variablen der CPU-Seite auf die MP/PP-Seite
      wird in der GDM mit Hilfe der "Crossreferenz" oder mittels des einge-
      zeichneten "Datenbaumes" zwischen CPU und MP/PP durchgefhrt.

      CPU-SEITE:    MP/PP-SEITE:
      pva_proc1  ³  pva        ÄÂÄÄÄÄ der Zusammenhang von MP1-Variablen muá zu
      var_lxy    ³  var_lxy    ÄÙ     den CPU-Variablen hergestellt werden
                 ÆÍÍÍÍÍÍÍÍÍÍÍÍÍÍ
      pva_proc2  ³  pva        ÄÂÄÄÄÄ der Zusammenhang von MP2-Variablen muá zu
      var_uv     ³  var_uv     ÄÙ     den CPU-Variablen hergestellt werden

    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.820
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  2003 Eingangs-/Ausgangskarte
    Die Spezifikation eines E/A Modul-Kanals wird ber folgendes Tabellen-
    Fenster durchgefhrt (eingetragene Werte stellen Standardvorgaben dar):

      ÄÄÄÄÄÄÄÄÄ2003-I/OÄÄÄÄÄÄÄÄÄÄ¿ zul„ssige Eingaben:
      ³ÉÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍ»³
      ³º Moduladr.  º 1          º³  0 .. 8 (Moduladresse) 
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³          ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ³º Modultyp   º            º³ ÄÄÄÄÄÄÄÄ´@828[  Modultypdefinition  ]³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³          ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
      ³º Kanal Nr.  º 1          º³  1 .. 8
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º Slot       º 1          º³  1 .. 4 (Steckplatz des Anpassungsmoduls)
      ³ÈÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍ¼³
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    Beim System B&R 2003 ist die Moduladresse durch den Controller und den
    Modulplatz bestimmt. Eine detaillierte Beschreibung der Moduladressierung
    finden Sie im "B&R SYSTEM 2000 B&R 2003 Anwenderhandbuch".

    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.821
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  2005 Eingangs-/Ausgangskarte
    Die Spezifikation eines E/A Modul-Kanals wird ber folgendes Tabellen-
    Fenster durchgefhrt (eingetragene Werte stellen Standardvorgaben dar):
                                                              ÚÄÄÄÄÄÄÄÄÄÄ¿
      ÄÄÄÄÄÄÄÄÄ2005-I/OÄÄÄÄÄÄÄÄÄÄ¿ zul„ssige Eingaben:       ³ Basis    ³
      ³ÉÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍ»³                           ³ Erweit.1 ³
      ³º Rckwand   º Basis      º³ ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´ Erweit.2 ³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³                           ³ Erweit.3 ³
      ³º Moduladr.  º 1          º³  1 .. 13 (Moduladresse)   ³ Erweit.4 ³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³                           ÀÄÄÄÄÄÄÄÄÄÄÙ
      ³º Modultyp   º            º³ ÄÄÄÄÄÄ¿      ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³        ÀÄÄÄÄÄÄ´@828[  Modultypdefinition  ]³
      ³º Kanal Nr.  º 1          º³  0 .. 255     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
      ³ÈÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍ¼³
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    Beim System B&R 2005 ist die Moduladresse durch den Steckplatz bestimmt.
    Die Z„hlung beginnt bei Steckplatz 3 mit der Moduladresse 1. Eine genaue
    Beschreibung dazu finden Sie im "B&R SYSTEM 2000 Hardware-Anwenderhandbuch".

    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.822
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  2010 Eingangs-/Ausgangskarte
    Die Spezifikation eines E/A Modul-Kanals wird ber folgendes Tabellen-
    Fenster durchgefhrt (eingetragene Werte stellen Standardvorgaben dar):

      ÄÄÄÄÄÄÄÄÄ2010-I/OÄÄÄÄÄÄÄÄÄÄ¿ zul„ssige Eingaben:
      ³ÉÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍ»³
      ³º Moduladr.  º 1          º³  1 .. 99 (Moduladresse)
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³         ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ³º Modultyp   º            º³ ÄÄÄÄÄÄÄ´@828[  Modultypdefinition  ]³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³         ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
      ³º Kanal Nr.  º 1          º³  0 .. 255
      ³ÈÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍ¼³
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    Beim System B&R 2010 ist die Moduladresse durch den Steckplatz bestimmt.
    Die Z„hlung der Adressierung beginnt bei Steckplatz 1 des I/O-Busses mit
    der Moduladresse 1. Eine detaillierte Beschreibung der Moduladressierung
    finden Sie im "B&R SYSTEM 2000 Hardware-Anwenderhandbuch".


    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.823
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  Remote Eingangs-/Ausgangskarte
    Die Spezifikation eines E/A Modul-Kanals wird ber folgendes Tabellen-
    Fenster durchgefhrt (eingetragene Werte stellen Standardvorgaben dar):

      ÄÄÄÄÄÄÄÄREMOTE-I/OÄÄÄÄÄÄÄÄÄ¿ zul„ssige Eingaben:
      ³ÉÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍ»³
      ³º Master     º 1          º³  1 .. 99
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º Slave      º 1          º³  1 .. 99                    ÚÄÄÄÄÄÄÄÄ¿
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³                             ³  2010  ³
      ³º Slavetyp   º            º³ ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´  2005  ³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³                             ³  2003  ³
      ³º Moduladr.  º 1          º³  1 .. 99 (Moduladresse)     ÀÄÄÄÄÄÄÄÄÙ
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³               ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ³º Modultyp   º            º³ ÄÄÄÄÄÄÄÄÄÄÄÄÄ´@828[  Modultypdefinition  ]³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³               ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
      ³º    ...     º    ...     º³


                                     @824[ Fortsetzung ]                   @001[ šbersicht ]
.824
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  Remote Eingangs-/Ausgangskarte

      ³º    ...     º    ...     º³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º Kanal Nr.  º 1          º³  0 .. 255
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º Slot       º 1          º³  1 .. 4 (Steckplatz des Anpassungsmoduls)
      ³ÈÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍ¼³
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    2010 ... Die Moduladresse ist durch den Steckplatz bestimmt. Die Z„hlung
             beginnt bei Steckplatz 1 mit der Moduladresse 1.
    2005 ... Die Moduladresse ist durch den Steckplatz bestimmt. Die Z„hlung
             beginnt bei Steckplatz 3 mit der Moduladresse 1.
    2003 ... Die Moduladresse ist durch den Controller und den Modulplatz be-
             stimmt.
    Detaillierte Informationen zur Moduladressierung (B&R 2010, B&R 2005 und
    B&R 2003) finden Sie im "B&R SYSTEM 2000 Hardware-Anwenderhandbuch" und
    im "B&R SYSTEM 2000 B&R 2003 Anwenderhandbuch".

    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.825
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  CAN Eingangs-/Ausgangskarte
    Die Spezifikation eines E/A Modul-Kanals wird ber folgendes Tabellen-
    Fenster durchgefhrt (eingetragene Werte stellen Standardvorgaben dar):

      ÄÄÄÄÄÄÄÄÄÄCAN-I/OÄÄÄÄÄÄÄÄÄÄ¿ zul„ssige Eingaben:
      ³ÉÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍ»³
      ³º@826[ Bus Nr.    ]º 1          º³  1 .. 8 (Bus-Nummer)
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º@826[ Slave      ]º 1          º³  1 .. 32 (Knotennummer des CAN-Slaves)
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º@827[ Moduladr.  ]º 1          º³  1 .. 8 (Moduladresse)
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³               ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
      ³º Modultyp   º            º³ ÄÄÄÄÄÄÄÄÄÄÄÄÄ´@828[  Modultypdefinition  ]³
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³               ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
      ³º Kanal Nr.  º 1          º³  1 .. 8
      ³ÇÄÄÄÄÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄ¶³
      ³º@827[ Slot       ]º 1          º³  1 .. 4 (Steckplatz des Anpassungsmoduls)
      ³ÈÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍ¼³
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.826
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  CAN Eingangs-/Ausgangskarte

    <Bus Nr.>:
    Mit der Bus-Nummer k”nnen Sie den gewnschten CAN-Bus ausw„hlen. Der ge-
    w„hlte CAN-Bus muá in der Systemkonfiguration parametriert sein. Siehe
    dazu Parameter @961[<CAN I/O>] des RPS-Konfigurationsmoduls (auch "$$sysconf"-
    Modul genannt).

    <Slave>:
    In diesem Eingabefeld muá die Knotennummer des CAN-Slaves angegeben werden.









                                       @825[ Zurck ]                      @001[ šbersicht ]
.827
    V D - VARIABLENDEKLARATION

    <MODULDEFINITION>:  CAN Eingangs-/Ausgangskarte

    <Moduladr.>:
    2010 ... Die Moduladresse ist durch den Steckplatz bestimmt. Die Z„hlung
             beginnt bei Steckplatz 1 mit der Moduladresse 1.
    2005 ... Die Moduladresse ist durch den Steckplatz bestimmt. Die Z„hlung
             beginnt bei Steckplatz 3 mit der Moduladresse 1.
    2003 ... Die Moduladresse ist durch den Controller und den Modulplatz be-
             stimmt.
    Detaillierte Informationen zur Moduladressierung (B&R 2010, B&R 2005 und
    B&R 2003) finden Sie im "B&R SYSTEM 2000 Hardware-Anwenderhandbuch" und
    im "B&R SYSTEM 2000 B&R 2003 Anwenderhandbuch".

    <Slot>:
    Steckplatz des Anpassungsmoduls. Dieser Wert ist nur bei Anpassungsmodulen
    von Bedeutung. Bei allen anderen Modulen muá der Standardwert "1" bei-
    behalten werden.


                                       @825[ Zurck ]                      @001[ šbersicht ]
.828
    V D - VARIABLENDEKLARATION

    <MODULTYPDEFINITION>:  šbersicht aller Modultypen fr E/A-Karten

      ÚÄ Modultyp Ä¿
      ³ Digit. In  ³  ... Digitale Eingangskarte
      ³ Digit. Out ³  ... Digitale Ausgangskarte
      ³ Analog In  ³  ... Analog Eingangskarte
      ³ Analog Out ³  ... Analog Ausgangskarte
      ³ Transp.In  ³  ... BIT-Feld-Zugriff ber analogen Datentyp auf Eing„nge
      ³ Transp.Out ³  ... BIT-Feld-Zugriff ber analogen Datentyp auf Ausg„nge
      ³ Status In  ³  ... Statusbereich auslesen (Feldklemmenstatus lesen, ...)
      ³ Status Out ³  ... Statusbereich beschreiben (Z„hler rcksetzen, ...)
      ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Es erfolgt NUR EINE ANZEIGE von GšLTIGEN MODULTYPEN, die Anhand von   º
    º Prfkriterien ermittelt werden (abh„ngig von Prozessortyp, Gltig-    º
    º keitsbereich, Datentyp).                                              º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @829[ Fortsetzung ]                    @800[     V D     ]                   @001[ šbersicht ]
.829
    V D - VARIABLENDEKLARATION

    Mit Hilfe der Hauptmen-Funktion <Einstellungen><Editor><Variable> k”nnen
    zwei verschiedene Anzeigem”glichkeiten eingestellt werden:

    <I/O-Krzel><AUS>:
    Textuelle Anzeige des Modultyps in folgender Form: 2003-I/O, 2005-I/O,
    2010-I/O, usw. Ist diese Option eingestellt, k”nnen Sie die HW-Zuordnung
    der PVs nicht direkt, sondern nur ber ein Popup-Auswahlfenster eingeben
    (Standardeinstellung).

    <I/O-Krzel><EIN>:
    Anzeige der HW-Zuordnung:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ @830[Modul-Typ]
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ @830[Bus-Typ]
    ³³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ @830[B&R-Hardware-Typ]
    ³³³ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ @831[genaue Adresse] (2, 3, 4 oder 5 Ziffern je nach Typ)
    ³³³ ³
    @830[mBx].@831[y1.y2.y3.y4.y5]

    @805[ I/O-Typ ]                        @800[     V D     ]                   @001[ šbersicht ]
.830
    V D - VARIABLENDEKLARATION

    Modul-Typ:        I .... Digit.In oder Analog In
                      Q .... Digit.Out oder Analog Out
                      IT ... Transp.In
                      QT ... Transp.Out
                      IS ... Status In
                      QS ... Status Out

    Bus-Typ:          P ... lokaler I/O-Bus
                      R ... Remote-Bus
                      C ... CAN-Bus

    B&R-Hardware-Typ: 1 ... B&R 2010
                      3 ... B&R 2003
                      5 ... B&R 2005

    Der B&R-Hardware-Typ wird nur beim lokalen I/O-Bus und beim Remote-Bus
    (nicht aber beim CAN-Bus) angezeigt.


                                       @829[ Zurck ]                      @001[ šbersicht ]
.831
    V D - VARIABLENDEKLARATION

Genaue Adresse (2, 3, 4 und 5 Ziffern je nach Typ):

ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍ»
º                    º mBx. ³   y1.   ³   y2.   ³   y3.   ³   y4.   ³   y5    º
ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍ¹
ºlokaler I/O-Bus 2010º mP1. ³Moduladr.³Kanal Nr.³         ³         ³         º
ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍ¹
ºlokaler I/O-Bus 2005º mP5. ³Rckwand ³Moduladr.³Kanal Nr.³         ³         º
ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍ¹
ºlokaler I/O-Bus 2003º mP3. ³Moduladr.³Slot (*) ³Kanal Nr.³         ³         º
ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍ¹
ºRemote I/O-Bus      º mRx. ³Master   ³Slave    ³Moduladr.³Slot (*) ³Kanal Nr.º
ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍ¹
ºCAN I/O-Bus         º mC   ³Bus Nr.  ³Slave    ³Moduladr.³Slot (*) ³Kanal Nr.º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍ¼

(*)...Die Steckplatznummer (Slot) wird nur bei Anpassungsmodulen angezeigt.


                                     @832[ Fortsetzung ]                   @001[ šbersicht ]
.832
    V D - VARIABLENDEKLARATION

    Beispiel: Remote I/O-Bus
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Modul-Typ: digitales oder analoges Ausgangsmodul
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Bus-Typ: Remote-Bus
    ³³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ B&R-Hardware-Typ: B&R 2010
    ³³³ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Master-Nummer 2
    ³³³ ³ ÚÄÄÄÄÄÄÄÄÄÄÄÄ Slave-Adresse 3 (Adresse der Slave-Station)
    ³³³ ³ ³ ÚÄÄÄÄÄÄÄÄÄÄ Moduladresse 2
    ³³³ ³ ³ ³ ÚÄÄÄÄÄÄÄÄ Kanalnummer 7
    QR1.2.3.2.7

    Beispiel: CAN I/O-Bus
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Modul-Typ: digitales oder analoges Eingangsmodul
    ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Bus-Typ: CAN-Bus
    ³³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Bus-Nummer 2
    ³³³ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Knotennummer 2 (Knotennummer des CAN-Slaves)
    ³³³ ³ ÚÄÄÄÄÄÄÄÄÄÄÄÄ Moduladresse 4
    ³³³ ³ ³ ÚÄÄÄÄÄÄÄÄÄÄ Slot 1 (Steckplatz des Anpassungsmoduls)
    ³³³ ³ ³ ³ ÚÄÄÄÄÄÄÄÄ Kanalnummer 2
    IC2.2.4.1.2
                                       @829[ Zurck ]                      @001[ šbersicht ]
.833
    V D - VARIABLENDEKLARATION

    Der <LANGNAME> kann nach den folgenden Regeln aus alphanumerischen
    Zeichen und Ziffern zusammengesetzt sein:

    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º  Buchstaben          ³  Zeichen             ³  Ziffern              º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
    º 'A'bis'Z', 'a'bis'z' ³       '_', '$'       ³       '0'bis'9'       º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

     - Das ERSTE ZEICHEN muá ein BUCHSTABE sein.

     - Der Langname kann bis zu 35 ZEICHEN LANG sein.

     - Der Langname muá aus einer DURCHGEHENDEN ZEICHENKETTE bestehen.





    @807[ VD-Langname ]                    @800[     V D     ]                   @001[ šbersicht ]
.834
    V D - POPUP GšLTGKEITSBEREICH

    ÚÄ GšLTIG ÄÄÄÄ¿
    ³ tk_global   ³  ...  Taskklassenglobale Variable
    ³ rps_global  ³  ...  RPS-globale Variable
    ³ proc_global ³  ...  Prozessor-globale Variable
    ³ dynamisch   ³  ...  Dynamisch (bei Adreáoperationen)
    ³ lokal       ³  ...  Lokale Variable
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    - Der GšLTIGKEITSBEREICH kann in der VD ber den ANFANGSBUCHSTABEN des
      gewnschten Datentyps oder mit Hilfe eines POPUP-Fensters mit MAUS oder
      TASTATUR ausgew„hlt werden.

    - Es stehen NUR KORREKTE AUSWAHLM™GLICHKEITEN zur Verfgung (Auswahl ber
      Anfangsbuchstabe und Anzeige in POPUP), die von den brigen Angaben der
      VD und dem Prozessortyp abh„ngig sind !!




                                     @800[     V D     ]                   @001[ šbersicht ]
.835
    V D - POPUP DATENTYP

    ÚÄ DATENTYP Ä¿
    ³ BIT        ³   1 Bit   , Variable mit dem Wert 0 oder 1
    ³ INT8       ³   7 Bit+VZ, Werte von        -128 bis        +127
    ³ INT16      ³  15 Bit+VZ, Werte von      -32768 bis      +32767
    ³ INT32      ³  31 Bit+VZ, Werte von -2147483648 bis +2147483647
    ³ BYTE       ³   8 Bit   , Werte von           0 bis         255
    ³ WORD       ³  16 Bit   , Werte von           0 bis       65535
    ³ LONG       ³  32 Bit   , Werte von           0 bis  4294967295
    ³ FLOAT      ³  32 Bit   , Werte von 1,18E-38(abs) bis 3,4E+38(abs)
    ³ typedef    ³  Benutzerdefinierte Datentypen:  STRUKTUREN
    ³ FUBAlias   ³  Zugriff auf FUB Ein- bzw. Ausg„nge ber einen Strukturtyp
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ

    - Der DATENTYP kann in der VD ber den ANFANGSBUCHSTABEN des gewnschten
      Datentyps oder mit Hilfe eines POPUP-Fensters mit MAUS oder TASTATUR
      ausgew„hlt werden.

    - Es stehen NUR KORREKTE AUSWAHLM™GLICHKEITEN zur Verfgung.

                                     @800[     V D     ]                   @001[ šbersicht ]
.836
    V D - POPUP I/O-TYP

    ÚÄ I/O Typ ÄÄ¿
    ³ MERKER     ³  ... STANDARDEINSTELLUNG: Zugriff auf SPEICHERSTELLE
    ³ 2010-I/O   ³  ... 2010 Eingangs-/Ausgangskarte
    ³ REMOTE-I/O ³  ... REMOTE Eingangs-/Ausgangskarte
    ³ 2005-I/O   ³  ... 2005 Eingangs-/Ausgangskarte
    ³ IP         ³  ... Intelligentes Peripherie-Modul
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ

    - Der I/O-TYP kann DIREKT in der VD ber den ANFANGSBUCHSTABEN des ge-
      wnschten I/O-TYPs oder mit Hilfe eines POPUP-Fensters mit MAUS oder
      TASTATUR ausgew„hlt werden.

    - Es stehen NUR KORREKTE AUSWAHLM™GLICHKEITEN zur Verfgung (Auswahl ber
      Anfangsbuchstabe und Anzeige in POPUP), die von den brigen Angaben der
      VD (Gltigkeitsbereich, Datentyp) abh„ngig sind !!




                                     @800[     V D     ]                   @001[ šbersicht ]
.837
    V D - VARIABLENDEKLARATION

    Die <VARIABLENDEKLARATION> = <VD> dient zur Definition der verwendeten
    Prozeávariablen (PVs). Symbolische Namen, Datentypen, ... werden in einem 
    TABELLENEDITOR eingegeben:
 ÉÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍsourcename.vdÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
 º Name     º gltig   ³ Datentyp ³ L„nge ³ I/O Typ  ³Init-Wert ³ Langname     º
 ÌÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
 ºpv_1      ºtk_global ³BYTE      ³1      ³MERKER    ³*remanent ³              º
 ºpv_2      ºtk_global ³LONG      ³1      ³MERKER    ³*remanent ³              º
 ºpv_arr1   ºtk_global ³WORD      ³5      ³MERKER    ³*remanent ³              º
 ºpv_str1   ºtk_global ³typedef   ³1      ³MERKER    ³*remanent ³              º
 ºpv_dyn    ºdynamisch ³LONG      ³1      ³Referenz  ³*remanent ³              º
 :          :          :          :       :          :          :              :
 ÈÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    <AUFRUF>    - Die VD des aktuellen SOURCE-CODES oder eines GDM-Symbols wird
                  mit der Hauptmen-Funktion <OBJEKT><VARIABLE> aufgerufen.
                - Existieren beim SPEICHERN eines SOURCE-CODES nicht definierte
                  PVs, werden Sie gefragt, ob die VD aufgerufen werden soll.

    @838[ Fortsetzung ]                    @800[     V D     ]                   @001[ šbersicht ]
.838
    V D - VARIABLENDEKLARATION

    <KONVENTIONEN fr AUSWAHLM™GLICHKEITEN>
      Alle vorgeschlagenen POPUP-Eingaben basieren auf bereits definierten
      VD-Eingaben. Anhand von Prfkriteren stehen prinzipiell NUR KORREKTE
      AUSWAHLM™GLICHKEITEN zur Verfgung.
    ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
    º Bei einer nachtr„glichen NICHT KOMPATIBLEN nderung von VD-Eingaben   º
    º wird eine entsprechende FEHLERMELDUNG oder WARNUNG in der letzten     º
    º Bildschirmzeile ausgegeben.                                           º
    º FATALE FEHLER k”nnen eine KORREKTUR der Eingabe ERZWINGEN, da andern- º
    º falls die Eingabezeile NICHT VERLASSEN werden kann.                   º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


    <KONVENTIONEN der ANZEIGE>
      - Fr die globale Darstellung des Tabelleneditors bietet die Hauptmen-
        Funktion <Einstellungen><Editor><Variable> verschiedene M”glichkeiten.
      - Eine gefilterte Anzeige der PVs kann mit der Fuázeilen-Funktion
        <F1 Filter> durchgefhrt werden.

                                     @800[     V D     ]                   @001[ šbersicht ]
.839
    V D - BEDIENUNG

    EDITOR-BEDIENUNG     :  Die Bedienung der Variablendeklaration erfolgt mit
                            TASTENKOMBINATIONEN oder MAUS:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <INS>                  ³ Wechselweises Umschalten zwischen EINFšGE-/   º
  º                        ³ šBERSCHREIB-Modus (VD-Feld "L„nge"/"Langname")º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ANFANGSBUCHSTABE>     ³ - im VD-Feld "Name":                          º
  º                        ³   Anw„hlen der entsprechenden PV-Namen, die   º
  º                        ³   mit dem gedrckten Buchstaben beginnen.     º
  º                        ³ - im VD-Feld "gltig"/"Datentyp"/"I/O-Typ":   º
  º                        ³   DIREKTAUSWAHL eines POPUP-Eintrages         º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @840[ Fortsetzung ]                    @800[     V D     ]                   @001[ šbersicht ]
.840
    V D - BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende 
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein VD-Feld der Zeile nach links/rechts       º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                   º
  º <ENTER>                ³ N„chstes VD-Feld anw„hlen (auch in Folgezeile)º
  º <TAB>                  ³ N„chstes VD-Feld in aktueller Zeile anw„hlen  º
  º <TAB>                 ³ Vorhergehendes VD-Feld der Zeile anw„hlen     º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf erstes/letztes VD-Feld der Zeile   º
  º <HOME>/<END>         ³ Cursor in erste/letzte VD-Zeile der Seite     º
  º ^<HOME>/^<END>         ³ Cursor in erste/letzte VD-Zeile               º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

                                     @800[     V D     ]                   @001[ šbersicht ]
.841
    V D - FUSSZEILE

    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick angew„hlt werden:

 ÉÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
 º@842[1 Filter]³                                                                 º
 ÈÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

   <F1> <Filter>:  SETZEN oder L™SCHEN eines Anzeigefilters

                   Wenn bereits ein Filter definiert wurde, erscheint ein
                   POPUP-Fenster zur Auswahl:
                   ÚÄÄÄÄÄÄÄÄÄÄ¿
                   ³ Setzen   ³ ... NDERN des gesetzten Filters
                   ³ L”schen  ³ ... AUSSCHALTEN der Filter-Funktion
                   ÀÄÄÄÄÄÄÄÄÄÄÙ     (Anzeige aller PVs)




                                     @800[     V D     ]                   @001[ šbersicht ]
.842
    V D - FUSSZEILEN FUNKTIONEN

    <FILTER>    Definition des Anzeige-Filters:
   ÚÄÄÄÄÄÄÄÄÄSuchmaskeÄÄÄÄÄÄÄÄÄ¿
   ³                           ³
   ³ Name    : *               ³  ... Namens-Filter (* ... alle)
   ³ gltig  : alle           ³  ... Gltigkeitsbereich mittels POPUP ausw„hlen
   ³ Datentyp: alle           ³  ... Datentyp mittels POPUP ausw„hlen
   ³ I/O Typ : alle           ³  ... I/O-Typ mittels POPUP ausw„hlen
   ³                           ³
   ³  [   OK   ]  [ Cancel ]   ³  ... Best„tigungsfelder
   ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    - Geben Sie im Feld NAME einen Filter ein, der beliebig die Platzhalter-
      zeichen "*" oder "?" enthalten darf (* ... alle PVs; e* ... alle PVs,
      die mit 'e' beginnen; ???max_? ... alle PVs die ab der 4.Stelle mit
      'max_' beginnen; usw.)
    - Es k”nnen PVs mit bestimmten Gltigkeitsbereich, Datentyp oder I/O-Typ
      angezeigt werden.
    - Die ANZEIGE-KRITERIEN k”nnen beliebig gemischt werden.

                                     @800[     V D     ]                   @001[ šbersicht ]
.843
    V D - STRUKTURDEKLARATION

    @812[< ALLGEMEINES         >]       AUFRUF und ALLGEMEINES

     ÉÍÍÍÍÍÍÍÍÍ< comp_name1 >ÍÍÍÍÍÍÍÍ»
    ÉÍÍÍ< comp_name1.comp_name2 >ÍÍÍ»º
    º@813[ KOMPONENTE ]³@814[ Datentyp ]³@815[ L„nge ]ºº
    ÌÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍ¹º
          :           :         :
    <KOMPONENTE>  ANZEIGE/EINGABE des Komponenten-Namen
    <Datentyp>    DATENTYP der Struktur-Komponente (bis zu 5 Unterkomponenten)
    <L„nge>       ANZAHL der Datentypen (ungleich 1 => Komponente ist ein Feld)


    @844[< Fuázeilenmen     >]    - nur bei Hauptmen-Funktion <Projekt><Datentyp>
    @846[< Popup-Men        >]    - nur bei VD-Definition <Datentyp><typedef>

    @845[< Hotkey-Tasten     >]    - Sondertasten-Belegung fr STRUKTURDEKLARATION

    @847[< Tastaturkommandos >]    - WECHSELN zwischen den EINGABEFELDERN

                                     @800[     V D     ]                   @001[ šbersicht ]
.844
    V D - STRUKTURDEKLARATION FUSSZEILE

    Im <DATENTYP-EDITOR> k”nnen unabh„ngig von einem ge”ffneten Objekt, Daten-
    typen ERSTELLT, EDITIERT und EXPORTIERT werden. Folgende Funktionen der 
    FUSSZEILE stehen zur Verfgung:

ÉÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º        ³2 Bewegen³3 Name³4 Komp +³5 Komp -³6 L”schen³                        º
ÈÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

   <F2> <Bewegen>   :  Verschieben des aktuellen Strukturelementes
   <F3> <Name>      :  ndern des STRUKTURELEMENT-NAMEN
   <F4> <Komp +>    :  EINFšGEN eines NEUEN Strukturelementes
   <F5> <Komp ->    :  L™SCHEN des aktuellen Strukturelementes
   <F6> <L”schen>   :  L™SCHEN des gesamten DATENTYPs (mit Nachfrage)

    - Die PROJEKTSPEZIFISCHE DATENTYPEN werden in der PROJEKT-DATENBANK
      gespeichert, und k”nnen im ganzen Projekt bentzt werden.
    - Die erstellten DATENTYPEN k”nnen mit dem DATENTYP-Editor oder ber
      <typedef> der VARIABLENDEKLARATION editiert werden.

    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.845
    V D - STRUKTURDEKLARATION HOTKEYS

    HOTKEY-TASTEN:          Folgende Hotkey-Tasten stehen zur Verfgung:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <INS>                  ³ EINGEBEN/NDERN des KOMPONENTEN-NAMEN         º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <DEL>                  ³ L™SCHEN der angew„hlten STRUKTUR-KOMPONENTE,  º
  º                        ³ und der gesamten Komponenten-Definition       º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ANFANGSBUCHSTABE>     ³ - im VD-Feld "Datentyp":                      º
  º                        ³   DIREKTAUSWAHL eines POPUP-Eintrages         º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼






    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.846
    V D - STRUKTURDEKLARATION POPUP-MENš

    Bei der DATENTYP-ERSTELLUNG mittels der Popup-Funktion <typedef> in der
    <VARIABLENDEKLARATION> stehen folgende Popup-Funktionen zur Verfgung:

  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ Einfgen    ³  ... EINFšGEN eines NEUEN Strukturelementes
  ³ L”schen     ³  ... L™SCHEN des aktuellen Strukturelementes
  ³ Bewegen     ³  ... Verschieben des aktuellen Strukturelementes
  ³ Name        ³  ... ndern des STRUKTURELEMENT-NAMEN
  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


    - Die PROJEKTSPEZIFISCHEN DATENTYPEN werden in der PROJEKT-DATENBANK
      gespeichert und k”nnen im ganzen Projekt bentzt werden.
    - Die erstellten DATENTYPEN k”nnen mit dem DATENTYP-Editor oder ber
      <typedef> der VARIABLENDEKLARATION editiert werden.




    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.847
    V D - STRUKTURDEKLARATION BEDIENUNG

    CURSOR POSITIONIERUNG:  Die CURSOR-Positionierung kann mit der Bildlauf-
                            leiste und einem Mausklick oder ber folgende 
                            Tastenkombinationen durchgefhrt werden:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                    º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein VD-Feld der Zeile nach links/rechts         º
  º <UP>/<DOWN>            ³ Eine Zeile aufw„rts/abw„rts                     º
  º <ENTER>                ³ N„chstes VD-Feld ausw„hlen (auch in Folgezeile) º
  º <TAB>                  ³ N„chstes VD-Feld in aktueller Zeile ausw„hlen   º
  º <TAB>                 ³ Vorhergehendes VD-Feld der Zeile ausw„hlen      º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                     º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf erstes/letztes VD-Feld der Zeile     º
  º <HOME>/<END>         ³ Cursor in erste/letzte VD-Zeile der Seite       º
  º ^<HOME>/^<END>         ³ Cursor in erste/letzte VD-Zeile                 º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @843[ Strukturdeklaration ]            @800[     V D     ]                   @001[ šbersicht ]
.850
    P V - MONITOR

    Mit dem PV-MONITOR besteht die M”glichkeit, VARIABLEN eines auf der
    RPS laufenden Tasks in einer Liste ANZUZEIGEN, in der eingestellten
    Abtastrate zu AKTUALISIEREN, und zu BESCHREIBEN.

 ÄÄÄÄÄÄÄÄÄÄÄÄÄÄlistenname.pvmÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
 ³³@852[Name          ]³@853[ gltig ]³@854[ Wert              ]³@855[F]³³
 ³ÆÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍµ²
 ³³  ÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÅ²ÄÄ VARIABLEN-NAME
 ³³              ³  ÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÅ²ÄÄ PV-Gltigkeitsbereich
 ³³              ³        ³ ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÅ²ÄÄ WERT der Variable
 ³³              ³        ³                   ³ ³²   (mehrere Anzeige-FORMATE)
 ³³              ³        ³                   ³ÅÅÄÄ Zustand der FORCE-Funktion
 ÀÄ< taskname : Run TK1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ
      ÀÄÄÄÄÄÄÄÄ Anzeige des Tasknamen, Zustand und Taskklasse

    @851[< PV-Monitor-Aufruf    >]  - Aufruf des PV-Monitors
    @856[< PV-Monitor-Bedienung >]  - Bedienung des PV-Monitors
                              - PV-Monitor-Funktionen und Tastenkombinationen

                                @564[ Source-Level-Debugger ]              @002[ DEBUGGING ]
.851
    P V - MONITOR AKTIVIERUNG

    ==> Aufruf ber die HM-Funktion <Objekt><Monitor> oder direkt ber die
        Funktionstaste <F11>. VORHER den gewnschten Task ausw„hlen:
        - MARKIEREN des gewnschten TASK-SYMBOLS in der GDM
        - oder LADEN des gewnschten Tasks in den entsprechenden Editor

    ==> Aktivierung des PV-Monitors mittels <Datei><™ffnen> einer *.pvm Datei

    ==> Umschalten der Bezugstask im PV-Monitor mit der Funktion <Task..>:
        - Es werden alle auf der RPS existierenden Tasks in einem POPUP_Fenster
          angezeigt. Der gewnschte Task kann aus Liste ausgew„hlt werden.

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º - Existiert auf der RPS ein Task mit dem gleichen Namen, der jedoch nicht º
  º   kompatibel zum DEBUG-Task ist, wird eine Meldung angezeigt, die Sie be- º
  º   st„tigen mssen. Anschlieáend wird der Task in der RPS korrigiert !!    º
  º - Existiert eine "*.pvm"-Liste mit dem Task-Namen, wird diese AUTOMATISCH º
  º   in das PV-Monitor-Fenster geladen.                                      º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @850[ Fortsetzung ]                                                    @002[ DEBUGGING ]
.852
    P V - MONITOR

    Eingabe-/Anzeige-Feld:  <Name>

    - In dieser Spalte werden alle VARIABLEN-NAMEN angezeigt, die mit der ein-
      gestellten Abtastrate kontinuierlich von der RPS gelesen (aktualisiert)
      werden (siehe auch HM-Funktion <Einstellungen><On-Line>).

    - Sie k”nnen eine BELIEBIGE VARIABLEN-LISTE zusammenstellen. Diese Liste
      (*.pvm) kann gespeichert und zu einem beliebigen Zeitpunkt wieder ver-
      wendet werden.
      Erweitern der Liste mittels PV-Auswahlliste (nur in Spalte <Name>) mit:
      - Hotkey <INS>
      - Doppel-Klick der Maus

    - Wenn bereits eine VARIABLEN-LISTE (*.pvm) mit dem Task-Namen vorhanden
      ist (gespeichert oder in GDM generiert), dann wird diese Variablenliste
      beim AUFRUF des PV-Monitor AUTOMATISCH GELADEN !

    - Anzeige von PV-Informationen ber die <SPACE>-Taste.

    @853[ Fortsetzung ]                    @850[ PV-Monitor ]                    @002[ DEBUGGING ]
.853
    P V - MONITOR

    Anzeige-Feld:  <gltig>

    - Die Anzeige dieser Spalte ist optional und kann ein- oder ausgeblendet
      werden (siehe HM-Funktion <Einstellungen><PV-Monitor>).

    - Der Gltigkeitsbereich der PV wird in dieser Spalte angezeigt (RPS_global,
      tk_global, ...). Bei einer lokalen PV wird direkt der entsprechende Task-
      name, fr den die PV gltig ist, angezeigt.

    - Der eigentliche PV-Name wird erst mit dieser Anzeige klar identifiziert,
      da gleichnamige PVs verschiedener Tasks gleichzeitig in einem PV-Monitor
      Fenster angezeigt werden k”nnen.

      z.B.: <Name>   <gltig>   <Wert>
             i        task_1     $03    ... lokale PV in Task task_1
             i        task_2     $00    ... lokale PV in Task task_2
             i        TC#3       $01    ... tk_globale PV der TC#3


    @854[ Fortsetzung ]                    @850[ PV-Monitor ]                    @002[ DEBUGGING ]
.854
    P V - MONITOR

    Anzeige-Feld:  <Wert>

    - In dieser Spalte werden die AKTUELLEN VARIABLEN-WERTE angezeigt und mit
      der EINGESTELLTEN ABTASTRATE kontinuierlich von der RPS gelesen (aktuali-
      siert).

    - Wenn die VARABLEN-LISTE im PV-Monitor GESTOPPT ist, werden die WERTE
      NICHT mehr AKTUALISIERT !!

    - Die Werte k”nnen in VERSCHIEDENEN ANZEIGE-FORMATEN dargestellt werden:

      ÚÄÄÄÄÄÄÄÄÄÄ¿  Eingabe-/Anzeigeformat  Allgemeines
      ³ Dezimal  ³                          ... immer m”glich
      ³ Hex      ³   $                      ... immer m”glich
      ³ Bin„r    ³   %%                      ... immer m”glich
      ³ Char     ³   ' '                    ... nur bei BYTE-PVs
      ³ String   ³   " "                    ... nur bei Array-Namen m”glich
      ³ Referenz ³   Adr.:                  ... Referenz-Adresse dynamische PV
      ÀÄÄÄÄÄÄÄÄÄÄÙ
    @855[ Fortsetzung ]                    @850[ PV-Monitor ]                    @002[ DEBUGGING ]
.855
    P V - MONITOR

    Anzeige-Feld:  <F> Force

    - Es k”nnen nur digitale PVs (Eing„nge/Ausg„nge) geforct werden.
    - Diese Spalte zeigt das FORCEN einer Variable an:
            ... PV kann nicht geforct werden
      d     ... dynamische PV (kann nicht geforct werden)
      .     ... PV kann geforct werden
      f     ... PV wird geforct, der Force-Wert wird in Spalte "Wert" angezeigt

    - Das Forcen ist ein ZWINGENDES BESCHREIBEN einer Variable mit dem ange-
      gebenen WERT. Die Variable wird dabei gezwungen, genau diesen Wert beizu-
      behalten. Beschreibt ein Task eine geforcte PV, dann bleibt der ge„nderte
      Wert NUR WHREND des aktuellen TASK-DURCHLAUFES ERHALTEN. Anschlieáend
      wird die PV automatisch mit dem FORCE-Wert šBERSCHRIEBEN.
    * Sobald eine PV auf der RPS geforct wird, leuchtet die FORCE-LED der CPU !
    * Die FORCE-Funktion ALLER GEFORCTEN Variablen, kann mit der HM-Funktion
      <System><Info><Forcen aus> DEAKTIVIERT werden.


                                     @850[ PV-Monitor ]                    @002[ DEBUGGING ]
.856
    P V - MONITOR  BEDIENUNG

    Wenn eine GLEICHNAMIGE Variablen-Liste (*.pvm) wie der best„tigte oder
    ausgew„hlte Task existiert, wird diese automatisch GELADEN.
    Der NAME einer GELADENEN LISTE wird im Fensterrahmen angezeigt. Besteht
    KEINE ENTSPRECHENDE LISTE, wird ein LEERES PV-Monitor Fenster mit dem 
    Standard-Fenstertitel "taskname.pvm" ge”ffnet.

    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄtaskname.pvmÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³ Die BEDIENUNG des PV-Monitors wird ber Funktionen der Fuázeile,    ³
    ³ der PV-Auswahlliste und den Hotkey-Tasten durchgefhrt:             ²
    ³                                                                     ²
    ³ @859[< PV-Auswahlliste   >]     - Einfgen von PVs oder Elementen         ²
    ³ @862[< PV-Infobox        >]     - Anzeige von PV-Informationen            ²
    ³                                                                     ²
    ³ @863[< Fuázeilenmen     >]     - Bedienung des PV-Monitors               ²
    ³ @857[< Hotkey-Tasten     >]     - PV-Monitor SONDER-TASTENBELEGUNG        ²
    ³ @858[< Tastaturkommandos >]     - Allgemeine Editor-Bedienung             ²
    ³                                                                     ³
    ÀÄ< Taskname : Run TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ

                                     @850[ PV-Monitor ]                    @002[ DEBUGGING ]
.857
    P V - MONITOR  BEDIENUNG

    HOTKEY-TASTEN        :  AUSFšHREN einer FUNKTION anhand von SONDERTASTEN:

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Hotkey                 ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <INS>                  ³ EINFšGEN einer neuen Variable in die Liste,   º
  º                        ³ mittels PV-Auswahlbox                         º
  º <DEL>                  ³ L™SCHEN der ausgew„hlten Variable             º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <SPACE>                ³ Anzeige der PV-Infobox                        º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼








    @858[ Fortsetzung ]                 @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.858
    P V - MONITOR  BEDIENUNG

    CURSOR POSITIONIERUNG:   CURSOR-Positionierung mittels Tastenkombinationen
                             oder Bildlaufleiste und Mausklick:
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <UP>/<DOWN>            ³ Eine PV-Monitor-Zeile aufw„rts/abw„rts        º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    EDITOR-BEDIENUNG     :   Mittels TASTENKOMBINATIONEN oder MAUS-Bedienung
  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Tastenkombination      ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º ^F3                    ³ Aufrufen der PV-Auswahlbox                    º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ESC>                  ³ Aufrufen des "SYSTEMMENšS" oder               º
  º                        ³ Abbrechen der gew„hlten Funktion              º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @857[ Hotkey-Tasten ]               @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.859
    P V - MONITOR  PV-AUSWAHLLISTE

    <PV-Auswahlliste>: Ausw„hlen der PVs zur Anzeige im PV-Monitor. Es stehen
      nur solche PVs zur Auswahl, die noch nicht in der Liste eingetragen sind.
      Die Auswahl von Struktur- oder Array-Elementen erfolgt mit Dialogboxen.

 ÚÄÄÄÄÄÄÄÄÄ<taskname>ÄÄÄÄÄÄÄÄÄ¿
 ³ PV-Name   : *              ³ <- SUCHMASKE fr entsprechende PV-Namen
 ³            ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ³
 ³            ³@860[û] pv_name1   ³ ³ <- Anzeige der PV-Namen im Auswahlfenster
 ³            ³  pv_name2   ² ³    - Auswahlliste: mit <SPACE> k”nnen die PVs
 ³ [   OK   ] ³û pv_name3   ° ³      selektiert (û) bzw. deselektiert werden
 ³ [ Cancel ] ³û pv_name4   ° ³    - Direktbest„tigung einer PV mit <ENTER>/<OK>
 ³ [@860[ Task.. ]] ³  pv_name5    ³ <- BESTTIGUNGSFELDER:
 ³            ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³    <OK>     .. angew„hlte PV(s) best„tigen
 ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <Cancel> .. Abbrechen der Funktion
                                   <Task..> .. Wechseln des Bezugstasks

    Aktivierung der PV-Auswahlliste ber: - DOPPELKLICK der MAUS
    (nur in Spalte <Name>)                - TASTENKOMBINATION:  ^F3
                                          - Hotkey <INS>
    @861[ Fortsetzung ]                 @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.860
    P V - MONITOR  PV-AUSWAHLLISTE

    <MARKIEREN> û: Mit Hilfe der Auswahlliste k”nnen mehrere PVs, fr die 
      Anzeige im PV-Monitor markiert werden. Mit der <SPACE>-Taste wird die 
      aktuelle PV des Auswahlfensters markiert, und der Cursor in die n„chste
      Zeile bewegt. Die Bet„tigung der <SPACE>-Taste auf einer selektierten PV,
      bewirkt die Aufhebung der Markierung.

    <TASK..>: In einem Popup werden alle auf der RPS existierenden Tasks
      angezeigt. Bei der Auswahl eines anderen Tasks werden die PVs des neuen
      Tasks in der PV-Auswahlliste angezeigt. Die Task-Dienste der Fuázeilen-
      funktion <F2 Task> beziehen sich ebenfalls auf den mittels <Task..> aus-
      gew„hlten Bezugstask.
      šber diese Funktion k”nnen gleichzeitig die PVs verschiedener Tasks im
      PV-Monitor angezeigt werden. Der entsprechende Gltigkeitsbereich der
      PV wird in der Spalte <gltig> angezeigt.
      Der aktuell angew„hlte Bezugstask ist in der linken, unteren Ecke des PV-
      Monitors ersichtlich.



    @861[ Fortsetzung ]                 @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.861
    P V - MONITOR  PV-AUSWAHLLISTE

    <Auswahl von Struktur- und Array-Elementen>:

    Bei einer erneuten Auswahl der PV-Auswahlliste auf einer Struktur "struct"
    oder auf einem Array "array" erscheint anstelle der PV-Auswahlliste ein
    Popup zur Auswahl des Struktur-Elementes und bei Arrays eine Dialogbox zur
    Auswahl der entsprechenden Feldindizes.


    <Anzeige von Struktur- und Array-Elementen>:

 ³Name          ³ gltig ³ Wert              ³F³
 ÆÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍµ
 ³struct        ³ TC#4   ³ typedef str_typ   ³ ³.. Strukturtyp wird angezeigt
 ³  .element1   ³        ³ $00000001         ³ ³.. Wertanzeige nur bei Elementen
 ³  .elementx   ³        ³ $0002             ³ ³
 ³array         ³ TC#4   ³ BYTE [10]         ³ ³.. Datentyp und [Feldl„nge]
 ³  [0]         ³        ³ $00               ³ ³.. Wertanzeige nur bei Elementen
 ³  [9]         ³        ³ $09               ³ ³

    @859[ Fortsetzung ]                 @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.862
    P V - MONITOR  PV-INFOBOX

    <PV-Infobox>:   In der PV-Infobox werden Informationen aus der Variablen-
                    Deklaration der aktuell angew„hlten PV angezeigt.

 ÚÄÄÄÄÄÄÄ<pv_name>ÄÄÄÄÄÄÄ¿ ... Anzeige des PV-Namen
 ³ gltig    : tk_global ³ ... Gltigkeitsbereich der PV
 ³ Datentyp  : BYTE      ³ ... Datentyp der PV
 ³ L„nge     : 10        ³ ... Elementl„nge der PV
 ³ I/O-Typ   : MERKER    ³ ... I/O-Typ der PV
 ³ Bezugstask: taskname  ³ ... Taskname der Bezugstask
 ³                       ³
 ³       [   OK   ]      ³
 ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    Aktivierung der PV-Infobox ber:      - Hotkey <SPACE>
    (nur in Spalte <Name>)




                                  @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.863
    P V - MONITOR  FUSSZEILENMENš
                                                                      @857[< WEITER >]
ÉÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍ»
º@867[1 Start/Stop]³@868[2 Task ]³@864[3 Schreiben]³@865[4 Force]³@866[5 Format]³6       ³7       ³@869[8 Editor]º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍ¼

   <F1> <Start/Stop>:  STARTEN/STOPPEN der PV-Listen AKTUALISIERUNG
   <F2> <Task>      :  TASK-Auswahl und Funktionen (Start/Stop/Steps/...)
   <F3> <Schreiben> :  BESCHREIBEN der ausgew„hlten Variable (Hotkey: <w>)
   <F4> <Force>     :  FORCEN der ausgew„hlen Variable AKTIVIEREN/DEAKTIVIEREN
   <F5> <Format>    :  ANZEIGE-FORMAT der ausgew„hlten Variable (dez/hex/bin„r)
   <F8> <Editor>    :  AKTIVIEREN des SOURCE-CODE-EDITORS des angezeigten Tasks


    Die Funktionen des Fuázeilenmens k”nnen direkt ber FUNKTIONSTASTEN oder
    ber Mausklick angew„hlt werden.





                                  @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.864
    P V - MONITOR  BEDIENUNG

    <Schreiben>:

    - Die Variable, auf der sich der Cursor befindet, wird mit einem beliebigen
      Wert beschrieben. Der aktuelle Wert wird im PV-Monitor in der Spalte
      <Wert> angezeigt.

    - Der WERT ist in einem EINGABE-FENSTER einzugeben.
         ........ dezimale Eingaben ohne vorangestelltes Zeichen
      %%  ........ einem BINREN Wert ist das Zeichen %% voranzustellen
      $  ........ einem HEXADEZIMALEN Wert ist das Zeichen $ voranzustellen
      '  ........ bei ASCII-Darstellung ist das Zeichen ' voranzustellen
      "  ........ bei STRING-Darstellung ist das Zeichen " voranzustellen
      FLOAT.0 ... GANZSTELLIGE FLOAT-Werte mssen mit einem DEZIMALPUNKT
                  (wert.0) eingegeben werden.

    - Dieser Dienst kann IMMER durchgefhrt werden - auch bei GESTOPPTEM
      PV-MONITOR !


    @863[ Fuázeilen-Men ]              @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.865
    P V - MONITOR  BEDIENUNG

    <Forcen>:

    - Es k”nnen nur digitale PVs (Eing„nge/Ausg„nge) geforct werden
    - Diese Spalte zeigt das FORCEN einer Variable an:
            ... PV kann nicht geforct werden
      d     ... dynamische PV (kann nicht geforct werden)
      .     ... PV kann geforct werden
      f     ... PV wird geforct; der Force-Wert wird in Spalte "Wert" angezeigt

    - Das Forcen ist ein ZWINGENDES BESCHREIBEN einer Variable mit dem ange-
      gebenen WERT. Die Variable wird dabei gezwungen genau diesen Wert beizu-
      behalten. Beschreibt ein Task eine geforcte PV, dann bleibt der ge„nderte
      Wert NUR WHREND des aktuellen TASK-DURCHLAUFES ERHALTEN. Anschlieáend
      wird die PV automatisch mit dem FORCE-Wert šBERSCHRIEBEN.
    * Sobald eine PV auf der RPS geforct wird, leuchtet die FORCE-LED der CPU !
    * Die FORCE-Funktion ALLER GEFORCTEN Variablen kann mit der HM-Funktion
      <System><Info><Forcen aus> DEAKTIVIERT werden.


    @863[ Fuázeilen-Men ]              @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.866
    P V - MONITOR  BEDIENUNG

    <Format>:

    - In einem POPUP-Men wird das ANZEIGEFORMAT der ausgew„hlten Variable
      eingestellt:
      ÚÄÄÄÄÄÄÄÄÄÄ¿  Eingabe-/Anzeigeformat  Allgemeines
      ³ Dezimal  ³          ... immer m”glich
      ³ Hex      ³   $      ... immer m”glich
      ³ Bin„r    ³   %%      ... immer m”glich
      ³ Char     ³   ' '    ... nur bei BYTE-PVs
      ³ String   ³   " "    ... nur bei Array-Namen m”glich
      ³ Referenz ³   Adr.:  ... Referenz-Adresse dynamische PV
      ÀÄÄÄÄÄÄÄÄÄÄÙ

    - Diese Einstellung bezieht sich auf die Spalte "Wert" im PV-Monitor.

    - Durch Voranstellen des entsprechenden Format-Zeichens k”nnen beim
      Beschreiben von PVs andere Formate verwendet werden, als im Anzeige-
      format definiert sind.

    @863[ Fuázeilen-Men ]              @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.867
    P V - MONITOR  BEDIENUNG

    <Start/Stop>:

    - Der PV-Monitor wird GESTARTET bzw. GESTOPPT.

    - GESTARTETER PV-MONITOR:
      - Die VARIABLEN-WERTE der Liste werden zyklisch mit der eingestellten
        Abtastrate AKTUALISIERT (HM-Funktion <Einstellungen><On-Line>).
      - Die n„chste Bet„tigung der <Start/Stop>-Taste bewirkt ein STOPPEN.
      :                                          :
      ÀÄ< taskname : Run TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... Task l„uft

      ÀÄ< taskname : Stop TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... Task ist gestoppt

    - GESTOPPTER PV-MONITOR:
      - Die VARIABLEN-WERTE der Liste werden NICHT AKTUALISIERT.
      - Die n„chste Bet„tigung der <Start/Stop>-Taste bewirkt ein STARTEN.
      :                                          :
      ÀÄ< taskname TC#1 >ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ3ÄÙ ... KEIN Task-Status

    @863[ Fuázeilen-Men ]              @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.868
    P V - MONITOR  BEDIENUNG

    <Task>:

    - Beim Aufrufen dieser Funktion erscheint ein POPUP-Men, in dem
      verschiedene Task-Funktionen ausgew„hlt werden k”nnen:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ Start       ³ ... STARTEN einer gestoppten Task
    ³ Stop        ³ ... STOPPEN eines laufenden Task
    ³ Step        ³ ... ZYKLUSWEISES Abarbeiten der Task (Hotkey: <s>)
    ³ Stepanzahl  ³ ... ANZAHL der TASK-ZYKLEN eingeben
    ³ Task..      ³ ... Wechseln des Bezugstasks mittels Auswahl-Popup
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    <Step>: Zyklusweises ABARBEITEN der Task. Die Task wird eine ANZAHL von
            Zyklen, die bei Stepanzahl definiert wurde, durchlaufen.


    - Der aktuelle TASK-ZUSTAND und die STEPANZAHL wird am linken, unteren
      Fensterrand angezeigt (Run, Stop, Step + Anzahl).

    @863[ Fuázeilen-Men ]              @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.869
    P V - MONITOR  BEDIENUNG

    <Editor>:

    - AUFRUFEN/AKTIVIEREN des entsprechenden SOURCE-CODE-EDITORS, deren
      Task (lauff„higes Modul) beim EINSTIEG in den PV-Monitor (mittels
      Popup-Fenster) ausgew„hlt wurde.














    @863[ Fuázeilen-Men ]              @856[ Editor-Bedienung ]                 @002[ DEBUGGING ]
.880
    PG2000 - BEDIENUNG

    Alle globalen Fenster- und Men-Arten, deren Bedienung in der gesamten
    Programmiersoftware einheitlich realisiert sind, werden in diesem Teil der
    Hilfe beschrieben.

    @908[< ONLINE-Verbindung   >]     - VERBINDUNGSSTATUS und CPU-TYP anzeigen
    @882[< HAUPTMENš           >]     - Bedienung des Hauptmens
    @884[< FUSSZEILEN-MENš     >]     - Bedienung des Fuázeilen-Mens
    @906[< TASTENKOMBINATIONEN >]     - PG-GLOBALE Tastenkombinationen

    @885[< FENSTER             >]     - Arbeiten mit Fenstern (Wechseln,Schlieáen,..)
    @886[< SYSTEMMENš          >]     - Fensterinhalt speichern und Fensterfunktionen
    @894[< Dateiauswahlbox     >]     - Dateiauswahlbox - Ausw„hlen von Dateien
    @898[< Dialogbox           >]     - Dialogboxen - Eingeben von Definitionen
    @902[< Popup-Men          >]     - Ausw„hlen aus vordefinierten Definitionen

    @903[< Meldungsfenster     >]     - LOG-Datei und Eintr„ge des Meldungsfensters
    @905[< System-Warnung      >]     - Best„tigen von Fehlermeldungen und Warnungen
    @881[< Konventionen        >]     - Verwendete Konventionen

                                                                     @001[ šbersicht ]
.881
    PG2000 - BEDIENUNG

    <KONVENTIONEN>

    In den PG MENš-FUNKTIONEN und im HILFE-SYSTEM werden folgende SYMBOLISCHE
    Darstellungen fr Sonder-Tasten oder Sonder-Funktionen verwendet:

       ... [SHIFT]-Taste
      ^ ... [CTRL]-Taste
       ... [ALT]-Taste

       ... deutet an, daá zu der entsprechenden Funktion noch eine DETAIL-
            Auswahl aus Unterfunktionen oder fix vorgegebnene Parametern folgt
            (meist mit Hilfe eines Popup-Fensters).

     <> ... Eckige Klammern werden im Hilfe-System vorwiegend zum hervorheben
            von Text-Passagen und Kapitel verwendet, als KENNZEICNUNG von MENš-
            FUNKTIONEN und zum Begrenzen der TASTEN von Tasten-Kombinationen.



                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.882
    PG2000 - HAUPTMENš
    <HAUPTMENš>                                                       @884[< WEITER >]
ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³Datei  Bearbeiten  Objekt  Projekt  System  Einstellungen  Tools  Hilfe       ³
ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
³ Die FUNKTIONEN einer       ³
³ HAUPTMENš-GRUPPE (Datei,   ³  <ANWHLEN EINES PULLDOWN-MENšS>:
³ Bearbeiten, Objekt, ...)   ³  - Durch BEWEGEN des MAUS-Cursors IM BEREICH der
³ k”nnen mit Hilfe eines     ³    Hauptmen-Zeile wird das Pulldown-Men der
³@883[ <PULLDOWN-MENšS>           ]³    entsprechenden Gruppe angezeigt.
³ angew„hlt werden. Das      ³
³ Pulldown-Men erscheint    ³  - <ANFANGSBUCHSTABE der HAUPTMENš-GRUPPE>
³ linksbndig, unterhalb der ³    z.B.: Das Pulldown-Men der Hauptmen-Gruppe
³ entsprechenden Gruppe.     ³    <Datei> kann mit D angezeigt werden.
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    Mit den Cursor-Tasten <LINKS>/<RECHTS> k”nnen
                                  die Pulldown-Mens der anderen Gruppen
                                  angezeigt werden.

    Die Funktionen des Hauptmens k”nnen jederzeit aus ALLEN EDITOREN und
    FENSTERN genutzt werden.

                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.883
    PG2000 - HAUPTMENš

ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³Datei  Bearbeiten  Objekt  Projekt  System  Einstellungen  Tools  Hilfe       ³
ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
³ Neu            ³
³ ™ffnen          ³  <ANWHLEN EINER PULLDOWN-MENš FUNKTION>:
³ Schlieáen  ^F4  ³
³ Sichern         ³  - mit einem Mausklick kann die gewnschte Funktion
³ Sichern als     ³    direkt AKTIVIERT werden.
³ L”schen         ³
³ Export         ³  - mit den Cursor-Tasten <aufw„rts>/<abw„rts> kann
³ Import          ³    die gewnschte Funktion ausgew„hlt werden. Mit
³ Import Library  ³    <ENTER> wird die ausgew„hlte Funktion AKTIVIERT.
³ Drucken        ³
³ Beenden    F4  ³  - DIREKT-AKTIVIERUNG einer Pulldown-Funktion ber
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    den entsprechend HERVORGEHOBENEN BUCHSTABEN.

  Das Pulldown-Men erscheint LINKSBšNDIG, UNTERHALB der entsprechenden
  Hauptmen-Gruppe (Datei, Bearbeiten, Objekt, ...).

    @882[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.884
    PG2000 - FUSSZEILEN-MENš
    <z.B. GDM-FUSSZEILEN-MENš>                                        @906[< WEITER >]
 ÉÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÑÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍ»
 º1 Symbol³2 Datenbaum³3 Kommentar³4 Zeile+³5 Zeile-³6    ³7 Ebene³8 Ebeneº
 ÈÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÏÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍ¼

    - Das FUSSZEILEN-MENš beinhaltet Editor abh„ngige Funktionen und ist daher
      fr jeden Editor UNTERSCHIEDLICH mit Funktionalit„ten belegt.

    - Wenn ein Fuázeilen-Men VORHANDEN ist, wird dieses in der VORLETZTEN
      Bildschirm-ZEILE ANGEZEIGT.

    - ZUSATZ-INFORMATIONEN einer aktivierten Fuázeilen-Funktion werden in der
      UNTERSTEN Bildschirm-ZEILE angezeigt. (Modus, Sonder-Tastenbelgung, ...)

    - AKTIVIERUNG von Fuázeilen-Funktionen:
      - DIREKT-AKTIVIERUNG ber Mausklick.
      - DIREKT-AKTIVIERUNG ber FUNKTIONSTASTEN <F1> bis <F8>.
        Die Funktionstasten-Nummer wird LINKS NEBEN der FUNKTION angezeigt:
        z.B.: <F1>... fr <1 Symbol>, <F2>... fr <2 Datenbaum>, usw.

                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.885
    PG2000 - FENSTER
                          <PG2000 - FENSTER>                          @886[< WEITER >]
ÚÄÄÄ> SYSTEMMENš-
³     Aktivierung     FENSTER- oder DATEI-Name         ÚÄÄÂÄÂÄÄ> Fenstergr”áe
³                              ³                       ³  ³ ³    (mit MAUS)
À ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄName.ExtÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
  ³ !! MAXIMAL 10 FENSTER GLEICHZEITIG !!                   Ä¿
  ³                                                         ° ³
  ³ @886[<SYSTEMMENš       >] ... Fensterinhalt speichern und     ° ³
  ³                         Fensterfunktionen               ² Ã> Bildlaufleiste
  ³                                                         ² ³  (Fensteraus-
  ³ @891[<FENSTER GR™SSE   >] ... Fenster-Gr”áe und Position      ° ³   schnitt mit
  ³                                                         ° ³   MAUS „ndern)
  ³ @892[<FENSTERAUSSCHNITT>] ... Cursor-Positionierug im Editor  ° ³
  ³                                                         ° ³
  ³ @893[<FENSTER WECHSELN >] ... Anw„hlen von anderen Fenstern   ° ³
  ³                                                         ÄÙ
  ÀÄEbene/Zeile/SpalteÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄ2ÄÙ
     ³                                           ³        ³
     ÀÄÄÄ> Zusatz-Info (Editor abh„ngig)   Fensterrahmen  ÀÄÄÄÄ> Fensternummer

                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.886
    PG2000 - SYSTEMMENš
 ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄName.ExtÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿ @894[<WEITER>]
 ³ÚÄÄÄÄÄ<SYSTEMMENš>ÄÄÄÄ¿ ==> kann in JEDEM FENSTER aktiviert werden. 
 ³³@887[ Schlieáen        ^F4]³ ... Fenster schlieáen                       °
 ³³@887[ Sichern             ]³ ... Fensterinhalt speichern                 ²
 ³³@888[ Wiederherstellen    ]³ ... Ursprngliche Fenstergr”áe herstellen   ²
 ³³@888[ Fenster „ndern   ^F5]³ ... Gr”áe/Position „ndern (ein-/ausschalten)°
 ³³@889[ Symbol              ]³ ... Fenstergr”áe maximieren                 °
 ³³@889[ Vollbild         ^F1]³ ... Fenstergr”áe minimieren                 °
 ³³@890[ N„chstes         F6]³ ... n„chstes Fenster aktivieren             °
 ³ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ                                             °
 ³ - Mit den CURSOR-Tasten oder mit dem ANFANGSBUCHSTABEN kann die    °
 ³   gewnschte Funktion angew„hlt werden.                            °
 ³ - Aktivieren der Men-Funktion mit <ENTER> oder Mausklick.         °
 ³                                                                    
 ÀÄEbene/Zeile/SpalteÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ2ÄÙ

  <AKTIVIERUNG des SYSTEMMENšS>               <Abbrechen aus SYSTEMMENš>
  - mit <ESC> ber die Tastatur               - nach Aktivierung mit <ESC>
  - mit einem Mausklick auf das Symbol ""    - Anklicken eines Fensterbereichs

    @885[ Fenster ]                       @880[ PG-Bedienung ]                   @001[ šbersicht ]
.887
    PG2000 - SYSTEMMENš FUNKTIONEN

    <SYSTEMMENš><Schlieáen>

      Das AKTIVE FENSTER wird geschlossen.
      - Vor dem Schlieáen erfolgt gegebenenfalls eine Abfrage, ob noch nicht
        gespeicherte NDERUNGEN gesichert werden sollen.
      - gleiche Funktionalit„t wie Hauptmen-Funktion <Datei><Schlieáen>.


    <SYSTEMMENš><Sichern>

      Diese Funktion bewirkt ein Sichern des aktuellen Fensterinhaltes. Je
      nach Art des Editors wird der FENSTERINHALT in Form einer DATEI oder
      in der DATENBANK (z.B. Variablendeklaration) GESPEICHERT.
      - Vor dem Speichern werden die eingestellten EDITOR-spezifischen Kontroll-
        OPTIONEN durchgefhrt (z.B.: alle PVs deklariert? ...).
      - gleiche Funktionalit„t wie Hauptmen-Funktion <Datei><Sichern>.



    @886[ Systemmen ]                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.888
    PG2000 - SYSTEMMENš FUNKTIONEN

    <SYSTEMMENš><Wiederherstellen>

      Darstellen des aktuellen Fensters in ORIGINAL-GR™SSE:
      - Die ORIGINAL-GR™SSE ist die Gr”áe eines Fensters, wenn es nicht als
        Vollbild oder Symbol dargestellt wird. Die ORIGINAL-GR™SSE wird, so-
        lange das Fenster ge”ffnet ist, zwischengespeichert.
      - Die Funktion <Wiederherstellen> ist nur dann aktiv, wenn das Fenster
        als <Vollbild> oder <Symbol> dargestellt wird.


      <SYSTEMMENš><Fenster „ndern>         oder Tastenkombination: ^<F5>

        Mit dieser Funktion wird in einen Modus umgeschaltet, in dem die
        FENSTER-GR™SSE und FENSTER-POSITION ge„ndert werden kann. Dieser Modus
        wird mit demselben Befehl oder mit der <ESC>-Taste wieder verlassen.

        <CURSOR-Tasten>  ... FENSTER-POSITION ver„ndern
        <CURSOR-Tasten> ... FENSTER-GR™SSE beliebig ver„ndern

    @886[ Systemmen ]                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.889
    PG2000 - SYSTEMMENš FUNKTIONEN

    <SYSTEMMENš><Symbol>                                                [][]¿
                                                                        ³ xxxx ³
      Das aktuelle Fenster wird als SYMBOL (minimale Gr”áe) darge-      ³ xxxx 
      stellt, und als solches am RECHTEN Bildschirmrand abgelegt.       ÀÄÄÄÄ2ÄÙ

      - Es k”nnen mehrere Fenster als Symbole abgelegt werden. Ein ben”tigtes
        Fenster kann dann im Bedarfsfall wieder vergr”áert werden.
      - Diese Funktion kann aus der Darstellungsweise <Original-Gr”áe> oder
        <Vollbild> aufgerufen werden.


    <SYSTEMMENš><Vollbild>

      Darstellen des aktuellen Fensters ber den GESAMTEN BILDSCHIRMBEREICH
      (maximale Gr”áe).
      - Diese Funktion kann aus der Darstellungsweise <Original-Gr”áe> oder
        <Symbol> aufgerufen werden.


    @886[ Systemmen ]                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.890
    PG2000 - SYSTEMMENš FUNKTIONEN

    <SYSTEMMENš><N„chstes>

      Aktivieren des Fensters mit der NCHSTH™HEREN Fensternummer.
      - Alle Fenster sind durchnummeriert. Die Fensternummer wird UNTEN/RECHTS
        im Fensterrahmen angezeigt.
      - Nach dem Aufruf der Funktion <N„chstes> Fenster wird das Fenster mit
        der n„chsth”heren Fensternummer AKTIVIERT. (Wenn es keine n„chsth”here
        Fensternummer gibt, wird das Fenster mit der niedrigsten Nummer
        aktiviert (das Meldungsfenster mit Fensternummer "1").










    @886[ Systemmen ]                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.891
    PG2000 - FENSTERGR™SSE UND -POSITION

    - Bedienung mit MAUS:
      Ä...ÄÄÄ[][]¿   [] ... Umschalten zwischen <Symbol> und <Original>-
      ³ x x x x x x ³           Darstellung
      ³ xxxx xxx xx    [] ... Umschalten zwischen <Vollbild> und <Original>-
      ÀÄ...ÄÄÄÄÄÄÄ2ÄÙ           Darstellung
                         ¿
          Fensterecken (ÀÙ) ... Durch Anklicken und Ziehen der Maus kann die
                                FENSTERGR™SSE beliebig ver„ndert werden.
          Fensterrahmen Ä³  ... Durch Anklicken des Fensterrahmens und Ziehen
                                der Maus kann das FENSTER verschoben werden.



    - TASTENKOMBINATION  ^<F5>  Fenster-Gr”áe und Position „ndern
                                (gleich wie <Systemmen><Fenster „ndern>)

    @886[- SYSTEMMENš-Funktionen: ]   <Vollbild>,<Symbol>,Wiederherstellen <Original>
                                und <Fenster „ndern>

    @885[ Fenster ]                       @880[ PG-Bedienung ]                   @001[ šbersicht ]
.892
    PG2000 - FENSTERAUSSCHNITT

   ...ÄÄÄ[][]¿     / ... 4 Zeilen aufw„rts/abw„rts
               
    Bedienung  °      °  ... Eine Seite aufw„rts (<PGUP>)
    mit        ²      ²  ... Aktueller Fensterausschnitt
    MAUS:      °      °  ... Eine Seite abw„rts (<PGDOWN>)
               
   ...ÄÄÄÄÄÄÄ2ÄÙ     Mit Mausklick setzen des CURSORS im Fensterausschnitt

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Bedienung mit TASTATUR ³ Beschreibung                                  º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <LEFT>/<RIGHT>         ³ Ein Zeichen/Symbol nach links/rechts          º
  º <UP>/<DOWN>            ³ Eine Zeile/Symbol aufw„rts/abw„rts            º
  º <PGUP>/<PGDOWN>        ³ Eine Seite aufw„rts/abw„rts                   º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <HOME>/<END>           ³ Cursor auf Anfang/Ende der Zeile              º
  º ^<HOME>/ 2x<HOME>      ³ Cursor zum Beginn des Textes                  º
  º ^<END>/ 2x<END>        ³ Cursor zum Ende des Textes                    º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
    @885[ Fenster ]                       @880[ PG-Bedienung ]                   @001[ šbersicht ]
.893
    PG2000 - FENSTER WECHSELN

      Alle Fenster sind durchnummeriert. Die entsprechende FENSTERNUMMER wird
      UNTEN/RECHTS im Fensterrahmen angezeigt.

      - Bedienung mit MAUS:  Durch ANKLICKEN des gewnschten Fensters IM
                             FENSTERBEREICH (INNERHALB des Fensterrahmens)
                             wird dieses Fenster AKTIVIVIERT.


      - TASTENKOMBINATION:  <FENSTERNUMMER> ... Direktes aktivieren des
                                                 Fensters mit <ALT> und dessen
                                                 Fensternummer
      - <SYSTEMMENš><N„chstes>               ... Fenster mit n„chsth”herer
                                                 Fensternummer aktivieren
      - <HAUPTMENš><Tools><Fensterliste>     ... Direktes aktivieren eines
                                                 Fensters durch Auswahl aus
                                                 der Fensterliste



    @885[ Fenster ]                       @880[ PG-Bedienung ]                   @001[ šbersicht ]
.894
    PG2000 - DATEIAUSWAHLBOX
                                                                      @898[< WEITER >]
    Die <DATEIAUSWAHLBOX> wird vor Dateiaktivit„ten angezeigt, um das Aus-
    w„hlen von Projekt-Dateien bersichtlich zu untersttzen:
 ÚÄÄÄÄÄÄÄÄÄÄÄÄ Dateien ÄÄÄÄÄÄÄÄÄÄÄÄ¿
 ³ Default : c:\projekt.pgp\pgm    ³ <- aktuelles PROJEKTVERZEICHNIS
 ³ Name    : *.*                  ³ <- SUCHMASKE mit HISTORY-Funktionalit„t
 ³ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ³
 ³³ ..\         ³ ³ file1.ext    ³ ³ <- Anzeige in 2 AUSWAHLFENSTERN:
 ³³ RPS1\       ° ³ file2.ext    ² ³    1. Verzeichnisse/Laufwerke "Pfade\","L:"
 ³³ A:           ³ file3.ext    ° ³    2. Dateiliste "file.ext"
 ³ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³ file4.ext    ° ³
 ³  [   OK   ]    ³ file5.ext     ³ <- BESTTIGUNGSFELDER:
 ³  [ Cancel ]    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ ³    <OK>    .. angew„hltes File best„tigen
 ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <Cancel>.. Abbrechen, Schlieáen der
                                                   Dateiauswahlbox ohne Funktion
    - Ein Mausklick auf ein Unterverzeichnis des Auswahlfensters bewirkt ein
      Wechseln in das entsprechende Verzeichnis.
    - Die gewnschte Datei kann ber die Bildlaufleiste des Auswahlfensters an-
      gezeigt und DIREKT ber einen Mausklick ausgew„hlt und best„tigt werden.

    @895[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.895
    PG2000 - DATEIAUSWAHLBOX

    <SUCHMASKE>
      - nur Dateien, die den Suchkritierien in der SUCHMASKE entsprechen,
        werden im AUSWAHLFENSTER angezeigt.
      - Der gewnschte DATEINAME kann anstelle der SUCHMASKE direkt eingegeben
        werden (mit absoluten oder relativen Pfadangaben).
        Durch eine anschlieáende EINGABEBESTTIGUNG mit <ENTER> wird die Datei
        entsprechend der Funktion geladen, compiliert, gel”scht, ...
    Getrennte <AUSWAHLFENSTER> fr Verzeichnisse und Dateien:
      - Befindet sich der Cursor in einem der Auswahlfenster, kann mit dem
        Anfangsbuchstaben oder den Cursor-Tasten der gewnschte Eintrag (Datei
        oder Verzeichnis) ausgew„hlt werden.
        Mit <ENTER> wird die Auswahl best„tigt und eine Aktion ausgefhrt:
        - im Verzeichnis-Fenster: ... Wechsel in das ausgew„hlte Verzeichnis
        - im Datei-Fenster:       ... die Datei wird entsprechend der Funktion
                                      geladen, compiliert, gel”scht, ...
    <BESTTIGUNGSFELDER>
      - Das hervorgehobene Best„tigungsfeld (standardm„áig <OK>) wird mit
        <ENTER> im Auswahlfeld, oder šBERALL mit ^<ENTER> AUSGEFšHRT.

    @896[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.896
    PG2000 - DATEIAUSWAHLBOX

    Optimale Bedienung ber TASTATUR:

    - Verzeichnisse und Dateien werden ber den Anfangsbuchstaben selektiert
      (es wird immer der n„chste Eintrag, der mit dem bet„tigten Buchstaben
      beginnt, markiert).
    - Die ausgew„hlte Datei wird mit <ENTER> best„tigt.


    Optimale Bedienung mit MAUS:

    - Eine Datei wird direkt durch einen Mausklick ausgew„hlt und best„tigt.
      K”nnen nicht alle Dateie in der Auswahlliste angezeigt werden, kann der
      Fensterausschnitt durch Klicken auf die Bildlaufleiste verschoben werden.






    @897[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.897
    PG2000 - DATEIAUSWAHLBOX

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Bedienung mit TASTATUR ³ Beschreibung                                    º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <CURSOR>-Tasten        ³ Markieren der Datei im Auswahlfenster           º
  º <ANFANGSBUCHSTABE File>³ Markieren der Datei im Auswahlfenster           º
  º <TAB>                  ³ Cursor wechselt zum n„chsten Dialogobjekt       º
  º                        ³ Auswahlfenster->Best„tigungen->Suchmaske        º
  º <TAB>                 ³ Cursor wechselt zum vorhergehenden Dialogobjekt º
  º                        ³ Auswahlfenster->Suchmaske->Best„tigungen        º
  º <TAB>                 ³ Wechseln zwischen OK und Cancel, ohne den       º
  º                        ³ Cursor auf diese Felder zu bewegen              º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º ^<ENTER>               ³ BESTTIGUNG der im Best„tigungsfeld getroffenen º
  º                        ³ Auswahl (entweder OK oder CANCEL):              º
  º                        ³    <OK> die markierte Datei wird geladen, ...   º
  º                        ³    <Cancel> Schlieáen ohne Funktion             º
  º <ESC>                  ³ Abbrechen der Dateiauswahlbox = <Cancel>        º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

    @894[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.898
    PG2000 - DIALOGBOX
                                                                      @902[< WEITER >]
    šber <DIALOGBOXEN> wird der Benutzer aufgefordert, fr den weiteren Ablauf
    einer PG-Sitzung notwendige Eingaben durchzufhren.

    Beispiel einer Dialogbox:
  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄSuchenÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³  Suche nach : °°°°°°°°°°°°°°°°° ³  <- TEXT-EINGABEFELD
  ³                                 ³
  ³Suchrichtung : Vor              ³  <- EINGABEFELD mit Popup-Auswahlfenster
  ³Startposition: Cursor           ³
  ³                                 ³
  ³     [   OK   ]  [ Cancel ]      ³  <- BESTTIGUNGSFELDER:
  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ     <OK>    ... EINGABEN best„tigen
                                          <Cancel>... Abbrechen und Schlieáen
                                                      der Dialogbox
     - Dialogboxen setzen sich im Allgemeinen aus verschiedenen TEILFELDERN
       zusammen, mit deren Hilfe die Benutzereingaben durchgefhrt werden.
     - Anstatt der Best„tigung <OK> k”nnen auch Funktionalit„ten wie Download,
       Brennen, ... die ebenso zum Schlieáen der Dialogbox fhren.

    @899[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.899
    PG2000 - DIALOGBOX

    <TEXT-EINGABEFELD>/<ZAHLEN-EINGABEFELD>
    - Eingabe von STRINGS oder ZAHLEN (Format und Wertigkeit wird angezeigt)
    - Mit <HOME> und <END> wird der Anfang oder das Ende des Strings erreicht.
    - Mit <BACKSPACE> kann die letzte Ziffer einer Zahl gel”scht werden.
    - Mit <ENTER> wird nur die aktuelle Eingabe best„tigt und der Cursor
      wechselt zum n„chsten Teilfeld der Dialogbox.

    <EINGABEFELD mit Popup-Auswahlfenster>
    - Kennzeichnung: Am linken Rand des Eingabefeldes schlieát ein '' an.
    - Durch eine beliebige Eingabe wird ein POPUP-FENSTER angezeigt, in dem
      verschiedene M”glichkeiten angeboten werden.
      - Mit den <CURSOR>-Tasten oder dem ANFANGSBUCHSTABEN kann der gewnschte
        Eintrag ausgew„hlt werden.
      - Mit <ENTER> oder einem Mausklick wird der angew„hlte Eintrag des
        Popup-Fensters best„tigt und in der Dialogbox angezeigt.




    @900[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.900
    PG2000 - DIALOGBOX

    Optimale Bedienung ber TASTATUR:

    - Nutzen Sie die Auswahl-Hilfe: Mit dem Anfangsbuchstaben markieren Sie
      den gewnschten Listeneintrags.
    - Vorgeschlagene korrekte Angaben mssen nicht definiert werden.
    - Mit ^<ENTER> k”nnen Sie die Dialogbox best„tigen, ohne mit dem Cursor
      auf das OK-Feld zu wechseln. Vorraussetzung ist, daá das OK-Feld hervor-
      hoben ist (kann mit <TAB> ge„ndert werden).


    Optimale Bedienung mit MAUS:

    - Ein Mausklick bewirkt:
      - die Auswahl eines TEILFELDES zum Eingeben der Angaben
      - eine Direkt-Best„tigung einer POPUP-FENSTER Funktion, und deren Anzeige
        in der Dialogbox.
      - die Durchfhrung der angeklickten BESTTIGUNG (OK, Cancel)
    - Vorgeschlagene korrekte Angaben mssen nicht definiert werden.

    @901[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.901
    PG2000 - DIALOGBOX

  ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
  º Bedienung mit TASTATUR ³ Beschreibung                                   º
  ÌÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
  º <TAB>/<DOWN>           ³ Wechseln zum n„chsten Teilfeld                 º
  º <UP>                   ³ Wechseln zum vorhergehenden Teilfeld           º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <TAB>                 ³ ndern der Auswahl im Best„tigungsfeld. Der    º
  º                        ³ Cursor muá dabei nicht auf einem Best„tigungs- º
  º                        ³ feld (OK, Cancel, ...) befinden.               º
  º ^<ENTER>               ³ Ausfhren des gew„hlten Best„tigungsfeldes     º
  ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
  º <ESC>                  ³ Abbrechen und Schlieáen der Dialogbox ohne     º
  º                        ³ eine Funktion auszufhren (= Cancel).          º
  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼





    @898[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.902
    PG2000 - POPUP-MENš / POPUP-FENSTER
                                                                      @903[< WEITER >]
    <POPUP-FENSTER> kommen im PG in den verschiedensten Varianten vor. Sie
    erleichtern die AUSWAHL aus fix VORDEFINIERTEN M™GLICHKEITEN in den Men-
    Funktionen, oder bieten eine FUNKTIONSšBERSICHT in Form von POPUP-MENšS
    in den verschiedenen Editoren.

     ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿  ÚÄÄÄÄÄÄÄÄÄÄ¿  ÚÄÄÄÄÄÄ¿           POPUP
     ³ Kontaktplan        ³  ³ auto     ³  ³   1  ³ <ÄÄÄÂÄÄÄÄ Fenster-Zeilen
     ³ PL2000             ³  ³ manuell  ³  ³   2  ³ <ÄÄÄ´
     ³ Anweisungsliste    ³  ÀÄÄÄÄÄÄÄÄÄÄÙ  ³   3  ³ <ÄÄÄÙ
     ³ Datenmodul         ³                ³   4  ³
     ³ Funktionsbaustein  ³                ³ SS1  ³
     ³ Text               ³                ³   :  ³
     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ                ³ EXC  ³
                                           ÀÄÄÄÄÄÄÙ
    - Die Auswahl eines Eintrags aus einem Popup-Fenster erfolgt mit den 
      <CURSOR>-Tasten oder dem ANFANGS-BUCHSTABEN/-ZAHL.
    - Die BESTTIGUNG des ausgew„hlten Eintrags erfolgt mit <ENTER>
      oder DIREKT mit einem Mausklick.

                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.903
    PG2000 - MELDUNGSFENSTER
                                                                      @905[< WEITER >]
 ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄMeldungenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
 ³                                                                      ³
 ³ ==> Ein Schlieáen des <MELDUNGSFENSTERS> bewirkt das L™SCHEN aller   °
 ³     MELDUNGS-EINTRGE im Meldungsfenster, das MELDUNGSFENSTER selbst °
 ³     kann jedoch NICHT GESCHLOSSEN werden !!                          °
 ³ ==> Ein EDITIEREN DES TEXTES im Meldungsfenster ist NICHT M™GLICH.   °
 ³ ==> AUFZEICHNEN der Meldungsfenster-Eintr„ge im @904[LOG-Datei] "PG.LOG"     
 ³                                                                      ³
 ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ1ÄÙ

    - Das Meldungsfenster erh„lt als erstes ge”ffnetes Fenster die
      FENSTERNUMMER "1". Es wird im unteren Bildschirmbereich angezeigt.
    - Im Meldungsfenster werden FEHLERMELDUNGEN oder BESTTIGUNGEN von
      Abl„ufen festgehalten.

    - Mittels <CURSOR>-Tasten oder durch Klicken mit der Maus auf die Bildlauf-
      leiste kann der TEXT im Fenster verschoben und AUSGEWERTET werden.
    - Die Fenster-Bedienung ist in diesem Kapitel unter "FENSTER" beschrieben.

                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.904
    PG2000 - LOGFILE

    Im PG-LOG-Datei werden alle PG-AKTIVITTEN die im MELDUNGSFENSTER angezeigt
    werden aufgezeichnet.
    - Fr jedes PROJEKT werden die Aufzeichnungen in der Datei "PG.LOG" im
      entsprechenden Projektverzeichnis gespeichert !!
    - Mit Hilfe der LOG-Datei k”nnen Abl„ufe aus der aktuellen oder von
      VORHERGEHENDEN PG-SITZUNGEN genau ANALYSIERT werden. Das kann vor allem
      bei FEHLERMELDUNGEN von Compilierungsabl„ufen oder bei den PROJEKT-
      Diensten von Vorteil sein.

    LOG-Datei-Informationen oder L”schen der LOG-Datei mit Hilfe der HM-Funktion
    <Tools><Logfile>:
  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄLogfileÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ Erzeugt : 11. 1.1995  13:35:26     ³ <- LOG-Datei-Informationen
  ³ Gr”áe   : 34657 Byte               ³
  ³                                    ³ <- BESTTIGUNGSFELDER:
  ³ [   OK   ]  [ L”schen ] [ Cancel ] ³    <OK>     .. Informationen best„tigen
  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ    <L”schen>.. LOG-Datei l”schen
                                            <Cancel> .. Abbrechen aus Dialogbox

    @903[ Fortsetzung ]                   @880[ PG-Bedienung ]                   @001[ šbersicht ]
.905
    PG2000 - SYSTEM-WARNUNG
                                                                      @881[< WEITER >]
    <SYSTEM-WARNUNGEN> werden in der Mitte des Bildschirmes farblich
    HERVORGEHOBEN angezeigt:

    z.B. Wenn keine MAUS an der dafr vorgesehenen Schnittstelle angesteckt ist:

               ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
               ³                                           ³
               ³  Maustreiber nicht geladen.               ³
               ³  Bedienung ber Tastatur m”glich.         ³
               ³                    OK                     ³
               ³                                           ³
               ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    - Eine System-Warnung ist eine zu BESTTIGENDE Warnung oder Fehlermeldung
      von FEHLFUNKTIONEN oder von SCHWERWIEGENDEN FEHLERN.

    - Zus„tzlich zu dem ausgegebenen Meldungs-Text kann bei schwerwiegenden
      Fehlern noch eine entsprechende Fehlernummer angezeigt werden.

                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.906
    PG2000 - GLOBALE TASTENKOMBINATIONEN
                                                                      @907[< WEITER >]
ÉÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º     º Hauptmen-Funktion  ³ Beschreibung                                     º
ÌÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º F9  º <Objekt><Compile>   ³ COMPILIEREN des angew„hlten Objektes             º
º F10 º <Hilfe><Hilfe>      ³ ONLINE-HILFE aktivieren                          º
º F11 º <Objekt><Monitor>   ³ MONITOR des angew„hlten Objektes aktivieren      º
º F12 º <Projekt><GDM>      ³ GRAPHISCHE-DESIGN-METHODE aktivieren             º
ÇÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º ^F1 º                     ³ Umschalten zwischen FENSTER VOLLBILD/ORIGINAL    º
º ^F3 º                     ³ Editorspezifisches POPUP-Men aktivieren         º
º ^F4 º <Datei><Schlieáen>  ³ SCHLIESSEN des angew„hlten Fensters              º
º ^F5 º                     ³ FENSTERGR™SSE- und POSITION NDERN               º
ÇÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º F5 º <Bearbeiten><Weiter>³ WEITERSUCHEN des Suchstrings                     º
º F9 º <Objekt><R U N>     ³ COMPILIEREN und šBERTRAGEN des aktiven Objektes  º
º F11º <Objekt><Debugger>  ³ DEBUGGER des angew„hlten Objektes aktivieren     º
ÈÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼


                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.907
    PG2000 - GLOBALE TASTENKOMBINATIONEN
                                                                      @885[< WEITER >]
ÉÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º        º Hauptmen-Funktion        ³ Beschreibung                            º
ÌÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¹
º F4    º <Datei><Beenden>          ³ PG-SITZUNG BEENDEN                      º
º F6    º                           ³ Umschalten auf das NCHSTE Fenster      º
ÇÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º <ESC>  º                           ³ Aufrufen des SYSTEM-MENšS               º
ÇÄÄÄÄÄÄÄÄ×ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º ^<INS> º <Bearbeiten><Kopieren>    ³ ZWISCHENSPEICHERN der Markierung        º
º <INS> º <Bearbeiten><Einfgen>    ³ ZWISCHENSPEICHER in Editor einfgen     º
º <DEL> º <Bearbeiten><Ausschneiden>³ ZWISCHENSPEICHERN und L™SCHEN der       º
º        º                           ³ Markierung                              º
ÈÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼






                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.908
    PG2000 - ONLINE VERBINDUNG

    °°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°
    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄMeldungenÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ[][]¿
    ³ Der VERBINDUNGSSTATUS einer AKTIVEN VERBINDUNG wird automatisch in   °
    ³ der MELDUNGSZEILE (Zeile unter dem Meldungsfenster) angezeigt:       °
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ1ÄÙ
    °°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°°
                                                         ³CP15x V1.90 ³COM1³R
                                                        ÀÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÙ
                                                           Verbindungsstatus
    Aktive Verbindung zwischen PG und RPS:
       ³CP15x V1.90 ³COM1³R
        ³     ³      ³    ÀÄÄ Modus, in dem sich der Prozessor befindet:
        ³     ³      ³           R ... RUN-Modus
        ³     ³      ³           S ... SERVICE-Modus
        ³     ³      ³           D ... DIAGNOSE-Modus
        ³     ³      ÀÄÄÄÄÄÄÄ Anzeige der Schnittstelle (COM1/COM2)
        ³     ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Anzeige der Betriebssystemversion
        ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ Anzeige des Prozessortyps

                                     @909[ Fortsetzung ]                   @001[ šbersicht ]
.909
    PG2000 - ONLINE VERBINDUNG (Fortsetzung)

    Keine Verbindung zwischen PG und RPS:
       ³ <OFF-LINE> ³    ³

    Mit Hilfe der Funktion <Einstellungen><Verbindung> kann eine Verbindung
    aufgebaut werden.














                                    @880[ PG-Bedienung ]                   @001[ šbersicht ]
.910
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr das "sysconf"-Symbol:
    Um die Auslastung Ihres Steuerungssystems zu optimieren, k”nnen Sie die
    Parameter des RPS-Konfigurationsmoduls (auch "$$sysconf"-Modul genannt)
    an Ihre Applikation anpassen (Systemkonfiguration). Dazu setzen Sie in der
    Task-Ebene ein Symbol fr das RPS-Konfigurationsmodul ("sysconf"-Symbol)
    und rufen die Funktion <Objekt><Parameter> auf. Durch diese Funktion wird
    das RPS-Konfigurationsmodul von der RPS geladen und eine Dialogbox mit den
    wichtigsten Parametern eingeblendet.











    @911[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.911
    PG2000  HAUPTMENš FUNKTIONEN

    Objekt-Parameter fr das "sysconf"-Symbol:

    ÚÄÄÄÄÄÄÄÄ RPSSW Konfiguration ÄÄÄÄÄÄÄÄ¿    Die Systemkonfiguration kann
    ³ @912[RPS Name       ]: test_rps           ³    jedoch auch mit dem RPSSW-
    ³ @912[Prozessor Typ  ]: CP10x              ³    Konfigurator durchgefhrt
    ³ @912[RPSSW Version  ]: V1.91              ³    werden.
    ³ @912[Paáwort        ]: bur                ³
    ³ @913[Konfiguriere   ]:              -->  ³
    ³ @979[Zielspeicher   ]: User Flash        ³
    ³ @981[Modul Info     ]                     ³
    ³     Version   : 1.91                ³
    ³     Datum     : 17.02.97            ³
    ³     Zeit      : 01:09:01,00         ³
    ³     L„nge     : 796                 ³
    ³                                     ³
    ³[@981[   OK    ]]  [@981[ Default ]]  [@981[ Cancel  ]]³
    ³[@981[              Drucken              ]]³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.912
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><RPS Name>:
    Prozessor-Name. Der hier festgelegte Prozessor-Name muá bei der Konfigu-
    ration einer Netzwerkankopplung (siehe Funktion <Projekt><Netzwerk>) an-
    gegeben werden.

    <OBJEKT><Parameter><Prozessor Typ>:
    Bezeichnung des verwendeten Prozessor-Typs.

    <OBJEKT><Parameter><RPSSW Version>:
    Verwendete Version des Betriebssystems RPSSW.

    <OBJEKT><Parameter><Paáwort>:
    In diesem Eingabefeld k”nnen Sie das Paáwort fr die Steuerung definieren
    (Standard-Paáwort: "bur"). Wenn das hier angegebene Paáwort vom Standard-
    Paáwort "bur" abweicht, wird der Anwender beim Verbindungsaufbau aufge-
    fordert, das korrekte Paáwort einzugeben. D.h. ohne Angabe des korrekten
    Paáwortes kann keine Verbindung zur RPS aufgebaut werden.


    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.913
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere>:
    šber das Popup-Men dieses Feldes k”nnen Sie die Dialoge zum Ver„ndern der
    RPSSW-Eigenschaften ”ffnen.
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³ @914[Speicher       ] ³
    ³ @918[Module         ] ³
    ³ @922[Taskklassen    ] ³
    ³ @928[HS Taskklassen ] ³
    ³ @934[Exceptions     ] ³
    ³ @937[Interrupts     ] ³
    ³ @940[TimeOut's      ] ³
    ³ @944[Betriebssystem ] ³
    ³ @951[Remote I/O     ] ³
    ³ @961[CAN I/O        ] ³
    ³ @969[2003 I/O       ] ³
    ³ @973[PB Indizes     ] ³
    ³ @974[Kommunikation  ] ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.914
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Speicher>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄSpeicherÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@915[Globale Merker   ]                        ³
    ³   Analog   (min:       0):    4096 Byte ³
    ³   Digital  (min:       0):    4096 Bits ³
    ³                                         ³
    ³@916[Permanente Merker]                        ³
    ³   Analog                 :       0 Byte ³
    ³   Digital                :       0 Bit  ³
    ³                                         ³
    ³@917[Speicher         ]                        ³
    ³   FIX-RAM                :       0 kByte³
    ³   Tempor„r               :      16 kByte³
    ³                                         ³
    ³          [   OK   ]  [ Cancel ]         ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ


    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.915
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Speicher><Globale Merker>:
    Analog:  Mit diesem Eingabefeld k”nnen Sie die Gr”áe des RPS-globalen bzw.
             prozessorglobalen Analogdatenbereichs ver„ndern.
             Der RPS-globale/prozessorglobale Analogdatenbereich ist jener Teil
             des DPRs, in den alle RPS-globalen/prozessorglobalen Variablen ge-
             legt werden, die nicht vom Datentyp BIT sind. In Klammern wird die
             mindestens erforderliche Gr”áe dieses Speicher fr das aktuelle
             Projekt angezeigt.
    Digital: Mit diesem Eingabefeld k”nnen Sie die Gr”áe des RPS-globalen bzw.
             prozessorglobalen Digitaldatenbereichs ver„ndern.
             Der RPS-globale/prozessorglobale Digitaldatenbereich ist jener
             Teil des DPRs, in den alle RPS-globalen/prozessorglobalen PVs mit
             dem Datentyp BIT (inklusive BIT-Felder) gelegt werden. In Klammern
             wird die mindestens erforderliche Gr”áe dieses Speicher fr das
             aktuelle Projekt angezeigt.




    @914[ Speicher ]                        @052[ HM-Objekt ]                    @001[ šbersicht ]
.916
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Speicher><Permanente Merker>:
    Analog:  Mit diesem Feld k”nnen Sie die Gr”áe des permanenten (kaltstart-
             sicheren) Analogdatenbereichs ver„ndern.
             Die PVs im permanenten Speicher werden auch bei einem Kaltstart
             nicht gel”scht. Ein L”schen dieses Speicherbereichs ist nur mit
             der Funktion <System><Service><Perm. Speicher l”schen> m”glich!
             Die Verwendung von permanentem Speicher ist erst ab Version 1.91
             der RPSSW m”glich. D.h. dieses Eingabefeld ist erst ab Version
             1.91 der RPSSW freigegeben!
    Digital: Mit diesem Feld k”nnen Sie die Gr”áe des permanenten (kaltstart-
             sicheren) Digitaldatenbereichs ver„ndern.
             Die PVs im permanenten Speicher werden auch bei einem Kaltstart
             nicht gel”scht. Ein L”schen dieses Speicherbereichs ist nur mit
             der Funktion <System><Service><Perm. Speicher l”schen> m”glich!
             Die Verwendung von permanentem Speicher ist erst ab Version 1.91
             der RPSSW m”glich. D.h. dieses Eingabefeld ist erst ab Version
             1.91 der RPSSW freigegeben!


    @914[ Speicher ]                        @052[ HM-Objekt ]                    @001[ šbersicht ]
.917
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Speicher><Speicher>:
    FIX-RAM:  Mit diesem Eintrag k”nnen Sie die Gr”áe des FIX-RAMs festlegen.
              Fr die Gr”áe des FIX-RAMs k”nnen nur die Werte 0 (kein FIX-RAM
              parametriert), 16 kByte oder Vielfache von 16 kByte angegeben
              werden.
              Das FIX-RAM ist jener Teil des Anwender-RAMs, der sich nach einem
              Kaltstart wie das Anwender-ROM verh„lt. D.h. Module, die im FIX-
              RAM gespeichert sind, werden bei einem Kaltstart nicht gel”scht.
    Tempor„r: Mit diesem Eintrag können Sie die Gröáe des tempor„ren Speicher-
              bereichs definieren.
              Der tempor„re Speicherbereich ist jener Teil des System-RAMs, der
              fr die serielle Kommunikation verwendet wird. Zus„tzlich haben
              Sie mit der Funktion TMP_alloc() (BURTRAP-Library) die M”glich-
              keit, Speicher fr dynamische Variablen im tempor„ren Speicher-
              bereich zu allokieren. Der so allokierte Speicher ist nicht ge-
              puffert und muá daher nach jedem Boot-Vorgang neu angefordert und
              reserviert werden.


    @914[ Speicher ]                        @052[ HM-Objekt ]                    @001[ šbersicht ]
.918
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Module>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄModuleÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@919[Logbucheintr„ge]          :    40 ³
    ³@919[Zyklische Tasks](min    3):    70 ³
    ³@920[Restzeittasks  ](min    0):     2 ³
    ³@920[System Tasks   ](min    1):    12 ³
    ³@921[B&R Module     ](min   96):   100 ³
    ³                                 ³
    ³     [   OK   ]  [ Cancel ]      ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ








    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.919
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Module><Logbucheintr„ge>:
    Alle schwerwiegenden Fehler, die im Rahmen einer Applikation auftreten
    (RPSSW-Fehler wie Zykluszeitverletzungen, Installationsfehler, usw.),
    werden vom Betriebssystem protokolliert. Dieses Protokoll wird in einem
    eigenen Fehlerlogbuch abgelegt und kann mit Hilfe der Funktion <System>
    <Analyse> gelesen werden.
    Im Eingabefeld <Logbucheintr„ge> k”nnen Sie die maximale Anzahl der Fehler-
    logbucheintr„ge definieren. D.h. wenn Sie an dieser Stelle den Wert 20 an-
    geben, werden jeweils die 20 jngsten RPSSW-Fehler im Fehlerlogbuch proto-
    kolliert. ltere Eintr„ge werden berschrieben. Fr die Anzahl der Fehler-
    logbucheintr„ge sind nur die Werte 16 bis 500 zul„ssig!

    <OBJEKT><Parameter><Konfiguriere><Module><Zyklische Tasks>:
    In diesem Eingabefeld legen Sie fest, wieviele zyklische Tasks (normale
    Tasks, superschnelle Tasks, Interrupt- und Exceptiontasks) maximal auf der
    RPS installiert werden k”nnen. Wenn Sie versuchen, mehr zyklische Tasks zu
    installieren als in diesem Eingabefeld definiert, wird der Installations-
    vorgang mit einer Fehlermeldung abgebrochen.

    @918[ Module ]                          @052[ HM-Objekt ]                    @001[ šbersicht ]
.920
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Module><Restzeittasks>:
    In diesem Feld k”nnen Sie festlegen, wieviele nichtzyklische Anwender-Tasks
    (auch Restzeittasks genannt) maximal auf der RPS installiert werden k”nnen.
    Wenn Sie versuchen, eine gr”áere Anzahl nichtzyklischer Anwender-Tasks zu
    installieren als in diesem Eingabefeld definiert, wird der Installations-
    vorgang mit einer Fehlermeldung abgebrochen. Restzeittasks ntzen die so-
    genannte Restzeit des Systems aus. Das bedeutet: Restzeittasks werden nur
    dann ausgefhrt, wenn sowohl das Betriebssystem als auch die zyklischen
    Tasks keine CPU-Zeit (Systemzeit) ben”tigen.

    <OBJEKT><Parameter><Konfiguriere><Module><System Tasks>:
    Mit diesem Eingabefeld wird festgelegt, wieviele Systemtasks maximal auf
    der RPS installiert werden k”nnen. Zu den Systemtasks z„hlen z.B. Spooler
    (zur Betreuung von Intelligenten I/O-Prozessoren), Debugger und Profiler-
    Runtime-Modul. Das Betriebssystem selbst installiert standardm„áig bereits
    mehrere Systemtasks. D.h. wenn Sie zus„tzliche Systemtasks wie Spooler,
    Debugger, usw. installieren, muá die Anzahl der Systemtasks eventuell ver-
    gr”áert werden.

    @918[ Module ]                          @052[ HM-Objekt ]                    @001[ šbersicht ]
.921
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Module><B&R Module>:
    Mit diesem Eingabefeld wird festgelegt, wieviele B&R-Module (compilierte
    Tasks, Exceptionhandler, Runtime-Module der Funktions-Libraries, usw.)
    maximal auf der RPS installiert werden k”nnen. Als Minimalwert gilt das
    Ergebnis der folgenden Formel:

    Zyklische Tasks + Restzeittasks + Systemtasks + Datenmodule + 10

    Wenn Sie versuchen, eine gr”áere Anzahl von B&R-Modulen zu installieren
    als in diesem Eingabefeld definiert, wird der Installationsvorgang mit
    einer Fehlermeldung abgebrochen.








    @918[ Module ]                          @052[ HM-Objekt ]                    @001[ šbersicht ]
.922
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Taskklassen>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄTaskklassenÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@923[Anzahl der Taskklassen] :     4       ³
    ³@923[Taskklasse            ] : TC 1       ³
    ³ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ³
    ³@924[Zeitverhalten         ]               ³
    ³  Dauer                :    10 ms    ³
    ³  Toleranz             :    20 ms    ³
    ³  Restzeit             :     0 ms    ³
    ³@926[Merker                ]               ³
    ³  analog   (min:     0):  1024 Byte  ³
    ³  digital  (min:     0):  1024 Bit   ³
    ³@927[Stackgr”áe            ]               ³
    ³  User                 :   768 Byte  ³
    ³  Interrupt            :   896 Byte  ³
    ³                                     ³
    ³       [   OK   ]  [ Cancel ]        ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.923
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Taskklassen><Anzahl der Taskklassen>:
    In diesem Eingabefeld k”nnen Sie die Anzahl der installierten normalen
    Taskklassen ver„ndern. Wenn Sie an dieser Stelle z.B. den Wert 2 eingeben,
    werden nur die Taskklassen TC 1 und TC 2 installiert! Sowohl die CPUs als
    auch die Multiprozessoren der Steuerungsgeneration B&R System 2000 bieten
    Ihnen bis zu 4 normale Taskklassen.
    Jede installierte aber nicht verwendete normale Taskklasse verbraucht
    Systemzeit und -ressourcen. Es wird daher empfohlen, nur die tats„chlich
    ben”tigte Anzahl von normalen Taskklassen zu installieren.

    <OBJEKT><Parameter><Konfiguriere><Taskklassen><Taskklasse>:
    Auswahl der zu parametrierenden Taskklasse ber ein Popup-Fenster. Nach An-
    wahl einer Taskklasse werden im unteren Teil der Dialogbox die wichtigsten
    Parameter der entsprechenden Taskklasse angezeigt.





    @922[ Taskklassen ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.924
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Taskklassen><Zeitverhalten>:
    Dauer:    Die Zykluszeit der angew„hlten Taskklasse kann in 10-ms-Schritten
              zwischen 10 ms (minimaler Wert) und 5 s (maximaler Wert) einge-
              stellt werden. Innerhalb dieser Zykluszeit werden alle Tasks der
              Taskklasse genau einmal abgearbeitet.
    Toleranz: Mit diesem Eintrag k”nnen Sie fr die angew„hlte normale Task-
              klasse eine erlaubte Zykluszeitverletzung definieren. Damit wird
              eine vordefinierte šberschreitung der Zykluszeit erlaubt.
    Restzeit: Tasks, die eine niedrigere Priorit„t als normale, superschnelle,
              Interrupt- und Exceptiontasks haben, werden in der Applikations-
              Restzeit abgearbeitet. Zu diesen Restzeit-Anwendungen z„hlt neben
              nichtzyklischen Anwender-Tasks, nichtzyklischen Betriebssystem-
              Routinen (z.B. Download/Overload von Tasks) und diversen Tools
              (z.B. Debugger) auch die Kommunikation ber serielle Schnitt-
              stellen. D.h. w„hrend der Laufzeit von zyklischen Tasks ist keine
              serielle Kommunikation m”glich! Aus diesem Grund kann es bei der
              seriellen Kommunikation in Systemen mit hoher Auslastung zu
              Reaktionszeitproblemen kommen!

    @925[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.925
    PG2000  HAUPTMENš FUNKTIONEN

              Um die Abst„nde zwischen den Restzeit-Bl”cken zu verringern und
              somit die Reaktionszeit der seriellen Kommunikation zu verkrzen,
              k”nnen Sie mit dem Eingabefeld <Restzeit> eine sog. Taskklassen-
              Restzeit in 0,5-ms-Schritten fr die angew„hlte normale Taskklasse
              konfigurieren. Dadurch wird in jedem Taskklassen-Zyklus nach Ab-
              arbeitung aller Tasks der entsprechenden Taskklasse die einge-
              stellte Taskklassen-Restzeit fr Restzeit-Anwendungen zur Ver-
              fgung gestellt. Wird innerhalb dieser Zeitspanne das Ende eines
              Systemzyklus (Systemzykluszeit = 10 Millisekunden) erreicht, wird
              die Taskklassen-Restzeit beendet. D.h. die Taskklassen-Restzeit
              kann nur kleiner als 10 ms sein. Im schlechtesten Fall, wenn die
              Abarbeitung der Tasks erst kurz vor Ende des Systemzyklus abge-
              schlossen wird, bleibt nur sehr wenig Taskklassen-Restzeit.
              Bei Verwendung der Standard-Zykluszeiten ist daher die normale
              Taskklasse 1 (Zykluszeit = 10 ms) Hauptanwendungsgebiet der
              Taskklassen-Restzeit.



    @922[ Taskklassen ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.926
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Taskklassen><Merker>:
    analog:  Hier k”nnen Sie die Gr”áe des taskklassenglobalen Analogdaten-
             bereichs der angew„hlten Taskklasse (Minimum = 16 Byte) angeben.
             In Klammern wird die mindestens erforderliche Gr”áe dieses Daten-
             bereichs fr das aktuelle Projekt angezeigt. Der taskklassen-
             globale Analogdatenbereich ist jener Teil des DPRs, in den alle
             tk_globalen Merker gelegt werden, die nicht vom Datentyp BIT sind.
    digital: Hier k”nnen Sie die Gr”áe des taskklassenglobalen Digitaldaten-
             bereichs der angew„hlten Taskklasse (Minimum = 16 Bit) angeben.
             In Klammern wird die mindestens erforderliche Gr”áe dieses Daten-
             bereichs fr das aktuelle Projekt angezeigt. Der taskklassen-
             globale Digitaldatenbereich ist jener Teil des DPRs, in den alle
             tk_globalen Merker mit dem Datentyp BIT (inklusive BIT-Felder)
             gelegt werden.

    Beim Festlegen der Gr”áe dieser Datenbereiche sollten Sie beachten, daá die
    normale Taskklasse 1 Ihren taskklassenglobalen Datenbereich sowohl mit den
    superschnellen Taskklassen als auch mit der IRQ- und EXC-Taskklasse teilt!

    @922[ Taskklassen ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.927
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Taskklassen><Stackgr”áe>:
    User:      Hier k”nnen Sie die Gr”áe des Userstacks fr die normalen Task-
               klassen definieren. Der vom Userstack verwendete Speicherbereich
               befindet sich im System-RAM. Die Gr”áe des ben”tigten Userstacks
               fr die Taskklassen h„ngt vom Stackbedarf der Funktionen ab, die
               in den Tasks dieser Taskklassen aufgerufen werden.
    Interrupt: Mit diesem Eingabefeld k”nnen Sie die Gr”áe des Supervisorstacks
               fr die normalen Taskklassen festlegen. Der vom Supervisorstack
               verwendete Speicherbereich befindet sich im System-RAM. Die
               Gr”áe des ben”tigten Supervisorstacks fr die normalen Task-
               klassen h„ngt von der Interruptbelastung des Systems und vom
               Stackbedarf der Funktionen ab, die in den Tasks dieser Task-
               klassen aufgerufen werden.

    Der Stackbedarf der einzelnen Funktionen (FUBs und Inline-Funktionen) ist
    in der Online-Hilfe der entsprechenden Funktions-Libraries angegeben.



    @922[ Taskklassen ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.928
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen>:

    ÚÄÄÄÄÄÄÄÄÄÄÄHS TaskklassenÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@929[Anzahl der HS-Klassen] :      4       ³
    ³@929[HS-Taskklassen       ] : HSTC 1      ³
    ³ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ³
    ³@930[Zeitbasis            ] : HW-Timer    ³
    ³@931[Zykluszeit           ] :     3 ms     ³
    ³@932[Unterbrechbar        ] : nie         ³
    ³                                     ³
    ³@933[Stackgr”áe           ]                ³
    ³  User                 :   512 Byte  ³
    ³  Interrupt            :   896 Byte  ³
    ³                                     ³
    ³       [   OK   ]  [ Cancel ]        ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ



    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.929
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen><Anzahl der HS-Klassen>:
    Mit diesem Eintrag k”nnen Sie die Anzahl der installierten superschnellen
    Taskklassen (HS-Taskklassen) festlegen. Die maximal m”gliche Anzahl der
    HS-Taskklassen ist abh„ngig vom Prozessor-Typ:
    ÉÍÍÍÍÍÍÍÍÍÍÍÍËÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍ»
    º Taskklasse ºCP10x/CP200³CP15x/CP260³XP152/IF152³MP100/IF10x³   IP151   º
    ÌÍÍÍÍÍÍÍÍÍÍÍÍÎÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍÍÍÍÍÍÍ¹
    º   HSTC 1   ºTimer-HSTC ³Timer-HSTC ³Timer-HSTC ³Timer-HSTC ³Timer-HSTC º
    º   HSTC 2   ºTimer-/    ³RIO-HSTC   ³RIO-/      ³MP-HSTC    ³RIO-/      º
    º            ºRIO-HSTC   ³           ³MP-HSTC    ³           ³MP-HSTC    º
    º   HSTC 3   ºTimer-HSTC ³    ---    ³    ---    ³    ---    ³    ---    º
    º   HSTC 4   ºTimer-HSTC ³    ---    ³    ---    ³    ---    ³    ---    º
    ÈÍÍÍÍÍÍÍÍÍÍÍÍÊÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍ¼

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen><HS Taskklasse>:
    Auswahl der zu parametrierenden Taskklasse ber ein Popup-Fenster. Nach An-
    wahl einer Taskklasse werden im unteren Teil der Dialogbox die wichtigsten
    Parameter der entsprechenden Taskklasse angezeigt.

    @928[ HS Taskklassen ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.930
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen><Zeitbasis>:
    HW-Timer:   Superschnelle Taskklassen werden normalerweise ber Hardware-
                Timer (Interrupts) aufgerufen. Diese HS-Taskklassen bezeichnen
                wir als Timer-HS-Taskklassen (kurz: Timer-HSTC).
    RIO/PP-FUB: Bei Remote I/O-Systemen kann neben den Timer-HSTCs auch eine
                eigene Remote-I/O-HS-Taskklasse (kurz: RIO-HSTC) konfiguriert
                werden. Die RIO-HSTC wird von einem Interrupt getriggert, der
                vom Remote Master am Ende jedes RIO-Zyklus generiert wird. Fr
                die RIO-HSTC muá immer die HS-Taskklasse 2 (HSTC 2) verwendet
                werden!
                Bei Multiprozessoren kann neben einer Timer-HSTC auch eine
                eigene MP-HS-Taskklasse (kurz: MP-HSTC) konfiguriert werden.
                Die MP-HSTC wird nicht durch einen Hardware-Timer, sondern ber
                die Funktion MP_trigger() in der Applikation der Haupt-CPU aus-
                gel”st. D.h. die Zykluszeit der MP-HSTC h„ngt ganz von der
                Zykluszeit jener Task ab, in der die Funktion MP_trigger()
                aufgerufen wird. Fr die MP-HSTC muá immer die HS-Taskklasse 2
                (HSTC 2) verwendet werden!

    @928[ HS Taskklassen ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.931
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen><Zykluszeit>:
    Die Zykluszeit der aktuellen HS-Taskklasse kann in 0,1-ms-Schritten zwi-
    schen 1,0 ms und 65,53 ms eingestellt werden.
    Abh„ngig von der definierten Zeitbasis ("HW-Timer" oder "RIO/PP-FUB") der
    aktuellen HS-Taskklasse hat die Zykluszeit unterschiedliche Bedeutungen:
    HW-Timer:   Innerhalb dieser Zykluszeit werden alle Tasks der Timer-HSTC
                genau einmal abgearbeitet.
    RIO/PP-FUB: Bei einer RIO-HSTC ist die angegebene Zykluszeit die minimale
                Zykluszeit. D.h. wenn der RIO-Zyklus 1 ms dauert, und fr die
                Zykluszeit der Wert 3 ms angegeben wird, so startet der Remote
                Master die RIO-HSTC nur alle 3 ms, obwohl der Zyklus bereits
                nach 1 ms abgeschlossen ist. Dadurch wird verhindert, daá ein
                sehr schneller RIO-Zyklus zur šberlastung des Systems fhrt.
                Eine MP-HSTC wird ber die Funktion MP_trigger() von der Haupt-
                CPU aus gesteuert. Die Zykluszeit der MP-HSTC h„ngt also ganz
                von der Zykluszeit jener Haupt-CPU-Task ab, in der die Funktion
                MP_trigger() aufgerufen wird. D.h. dieses Eingabefeld hat fr
                die MP-HSTC keine Bedeutung!

    @928[ HS Taskklassen ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.932
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen><Unterbrechbar>:
    Die HS-Taskklassen sind h”herprior als alle normalen Taskklassen, sie haben
    jedoch niedrigere Priorit„t als die EXC-Taskklasse. D.h. eine HS-Taskklasse
    kann alle normalen Tasks unterbrechen, sie kann aber selbst von einer EXC-
    Task unterbrochen werden. Zus„tzlich k”nnen Sie ber dieses Eingabefeld
    festlegen, ob die angew„hlte HS-Taskklasse von der IRQ-Taskklasse und/oder
    anderen HS-Taskklassen unterbrochen werden kann:
    immer:        Die Tasks der angew„hlten HS-Taskklasse k”nnen sowohl von
                  Tasks anderer HS-Taskklassen als auch von IRQ-Tasks unter-
                  brochen werden.
    durch IRQ-TK: Die Tasks dieser HS-Taskklasse k”nnen zwar von IRQ-Tasks,
                  nicht aber von Tasks anderer HS-Taskklassen unterbrochen
                  werden.
    nie:          Die Tasks der angew„hlten HS-Taskklasse k”nnen weder von
                  IRQ-Tasks noch von Tasks anderer HS-Taskklassen unterbrochen
                  werden. Durch diese Einstellung wird eine vollst„ndige Zeit-
                  konsistenz der angew„hlten HS-Taskklasse erreicht.


    @928[ HS Taskklassen ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.933
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><HS Taskklassen><Stackgr”áe>:
    User:      Hier k”nnen Sie die Gr”áe des Userstacks fr die angew„hlte HS-
               Taskklasse definieren. Der vom Userstack verwendete Speicher-
               bereich befindet sich im System-RAM. Die Gr”áe des ben”tigten
               Userstacks fr eine HS-Taskklasse h„ngt vom Stackbedarf der
               Funktionen ab, die in den Tasks dieser Taskklasse aufgerufen
               werden.
    Interrupt: Mit diesem Eingabefeld k”nnen Sie die Gr”áe des Supervisorstacks
               fr die angew„hlte HS-Taskklasse festlegen. Der vom Supervisor-
               stack verwendete Speicherbereich befindet sich im System-RAM.
               Die Gr”áe des ben”tigten Supervisorstacks fr eine HS-Taskklasse
               h„ngt von der Interruptbelastung des Systems und vom Stackbedarf
               der Funktionen ab, die in den Tasks dieser Taskklasse aufgerufen
               werden.

    Der Stackbedarf der einzelnen Funktionen (FUBs und Inline-Funktionen) ist
    in der Online-Hilfe der entsprechenden Funktions-Libraries angegeben.


    @928[ HS Taskklassen ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.934
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Exceptions>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄExceptionsÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³                                     ³
    ³@935[Anzahl]    (min:    0) :    16        ³
    ³@935[Unterbrechbar        ] : nie          ³
    ³                                     ³
    ³@936[Stackgr”áe           ]                ³
    ³  User                 :   512 Byte  ³
    ³  Interrupt            :   896 Byte  ³
    ³                                     ³
    ³       [   OK   ]  [ Cancel ]        ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ






    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.935
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Exceptions><Anzahl>:
    Durch diesen Eintrag wird festgelegt, wieviele Exceptiontasks maximal auf
    der RPS installiert werden k”nnen. Wenn Sie versuchen, mehr Exceptiontasks
    zu installieren als in diesem Eingabefeld definiert, tritt ein Fehler auf.

    <OBJEKT><Parameter><Konfiguriere><Exceptions><Unterbrechbar>:
    Die Exception-Taskklasse hat die h”chste Priorit„t, d.h. sie kann nicht
    unterbrochen werden. In diesem Feld steht daher immer der Eintrag "nie"!











    @934[ Exceptions ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.936
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Exceptions><Stackgr”áe>:
    User:      Hier k”nnen Sie die Gr”áe des Userstacks fr die EXC-Taskklasse
               festlegen. Der vom Userstack verwendete Speicherbereich befindet
               sich im System-RAM. Die Gr”áe des ben”tigten Userstacks fr die
               EXC-Taskklasse h„ngt vom Stackbedarf der Funktionen ab, die in
               den Tasks dieser Taskklasse aufgerufen werden.
    Interrupt: Mit diesem Eingabefeld k”nnen Sie die Gr”áe des Supervisorstacks
               fr die EXC-Taskklasse festlegen. Der vom Supervisorstack ver-
               wendete Speicherbereich befindet sich im System-RAM. Die Gr”áe
               des ben”tigten Supervisorstacks fr die EXC-Taskklasse h„ngt
               von der Interruptbelastung des Systems und vom Stackbedarf der
               Funktionen ab, die in den Tasks dieser Taskklasse aufgerufen
               werden.

    Der Stackbedarf der einzelnen Funktionen (FUBs und Inline-Funktionen) ist
    in der Online-Hilfe der entsprechenden Funktions-Libraries angegeben.



    @934[ Exceptions ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.937
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Interrupts>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄInterruptsÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³                                     ³
    ³@938[Anzahl]    (min:    0) :     2        ³
    ³@938[Unterbrechbar        ] : nie         ³
    ³                                     ³
    ³@939[Stackgr”áe           ]                ³
    ³  User                 :   512 Byte  ³
    ³  Interrupt            :   896 Byte  ³
    ³                                     ³
    ³       [   OK   ]  [ Cancel ]        ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ






    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.938
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Interrupts><Anzahl>:
    Durch diesen Eintrag wird festgelegt, wieviele Interrupttasks maximal auf
    der RPS installiert werden k”nnen.

    <OBJEKT><Parameter><Konfiguriere><Interrupts><Unterbrechbar>:
    Die IRQ-Taskklasse ist h”herprior als alle normalen Taskklassen, sie hat
    jedoch niedrigere Priorit„t als die EXC-Taskklasse. D.h. die IRQ-Taskklasse
    kann alle normalen Tasks unterbrechen, sie kann aber selbst von einer EXC-
    task unterbrochen werden. Zus„tzlich k”nnen Sie ber dieses Eingabefeld
    festlegen, ob eine Interrupttask von anderen Interrupttasks und/oder super-
    schnellen Tasks unterbrochen werden kann:
    immer:        Die Tasks der IRQ-Taskklasse k”nnen sowohl von anderen IRQ-
                  Tasks als auch von superschnellen Tasks unterbrochen werden.
    durch IRQ-TK: Die Tasks der IRQ-Taskklasse k”nnen zwar von anderen IRQ-
                  Tasks, nicht aber von HS-Tasks unterbrochen werden.
    nie:          Die Tasks der IRQ-Taskklasse k”nnen weder von anderen IRQ-
                  Tasks noch von superschnellen Tasks unterbrochen werden. Die
                  IRQ-Taskklasse hat dann die zweih”chste Priorit„t im System!

    @937[ Interrupts ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.939
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Interrupts><Stackgr”áe>:
    User:      Hier k”nnen Sie die Gr”áe des Userstacks fr die IRQ-Taskklasse
               festlegen. Der vom Userstack verwendete Speicherbereich befindet
               sich im System-RAM. Die Gr”áe des ben”tigten Userstacks fr die
               IRQ-Taskklasse h„ngt vom Stackbedarf der Funktionen ab, die in
               den Tasks dieser Taskklasse aufgerufen werden.
    Interrupt: Mit diesem Eingabefeld k”nnen Sie die Gr”áe des Supervisorstacks
               fr die IRQ-Taskklasse festlegen. Der vom Supervisorstack ver-
               wendete Speicherbereich befindet sich im System-RAM. Die Gr”áe
               des ben”tigten Supervisorstacks fr die IRQ-Taskklasse h„ngt
               von der Interruptbelastung des Systems und vom Stackbedarf der
               Funktionen ab, die in den Tasks dieser Taskklasse aufgerufen
               werden.

    Der Stackbedarf der einzelnen Funktionen (FUBs und Inline-Funktionen) ist
    in der Online-Hilfe der entsprechenden Funktions-Libraries angegeben.



    @937[ Interrupts ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.940
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><TimeOut's>:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄTimeOut'sÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@941[Erlaubte Systemzeit Verletzung] :  0.01 sec ³
    ³Warten                                     ³
    ³   @942[bei Kaltstart              ] :     0 sec ³
    ³   @942[bei Warmstart              ] :     0 sec ³
    ³   @943[auf POWERFAIL              ] :     0 sec ³
    ³                                           ³
    ³            [   OK   ]  [ Cancel ]         ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ








    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.941
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><TimeOut's>
    <Erlaubte Systemzeit Verletzung>:
    Analog zur TC-Zykluszeit wird auch die Systemzykluszeit (10-ms-Takt) vom
    Betriebssystem berwacht. Um zu verhindern, daá bei einem kurzzeitigen
    Zeit-Engpaá (z.B. wenn HS-Tasks und IRQ-Tasks die gesamte Systemzeit ver-
    brauchen) sofort eine Systemzykluszeitverletzung auftritt, k”nnen Sie an
    dieser Stelle eine erlaubte Systemzykluszeitverletzung in 10-ms-Schritten
    einstellen. Dadurch wird eine vordefinierte šberschreitung der System-
    zykluszeit erlaubt (einfache Systemzykluszeitverletzung).
    Wenn jedoch auch die erlaubte Systemzykluszeitverletzung berschritten
    wird, l”st das Betriebssystem die Exception "Systemzykluszeitverletzung"
    (Exception-Nr. 162) aus.







    @940[ TimeOut's ]                       @052[ HM-Objekt ]                    @001[ šbersicht ]
.942
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><TimeOut's><Warten>:
    bei Kaltstart: Damit nach einem Kaltstart alle Module der Steuerung ihre
                   Initialisierung korrekt abschlieáen k”nnen, wird vor dem
                   ersten Zugriff auf die I/O- und Systemmodule der RPS die
                   in diesem Eingabefeld festgelegte Zeitspanne abgewartet.
    bei Warmstart: Damit nach einem Warmstart alle Module der Steuerung ihre
                   Initialisierung korrekt abschlieáen k”nnen, wird vor dem
                   ersten Zugriff auf die I/O- und Systemmodule der RPS die
                   in diesem Eingabefeld festgelegte Zeitspanne abgewartet.
    auf POWERFAIL: Ein Stromausfall ist einem Warmstart gleichzusetzen, d.h.
                   die Applikation wird automatisch gestartet. Bei Systemen
                   mit I/O-Busexpansion bzw. Remote I/O-Bus kann es bei unter-
                   schiedlicher Belastung der Netzteile jedoch vorkommen, daá
                   die Stromversorgung einiger Anlagenteile bereits zusammen-
                   gebrochen ist, w„hrend die Haupt-CPU noch versorgt wird.
                   Erkennt die Haupt-CPU den Ausfall dieser Anlagenteile noch
                   bevor auch die lokale Stromversorgung zusammenbricht, wird
                   ein fataler Fehler gemeldet.

    @943[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.943
    PG2000  HAUPTMENš FUNKTIONEN

                   In diesem Fall l„uft die RPS nach dem Wiedereinschalten der
                   Stromversorgung im ERROR-Modus hoch und geht in den SERVICE-
                   Modus. D.h. die Applikation ist inaktiv und muá manuell
                   (durch einen Warmstart) gestartet werden. Um zu verhindern,
                   daá die Steuerung bei einem Stromausfall an der Peripherie
                   im SERVICE-Modus hochl„uft, k”nnen Sie in diesem Eingabefeld
                   Verz”gerungszeit fr das Ausl”sen eines fatalen Fehlers
                   parametrieren. D.h. beim Auftreten eines fatalen Fehlers
                   wird die eingegebene Verz”gerungszeit abgewartet, ob ein
                   Stromausfall die Ursache des Fehlers ist. Kommt es w„hrend
                   dieser Verz”gerungszeit zu einem Stromausfall, wird der
                   fatale Fehler nicht protokolliert und die Steuerung wird
                   nach dem Wiedereinschalten der Stromversorgung mit einem
                   Warmstart hochgefahren, d.h. die Applikation wird auto-
                   matisch gestartet.




    @940[ TimeOut's ]                       @052[ HM-Objekt ]                    @001[ šbersicht ]
.944
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Betriebssystem>:
    ÚÄÄÄÄÄÄÄÄÄÄÄBetriebssystemÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@945[Kommunikation]                       ³
    ³  Kommunikations Kan„le:         5  ³
    ³  Ger„tetreiber        :         6  ³
    ³  Force Auftr„ge       :        10  ³
    ³  PV Tabellen          :        77  ³
    ³  Log. Variablenlisten :        10  ³
    ³  Phy. Variablenlisten :        10  ³
    ³@948[B&R OS       ]                       ³
    ³  Systembuskarten      :         9  ³
    ³  AVT-Eintr„ge         :       152  ³
    ³  Semaphoren           :         4  ³
    ³  Queues               :         4  ³
    ³  Daten/Stack          : $0000bc00  ³
    ³  Hochlauf nach Fehler Diagnose    ³
    ³                                    ³
    ³        [   OK   ]  [ Cancel ]      ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.945
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Betriebssystem><Kommunikation>:
    Kommunikations Kan„le: Hier kann die maximale Anzahl der gleichzeitig ver-
                           wendeten Schnittstellen konfiguriert werden. Dieser
                           Wert spielt nur dann eine Rolle, wenn kein Standard-
                           Netzwerkkonfigurationsmodul verwendet wird.
    Ger„tetreiber:         Mit diesem Eintrag legen Sie fest, wieviele Treiber-
                           Module auf der RPS installiert werden k”nnen. Wenn
                           Sie versuchen, mehr Treiber-Module als festgelegt zu
                           installieren, wird ein Fehler gemeldet.
                           Im Lieferumfang des PG2000-Softwarepakets sind
                           Treiber fr verschiedene Protokolle (Frame-Treiber,
                           NET2000-Treiber, MININET-Treiber, usw.) enthalten.
                           Diese Treiber sind nachladbare Betriebssystem-
                           Erweiterungen, d.h. fr jeden verwendeten Treiber
                           muá das entsprechende Treiber-Modul auf der RPS
                           installiert werden.



    @946[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.946
    PG2000  HAUPTMENš FUNKTIONEN

    Force Auftr„ge: Mit diesem Eintrag k”nnen Sie die Anzahl der gleichzeitig
                    m”glichen Force-Auftr„ge definieren. D.h. wenn Sie an
                    dieser Stelle den Wert 10 angeben, k”nnen maximal 10
                    Variablen gleichzeitig geforct werden.
                    Mit der Force-Funktion, die in einigen Debug-Werkzeugen des
                    Programmiersystems PG2000 zur Verfgung steht, k”nnen Sie
                    Variablen zwingend mit bestimmten Werten beschreiben.
    PV Tabellen:    Hier wird die maximale Anzahl von PV-Tabellen, die gleich-
                    zeitig auf der RPS sein k”nnen, festgelegt. Pro Tabelle
                    k”nnen max. 256 Prozeávariablen eingetragen werden. Als
                    Minimalwert gilt die maximale Anzahl der zyklischen Tasks
                    (normale, superschnelle, IRQ- und EXC-Tasks). Wir empfehlen
                    eine Dimensionierung nach folgender Formel:
                    Maximale Anzahl von PV-Tabellen = 1,1 * (Anzahl der zy-
                                                             klischen Tasks)




    @947[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.947
    PG2000  HAUPTMENš FUNKTIONEN

    Log. Variablenlisten: Mit diesem Eintrag legen Sie fest, wieviele logische
                          Variablenlisten gleichzeitig auf der RPS verwendet
                          werden k”nnen. Logische Variablenlisten werden z.B.
                          bei der Kommunikation mit Panelware-Tableaus bzw. bei
                          Profibus-Variablenlisten verwendet. Abh„ngig von der
                          Anzahl der Kommunikationspartner bzw. der Anzahl der
                          gelesenen Prozeávariablen sind mehrere logische
                          Variablenlisten notwendig.
    Phy. Variablenlisten: Mit diesem Eintrag legen Sie fest, wieviele physika-
                          lische Variablenlisten gleichzeitig auf der RPS ver-
                          wendet werden k”nnen. Physikalische Variablenlisten
                          werden bei der PG-Kommunikation (PV-Monitor) ver-
                          wendet. Abh„ngig von der Anzahl der gleichzeitig
                          ge”ffneten PV-Monitor-Fenster und der Anzahl der
                          gelesenen Prozeávariablen sind mehrere physikalische
                          Variablenlisten notwendig.



    @944[ Betriebssystem ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.948
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Betriebssystem><B&R OS>:
    Systembuskarten: In diesem Eingabefeld k”nnen Sie die maximale Anzahl von
                     Systemmodulen (z.B. Remote Master, PROFIBUS Netzwerkmodul,
                     Multiprozessor, usw.) festlegen.
    AVT-Eintr„ge:    Mit diesem Eintrag k”nnen Sie die Anzahl der m”glichen
                     Eintr„ge in der Adreáverteiler-Tabelle (kurz: AVT) fest-
                     legen. Die AVT ist eine zentrale Systemtabelle, in die
                     der Anwender mit Hilfe spezieller Funktionen der BURTRAP-
                     Library Informationen eintragen kann. Auch alle AVT-
                     Funktionsbl”cke werden beim šbertragen automatisch in
                     die AVT eingetragen.
    Semaphoren:      Der hier angegebene Wert legt die Anzahl der Semaphore im
                     Betriebssystem fest (minimaler Wert = 4). Semaphore sind
                     "Variablen", mit denen z.B. der Zugriff auf von mehreren
                     Tasks gemeinsam benutzte Datenbereiche verriegelt werden
                     kann.



    @949[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.949
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Betriebssystem><B&R OS>:
    Queues:       In diesem Eingabefeld k”nnen Sie die Anzahl der Queues fest-
                  legen (minimaler Wert = 4). Queues werden verwendet, um
                  Meldungen zwischen nichtzyklischen System- und Anwender-
                  Tasks (Restzeittasks) auszutauschen. Standardm„áig ben”tigt
                  jede Restzeittask eine Queue. šber diese Queue kann die
                  Task mit Hilfe der Funktionen UT_sendmsg() und UT_recmsg()
                  (BURTRAP-Library) Meldungen senden und empfangen. Aber nicht
                  nur Restzeittasks, sondern auch einige Betriebssystem-
                  Erweiterungen wie z.B. der Spooler fr die Betreuung eines
                  Intelligenten I/O-Prozessors, ben”tigen Queues zum Daten-
                  und Meldungsaustausch.
    Daten/Stack:  In diesem Eingabefeld k”nnen Sie die Gr”áe jenes Teils des
                  System-RAMs definieren, in dem neben Betriebssystemdaten
                  auch die globalen Daten und Stacks von Restzeittasks abgelegt
                  werden.



    @950[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.950
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Betriebssystem><B&R OS>:
    Hochlauf nach Fehler: Bei einem fatalen Fehler im Hochlauf wird die RPS
                          standardm„áig im Diagnose-Modus hochgefahren. Nach
                          einem Hochlauf im Diagnose-Modus kann die RPS nur
                          durch einen Kaltstart gestartet werden. D.h. es muá
                          (MANUELL) ein Kaltstart ausgel”st werden, damit die
                          Applikation wieder l„uft. Um das zu umgehen, k”nnen
                          Sie mit diesem Eingabefeld den Hochlauf-Modus nach
                          einem fatalen Fehler (Diagnose oder Kaltstart) fest-
                          legen. Bei der Einstellung "Kaltstart" wird nach
                          einem fatalen Fehler sofort ein Kaltstart ausgel”st,
                          d.h. die Applikation wird AUTOMATISCH gestartet. Bei
                          der Einstellung "Diagnose" l„uft die RPS nach einem
                          fatalen Fehler im Diagnose-Modus hoch.
                          Die Einstellung diesem Eingabefeld gilt nur fr
                          fatale Fehler, die w„hrend des Betriebs auftreten.
                          Bei einem fatalen Fehler im Hochlauf wird die RPS
                          IMMER im Diagnose-Modus gestartet!

    @944[ Betriebssystem ]                  @052[ HM-Objekt ]                    @001[ šbersicht ]
.951
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O>:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄRemote I/OÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@952[Konfigurationen      ] :      1          ³
    ³@952[Einstellungen fr    ] : RIO  1         ³
    ³ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ³
    ³@953[Steckplatz           ] :           0     ³
    ³@953[Betriebsart          ] : Master         ³
    ³@954[Baudrate             ] : 500k           ³
    ³@955[Redundanz            ] : Nein           ³
    ³@955[TimeoutIO            ] :         400  ms ³
    ³@956[SlaveTimeOut         ] :           0  sec³
    ³@956[Max. Stationen       ] :          32     ³
    ³@956[Repeat Count         ] :          16    ³
    ³@957[Priorit„ten          ] :           2    ³
    ³@958[Slaves               ] : Default        ³
    ³   @959[Ausfall quittieren] :                ³
    ³   @960[Ready   quittieren] :                ³
    ³         [   OK   ]  [ Cancel ]         ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.952
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Konfigurationen>:
    Um die Definition von unterschiedlichen Remote-Parametern fr die einzelnen
    Remote Master-Module zu erm”glichen, mssen Sie in diesem Eingabefeld die
    Anzahl der Remote Master im Remote I/O-System angeben. Nach Eingabe des
    entsprechenden Zahlenwerts k”nnen Sie die Remote-Parameter fr jedes Remote
    Master-Modul einzeln definieren.

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Einstellungen fr>:
    šber dieses Eingabefeld k”nnen Sie ein Popup-Fenster ”ffnen, in dem Sie das
    zu konfigurierende Remote Master-Modul anw„hlen k”nnen. Nach der Selektion
    des gewnschten Remote Master-Moduls k”nnen Sie (mit Hilfe der anderen
    Eingabefelder der Dialogbox) die Remote-Parameter des entsprechenden Remote
    Master-Moduls definieren.






    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.953
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Steckplatz>:
    Hier mssen Sie die Moduladresse des angew„hlten Remote Master-Moduls an-
    geben.
    B&R 2010 ... Die Moduladresse wird durch einen Nummernschalter eingestellt.
    B&R 2005 ... Die Moduladresse ist durch den Steckplatz bestimmt.

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Betriebsart>:
    Master: Das Remote Master-Modul wird als Remote Master verwendet.
    ISL:    Das Remote Master-Modul wird als intelligenter Slave (kurz: ISL)
            verwendet. Ein intelligenter Slave ist ein Remote Master-Modul mit
            einem eigenen CPU-Modul. Die CPU des ISL kann Berechnungen durch-
            fhren oder Daten aufbereiten, bevor diese an den Remote Master
            zurckgeschickt werden. Durch die Verwendung von ISLs kann die CPU
            des Remote Masters (Haupt-CPU) wesentlich entlastet werden.
            ISLs werden ber spezielle FUBs parametriert. D.h. nach der Selek-
            tion der Betriebsart "ISL" mssen Sie fr den ISL keine weiteren
            Einstellungen vornehmen. Aus diesem Grund sind bei der Betriebsart
            "ISL" die anderen Eingabefelder der Dialogbox gesperrt.

    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.954
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Baudrate>:
    šber dieses Eingabefeld k”nnen Sie ein Popup-Fenster zum Festlegen der
    Baudrate (šbertragungsrate) des Remote Masters ”ffnen:
       2M ..... šbertragungsrate = 2000 kBaud
       1M ..... šbertragungsrate = 1000 kBaud
       500k ... šbertragungsrate = 500 kBaud
       181k ... šbertragungsrate = 181 kBaud
       100k ... šbertragungsrate = 100 kBaud
    Die maximal m”gliche šbertragungsrate eines Remote Masters ist von der
    Ausdehnung des entsprechenden Remote Systems abh„ngig:

     Ausdehnung [m] ³ šbertragungsrate [kBaud]
    ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ
          1200      ³           100
          1000      ³           181
           400      ³           500
           200      ³          1000
           100      ³          2000

    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.955
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Redundanz>:
    Auf allen Remote Modulen des Systems B&R 2010 sind 2 RS485-Schnittstellen
    vorhanden, wobei die untere Schnittstelle fr den normalen Betrieb des
    Remote Systems verwendet wird. Die obere Schnittstelle ist fr den
    Redundanz-Betrieb vorgesehen. In dieser Betriebsart wird der Remote-Bus
    ein zweites Mal verdrahtet. Bei Ausfall einer Schnittstelle wird der Daten-
    transfer ber die Redundanz-Schnittstelle und den zweiten Bus durchgefhrt.
    Diese Hardware-Funktionalit„t wird in der Version 4.30 der Remote-I/O
    Firmware noch nicht untersttzt, d.h. die Einstellung im Eingabefeld
    <Redundanz> wird derzeit nicht ausgewertet!

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><TimeoutIO>:
    In diesem Eingabefeld k”nnen Sie festlegen, nach welcher Zeitspanne
    (Angabe in Millisekunden) die Inputs/Outputs der Slave-Station zurck-
    gesetzt werden, wenn die Kommunikation mit dem Master unterbrochen wurde
    (z.B. Buskabel abgesteckt).



    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.956
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><SlaveTimeOut>:
    Nach einem Hochlauf mssen sich alle Remote Slaves innerhalb der hier ein-
    gestellten Zeitspanne beim Remote Master melden, sonst tritt ein Timeout-
    Fehler auf (Angaben in Sekunden).

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Max. Stationen>:
    Mit diesem Eintrag legen Sie die h”chstzul„ssige Slave-Nummer im Remote
    System fest.

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Repeat Count>:
    Hier k”nnen Sie die Wiederholungsrate fr den Datentransfer am Remote I/O-
    Bus angeben. Die Wiederholungsrate legt fest, wie oft der Datentransfer am
    Remote I/O-Bus beim Auftreten eines šbertragungsfehlers wiederholt wird,
    bevor eine Exception ausgel”st wird.





    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.957
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Priorit„ten>:
    Im Popup-Fenster dieses Eingabefeldes k”nnen Sie festlegen, ob alle I/O-
    Auftr„ge am Remote I/O-Bus mit derselben Priorit„t oder mit zwei verschie-
    denen Priorit„ten abgewickelt werden:
    1 ... Bei dieser Einstellung werden alle I/O-Auftr„ge (Schaufelauftr„ge) am
          Remote I/O-Bus mit derselben Priorit„t ausgefhrt.
    2 ... Zwei verschiedene Priorit„ten fr die I/O-Auftr„ge. Bei dieser Ein-
          stellung werden alle I/O-Auftr„ge der RIO-HSTC sowie die I/O-Auftr„ge
          jener RIO-FUB, fr die die Priorit„t 0 (hohe Priorit„t) definiert
          wurde, hochprior behandelt. Die Schaufelauftr„ge aller anderen Task-
          klassen und der RIO-FUB mit der Priorit„t 1 (niedrige Priorit„t)
          werden in der RIO-Restzeit abgewickelt. Dies kann je nach ver-
          bleibender RIO-Restzeit bis zu mehreren RIO-HSTC-Zyklen dauern!
          D.h. die RIO-HSTC und alle RIO-FUBs mit der Priorit„t 0 sind
          besonders fr sehr wichtige Remote I/O-Daten geeignet, da durch
          die hohe Priorit„t der I/O-Auftr„ge die schnellst m”gliche RIO-
          Aktualisierungszeit garantiert wird.


    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.958
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Slaves>:
    Enable:  Alle Remote Slaves am Remote I/O-Bus werden automatisch aktiviert
             und damit kontrolliert.
    Disable: Die Aktivierung jedes einzelnen Slaves erfolgt manuell ber die
             Funktion RIO_mode() (RIO-Library) in der Applikation.














    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.959
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Ausfall quittieren>:
    In diesem Feld k”nnen Sie angeben, ob der Ausfall eines Remote Slaves
    quittiert werden muá. Ein Remote Slave f„llt z.B. aus, wenn die Strom-
    versorgung fr den Slave zusammenbricht oder das Buskabel abgesteckt wird.
    Inaktiv:   Das Remote System setzt beim Ausfall eines Slaves die Bedienung
               der restlichen Slaves automatisch fort.
    Aktiv:     Der Ausfall eines Remote Slaves muá mit der Funktion RIO_quit()
               (siehe RIO-Library) quittiert werden, bevor die Bedienung der
               restlichen Slaves fortgesetzt wird. Es wird keine Exception aus-
               gel”st. Bis zur Quittierung bleibt der Remote Slave im ERROR-
               Zustand.
    Exception: Beim Ausfall eines Remote Slaves wird eine Exception ausgel”st
               (Exception-Nr. 177). Der Ausfall muá mit der Funktion RIO_quit()
               der RIO-Library quittiert werden, bevor die Bedienung der rest-
               lichen Slaves fortgesetzt wird. Bis zur Quittierung bleibt der
               Remote Slave im ERROR-Zustand.
    Mit der Funktion RIO_mode() (RIO-Library) k”nnen Sie diese globale Ein-
    stellung fr einzelne Remote Slaves des Remote Systems „ndern.

    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.960
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Remote I/O><Ready quittieren>:
    Nimmt ein Remote Slave nach einem Ausfall wieder den Betrieb auf (Slave
    wird READY), so wird er entweder automatisch ("Inaktiv") oder erst nach der
    Quittierung dieses Zustands ("Aktiv" oder "Exception") in den RIO-Zyklus
    eingebunden:
    Inaktiv:   Wird ein Slave nach einem Ausfall wieder READY, wird er auto-
               matisch (ohne Quittierung!) in den RIO-Zyklus eingebunden.
    Aktiv:     Bevor ein Slave mit dem Zustand READY wieder in den RIO-Zyklus
               eingebunden wird, muá dieser Zustand mit der Funktion RIO_quit()
               (RIO-Library) quittiert werden. D.h. dieser Slave bleibt solange
               im READY-Zustand, bis die Quittierung durchgefhrt wird.
    Exception: Wenn ein Slave nach einem Ausfall wieder READY wird, l”st das
               Betriebssystem eine Exception (Exception-Nr. 177) aus. Bevor
               dieser Remote Slave wieder in den RIO-Zyklus eingebunden wird,
               muá der Zustand READY mit der Funktion RIO_quit() (RIO-Library)
               quittiert werden.
    Mit der Funktion RIO_mode() (RIO-Library) k”nnen Sie diese globale Ein-
    stellung fr einzelne Remote Slaves des Remote Systems „ndern.

    @951[ Remote I/O ]                      @052[ HM-Objekt ]                    @001[ šbersicht ]
.961
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O>:
    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄCAN I/OÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@962[Konfigurationen      ] :      1          ³
    ³@962[Einstellungen fr    ] : CAN  1         ³
    ³ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ³
    ³@963[Steckplatz           ] : Lokal          ³
    ³@964[Subslot              ] : kein SS        ³
    ³@964[Interface            ] : IF2            ³
    ³@965[Baudrate             ] : 250kBIT/s      ³
    ³@965[SlaveTimeOut         ] :        8000  ms ³
    ³@965[TimeoutIO            ] :        1000  ms ³
    ³@966[Repeat Count         ] :           3     ³
    ³@966[Unterbrechbar        ] : Nein           ³
    ³@966[Anzahl ANP Module    ] :          80    ³
    ³@967[Anzahl Slaves        ] :           3    ³
    ³@967[Exception ausl”sen   ] : Nein           ³
    ³   [   OK   ]  [@968[ Slaves ]]  [ Cancel ]   ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.962
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Konfigurationen>:
    Um die Definition von unterschiedlichen CAN-Parametern fr die einzelnen
    CAN-Busse zu erm”glichen, mssen Sie in diesem Eingabefeld die Anzahl der
    verfgbaren CAN-Busse angeben. Nach Eingabe des entsprechenden Zahlenwerts
    k”nnen Sie die CAN-Parameter fr jeden CAN-Bus einzeln definieren.

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Einstellungen fr>:
    šber dieses Eingabefeld k”nnen Sie ein Popup-Fenster ”ffnen, in dem Sie
    den zu konfigurierenden CAN-Bus anw„hlen k”nnen. Nach der Selektion des
    gewnschten CAN-Busses k”nnen Sie (mit Hilfe der anderen Eingabefelder
    der Dialogbox) die CAN-Parameter des entsprechenden CAN-Busses definieren.








    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.963
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Steckplatz>:
    Mit diesem Eintrag legen Sie den Steckplatz des Moduls (auf dem sich die
    CAN-Schnittstelle befindet) fest:
    Lokal .......... Die CAN-Schnittstelle befindet sich auf dem Prozessor-
                     modul (z.B. CP104, usw.).
    PS ............. Die CAN-Schnittstelle befindet sich auf dem Netzteil.
    SL1 bis SL13 ... šber die Einstellung SL1 bis SL13 kann die Moduladresse
                     des Schnittstellenmoduls festlegt werden (zum Beispiel:
                     "SL1" = Moduladresse 1).
                     B&R2003 ... Die Moduladresse ist durch den Controller und
                                 den Modulplatz bestimmt (Details finden Sie im
                                 "B&R SYSTEM 2000 B&R 2003 Anwenderhandbuch").
                     B&R2005 ... Die Moduladresse ist durch den Steckplatz be-
                                 stimmt. Die Z„hlung beginnt bei Steckplatz 3
                                 mit der Moduladresse 1.
                     B&R2010 ... Die Moduladresse ist durch den Steckplatz be-
                                 stimmt. Die Z„hlung beginnt bei Steckplatz 1
                                 mit der Moduladresse 1.

    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.964
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Subslot>:
    Steckt das Schnittstellenmodul (auf dem sich die CAN-Schnittstelle be-
    findet) in einem Einschubsteckplatz (Subslot), so muá hier die Nummer
    dieses Einschubsteckplatzes ("SS1", "SS2" oder "SS3") angegeben werden.
    Diese Einstellung ist nur fr die Module CP260 und IF260 notwendig, da
    zur Zeit nur diese Module ber Einschubsteckpl„tze verfgt. Fr alle
    anderen Module muá die Einstellung "kein SS" verwendet werden.
    kein SS ... Fr Module ohne Einschubsteckpl„tze
    SS1 ....... Einschubsteckplatz 1
    SS2 ....... Einschubsteckplatz 2
    SS3 ....... Einschubsteckplatz 3

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Interface>:
    Hier k”nnen Sie die Nummer der zu parametrierenden CAN-Schnittstelle
    angeben (IF1, IF2, IF3 oder IF4).




    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.965
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Baudrate>:
    šber dieses Eingabefeld k”nnen Sie ein Popup-Fenster zum Festlegen der
    Kommunikationsgeschwindigkeit am CAN-Bus (20, 125, 250 oder 500 kBIT/s)
    ”ffnen.

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><SlaveTimeOut>:
    Nach einem Hochlauf mssen sich alle Slave-Stationen innerhalb der hier
    eingestellten Zeitspanne beim Master melden, sonst tritt ein Timeout-
    Fehler auf (Angaben in Millisekunden).

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><TimeoutIO>:
    Jeder CAN-Knoten muá sich zyklisch innerhalb der hier eingestellten Zeit-
    spanne (Angabe in Millisekunden) beim Master melden, sonst wird der ent-
    sprechende CAN-Knoten abgemeldet.





    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.966
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Repeat Count>:
    Hier k”nnen Sie die Wiederholungsrate fr den Datentransfer am CAN-Bus
    angeben. Die Wiederholungsrate legt fest, wie oft der Datentransfer am
    CAN-Bus beim Auftreten eines šbertragungsfehlers wiederholt wird, bevor
    ein Fehler gemeldet wird.

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Unterbrechbar>:
    Ja:   Bei dieser Einstellung kann der I/O-Datentransfer am CAN-Bus durch
          das zyklische Applikationsprogramm unterbrochen werden.
          Bei einem lokalen CAN-Bus (ist es sinnvoll, daá das zyklische System
          den CAN I/O-Datentransfer unterbricht.
    Nein: Bei dieser Einstellung kann der I/O-Datentransfer am CAN-Bus vom
          zyklischen System nicht unterbrochen werden.

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Anzahl ANP Module>:
    Mit diesem Eintrag legen Sie fest, wie viele Anpassungsmodule am CAN-Bus
    untersttzt werden.


    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.967
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Anzahl Slaves>:
    Hier k”nnen Sie festlegen, wie viele Slaves am CAN-Bus untersttzt werden.

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Exception ausl”sen>:
    Ja:   Wenn bei dieser Einstellung ein Fehler am CAN I/O-Bus auftritt (z.B.:
          CAN-Knoten weggefallen), wird eine Exception (Exception-Nr. 170) aus-
          gel”st. In einer Exceptiontask kann mit Hilfe der Funktion CANIOexc()
          (CANIO-Library) der Typ der aufgetretenen Exception ausgelesen
          werden.
    Nein: Wenn Sie diese Einstellung w„hlen, wird beim Auftreten eines Fehlers
          am CAN I/O-Bus keine Exception ausgel”st.







    
    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.968
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><CAN I/O><Slaves>:
    Es wird eine weitere Dialogbox zum Konfigurieren der Eigenschaften der
    einzelnen Slaves eingeblendet. Dadurch k”nnen Sie das Zeitverhalten an
    besondere Anforderungen Ihrer Applikation anpassen:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄSlavesÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³Parameter fr Slave Nr.:  1                    ³
    ³Knoten Nummer:     1                            ³
    ³ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ³
    ³Idlezeit                                        ³
    ³  Digital IN :   640 ms   Digital OUT:   640 ms ³
    ³  Analog  IN :   640 ms   Analog  OUT:   640 ms ³
    ³Zykluszeit              Inhibitzeit             ³
    ³  Digital IN :   100 ms   Digital IN :     5 ms ³
    ³  Analog  IN :   100 ms   Analog  IN :    20 ms ³
    ³                                                ³
    ³              [   OK   ]  [ Cancel ]            ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
    
    @961[ CAN I/O ]                         @052[ HM-Objekt ]                    @001[ šbersicht ]
.969
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><2003 I/O>:
    Standardm„áig werden alle zyklischen I/O-Kan„le in einem I/O-Zyklus einmal
    geschaufelt und damit aktualisiert. Damit h„ngt die Dauer eines I/O-Zyklus
    von der maximalen Anzahl der auf einem Analog-Interfacemodul vorhandenen
    zyklischen Kan„len (Summe ber alle Anpassungsmodule) ab. In Systemen mit
    mehreren Analog-Interfacemodulen kann sich das sehr negativ auswirken. Wenn
    z.B. auf einem Analog-Interfacemodul viel mehr Kan„le als auf einem anderen
    vorhanden sind, gibt das Analog-Interfacemodul mit den meisten Kan„len die
    I/O-Zyklus-Dauer vor.
    Um dieses Verhalten in speziellen Applikationen ver„ndern zu k”nnen, haben
    Sie mit dem Eingabefeld <2003 I/O> die M”glichkeit, die Anzahl der in einem
    I/O-Zyklus geschaufelten Kan„le festzulegen:

    Alle ... Alle zyklischen I/O-Kan„le werden in einem I/O-Zyklus geschaufelt.
    1 ...... Pro I/O-Zyklus wird ein zyklischer I/O-Kanal geschaufelt.
    2 ...... Pro I/O-Zyklus werden zwei zyklische I/O-Kan„le geschaufelt.
    :        :
    16 ..... Pro I/O-Zyklus werden sechzehn zyklische I/O-Kan„le geschaufelt.

    @970[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.970
    PG2000  HAUPTMENš FUNKTIONEN

    Wenn auf einem Analog-Interfacemodul mehr zyklische I/O-Kan„le vorhanden
    sind als im Eingabefeld <2003 I/O> konfiguriert, so werden die Kan„le
    dieses Analog-Interfacemoduls auf mehrere I/O-Zyklen aufgeteilt. D.h. das
    I/O-Abbild dieses Analog-Interfacemoduls wird nur in jedem n-ten I/O-Zyklus
    (n > 1) aktualisiert.














    @971[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.971
    PG2000  HAUPTMENš FUNKTIONEN

    Beispiel 1: Alle Kan„le in einem I/O-Zyklus
    CP472 mit 2 Analog-Interfacemodulen: 1 IN + 1 OUT (Analog-Interfacemodul 1)
                                         4 IN         (Analog-Interfacemodul 2)

                                I/O-Zyklus
          ³<------------------------------------------------->³
          ³ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ³
    AIF 1 ³ ³ ANP 1 ³ ³ ANP 2 ³ ³ Dummy ³ ³ Dummy ³ ³ Dummy ³ ³
          ³ ³Kanal 1³ ³Kanal 1³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ ³
          ³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ                               ³
          ³ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ³
    AIF 2 ³ ³ ANP 1 ³ ³ ANP 1 ³ ³ ANP 1 ³ ³ ANP 1 ³ ³ Dummy ³ ³
          ³ ³Kanal 1³ ³Kanal 2³ ³Kanal 3³ ³Kanal 4³ ÀÄÄÄÄÄÄÄÙ ³
          ³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ           ³

    Anmerkungen: AIF ... Analog-Interfacemodul
                 ANP ... Anpassungsmodul


    @972[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.972
    PG2000  HAUPTMENš FUNKTIONEN

    Beispiel 2: 2 Kan„le in einem I/O-Zyklus
    CP472 mit 2 Analog-Interfacemodulen: 1 IN + 1 OUT (Analog-Interfacemodul 1)
                                         4 IN         (Analog-Interfacemodul 2)

                    1. I/O-Zyklus                   2. I/O-Zyklus
                   (AIF 1 aktuell)             (AIF 1 + AIF 2 aktuell)
          ³<----------------------------->³<----------------------------->³
          ³ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ³ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ³
    AIF 1 ³ ³ ANP 1 ³ ³ ANP 2 ³ ³ Dummy ³ ³ ³ ANP 1 ³ ³ ANP 2 ³ ³ Dummy ³ ³
          ³ ³Kanal 1³ ³Kanal 1³ ÀÄÄÄÄÄÄÄÙ ³ ³Kanal 1³ ³Kanal 1³ ÀÄÄÄÄÄÄÄÙ ³
          ³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ           ³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ           ³
          ³ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ³ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ÚÄÄÄÄÄÄÄ¿ ³
    AIF 2 ³ ³ ANP 1 ³ ³ ANP 1 ³ ³ Dummy ³ ³ ³ ANP 1 ³ ³ ANP 1 ³ ³ Dummy ³ ³
          ³ ³Kanal 1³ ³Kanal 2³ ÀÄÄÄÄÄÄÄÙ ³ ³Kanal 3³ ³Kanal 4³ ÀÄÄÄÄÄÄÄÙ ³
          ³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ           ³ ÀÄÄÄÄÄÄÄÙ ÀÄÄÄÄÄÄÄÙ           ³

    Erkl„rung: AIF ... Analog-Interfacemodul
               ANP ... Anpassungsmodul

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.973
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><PB Indizes>:
    Durch diese Funktion wird eine Dialogbox eingeblendet, in der Sie die
    Indizes fr die PROFIBUS-Objekte festlegen k”nnen.
    Die Standardeinstellungen der Indizes sind fr alle PROFIBUS-f„higen
    B&R-Komponenten (RPS/SPS-Systeme B&R 2010 und B&R 2005, Tableaus,
    Industrie-PCs, usw.) gltig!
    Es wird empfohlen, die Standardeinstellungen in dieser Dialogbox nur
    in Zusammenarbeit mit dem technischen Support von B&R zu „ndern!











    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.974
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Kommunikation>:
    Ab der Version V2.00 des Betriebssystem RPSSW k”nnen mehrere Schnittstellen
    als Online-Schnittstellen konfiguriert werden:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄKommunikationÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³@975[Anzahl der Konfigurationen ]:      1  ³
    ³@975[Konfigurations Nummer      ]:  1     ³
    ³ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ³
    ³@976[Steckplatz               ]: Lokal    ³
    ³@977[Subslot                  ]: kein SS  ³
    ³@977[Interface                ]: IF1      ³
    ³@978[Typ                      ]: Seriell  ³
    ³                        [@978[ Parameter ]]³
    ³                                     ³
    ³       [   OK   ]  [ Cancel ]        ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ



    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.975
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Anzahl der Konfig.>:
    Um die Definition von unterschiedlichen Schnittstellenparametern fr die
    einzelnen Online-Schnittstellen zu erm”glichen, mssen Sie in diesem Ein-
    gabefeld die Anzahl der Online-Schnittstellen angeben. Nach Eingabe des
    entsprechenden Zahlenwerts k”nnen Sie die Parameter fr jede Online-
    Schnittstelle einzeln definieren.

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Konfigurations Nummer>:
    šber dieses Eingabefeld k”nnen Sie ein Popup-Fenster ”ffnen, in dem Sie
    die zu konfigurierenden Online-Schnittstelle anw„hlen k”nnen. Nach der
    Selektion der gewnschten Schnittstelle k”nnen Sie (mit Hilfe der anderen
    Eingabefelder der Dialogbox) die Schnittstellenparameter definieren.







    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.976
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Steckplatz>:
    Mit diesem Eintrag legen Sie den Steckplatz des Moduls (auf dem sich die
    Online-Schnittstelle befindet) fest:
    Lokal .......... Die Online-Schnittstelle befindet sich auf dem Prozessor-
                     modul.
    SL1 bis SL13 ... šber die Einstellung SL1 bis SL13 kann die Moduladresse
                     des Schnittstellenmoduls festlegt werden (zum Beispiel:
                     "SL1" = Moduladresse 1).
                     B&R2003 ... Die Moduladresse ist durch den Controller und
                                 den Modulplatz bestimmt (Details finden Sie im
                                 "B&R SYSTEM 2000 B&R 2003 Anwenderhandbuch").
                     B&R2005 ... Die Moduladresse ist durch den Steckplatz be-
                                 stimmt. Die Z„hlung beginnt bei Steckplatz 3
                                 mit der Moduladresse 1.
                     B&R2010 ... Die Moduladresse ist durch den Steckplatz be-
                                 stimmt. Die Z„hlung beginnt bei Steckplatz 1
                                 mit der Moduladresse 1.


    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.977
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Subslot>:
    Steckt das Schnittstellenmodul (auf dem sich die Online-Schnittstelle
    befindet) in einem Einschubsteckplatz (Subslot), so muá hier die Nummer
    dieses Einschubsteckplatzes ("SS1", "SS2" oder "SS3") angegeben werden.
    Diese Einstellung ist nur fr die Module CP260 und IF260 notwendig, da
    zur Zeit nur diese Module ber Einschubsteckpl„tze verfgt. Fr alle
    anderen Module muá die Einstellung "kein SS" verwendet werden.
    kein SS ... Fr Module ohne Einschubsteckpl„tze
    SS1 ....... Einschubsteckplatz 1
    SS2 ....... Einschubsteckplatz 2
    SS3 ....... Einschubsteckplatz 3

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Interface>:
    Hier k”nnen Sie die Nummer der Online-Schnittstelle angeben (IF1, IF2,
    IF3 oder IF4).




    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.978
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Typ>:
    Mit diesem Eintrag k”nnen Sie den Typ der Schnittstelle ("Seriell", "CAN"
    oder "PB") festlegen. Ab Version V2.00 des Betriebssystems RPSSW k”nnen
    serielle Schnittstellen, CAN-Schnittstellen und PROFIBUS-Schnittstellen
    als Online-Schnittstellen verwendet werden.

    <OBJEKT><Parameter><Konfiguriere><Kommunikation><Parameter>:
    Durch Anwahl dieses Feldes wird eine Dialogbox ge”ffnet, in der Sie (ab-
    h„ngig vom definierten Typ der Schnittstelle) weitere Schnittstellen-
    parameter definieren k”nnen:
    "Seriell": Baudrate ... Baudrate fr die serielle Kommunikation.
               Parity ..... Die Einstellungen "none" (keine Parit„t), "even"
                            (gerade Parit„t) und "odd" (ungerade Parit„t) sind
                            m”glich.
               Databits ... Anzahl der Datenbits.
               Stopbits ... Anzahl der Stoppbits.
    "CAN":     Baudrate ... Baudrate fr die CAN-Kommunikation.
               Basis ID ... Basis zur Berechnung der CAN-IDs (CAN-Identifier).

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.979
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Zielspeicher>:
    User Flash:   Ist als Zielspeicher "User Flash" eingestellt, wird das RPS-
                  Konfigurationsmodul ("sysconf"-Symbol) beim Ausfhren der
                  Funktion <Objekt><Speichern> ins Anwender-ROM bertragen.
    System Flash: Ist als Zielspeicher "System Flash" eingestellt, wird das
                  RPS-Konfigurationsmodul beim Ausfhren der Funktion <Objekt>
                  <Speichern> ins System-ROM bertragen.
                  Diese Einstellung ist erst ab der Version 1.92 des Betriebs-
                  systems RPSSW m”glich. Ab dieser RPSSW-Version k”nnen sowohl
                  Systemmodule als auch das RPS-Konfigurationsmodul im System-
                  ROM installiert werden. Dieser Speicher kann jedoch nicht fr
                  Applikationsdaten benutzt werden. Beim L”schen eines Moduls
                  wird der (vom Modul belegte) Speicher nicht freigegeben.
                  Um den Speicher wieder verwenden zu k”nnen, muá das gesamte
                  System-ROM (inklusive Betriebssystem) gel”scht werden.
                  Dies wird nur durch Aufruf der Funktion <System><Service>
                  <RPSSW Update> (Betriebssystem-Update) erreicht.
    MEM Card:     Ist als Zielspeicher "MEM Card" eingestellt, wird das RPS-

    @980[ Fortsetzung ]                     @052[ HM-Objekt ]                    @001[ šbersicht ]
.980
    PG2000  HAUPTMENš FUNKTIONEN

                  Konfigurationsmodul beim Ausfhren der Funktion <Objekt>
                  <Speichern> auf die PCMCIA-Speicherkarte (auch "MEM-Card"
                  genannt) bertragen.
















    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]
.981
    PG2000  HAUPTMENš FUNKTIONEN

    <OBJEKT><Parameter><Modul Info>:
    Version:      Anzeige der Version des Betriebssystems RPSSW.
    Datum:        Anzeige des Erzeugungsdatums
    Zeit:         Anzeige der Erzeugungsuhrzeit.
    L„nge:        Anzeige der Modul-L„nge (in Byte).

    <OK> ........ Alle Eingaben werden geprft und bernommen. Anschlieáend
                  wird einer Dialogbox abgefragt, ob die neuen Eingaben auf
                  der RPS gespeichert werden sollen.                  
    <Default> ... Einstellen der Standardwerte fr den ausgew„hlten Prozessor-
                  Typ.
    <Cancel> .... Abbrechen ohne šbernahme der nderungen. Wenn nderungen
                  durchgefhrt wurden, wird der Anwender in einer Dialogbox
                  darauf hingewiesen, daá Daten verloren gehen.
    <Drucken> ... Ausdrucken aller Einstellungen des RPS-Konfigurationsmoduls
                  (auch "$$sysconf"-Modul genannt). Die Auswahl des Druckers
                  und die Einstellung der Drucker-Optionen werden mittels der
                  Men-Funktion <Einstellungen><Dokumentation> vorgenommen.

    @110[ Objekt-Parameter ]                @052[ HM-Objekt ]                    @001[ šbersicht ]

.End
